# 奖池提留代理业绩 — 客户端/工具侧接口调用文档

> 配套方案文档：`doc/奖池提留代理业绩分配方案.md`（服务端实现细节、部署）
> 本文档面向客户端（Unity）与策划/运营工具开发，只描述**怎么调、传什么、收什么**。
> 状态：待服务端实施后生效（接口 1、接口 5 为新增；接口 2/3/4 为既有接口复用，现在即可对接）。

---

## 1. 通用协议（所有接口共用）

### 1.1 两条命令通道

客户端登录后持有自己的 Account 实体（base 端），所有命令通过以下两个**客户端可直接调用**的实体方法发出：

| 通道 | 调用方法（客户端→服务器） | 响应回调（服务器→客户端） | 用途 |
|---|---|---|---|
| 大厅命令通道 | `reqHallCommand(param, context)` | `onHallCommand(ret_code, result)` | 大厅业务命令（业绩查询、授权列表等） |
| 账号命令通道 | `reqAccountCommand(param, context)` | `onAccountCommand(ret_code, result)` | 账号管理命令（设置玩家属性等） |

### 1.2 请求格式

- `param`：**JSON 字符串**，必须包含 `"header"` 字段（值为命令名），其余字段为命令参数。
- `context`：字符串，**新版本格式为 `"p@<任意上下文串>"`**（@ 前为通道标记，@ 后内容原样回传，用于请求-响应配对）。

```json
// param 示例
{"header": "异步_查询_代理_奖池业绩_信息", "date": 0}
```

### 1.3 响应格式

回调第二个参数 `result` 是 JSON 字符串，外层固定包裹为：

```json
{
  "context": "<请求时@后的context原样回传>",
  "result": { /* 各接口自己的返回体，见下文 */ }
}
```

> **异常路径要注意（形态不同）**：以下三种情况服务端**只回** `{"context":"<ctx>"}`、**没有 `result` 键**且 `ret_code = 0x301`：① `header` 不存在（命令名写错）；② `param` 不是合法 JSON 或缺 `header`；③ 服务端内部异常。而业务失败（如权限不足）走**正常包裹**（带 `result`）。**客户端请先判空再解析**。

`ret_code` 返回码（`scripts/common/GlobalConst.py`）：

| 常量 | 值 | 含义 |
|---|---|---|
| `GC_OK` | `0x200`（512） | 成功 |
| `GC_FAILED` | `0x301`（769） | 失败（命令不存在、参数错误、权限不足等） |
| `GC_UNKOWN` | `0x500`（1280） | 服务端本次不返回，等待后续消息（本组接口不会出现） |

注意：命令名中带"异步"的接口走服务端协程处理，调用方式与同步命令**完全相同**，只是响应延迟略长（含数据库查询）。

---

## 2. 接口 1：查询代理奖池业绩（新增）

查询当前登录代理某日期的奖池业绩、全平台提留总额及业绩比例。数据来源为普通房每局大赢家贡献的奖池提留。

- **命令名**：`异步_查询_代理_奖池业绩_信息`
- **通道**：大厅命令通道（`reqHallCommand` / `onHallCommand`）
- **权限**：任何登录账号均可调用。`proxy_performance` 是**调用者自己**的净业绩（非代理/无业绩为 0）；`all_performance` 是**平台当日提留总额 × 2**，对**所有**调用者都返回真实值（与旧"业绩"接口的暴露面一致，客户端只对代理展示该界面即可）

### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `header` | string | 是 | `异步_查询_代理_奖池业绩_信息` |
| `date` | int | 否 | 相对今天的天数偏移：`0`=今天（默认），`-1`=昨天，`-2`=前天，以此类推 |

### 请求示例

```json
{"header": "异步_查询_代理_奖池业绩_信息", "date": 0}
```

### 响应示例（ret_code = 0x200）

```json
{
  "context": "my_ctx_001",
  "result": {
    "ListJackpotPerformanceInfo": {
      "all_performance": 4000,
      "my_performance": 1200,
      "granted_performance": 600,
      "proxy_performance": 600,
      "date": "2026-09-22",
      "proxy_guuid": "ID12345"
    },
    "context": "my_ctx_001"
  }
}
```

### 响应字段说明（`result.ListJackpotPerformanceInfo`）

| 字段 | 类型 | 说明 |
|---|---|---|
| `all_performance` | int | **当日全平台奖池提留总额 × 2**（单位：**分**）。×2 即"平台保留一半"的口径，直接做比例分母 |
| `my_performance` | int | **我（毛）**：本人链下所有玩家贡献提留合计（分），未扣减已下发部分 |
| `granted_performance` | int | **下发**：本人已授权下级的业绩合计（分）；没有已授权下级时为 0 |
| `proxy_performance` | int | **剩（净）** = `my_performance − granted_performance`（分）；**含义与旧接口的 `proxy_performance` 完全一致**（客户端原有比例/返款逻辑不用改） |
| `date` | string | 统计日期 `YYYY-MM-DD` |
| `proxy_guuid` | string | 当前代理 guuid |

> 上例含义：当日提留总额 20 元（=2000 分），`all_performance` = 2000 × 2 = 4000 分；我（毛）12 元、下发 6 元、剩（净）6 元 —— 三值恒满足 **`我 = 下发 + 剩`**，可直接用于客户端"业绩下发"界面底部（下级列表见接口 5）。

当日无提留数据时，各项数值为 0（接口正常返回，不报错）。

> 注意区分两种"0"：**当日平台有提留、但当前代理当天无业绩**时，`all_performance` 仍返回真实总额（非 0），`proxy_performance` 为 0，比例按 0% 显示——这样界面才能正确展示"当日总收益"。

### 客户端计算

```
业绩比例(剩) = proxy_performance / all_performance      （all_performance > 0 时；为 0 则比例显示 0）
我 比例      = my_performance / all_performance         （"业绩下发"界面底部的"我"）
下发 比例    = granted_performance / all_performance    （"业绩下发"界面底部的"下发"）
业绩金额(元) = proxy_performance / 100
可返款(元)   = proxy_performance / 200                  （= 业绩金额 ÷ 2）
```

> 业务规则：代理当天结束后凭"业绩比例"向平台**线下申请返款**，返款金额 = 比例 × 当日提留总额 = 净业绩 ÷ 2。界面建议同时展示：业绩金额、业绩比例（%）、预计可返款金额。
> 单位说明：服务端金额一律以**分**返回（提留额 × 100），客户端展示前除以 100 换算为元。

### 注意

- 该接口**旧字段与既有接口"异步_查询_代理_业绩_信息"完全一致**（见附录 A；另**新增** `my_performance` / `granted_performance`，旧界面忽略即可），现有业绩界面可直接复用，仅需切换命令名与解析 key（`ListJackpotPerformanceInfo`）。
- **界面文案建议改为"奖池业绩"**（口径是奖池提留，不是玩家输赢），并把金额按"分 → 元"（`/100`）展示，避免与既有"业绩"界面混淆。
- **对应两张客户端界面**：① 图1「业绩比例」用本接口的**剩**（`proxy_performance / all_performance`）；② 图2「业绩下发」底部的"我 / 下发 / 剩"用本接口三个值，其**下级列表用接口 5**——同一天调用时两接口的"剩"完全相等（服务端用的是同一集合）。
- 当天数据为**实时累计**（每局结束即写入），当天结束前的查询结果为动态值。
- **两种"未加载"情形要区分开**（服务端代理数据按需加载、按链归因）：
  1. **写入侧**：某笔提留发生时，若玩家链上有代理尚未加载，该笔提留**不会**计入这些代理的业绩 → **永久少计**，重查不会恢复，由平台每日对账发现并修正；
  2. **查询侧**：上级"扣除被授权下级"依赖被授权代理已加载，极端情况下漏扣会让上级业绩**偏高**（不是少计）。
- 因此：**本接口返回值用于界面展示与参考，实际返款以平台每日对账结果为准**（对账口径：返款 = 净业绩 ÷ 2）。

---

## 3. 接口 5：查询我的下级代理业绩列表（新增，客户端"业绩下发"界面）

列出**当前代理已授权的下级代理**及各自业绩，并给出底部"我 / 下发 / 剩"三值汇总与分页信息。对应客户端"业绩下发"界面（表头"下级代理 / **昨日占比**"，界面按昨日查询 → 传 `date=-1`）。

- **命令名**：`异步_查询_代理_奖池业绩_列表`
- **通道**：大厅命令通道
- **权限**：任何登录账号均可调用（返回的是**调用者自己**的下级）

### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `header` | string | 是 | `异步_查询_代理_奖池业绩_列表` |
| `date` | int | 否 | 相对今天的天数偏移：`0`=今天（默认），`-1`=昨天（"业绩下发"界面用这个），`-2`=前天 |
| `count` | int | 否 | 每页条数，默认 10 |
| `page` | int | 否 | 页码，从 0 开始，默认 0 |

### 请求示例

```json
{"header": "异步_查询_代理_奖池业绩_列表", "date": -1, "count": 10, "page": 0}
```

### 响应示例（ret_code = 0x200）

```json
{
  "context": "my_ctx_005",
  "result": {
    "ListJackpotPerformanceList": {
      "list": [["894948", "暑假开始", 100], ["590326", "8LL", 0]],
      "my_performance": 400,
      "granted_performance": 100,
      "proxy_performance": 300,
      "all_performance": 4000,
      "date": "2026-09-21"
    },
    "number": "0",
    "count": "2",
    "context": "my_ctx_005"
  }
}
```

> 注：示例中 `894948 / 暑假开始` 为**客户端设计稿数据**（线上 `tbl_Account` 不存在该账号），不要拿它去线上核对；线上当前唯一被授权账号是 `189836 / 8L`（其上级为老板 `648425`），联调可直接用它——若要看多行/分页，先给**同一条链上**的其它代理授权。

### 响应字段说明（`result.ListJackpotPerformanceList`）

| 字段 | 类型 | 说明 |
|---|---|---|
| `list` | array | 每项 `[下级guuid, 下级昵称, 业绩(分)]`。**只含"我已授权（`client_prop='True'`）且在我链上"的下级**（同一条线只列最上面一个，与"上级扣减"同一集合），**按业绩倒序（同业绩按 guuid 升序，行序稳定，翻页不跳位）**，服务端已按 `count`/`page` 分页；**下级当天无业绩也会出现**（业绩为 0） |
| `my_performance` | int | **我（毛）**：本人链下玩家贡献提留合计（分） |
| `granted_performance` | int | **下发**：已授权下级业绩合计（分） |
| `proxy_performance` | int | **剩（净）** = `my_performance − granted_performance`（分）；与接口 1 **同日期**调用时完全相等 |
| `all_performance` | int | 占比分母 = 当日全平台提留总额 × 2（分） |
| `date` | string | 统计日期 `YYYY-MM-DD`（回显服务端实际统计日，界面可标注"统计日：…"） |

> **分页字段在上一层**：`number`（当前页码）/ `count`（下级代理总数）与 `ListJackpotPerformanceList` **同级**（与接口 2 同风格，字符串），取值路径为 `result.number` / `result.count`。

### 客户端计算

```
某下级占比 = 该下级业绩 / all_performance          （all_performance > 0 时）
我 占比    = my_performance / all_performance
下发 占比  = granted_performance / all_performance
剩 占比    = proxy_performance / all_performance
金额(元)   = 对应分值 / 100
```

> 图2 底部"业绩总比例：我 x% / 下发 y% / 剩 z%"直接用上表取值即可，恒满足 `我 = 下发 + 剩`。

### 注意

- **列表口径 = "我已授权且在我链上"**，与"上级业绩扣减"用的是同一集合 → 与接口 1 的"剩"不会互相矛盾（同日期调用应完全相等）。
- 本接口**只读**：名单的维护（业绩开通）由**新后台管理系统**通过 HTTP 写 `client_prop`（见《新后台管理系统接口文档》§4.4 与方案 §5 步骤 5），或由工具侧/GM 通过接口 3 写；客户端不做自助下发。
- 下级**昵称**取自 `tbl_Account.sm_name`（与接口 2 同源）。
- 空态：没有已授权下级时 `list` 为 `[]`、`count` 为 `"0"`，接口正常返回（不报错）。
- 金额单位与接口 1 一致：**分**（展示前 `/100` 换算为元）。

---

## 4. 接口 2：查询总业绩授权列表（复用既有）

分页列出所有已被授权（`client_prop='True'`）的代理，用于"业绩下发"管理界面选择/展示授权对象。

- **命令名**：`异步_查询_总业绩授权_列表`
- **通道**：大厅命令通道
- **权限**：任何登录账号

### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `header` | string | 是 | `异步_查询_总业绩授权_列表` |
| `count` | int | 否 | 每页条数，默认 10 |
| `page` | int | 否 | 页码，从 0 开始，默认 0 |

### 请求示例

```json
{"header": "异步_查询_总业绩授权_列表", "count": 10, "page": 0}
```

### 响应示例

```json
{
  "context": "my_ctx_002",
  "result": {
    "ListPerformanceList": [
      ["ID12345", "代理甲"],
      ["ID23456", "代理乙"]
    ],
    "number": "0",
    "count": "2",
    "context": "my_ctx_002"
  }
}
```

| 字段 | 说明 |
|---|---|
| `ListPerformanceList` | 数组，每项为 `[代理guuid, 代理名字]` |
| `number` | 当前页码（字符串） |
| `count` | 被授权代理总数（字符串，用于分页） |

---

## 5. 接口 3：设置玩家属性（复用既有，授权/取消授权）

把某个下级代理的 `client_prop` 置为 `'True'`（授权，即"业绩下发"）或 `''`（取消授权）。**这就是"下发业绩"的操作入口**，服务端零改动。

- **命令名**：`设置_玩家_属性`（注意：**没有"异步_"前缀**，且走**账号命令通道**，勿与大厅通道的"异步_设置_玩家_属性"混淆——后者走 `reqSetProperty`，**不支持 `client_prop`**）
- **通道**：账号命令通道（`reqAccountCommand` / `onAccountCommand`）
- **权限**：**仅 level ≥ 99（boss/策划工具账号）**，普通代理客户端不可调用

### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `header` | string | 是 | `设置_玩家_属性` |
| `target_guuid` | string | 是 | 目标代理 guuid |
| `client_prop` | string | 是 | `'True'` = 授权（下发业绩）；`''` = 取消授权 |

### 请求示例

```json
{"header": "设置_玩家_属性", "target_guuid": "ID23456", "client_prop": "True"}
```

### 响应示例

```json
{"context": "my_ctx_003", "result": {}}
```

| 场景 | ret_code | result |
|---|---|---|
| 受理成功 | 0x200 | `{}` |
| 权限不足（level < 99） | 0x301 | `{}` |
| 参数业务错误 | 0x301 | `{"error": "<错误描述>"}` |

### 注意

- 服务端通过 `createBaseFromGUUID` **异步加载目标账号后写库**，接口立即返回 0x200 仅代表"已受理"；属性落库通常在毫秒内完成，随后用接口 4 查询可确认。
- 授权后：该下级代理用接口 1 查询即可看到自己的业绩比例；其上级的 `proxy_performance` 自动扣除该下级的业绩（同一条线上授权多个下级时，上级只扣最上面一个，不重复扣）。
- **本表只描述 `client_prop` 的用法**：该命令对 `supper_agentid` / `super_percent` / `big_agentid*` 等属性存在**权限校验更宽松的分支**（`Account.py:4363-4387`），工具侧**不要传这些属性名**。

---

## 6. 接口 4：获取玩家属性（复用既有，查询授权状态）

查询指定账号的 `client_prop` 当前值，用于授权操作后的确认/界面回显。

- **命令名**：`异步_获取_玩家_属性`
- **通道**：大厅命令通道
- **权限**：任何登录账号（建议仅工具侧使用）

### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `header` | string | 是 | `异步_获取_玩家_属性` |
| `guuid` | string | 是 | 目标账号 guuid |
| `name` | string | 是 | 属性名，固定 `client_prop` |

### 请求示例

```json
{"header": "异步_获取_玩家_属性", "guuid": "ID23456", "name": "client_prop"}
```

### 响应示例

```json
{"context": "my_ctx_004", "result": "True"}
```

`result` 为 JSON 字符串：`"True"` 表示已授权，`""` 表示未授权；目标不存在时 ret_code = 0x301。

> **批量核对授权列表时不要循环调用本接口**：服务端实现会通过 `kbe.createEntityFromDBID` 把目标账号**加载进 baseapp 内存**（`Hall.py:8539`），批量扫描会造成大量实体加载。批量场景请**直接查库**（即接口 2 的数据源）：
> `select sm_guuid, sm_name, sm_client_prop from tbl_Account where sm_client_prop = 'True';`

---

## 7. 典型业务流程

### 7.1 代理查看业绩比例（客户端图1）

```
1. 代理打开"代理中心"(图1), 红圈"业绩比例"按钮
2. 客户端: reqHallCommand('{"header":"异步_查询_代理_奖池业绩_信息","date":0}', 'p@jackpot')
3. 服务端 onHallCommand → 解析 result.ListJackpotPerformanceInfo
4. 界面展示: 比例 = proxy_performance/all_performance; 可附: 我/下发/剩 三值、可返款 proxy_performance/200 元
5. (如需昨日结算口径) 重复步骤2, date=-1
6. 当天结束后, 代理凭界面比例/金额, 线下向平台申请返款(平台人工审核打款)
```

### 7.2 代理查看"业绩下发"明细（客户端图2）

```
1. 代理打开"业绩下发"界面(图2), 表头"下级代理 / 昨日占比"
2. 客户端: reqHallCommand('{"header":"异步_查询_代理_奖池业绩_列表","date":-1,"count":10,"page":0}', 'p@grant_list')
3. 服务端 onHallCommand → 解析 result.ListJackpotPerformanceList
4. 列表逐行: 昵称 + ID + 占比(业绩/all_performance); 底部三值: my / granted / proxy (我/下发/剩)
5. (翻页) 重复步骤2, page 递增; number/count 用于画页码
6. 界面只读: 调整下发需 GM/策划工具(见 7.3)
```

### 7.3 上级下发业绩给下级（工具侧/GM）

```
1. 工具用接口2翻页浏览/搜索目标下级代理
2. 工具: reqAccountCommand('{"header":"设置_玩家_属性","target_guuid":"ID23456","client_prop":"True"}', 'p@grant')
3. 工具用接口4确认 client_prop == "True"
4. 下级此后用接口1可查到自己的业绩比例, 用接口5可看到自己下发的下级; 上级的三值随之变化
5. 取消下发: 步骤2中 client_prop 传 ''
```

---

## 附录 A：与旧接口的对照（供界面复用）

| | 旧接口（不动，继续可用） | 新接口（本方案） |
|---|---|---|
| 命令名 | `异步_查询_代理_业绩_信息` | `异步_查询_代理_奖池业绩_信息` |
| 响应 key | `ListProxyPerformanceInfo` | `ListJackpotPerformanceInfo` |
| 统计口径 | 玩家**输赢**（历史设计，未采用） | 奖池**提留**（本需求） |
| 请求参数 | 相同（`date` 偏移） | 相同 |
| 响应字段 | `all_performance` / `proxy_performance` / `date` / `proxy_guuid` | 旧字段**完全相同**，另**新增** `my_performance`（我）/ `granted_performance`（下发） |
| 金额单位 | 筹码（旧口径，界面一般不展示金额） | **分**（提留额 × 100，展示前 /100 换算为元） |
| 比例公式 | proxy_performance / all_performance | 相同（×2 口径一致） |

> 即：客户端把旧界面的命令名和解析 key 换掉即可复用全部展示逻辑。
> 另新增**接口 5（我的下级业绩列表）**驱动"业绩下发"界面（图2），旧接口没有对应能力。
