# 登录快速注册弹窗实施交接

日期：2026-09-06；目录：`/Volumes/SSD/qing`；分支：`main`。确认稿已写入正式 Cocos Prefab 与资源；没有构建、提交或推送。

## 用户确认的方向

- 后续页面的主视觉、色调、边框和字体只以 `design-previews/2026-09-04-V7确认风格六页统一版/` 为依据，重点对照其中登录、大厅、我的三页；早期 `1jpg严格统一风格-v2` 和近期偏黄稿不得混入。
- 快速注册不是独立页面，而是 V7 登录页上的模态弹窗：背景登录页保留并加深蓝遮罩，居中显示注册资料框，右上角关闭；不出现页面级返回箭头或顶部导航栏。
- 弹窗内容必须沿用现有 `assets/resources/UI/panelLogin.prefab`：`新用户注册`、头像选择、邀请码、昵称、账号、密码、确认密码、防盗号保护、注册状态、确认注册和安全提醒；不增加新业务字段。

## 最终视觉依据

- 文件：`design-previews/2026-09-06-V7快速注册弹窗效果图-v3/01-快速注册弹窗-大字版.png`，941×1672。V2 因内容文字偏小、行距不足已被本版替代。
- 使用内置图像生成工具，以 V7 六页目录的登录、大厅、我的三张为参考；画面明确表现登录页背景、半透明遮罩和居中注册弹窗。
- V3 将五项字段标题和提示、防盗号说明、状态与安全提醒整体放大，并增加输入行高度、左右内边距和各段垂直间距；弹窗适度加高但仍保留四周登录背景。
- 状态：已获用户确认并实施。

## 已实施范围

- `assets/resources/UI/panelLogin.prefab`：使用确认稿 731×1299 完整弹窗直切底，注册框固定为 583×1035 并整体居中；长屏只增加弹窗外留白，内部间距、背景纹理和构图均不拉伸。五行原生 EditBox、头像、开关、状态、提交 Button 及原节点名/事件均保留；空状态显示确认稿自带字段图，输入时才启用动态干净行与文字。
- `assets/resources/V7/register_*_exact.png`：标题、规则线、8L 小盾牌、关闭、五行输入框及图标/固定标题、防盗号、提交、头像环、左右箭头和关闭图标均由确认稿直接切取；不重新生成美术字或替换近似图标。只有必须承载动态内容的干净底板和确认稿未提供的动态状态按同一视觉制作。
- `assets/scripts/UI/panelLogin.ts`：只补全“上一头像/下一头像”的实际选择逻辑，原注册校验与请求不变。
- 主弹窗动态头像为 174×174，铺满确认稿直切的 180×180 透明圆环内沿；圆环提取边界不得包含下方“点击选择头像”的文字碎片。头像选择弹窗保留 20 个动态头像、选中环和换一批逻辑；关闭图标、刷新底板、刷新文字与透明点击节点分层，避免渲染组件互相覆盖。
- 防盗号选中会显示动态状态句；`V7注册动态状态底` 的渲染顺序固定在状态文字和图标之前，并与确认按钮顶部保留明确间隙，避免蓝底遮住提示或吃掉确认按钮金边。
- 工具：`tools/extract_v7_login_register_exact_assets.py`、`tools/apply_v7_login_register_exact.py`、`tools/validate_v7_login_register_exact.py`。应用脚本负责编辑器时写入 Prefab，不在运行时批量造皮肤节点。

## 验证与边界

- `tools/validate_v7_login_register_exact.py` 通过：正式资源引用、注册结构、动态控件、箭头交互和头像选择弹窗均满足约束。
- `tools/validate_v7_responsive_layout.py` 通过：1334/1500/1624/1778 四档竖屏锚点和安全区检查通过；`git diff --check` 通过。
- 重启同一个 Cocos Creator 2.4.13 实例后在网页预览核对：固定弹窗居中且内部间距不随长屏变化；头像完整铺满干净圆环；头像选择层右上关闭和“换一批头像”均可见，刷新后 20 个头像实际变化；勾选防盗号后动态提示完整显示，蓝底未遮字，确认注册按钮顶部金边连续。
- 未执行 Creator 构建、真机测试、真实注册提交/服务端回包、提交或推送；这些状态不能由静态和网页预览结果推导。

## 后续强制规则

- 采用确认效果图换皮时，确认图中的静态美术字、图标、徽章与固定按钮文字必须直接从确认图切出，禁止重新生成、重绘或用系统字体/近似图标替换；真实输入、数据、头像与业务状态继续作为动态节点。
- 只使用用户已打开的 Cocos Creator 实例刷新/验收，不重复启动多个实例。
