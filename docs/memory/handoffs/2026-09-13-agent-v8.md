# 代理模块 V8-new 实施与质感修正

日期：2026-09-13。目录 `/Volumes/SSD/qing`，分支 `main`；本任务未提交、推送、构建或发布。保留同工作区其他未提交改动。

## 目标与正式实现

唯一美术依据：`design-previews/效果图V8-new/05-后续模块/08` 至 `23`。用户随后对照截图否决初版底框偏平、数字不匹配、首页按钮留空和标题横线缺失，以下为修正后的状态。

- `assets/resources/UI/panelHongli.prefab`：首页、六个列表和十一种真实弹窗，共 18 个独立视图。推广按用户最新要求改为共用“我的”现有页面，见[共用推广交接](2026-09-13-promotion-shared.md)。七个全页复用大厅 `lobby_scene_long_v8.png`；真实列表中段拉伸/滚动，分页固定底部，弹窗固定内部构图并整体居中。
- 五个正式行 Prefab：`玩家对象`、`贡献对象`、`盟主对象`、`总业绩对象`、`红利提取记录对象`。保持原业务节点名、父级、Button/Toggle 事件和三项 EditBox 输入契约。
- 63 张 `agent_v8_*.png` 加一份 BitmapFont：独立标题、按钮、盾牌、底框和分隔线。原稿无内容的蓝色区域用于重建被动态文字遮住的内面，保留亮边、完整圆角和较宽内侧柔光，避免把细窄原始扫描线拉成竖纹。九宫格只用于可伸缩底板，图标/字图保持比例。
- 数字从定稿取字形成可变宽位图字体，保留浅金渐变与边缘阴影，仍使用原 Label 的真实数值。未更改金额单位、取整和权限协议；预览中 `8,888.00` 等只是离线样例，不代表新增了业务格式化。
- 首页补齐“代理管理”和“代理数据”标题两侧横线、统计/数据纵线及上下排分隔线。按钮顺序为我的玩家、我的业绩、我的盟主、提取记录、游戏推广、总业绩；权限隐藏后由原生 Grid 连续补位，4 项两行两列，5/6 项两行三列。业务父级/事件不变，图层与原生 Layout 都在 Prefab；脚本仅在权限变化后按可见数量调整容器宽度并刷新布局。这是本次用户明确要求的动态补位。

## 业务范围

`assets/scripts/UI/panelHongli.ts` 保留原协议和服务器权威：新增摘要同步方法复用已有服务器字段；修正“总业绩2/我的分红”指向不存在旧页面时的空引用，回落到现存分红确认面板；代理推广改为复用大厅现有页，二维码/复制/保存走大厅同一套逻辑。真实授权、比例确认、提取和分页仍使用原逻辑。未添加参考图中没有实际业务支持的统计列或输入字段。

## 已验证与待验证

- `python3.13 tools/validate_v8_agent.py`：原节点/事件/输入契约、18 个视图 × 5 档高度、63 张纹理引用和九宫格四角保真通过。
- `node tools/tests/agent_v8_layout_engine.js`：直接运行安装版 Cocos 2.4.13 Layout，5 种权限/配置组合（3/4/5/6 按钮，含关闭推广）顺序/位置/两行检查通过。
- `node tools/tests/agent_v8_regression.js`：25 项真实业务方法、服务器字段同步、权限、授权/比例校验、分红确认、共用推广入口和分页回归通过；所有外部写入均为 mock。
- `node tools/tests/wallet_editbox_alignment_regression.js assets/resources/UI/panelHongli.prefab 总业绩,添加盟主面板,修改盟主面板 3`：3 个 EditBox / 6 个文字节点的实际引擎初始化、文字、重入和尺寸变化检查通过。
- 最新静态预览由 `tools/render_v8_agent.py` 读取正式 Prefab，含位图字体/原生 Grid 几何、六/四/五按钮首页、列表/弹窗（旧推广预览不再更新），存于 `art_sources/v8-repairs/agent/qa/`；这些不是实际网页截图。
- **Creator 导入分阶段记录**：用户最新质感反馈之前，六份 Prefab 与当时 58 张图片的导入一致性曾通过；网页 iPhone 6/X 初版首页、列表样例和实际按钮开页也已查。随后这次质感/字体/Grid 修正尚未取得导入成功结果，`--imports` 明确报 `stale imported Prefab: panelHongli.prefab`，新 BitmapFont 还没有对应 Library 导入文件。现有 Creator 仍为 qing/login.fire（无未保存星号）；尝试资源检索、重新加载界面和前台切换后缓存仍旧，未另开实例或覆盖 Library。**不能将初版网页或旧导入结果视为本次最终修正已验收。**
- **同日后续已刷新**：处理推广共用时重跑 `--imports` 通过，六份 Prefab、63 PNG 和数字字体均已导入，两个脚本已编译。最新代理首页及共用推广浏览器样例可见，详细证据见[共用推广交接](2026-09-13-promotion-shared.md)；上段缓存旧记录只反映此前阶段。其余页面新版视觉、真实账户授权/比例/提取回包、真机、构建和发布仍待验。

## 工具与恢复

切图：`tools/extract_v8_agent_assets.py`；数字字体：`tools/extract_v8_agent_font.py`；正式写入：`tools/apply_v8_agent.py`。三者有写入副作用，验证只运行独立 validate/tests。裁切坐标与源图关系见 `art_sources/v8-repairs/agent/cuts.json`、`number-glyphs.json`；原始本次前置文件见同目录 `before/`，不要整批覆盖其他任务的新修改。
