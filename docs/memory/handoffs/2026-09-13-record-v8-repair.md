# 战绩界面 V8-new 美术纠错

日期：2026-09-13；目录 `/Volumes/SSD/qing`；分支 `main`。本次未提交、未推送。进入时已有赠送、钱包及相关文档改动，保持原样。

## 目标与实现

用户要求纠正战绩换皮，与 `design-previews/效果图V8-new/01-主页面/04-战绩.png` 对齐；背景直接复用大厅，适合拉伸的底板用九宫格。大厅场景复用是本次明确指定的背景差异；日期仍默认今日，源稿昨日选中用于核对选中外观。

- `assets/resources/UI/panelRecordList.prefab`：清空旧根/中下背景/平铺延展的 SpriteFrame，启用原 `bg` 节点并引用大厅已有 `lobby_scene_long_v8.png`；场景采用同大厅的 750×1875.4 顶部对齐方式。
- `统计` 改为源稿盾牌透明切图，去掉旧 941×561 场景图中烘焙的日期栏，修复两条日期栏叠加。标题、盾牌、条件和表头按实际 V8-new 坐标重新写入，替代残留的旧 Prefab 尺寸。
- 日期正常底板 240×68、选中底板 112×58、列表底框 224×240、行框 224×96 均为九宫格；先归一到 750 画布单位再设置边距，圆角完整保留。正常/选中文字为独立源稿字形，不跟着底板拉伸。
- `assets/resources/Prefabs/战绩对象.prefab` 保留根 Button 和四个真实字段，独立增加查看回放图标。数字用源稿字形制作 `v8_record_digits.labelatlas`，包含数字、正负号、斜杠和小数标点，继续由 Label 字符串驱动；最终字号 25。未烘焙任何样例记录到正式页面。
- 列表上方固定在源图 Y=730，视口底部预留 122 源像素；背景框下延至源图底边 37 像素。默认显示保留近 30 天提示，多页时由既有脚本改为分页；分页复用现有金币页按钮和页码底框，没有另存重复分页图片。
- `tools/repair_v8_record_art.py` 为该页作者工具，`tools/apply_v8_record.py` 转调；`tools/v8_record_crops.json` 指向本轮准备好的组件切图。`tools/render_v8_record.py` 是只读离线预览，样例只存在其内存副本。

## 包体与保留契约

- 对战绩本轮涉及的 PNG（包括新增与移除）按 Git HEAD 比较：1,733,547 → 522,901 字节，净减少 **1,210,646 字节，约 1.15 MiB**。这是资源源文件体积，未执行 Creator 构建，不能等同 APK/IPA 最终差值。
- 原 `record_bg_exact.png`、三张旧整栏日期图和三张旧带字选中图确认在 `assets` 无 UUID/路径引用后移至 `art_sources/v8-repairs/record/retired/`，保留各自 `.meta` 便于恢复；它们不再位于 resources 打包目录。未改大厅背景图片或其 `.meta`。
- 既有两份 Prefab 的组件脚本、ClickEvent、业务节点名与 UUID 保留。`panelRecordList.ts` 未改：日期参数、每页六条、房间号/底注/带入/输赢、查看回放及尾页请求沿用原实现。
- 进入时的目标 Prefab 副本位于 `art_sources/v8-repairs/record/before/`；源稿哈希、切图区域、边距和 PNG 哈希见 `cuts.json`。

## 验证与边界

- 1334、1624、1778、1860 四档离线排版以及三种日期选中/多页底栏检查；六行完整放入视口，字号、列中心、回放入口和提示无相互遮挡。预览在 `art_sources/v8-repairs/record/qa/`，其中数字和记录是明确的源稿样例。
- Creator 2.4.13 当前 qing 实例已显示正式战绩 Prefab；全部 16 张本轮 PNG、LabelAtlas 与两个 Prefab 的导入已核对。导入 Prefab 比较仅忽略 Creator 对顶层资源 `_name` 的正常补名。最后字号修改曾未及时自动导入，已通过编辑器 `Editor.assetdb.refresh` 单独刷新战绩行，确认导入内容一致。
- 现有登录态网页 iPhone X/6 已检查：共用背景、单日期栏、今日/昨日/前日切换、短屏六行临时样例、最终字号 25 的真实引擎渲染。临时 `__record_art_qa_` 节点的清除回执已收到，已恢复 iPhone X 并关闭、重开战绩；首次新版加载确认默认今日。最后一次重入状态只读复核遇到自动审批超时，用户要求继续后原浏览器连接报告 Debugger unattached，因此未取得该最后状态快照。
- 当前账号三种日期均无真实记录；未验证真实多页回包或点击真实回放，未进行游戏交易、Creator 构建、真机或发布。全局滚动通知偶尔覆盖顶部属于原有独立通知层，本次未改它的行为或资源。
- 编辑器历史日志包含赠送记录头像前框的循环嵌套错误，与进入时已有赠送改动有关；本次不修改赠送资源。战绩新节点的 PrefabInfo 根引用已单独检查，避免产生相同问题。

下一步：由用户复核外观；有真实战绩的账号再检查分页和回放。当前状态和全套清单仅更新战绩项。文档只读检查为 0 个错误；CURRENT 与 v7-ui 保留已有体积警告，本次未进行无关归档。
