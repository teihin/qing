# 2026-09-07 桌内菜单、快捷图标与空位徽标交接

## 范围

- 工作目录：`/Volumes/SSD/qing`
- 分支：`main`
- 确认稿：`design-previews/2026-09-07-V7桌内统一风格重设计-v6/01-左上角小型牌局菜单.png`
- 目标：正式替换左上牌局菜单和桌内快捷图标；权限隐藏的菜单项不留空；重新设计空位徽标。

## 已实施

- `assets/resources/UI/panelGameView.prefab` 与 `assets/Scenes/drh8.fire` 同步序列化菜单和桌内图标，避免编辑器场景实例继续引用旧资源。
- 菜单面板、八个按钮的图标和美术字直接从确认稿切取为 `assets/resources/V7/ingame_menu_*_exact.png`；节点名、按钮组件和原事件不变。
- `ConfigMain` 使用固定 420×502 构图及两列 `cc.Layout` 网格。业务脚本仍只切换权限项的 `active`，布局组件会跳过非激活子项并把后续入口依次前移，不再产生空格。
- 左上菜单、右上回顾、左下牌型/客服/实时记录、右下语音使用 `ingame_icon_*_exact.png`；首轮缩取图标因中心和占比不佳被用户否决，现已统一重绘为 128×128 冷蓝金三环圆盘，所有内图标放大并单独校正中心，“菜单”为三横线、“记录”为项目列表，避免语义重复。原点击节点和 Widget 约束保留。
- `坐下控制/坐下0..7` 使用新 `ingame_empty_seat_exact.png`。首版纯文字三环圆牌因缺乏设计感被用户否决，现正式资源改为透明外底、冷蓝金多层徽标，主体包含软包贵宾椅、右上入座加号和底部“空位”铭牌；用户继续反馈文字过小后，铭牌扩大到源图 86×31、字体从 22 提到 29，实际 96×96 节点显示约 19px。原八个坐下按钮、座位坐标及事件不变。
- 生成、应用和检查入口分别为 `tools/extract_v7_ingame_menu_exact_assets.py`、`tools/apply_v7_ingame_menu_exact.py`、`tools/validate_v7_ingame_menu_exact.py`。

## 验证与边界

- 专用 Prefab/Scene 结构、SpriteFrame UUID、八个菜单按钮、八个空位按钮和点击组件校验通过。
- 静态菜单渲染确认面板与八项构图；整桌 750×1334 静态合成确认六个快捷图标在实际节点尺寸下居中且可辨；`git diff --check` 通过。
- 按用户约定只重启一个 Cocos Creator 2.4.13 实例，六个快捷图标及空位资源的 raw UUID 均已出现在 `library/imports`；新版空位正式 PNG 与导入 PNG 的 SHA-1 一致。
- 未执行 Creator 构建、真机或真实房间交互；房主显示解散与非房主隐藏解散后的实际点击顺序仍需登录态复核。
- 本轮未提交、未推送；工作树同时保留此前登录页等未提交改动。

## 同轮文件整理

- 已把当时确认的最终效果图汇总复制到 `/Volumes/SSD/8L游戏最终版效果图-2026-09-07`，共 59 个 PNG、约 64 MB。该目录位于项目外，不属于 Git 改动。
