# 2026-09-13 系统设置 V8-new 组件化与图标横线修正

工作目录 `/Volumes/SSD/qing`，分支 `main`，当前 Creator 2.4.13。本任务未提交或推送；保留其他页面的未提交修改。

## 目标与结果

用户随后明确停用系统设置中的“修改预留信息”。现已将该入口节点设为 inactive，页面不再显示或接收该项点击；聊天语音、游戏音效、防盗号及状态提示整体上移 108 逻辑像素，保留连续行间距。节点名和底层独立页面资源保留，其他模块的预留资料流程不在此次删除范围。专用应用器同步移除该行配置，避免重跑恢复入口。

用户要求系统设置按真实功能重新换皮，不能再将整张效果图直接作为页面；使用 V8-new 风格和大厅背景。随后截图指出所有图标旁有横杠。本次已停用七处 `V8图文分隔` 节点及其 Sprite，并清空引用，图标与文字之间保留留白，标题下方分隔线保留。

- 正式实现仅修改 `assets/resources/UI/panelMain.prefab` 的 `设置` 子树。旧 `V7系统设置母版` 已关闭并移除 SpriteFrame 引用，名称保留；大厅背景、盾牌、标题、面板、行控件、图标和文字分别序列化。
- 大厅背景复用 `lobby_scene_long_v8.png`；盾牌、返回箭头保持比例，面板和行框复用现有 V8 蓝金九宫格。九个浅金图标为独立透明 PNG，开关复用注册页蓝色开/关素材。顶部标题避让返回箭头。
- 顶部固定，内容面板伸缩，切换账号固定在面板底部；当前显示修改登录密码、交易密码、聊天语音、游戏音效、防盗号和切换账号六项。
- 交易密码根据原 `bNeedInitJYPwd` 分流到初始化或修改；没有添加参考图中的虚构振动功能。防盗号提示恢复为真实动态 Label，保留服务器状态、确认、取消、处理中与错误反馈。
- `panelMain.ts` 本轮未修改；原业务节点名、Button/ToggleEx 组件及事件绑定保留。运行脚本不负责换肤。

## 文件与复现

- `tools/apply_v8_settings.py`：设置子树专用应用入口。旧 `apply_v7_followup_main_exact.py` 的全图设置样式不再代表当前实现，不要重跑覆盖。
- `tools/prepare_v8_settings_icons.py`：透明源图九宫格切分，逐图保持原始 RGBA 像素。资源为 `assets/resources/V7/settings_v8_icon_*.png` 及同名 `.meta`。
- `tools/validate_v8_settings.py`：范围、原绑定、字号布局边界、横线停用、透明像素及可选 Creator 导入检查。
- `tools/tests/settings_v8_regression.js`：生产处理函数与正式 Prefab 路径的 20 项离线回归；账号、存储和重启全部隔离在内存中。
- `art_sources/v8-repairs/settings/`：修改前 Prefab/脚本快照、透明源图、切分清单及 `qa/settings-static-{1334,1624}.png`。静态图仅为离线几何预览，字体/色彩不等同于引擎最终渲染；实际网页画面也已分别检查。

## 验证及边界

- `python3 tools/validate_v8_settings.py --imports` 通过：设置范围外对象与本轮开始快照逐项一致；原业务脚本一致，未新增重复 Prefab fileId；五档高度 1334/1500/1624/1778/1860 无控件越界或重叠；九图 alpha 与切分像素一致；完整 Prefab 与 17 张共享/新增图片已由 Creator 导入。仅忽略 Creator 对顶层资源 `_name` 的正常补名。
- `node tools/tests/settings_v8_regression.js` 通过 20 项：八组保存状态恢复、语音/音效开关存储、密码入口和预留信息入口停用、关闭重入、防盗号取消和处理中、模拟切换账号。
- 独立网页测试页实例化正式已导入 Prefab，iPhone 6/X 实际画面已核对。七个分隔节点均为 inactive、Sprite disabled、无 SpriteFrame；标题与箭头不重叠。上一阶段使用原 UIViewBase 事件绑定实际点击修改登录密码和返回通过，ToggleEx 开/关视觉也已核对。
- 没有操作真实账号密码、防盗号绑定或退出登录；没有 Creator 构建、真机或发布验证。用户原有预览页未刷新，测试页收尾关闭。
- `git diff --check` 通过。项目记忆检查 17 份活动文档、0 错误、2 条既有体积建议警告；结构通过不代表真实账号业务验收。

移除入口阶段使用 `--baseline art_sources/v8-repairs/settings/before-remove-reserved/panelMain.prefab` 比较，保护其间已改的密码页。五档布局及 20 项回归通过；完整主 Prefab 导入通过后已在 iPhone X 引擎预览确认六项列表、连续间距和入口关闭。之后密码兼容字段单独修正，当前设置子树已再次与 Creator 导入逐项核对一致。`qa/` 静态图同步更新为六项。

## 图标生成来源

参考 `design-previews/效果图V8-new/05-后续模块/03-系统设置.png`，使用本轮 imagegen 工具生成九宫格图标（齿轮、锁、人民币盾牌、资料卡、话筒、喇叭、安全盾牌、退出门、箭头）。透明修正版为 `icons-transparent.png`，哈希及每格裁切范围见 `icons.json`。非透明候选未用作生产资源；切分不改变透明度或图像像素，不通过窄条复制生成底框。

最终采用的透明修正提示词：

> Edit the provided icon atlas: KEEP ALL NINE GOLD ICONS and their exact positions/size/style unchanged. Remove the gray checkerboard completely, including through the gear and door holes. Output genuine transparency using a PNG alpha channel, with completely transparent (alpha=0) empty areas. NO checkerboard visualization painted into pixels. No gray background, no grain, no texture, no floor, no cast shadow outside icons. These are production sprites for compositing onto an existing blue UI. Preserve the gold icon bevels and clean antialiased edges. The only change is replacing every background pixel with true transparent alpha; do not redraw or rearrange the icons.
