# XuanManager 提现开关正式部署

日期：2026-09-13。目录 `/Volumes/SSD/qing`，分支 `main`。用户明确要求“后台网站需要部署更新到服务器上”，范围为上一阶段的三提现开关和 USDT 汇率后台前后端。没有 Git 提交/推送，也没有发布游戏客户端或操作 Cocos。

## 发布状态

- 正式后台：`http://154.37.155.17/xuanmanager/`，Go 继续只监听 `127.0.0.1:8891`，Caddy 原路由保持。
- 服务器实际根目录为 `/www/html/.xuanmanager`，静态目录为根下 `web`，不是 `web/dist`。先核对旧进程、环境白名单字段、资源哈希和实际启停脚本，再执行切换。
- 先在 `candidates/20260913-withdrawal` 启动本机 `8892` 候选，明确关闭轮播、清除候选 bootstrap 密码变量。候选健康、匿名支付配置 API 返回 401、页面/JS/CSS 逐文件一致通过。
- 保留旧版于 `/www/html/.xuanmanager/backups/20260913-withdrawal`（旧二进制、完整静态目录及校验清单）；使用现有 stop/start 脚本切换正式二进制和静态目录，失败恢复脚本已准备。本次切换成功，没有触发回滚。
- 正式服务 PID 为本次观察的 `10444`，正式轮播为 `true`。最终只保留正式服务，候选 `8892` 已停止；PID 是瞬时证据，后续须重新核实。

## 产物证据

Linux amd64 静态编译，沿用上一阶段最终源代码之后通过的 Go test/vet、前端 lint/build；此次无业务源码修改。

| 文件 | SHA-256 |
| --- | --- |
| `bin/xuanmanager` | `8f15b24ffbe75c49862e4b8434a511b4df8b6a61ec0c4b2e7b893ddd04569e78` |
| `web/assets/index-0z3ysDiY.js` | `c2b83711b5ee7ea438f93a81dec982583e2b305600c01f7d36b1eb937f2c507a` |
| `web/assets/index-BuPpJvd5.css` | `0fe8df591d93bcbdf8d21a639da86186d5d2ab8007b8dae025434d5e491eebf7` |
| `web/index.html` | `732f0d7bfcfcdc2e63e5f908288ebd4265fbece5cb45f8715da3fb7c73c03e09` |

上传包、候选解压文件、正式文件均校验成功；公网健康返回 200，首页和两个前端资源的实际响应哈希与上表一致。保留旧的不可变前端 bundle，避免已有标签页资源立即失效；新首页引用新 JS。

## 登录态验证与边界

- 使用用户现有 Edge 后台登录标签页，部署前待保存变更为 0；部署后刷新成功加载支付配置。
- 页面“全局支付与提现设置”显示独立的银行卡、支付宝、USDT 开关，以及 USDT 汇率输入；数据读取完成且待保存变更仍为 0。
- 当时只读结果：三个提现开关均关闭，USDT 汇率为空；本次没有切换开关、填写汇率或点击保存，不替用户设定业务参数。
- 原支付通道数量/启用数量与部署前一致（16/2）；页面登录保持，未创建新账号或重置会话。
- 未执行真实支付/提现、生产配置写入或游戏客户端发布。用户需按实际业务设置并保存开关/汇率，新游戏客户端重新进入提现页读取。

## 部署校验注意

候选直接请求 Go 服务时使用 `/assets/...`，公网 Caddy 才使用 `/xuanmanager/assets/...` 并去除前缀。首次候选检查误用了公网前缀，收到 SPA 首页导致 cmp 不匹配；核对现行 Go 静态处理和 Caddy 配置后改用正确路径，全部通过。没有为了检查调整线上路由。

功能说明与测试覆盖见[实现交接](2026-09-13-wallet-withdrawal-controls.md)。后续部署重新核对当前实例及哈希，不把本文件中的 PID/版本直接当作实时状态。
