# 大厅房间列表底框重做（2026-09-15）

- 目录/分支：`/Volumes/SSD/qing`，`main`，起点 `e2940da`；本轮改动未提交。
- 目标：用户反馈大厅 → 发现 → 房间列表每行“房间底框”抠图锯齿；先要求修复，退回后要求用 Seedream 重新生成。

## 过程

1. 首次只修抠图：`tools/repair_v8_lobby_room_card.py` 按定稿轮廓重抠（内容派生抗锯齿遮罩），UUID/Prefab 未变、源像素校验通过；用户否决。
2. 判定根因在源图：卡片是从 941×1672 效果图里切出的，边缘本身就是软光，再抠也难锐利。
3. Seedream 5.0 出两版候选（2K，输入=当前卡片+定稿大厅上下文两张参考图），提示词与候选存 `gen/`；采用 `cand2.png`。

## 结果与验证

- 字核（铁律）：`BL POKER`、`8L`、`POKER GAME`、`底皮`、`局数` 逐字放大核对全部正确，无错别字/缺字。
- 布局：候选与旧图的徽章、标签、图标横向位置相对卡片宽度偏差 <0.5%，Prefab 动态数字位置不用改。
- 抠图：`tools/repair_v8_lobby_room_card.py` 重建正式资源（886×136，`cand2` 裁 `(496,325,3343,765)`，4x 超采样轮廓遮罩 + 真实外发光），UUID 未变，Prefab 未动。
- `tools/v8_lobby_crops.json` 移除 room_card 条目：该图不再是定稿直切，源像素一致性规则不再适用。
- 校验：`validate_v8_pages.py` 的 crops 检查全过；`list boundaries` 断言改动前即失败，与本次无关。
- 用户复反馈“内部不干净”，已在同一工具内加入 `clean_interior()`：中值 9 + 高斯 4 压平背景噪纹，`|rgb-模糊|` 膨胀 11px 的细节掩码保留徽章/文字/图标/描边；背景不再有斜向噪纹。
- 证据：`art_sources/v8-repairs/lobby-room-card/`（`before-after.png`、`clean_vs_raw.png`、`gen/`）。

## 未完成项与下一步

- 未在 Creator 内确认重新导入（尺寸与上一次一致 886×136，仍需切回 Creator 触发刷新）。
- 未构建、未出热更包、未真机；新卡片相对定稿效果图的色调略有提亮，需用户视觉验收。
- 卡片仍按 130pt 高度非等比绘制（定稿折合约 108.4pt），属既有布局取值，本轮未改。