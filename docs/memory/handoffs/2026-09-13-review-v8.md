# 结算内牌局回顾 V8-new 换肤

日期：2026-09-13；目录 `/Volumes/SSD/qing`；分支 `main`。在已有结算换肤及其他未提交修改上继续实施；本次未提交、推送、构建或发布。

## 最终实现

- 用户要求结算内回顾匹配 V8-new 色调、复用结算背景、文字清楚并保留扑克牌。正式修改 `assets/resources/UI/panelRecordInfo.prefab` 的“牌局回顾”子树，以及 `assets/resources/Prefabs/回顾对象2.prefab`、`文字牌谱对象2.prefab`；本轮未修改业务脚本。
- 共用 `lobby_scene_long_v8.png` 长背景，复用正式蓝色面板、浅金选中标签和圆形分页按钮。标题、信息条、图例、切换标签、分页独立组成；顶部与底部固定，中段沿用原 ScrollView。
- 玩家名、手牌类型、分数及文字牌谱使用项目字体与浅金色，增加暗色细描边。修正头像框遗留 Widget 导致的纵向拉伸；扑克图片、花色、尺寸与动态加载保持原实现。
- 用户最新截图修正：第一轮/第二轮/第三轮标题底框保留；每条操作行关闭根 Sprite，改为透明底及下方 676×2 横线。姓名、操作数额和剩余列维持 24 号字。
- “大、跟、敲”等决策图片原始尺寸为 49×36，旧节点强制 42×42 导致变形。现由正式 Prefab 设置普通 Sprite、RAW 尺寸模式、等比缩放，沿用原动态决策图片和 `setPaiPuItem`。
- 检查期间用户在 Creator 保存了 `回顾对象2`：四张牌下方高亮线的位置/高度及序列化顺序发生变化。已保留该版，不以旧快照覆盖。快照见 `art_sources/v8-repairs/review/before/回顾对象2.user-saved.prefab`；最终文字行调整仅写 `文字牌谱对象2`。

## 验证

- `python3 tools/validate_v8_review.py --imports` 通过：结算子树之外保持原样，原节点路径/事件/业务脚本保留，当前用户保存的扑克牌子树未再变动；3 份 Prefab 与 12 个复用资源和 Creator 导入内容一致。检查按节点路径解析引用，兼容 Creator 保存后的对象重排。
- `node tools/tests/settlement_v8_regression.js` 原有 21 项回归通过；`git diff --check` 通过。
- Creator 2.4.13 已定向刷新正式资源；最终文字行刷新取得 `V8_TEXT_ROW_FINAL OK`。
- 独立网页预览以正式 Prefab 创建内存样例，在 iPhone X（750×1624）和 iPhone 6（750×1334）检查回顾与文字牌谱。三轮文字不重叠，操作行无底框、分隔线可见；原 `setPaiPuItem` 加载的大/跟/敲均实测 49×36，Label 字号/实际字号均为 24。
- 使用原按钮/Toggle 事件检查两种视图切换、下一页→尾页→下一页边界→上一页→首页→上一页边界，页码结果为 2、3、3、2、1、1；返回结算、重新进入回顾通过。短屏滚动到末尾时 offset=maxOffset=171，最后玩家和固定底栏均可见。
- 样例替换了数据查询回调，未发送真实牌局查询、排队或资金请求。真实服务端回包、原生构建和真机仍待验证。临时预览页已关闭，用户原有预览未刷新。

## 后续入口

- 作者工具 `tools/apply_v8_review.py`；只修文字行使用 `--text-rows-only`，避免重写用户正在编辑的其他区域。
- 本条替代旧回顾 `wallet_bg_exact.png` 背景与每条文字操作独立圆角底框的实现；“第一/二/三轮”标题底框继续保留。
