# 2026-09-20 桌内通用弹窗 V8 换肤

目录 `/Volumes/SSD/qing`，分支 `main`，本轮开始 HEAD `f04a05b`。未提交、未推送。

## 实施范围

- `panelGameView.prefab` 与 `drh8.fire` 同步：系统设置、芒果/扣费/解散/GPS/举报扣费提醒、举报窗口、内嵌排队、牌型提示外框。保留牌型内容、输入、动态文字、Toggle/Button事件和两秒提示动画。
- 独立 `panelQueueMatch`、`panelMsgView`、`panelRoomInvite`、`panelVipInfo`、`panelTalk` 六份Prefab（含panelGameView）组件化蓝金换肤。十个既有表情保持。
- 用户追加明确 `panelGameView/提示` 保留原半透明样式；Prefab/Scene该子树逐对象恢复本轮HEAD且迁移脚本不再处理它。
- 新增两张无文字的共享九宫格：标题144×58/边距25、金按钮96×66/边距16，总13448字节。复用既有V8底框、次按钮、关闭、开关、内容框；所有文字保留为组件。见 [资源及预览说明](../../../art_sources/ingame-dialogs-v8/README.md)。

## 验证

- 七个正式序列化文件引用/JSON/业务路径检查通过；桌内目标子树Prefab/Scene一致，迁移工具可重复运行；用户指定提示子树与HEAD一致。
- 原嵌字Sprite转Label需解除原Sprite的节点组件引用，仅disabled会在Cocos2.4中抢占渲染组件。离线引擎发现设置四项文字未显示，解除后已实际截图复查恢复；VIP同类节点同步修复；同时清理固定布局子节点遗留Widget，避免运行时把标题/内框拉伸到屏幕外。
- 所有弹窗静态渲染、单双按钮、排队空/两行模拟列表、邀请、VIP确认、表情面板检查；切图透明边缘含抗锯齿像素且内部透明孔洞0。
- 通过本地只读代理向Cocos预览提供正式源Prefab，隔离业务组件后检查设置、排队、邀请、芒果动态文字、举报输入区、会员字段、表情和消息单双按钮。此方式没有改Creator library缓存，不等于Creator正式重新导入或真实登录业务验证。
- Creator现有场景有未保存标记，原生工具未能可靠打开资源刷新控制台；没有保存旧场景覆盖本轮新文件，没有强制重启编辑器。重新打开项目后应先确认外部文件导入，再做真实长短屏/交互验收。
- 未真实登录、未扣费/购买、未构建、未真机、未发布。

## 后续验收

1. Creator重新导入两张PNG、六份Prefab和drh8.fire，避免保存旧的未刷新编辑器场景覆盖磁盘。
2. 游戏内点菜单设置/排队、触发提醒，确认关闭与单双按钮；检查长短屏、选择牌背、音效语音、举报输入与列表。
3. 真实VIP/队列/邀请服务端回包与真机另行验收，不把本轮模拟数据当业务成功。
