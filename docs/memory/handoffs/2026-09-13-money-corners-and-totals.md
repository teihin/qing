# 金币流向累计项与圆角修复

日期：2026-09-13；目录 `/Volumes/SSD/qing`；分支 `main`；保留全部既有未提交修改，未提交、未推送。

> 后续纠正：前次“返回并恢复金币页”为手动导航，并未覆盖钱包关闭自动回到来源。该缺陷及初次金额深色问题已另行修复，见[返回与金额状态](2026-09-13-wallet-return-and-amount-state.md)。

## 目标与原因

用户提供当前页面及效果图对照，要求去掉没有数据的累计充值/赠送/提现，并修复底框圆角及显示差异。唯一源图为 `design-previews/效果图V8-new/05-后续模块/02-金币流向.png`。

旧金币页复用了提现的 `wallet_v8_panel.png`/输入行底框，其提取工具的圆角半径 44 大于九切保护边 32，部分圆弧进入拉伸区；源图本身还有边缘偏移，造成双边线和角部变形。标题栏、分页也采用钱包组件，与金币页定稿不同。

## 改动

- `assets/resources/UI/panelMain.prefab` 仅调整资金明细子树：累计标题、数值及分隔节点关闭，概况卡缩短、记录区上移；保留真实当前金币与充值入口。专用源图标题栏保留原静态箭头/标题，独立返回按钮作为透明热区。
- `assets/resources/Prefabs/资金明细对象.prefab`：专用圆角行底框、静态列分隔，时间/类型/金额/余额和类型图标仍为独立节点。
- `assets/resources/V7/money_v8_{header_exact,summary_panel,list_panel,row_panel,page_value,table_exact,page_first,page_prev,page_next,page_last,row_rule}.png` 及 `.meta`：11 张金币页专用组件。底框先按 750 逻辑宽等比缩小，再设置完整保护角区；只拉伸空白中部。清空样例的列表底色来自原图没有记录和按钮的间隙行，避免四边亮色插值产生黑块。场景继续使用已有共用 `lobby_scene_long_v8.png`，没有重新生成背景。
- `assets/scripts/UI/panelMain.ts`：将后台已明确分类为游戏流水的“带入/打局/结算/芒皮/揍芒/休芒”匹配到既有游戏图标；金额、正负、文字和接口不变。
- `tools/repair_v8_money_art.py`：本页专用可重复执行修复；`apply_v8_wallet_pages.py` 金币入口转交该工具，避免以后恢复被否决的底框/累计项。`validate_v8_wallet_pages.py` 更新累计隐藏及表头空白材质少量重叠的检查口径；旧全钱包校验仍受此前用户保存后组件下标变化影响，本次没有运行它。
- 改前两个 Prefab 在 `art_sources/v8-repairs/money-corners/before/`；提取来源和哈希在同目录 `cuts.json`，四档静态合成及结构结果在 `qa/`。QA 样例只用于内存合成，未写入运行 Prefab。

## 验证

- 与本次基线逐对象比较：Main 的非资金明细对象、原 Button/ClickEvent 和控制器序列化数据均未改变。
- 1334/1624/1778/1860 四档布局通过：概况、记录标题、表头不遮挡；列表内部遮罩与固定分页分离。
- 11 张正式 PNG 和 Creator `library/imports` 图像逐字节一致；导入的 Main Prefab 已包含新页码底框。
- `node tools/tests/v8_money_record_regression.js` 通过 10 项；含实际“结算”“带入”类型、正负金额、余额、日期、未知类型和钱包状态映射，两个控制器转译通过。
- 现有 Creator 2.4.13 / `localhost:7456` 刷新后，实际账号余额及两条流水正常；iPhone X 与 iPhone 6 实际网页均显示新组件，累计项已消失。关闭并从“我的 → 金币流向”重入通过；1/1 的首页/上一页/下一页/尾页边界保留既有提示及页码。“金币充值”入口已打开钱包充值页，随后返回并恢复 iPhone X 金币页；没有点击确认充值。真实多页、溢出滚动未具备数据条件。
- Python 语法、本批差异空白检查通过。没有构建、真机或发布，没有提交充值或任何交易。
- 项目记忆只读检查：17 份活动文档，0 错误；CURRENT 与 v7-ui 两份文档超过建议字节预算，属于文档长度警告，不代表业务失败。

当前余额/充值入口继续保留、累计三项移除，是本次用户要求，替代早期设计期“仅明细与分页”和前批“累计显示 —”的描述。后续以此页正式 Prefab 和用户最终视觉反馈为准。
