# 代理与我的共用游戏推广

日期：2026-09-13。目录 `/Volumes/SSD/qing`，分支 `main`。未提交、推送、构建或发布；保留同工作区其他修改。

## 用户要求与实现

用户明确要求代理“游戏推广”使用“我的”现有推广界面，停用原代理独立页。本要求替代代理 `15-代理-推广二维码.png` 的单独实施目标；本轮不重新制作“我的”推广美术。

- `assets/scripts/UI/panelMain.ts`：统一 `OpenPromotionPanel`，两个入口使用同一个正式 `panelMain/推广二维码` 节点实例。每次打开刷新真实推广 ID、链接和二维码，并先清空 Graphics，避免重复叠画。
- `assets/scripts/UI/panelHongli.ts`：游戏推广调用大厅共用页，临时隐藏当前代理节点；关闭推广时恢复原代理实例，保留原列表/分页状态。从“我的”打开则返回“我的”。缺失/未启用大厅时保留代理页并提示重试。
- 复制 ID、复制地址、分享海报和保存相册沿用“我的”原按钮与处理逻辑。未复制另一套页面，也未改推广子树的 Prefab、美术、布局或绑定。
- 原 `panelHongli/推广二维码` 保持关闭，旧二维码/分享分支删除；保留旧节点和资源 UUID 以免破坏既有资源契约。代理生成工具不再重新制作独立页，渲染工具排除它，校验要求它关闭。推广配置开关仍生效，开关变化后重新计算现有 Grid，使隐藏按钮后继续补位。

## 验证证据

- `node tools/tests/promotion_shared_regression.js`：14 项真实控制器＋完整正式 Prefab 节点树回归通过，涵盖两个入口引用同一对象、返回来源、反复开关/账号数据刷新、二维码清空、复制/保存、来源销毁/脱离、大厅缺失和推广配置补位。网络、剪贴板和截屏外部动作全部 mock。
- `node tools/tests/agent_v8_regression.js`：25 项代理回归；`agent_v8_layout_engine.js`：真实 Cocos Layout 的 3/4/5/6 按钮共 5 种组合；钱包返回 12 项、设置 20 项均通过。
- `python3.13 tools/validate_v8_agent.py --imports`：18 个独立代理视图、五档高度、63 纹理、数字字体及六份正式 Prefab 与 Creator 导入一致。两个正式脚本的本地 quick-scripts 编译文件已含共用入口。
- 独立浏览器标签页载入正式 Prefab 和最新编译控制器，使用测试 ID `123456` 和 `https://example.test`，隔离页面初始化中的账号/网络动作。iPhone X 实际点击代理游戏推广→返回代理，再点击“我的”推广→返回我的，检查均通过；两次打开为同一节点，旧代理页保持关闭。
- `art_sources/v8-repairs/promotion-shared/agent-1624.png` 与 `mine-1624.png` 为网页实际截图，逐像素比较完全相同。`mine-1334.png` 为 iPhone 6 推广布局及返回检查；`agent-home-1334.png` 仅记录代理首页，不能算短屏代理打开推广成功的截图。短屏第二次代理点击未命中，未计入成功项；长屏两个入口及短屏共用页分别已验。测试标签页已关闭，用户原预览未修改。

真实登录账号、手机相册保存/分享和构建/真机仍待验证；浏览器样例及 mock 测试不代表上述操作已经完成。

## 后续

本次已解决上一轮记录的 Creator 缓存未刷新问题；代理新版首页可见，所有代理列表/弹窗的最新视觉和真实账号回包仍按[代理交接](2026-09-13-agent-v8.md)单独验收。
