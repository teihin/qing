# 排行榜改为无限滚动

目录 `/Volumes/SSD/qing`，分支 `main`。用户于 2026-09-14 明确要求接近底部提前加载下一页、去掉分页组件，替代此前的分页要求。此前换肤及未提交改动保留；未提交、推送、构建或发布。

## 当前改动

- 三个榜单共用 `LeaderboardScrollView.ts`，直接继承原生 `cc.ScrollView`。距底部不足 0.75 个视口（至少 360 逻辑像素）时预取；每批仍按原协议请求 30 条。追加使用正式行模板与原生 Layout，不改静态美术；保持顶部锚点，追加不停止惯性、不跳回顶部。
- 一次只请求下一批；已在途、已结束或隐藏的榜单不重复发起。兼容行内/顶层 count、CS rowsTotal/pageNum；无总量时根据满批/短批判断。空尾批、重复整批停止，跨批重叠用户去重。数据不足视口时继续预取。
- `panelPaihangbang.ts` 保留六类原始查询、手数档位参数和领取条件。切换主榜/皮数递增请求代次、清空行/滚动位置，同一回包通道串行处理旧请求后再发送最新目标，避免旧档位数据追加进新列表。
- 返回错误或无效数据会释放加载锁，保留已显示行，下一次触摸/滚轮手势重试失败批次；首批失败显示“加载失败，请滑动重试”。断线/重进从首批重新加载。
- `排行榜.prefab` 三个分页根节点停用并移除 PageEx 组件引用，保留历史节点名/UUID。表格底框底边由 144 改到 28，列表底边由 158 改到 42，列表增加 116 逻辑像素；大厅背景、三主 Tab、1–20皮金色选中层和独立圆角资产不变。
- `tools/apply_v8_leaderboard.py` 同步最新布局；本次只对正式 Prefab 作定向几何/组件修改，没有重跑全页生成器。只读检查和模拟预览脚本同步无限加载；旧分页资产未删除。

## 验证与明确边界

- `node tools/tests/leaderboard_v8_regression.js`：15 个生产代码模拟场景通过。覆盖三榜 30→60→61、提前触发/单请求、追加保持偏移、不重复追加、末尾/空列表、元数据兼容、快速切档/离开再回来、失败手势重试、断线重进、选中层与领取参数。全部请求为模拟，没有实际领奖。
- `python3 tools/validate_v8_leaderboard.py`：正式组件引用、1180/1280/1334/1624/1800 五档高度、无分页且圆角留边、字形/图片哈希和透明角检查通过。
- 三个相关 TypeScript 文件定向类型检查通过（排除项目既有 require 声明缺失）。本地 Creator 2.4.13 引擎源码确认顶部锚点与高度增加行为；这不替代实际网页滚动。
- **Mac 锁定，CUA 提示自动解锁失败。Creator imported Prefab 仍显示旧分页，quick-scripts 仍是旧分页实现，本次未导入验证。** 没有手工篡改 library/temp 缓存。此前 `preview/` 的长短屏截图属于分页版本，本次不沿用为无限滚动证据。
- 下一次解锁后激活现有 Creator 触发资源导入，确认三个分页 inactive、脚本含 checkNearEnd，再完整刷新 localhost:7456。使用 `tools/preview_v8_leaderboard.js` 的独立模拟预览检查长/短屏连续滑动、近底预取、追加无跳动、五档切换和失败重试。真实账号回包/领奖、构建、真机均待验。

用户另有当次“完成任务之后关闭计算机”的明确请求。收尾尝试 sudo -n /sbin/shutdown -h +1，系统返回需要管理员密码，退出码 1，**没有安排或执行关机**；Mac 同时处于锁定状态，不能完成界面关机。未收集或尝试密码，也未以睡眠代替关机。需用户解锁后完成关机；当次关机授权不跨任务保留。
