# 钱包其余页面与金币流向 V8 换肤

日期：2026-09-13；检出 `/Volumes/SSD/qing`；分支 `main`。本批为用户最新明确指定范围，未提交、未推送，未操作 Cocos Creator。

> 后续更新：本页金币流向的累计占位、钱包共用底框及“Creator 未操作”状态已由[金币流向修复](2026-09-13-money-corners-and-totals.md)替代；钱包其余页面的历史验证范围不变。

## 范围与依据

- 充值渠道按实际行数收紧已完成，见[充值交接](2026-09-13-wallet-recharge-v8.md)。
- 随后按用户要求处理 `效果图V8-new/03-钱包/02-提现.png`、`03-记录.png`、`04-实名认证与交易密码设置.png` 和 `05-后续模块/02-金币流向.png`。
- 全部复用 `lobby_scene_long_v8.png`；背景允许与效果图不同。按钮、标题、图标、空面板及真实数据独立，不再使用整页效果图作背景。银行选择、充值资料与订单详情没有单独新稿，复用同组面板与按钮材料统一样式。

## 实施

- `assets/resources/Prefabs/钱包.prefab`：提现余额卡、三种提现方式、五个默认输入、可选支行、提示及提交按钮；记录表头、内部遮罩列表和固定分页；实名认证的独立盾牌、两级标题、五输入、确认按钮与完整四条重要提示；附属银行选择和订单详情统一处理。
- 提现表单按新稿高度紧凑排列，长手机不再把大量空白塞进表单。服务器要求支行时，六行在原表单高度内排布，切换模式恢复原位置；旧银行回包不会把支行显示到其他提现方式。
- `assets/resources/UI/panelMain.prefab` 的 `资金明细`：共用长背景，独立盾牌、当前金币、充值入口、累计统计占位、记录标题、表头、内部滚动列表及固定分页。仅本分支的美术改变。
- `交易查询对象.prefab`、`资金明细对象.prefab`、`银行对象.prefab` 复用空行底板，动态金额、类型、时间、状态仍是 Label。资金流水的增减依据原服务器余额差计算，正数绿、负数红；日期只压缩显示为月/日时分，不修改原数据。类型图标仅按真实类型显示；未知类型不虚构分类。
- 金币流向当前余额接入既有金币字段，充值按钮复用既有钱包入口。当前接口未找到累计充值/赠送/提现字段，三项显示 `—`，没有写入效果图样例金额，也没有把单页流水误当累计值；已向用户异步说明，尚未收到接口信息。
- 充值/提现协议、权限、输入长度、密码模式、实际订单状态及原有“暂未开通”充值拦截保持不变。

## 资源与工具

- `tools/apply_v8_wallet_pages.py` 只读取上述 V8-new 图，按实际坐标切图并写入正式 Prefab；49 张独立组件 PNG 共 1,710,056 字节，无新增场景背景。
- 37 张已无资产/运行脚本引用的旧钱包及资金流向图片共 3,712,552 字节已移出 `assets/resources`，PNG 净减少 2,002,496 字节；这是资源体积统计，未构建安装包。备份在 `art_sources/v8-repairs/wallet-pages/retired-assets/`，改前 Prefab/脚本在同级 `before/`，清单在 `retired-candidates.json`。
- 已移除 `tools/v8_wallet_crops.json` 中整页实名认证背景及旧标题的生成项。旧 V7 钱包/资金流向生成器和校验器依赖已退役资源，仅作历史线索，不应再运行；后续应使用本次 V8 应用工具。不要为了运行旧工具恢复被删除的历史效果图或资源。
- `cuts.json` 记录新图来源、裁切坐标、生成方式和哈希；`qa/` 为离线合成，不进入运行时资源目录。

## 验证与限制

- `python3.13 tools/validate_v8_wallet_pages.py` 通过：49 份图像来源/像素/元数据、20 个既有 EditBox 的模式/长度/事件契约，四页在 1334/1500/1624/1778/1860 五档逻辑高度的共 20 个布局检查；表头/分页不进入滚动遮罩，背景覆盖长屏。
- `node tools/tests/wallet_channel_selection_regression.js` 通过：9 个序列化通道标记、16 项选择/回包测试、75 项动态渠道高度、15 项可选支行布局检查。执行实际 Cocos Grid/Toggle 代码，没有启动 Creator。
- `node tools/tests/v8_money_record_regression.js` 通过：8 项真实值、符号、日期、类型图标、状态映射；两个控制器转译通过。
- 查看了正式 Prefab 合成的提现、记录、实名认证、金币流向短/长屏，以及订单详情、银行选择框。离线预览里的记录样例仅存在于工具的内存副本，未写入运行时 Prefab。
- Python 语法及本批 `git diff --check` 通过。用户明确要求自行验证，故 Creator 导入、网页交互、真机及构建均未执行，不能称为像素级实机验收。

## 收尾

本批指定页面的资源实施和离线检查完成。用户本次再次明确要求完成本批后关机；收尾发起系统正常关机，避免强制退出造成未保存内容丢失。此指令仅适用于本次，不代表其余 56 图全套已完成，也不跨任务继承。
