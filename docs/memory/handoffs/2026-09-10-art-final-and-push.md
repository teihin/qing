# 2026-09-10 美术定稿与提交推送交接

## 目标与基线

- 用户确认美术已定稿，并手动删除历史设计以避免后续读错；要求提交并推送所有项目改动。
- 实际目录 `/Volumes/SSD/qing`，分支 `main`，本轮开始基线 `4391b17`，远端 `origin` 对应 `teihin/qing`。提交、推送由主任务执行；本交接记录文档阶段状态，不代表 Git 操作已经完成。

## 定稿与范围

- `design-previews` 的图片仅保留 `效果图V8-new/`，共 56 张 PNG；七组数量为 6/5/4/5/26/7/3，明细见[定稿入口](../../../design-previews/README.md)。Git 初始差异为删除 175 个历史设计文件、新增 56 张定稿。
- 新增 `tools/v8_grade_previews.swift` 为既有预览校色工具：根据显式输入/输出参数写 PNG，未在本轮执行。定稿后不得用其自动改写确认图。
- 本轮未改动正式 `assets`、`art_sources`、Prefab/Scene、业务运行脚本和 `build-templates`；设计确认不等于 V8-new 已迁移进游戏。现有 V7 实施状态与真实账号、Creator 构建、真机等缺口继续保留。
- 根入口、当前状态、决策、专题索引、V7 专题头部统一指向 V8-new；旧图路径只保留为历史实施证据，不能恢复旧图或套用旧工具裁切坐标。编辑前完整 CURRENT 已[原样归档](../archive/2026-09-10/README.md)，2026-09-05 不可变归档未改动。
- 主任务正在将本地 Codex 修复备份排除出提交并保留本地；该目录含会话/数据库备份，不属于游戏项目交付内容。本子任务未读取其文件内容，也未操作 `.gitignore` 或 Git 状态。

## 验证与边界

- 只读目录与 Git 路径核查：56 张新稿、175 个旧设计文件删除；`assets`、`art_sources`、`build-templates` 无本轮差异。
- `assets`、`build-templates`、`project.json`、`settings` 内检索 `design-previews` / `效果图V8` 无命中，未发现游戏运行时直接依赖被删除设计路径。
- 旧工具仍含已删除设计路径；`tools/validate_v7_login_exact.py` 明确先断言旧登录稿存在，现已不适合作为当前默认检查。未为通过旧工具恢复图片、修改运行资源或切换到新图后套旧坐标。
- 文档更新前针对 `design-previews`、`tools`、CURRENT 的 `git diff --check HEAD` 通过。文档最终检查结果在实际执行后补充。
- 未运行美术生成器、Creator 导入/预览、构建、真机或业务测试；本轮仅设计文件保存、文档整理和 Git 交付。

## 下一步

- 主任务完成最终差异、备份排除和静态检查后，提交并推送已授权的项目改动，核实本地提交与远端分支一致。
- 文档阶段提交/推送均为待执行；远端 `ls-remote` 的自动审批曾因审核模型容量暂时失败，不能据此记录推送成功。最终状态以主任务实际 Git 结果为准。
- 下一次正式换肤任务，从对应 V8-new 定稿和实际 Prefab 开始逐页实施，不把本轮定稿误写为已完成全部游戏美术迁移。
