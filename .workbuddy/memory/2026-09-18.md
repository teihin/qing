# 2026-09-18

## 游戏改名 BL → BY：login_shield_exact 盾牌图

- 目标图：`assets/V7/login_shield_exact.png`（417×514 RGBA，带透明通道，四角 alpha=0）。
  图内旧名两处：中央金色大 LOGO「8L」（=BL 艺术写法）、顶部小字「BL POKER」；底部「POKER GAME」为通用词不改。
- 用户确认规则：两处都改 → 中央 `8L→BY`，顶部 `BL POKER→BY POKER`。

### 关键结论（重要）
- **单纯图生图改不了「效果不变」**：Seedream 4.5 会把整只盾牌重画——轮廓、金边层数、蓝菱格尺寸、底部黑桃变黑。
  试了 7 版提示词（含透明底输入、显式保真清单、局部重绘框架），最好的一版（gen_D）轮廓接近但仍是重绘。
- **Ark 生图 API 无 mask/inpaint 参数**，做不到真正的局部重绘。
- 因此改用**像素级合成**：原图当底，只重建文字区背景 + 贴新字形，其余像素（盾牌、金边、材质、透明通道）原样不动。
- 产物：`art_sources/rename-BY/compose/login_shield_exact_BY.png`（417×514 RGBA，可直接覆盖原文件，`.meta`/UUID 不变）。

### 踩过的坑（复用要点）
1. **4.5 的 size 下限不是 1280×720**，实测报错要求 ≥ 3,686,400 px。显式像素尺寸要按这个来（本次用 2048×2524）。
2. `np.nonzero` 按行序返回，`a[0]`/`a[-1]` **不是 min/max**，取包围盒必须用 `.min()/.max()`——曾因此把字形切片切空。
3. 旧文字周围有暗色投影，归一化卷积填背景时会从「暗环」插值导致整块发黑；改用**离掩码 ≥14px 的远场做三次多项式曲面拟合外推**才干净。
4. 大 sigma 的归一化卷积会把远处金色边框颜色拉进来，得到灰褐色，务必限制采样范围。
5. 蓝底是**不规则有机纹理**（不是规则菱格），无法平铺，用「干净区频谱 + 随机相位」合成高频纹理。

### 字形来源
- 从 gen_D（透明底输入那版）抠出金色 `BY`（1160×768）与顶部 `Y`（88×117），按 R−B 软阈值做 alpha，
  缩放到原图尺寸后合成；中央 BY 目标 234×155，中心 (203,228)；顶部 Y 19×25，中心 (170,107)。

### 用户裁定（10:46）
- **合成版被否**：`login_shield_exact_BY.png` 底下残留旧「8L」的影子。根因是文字掩码用「金色」判定（R>B+18），
  旧字的**暗部/倒角笔画**不满足该条件没被识别，dilate 4px 也盖不住。若今后还要走合成路线，掩码必须改成
  「偏离平滑蓝底模型」而非单纯金色判定。
- **采用 AI 重绘版（gen_D）**，用户认可「alt_D_transparent 这版没问题」。
- **硬约束：新图不能比原图大太多，会影响安装包体积。** 原图 417×514 = 351 KB。

### 最终产物
- `art_sources/rename-BY/compose/final/login_shield_exact.png` —— **417×514 RGBA，207 KB**（比原图小 41%）。
  做法：gen_D 抠透明底 → LANCZOS 缩到 417×514 → RGB 每通道 6bit 量化（÷4×4）+ optimize/compress_level=9。
- 量化前后在 3 倍放大下无可辨差异；深度/品红底合成检查边缘无浅色描边。
- 母版：`art_sources/rename-BY/compose/alt_D_transparent.png`（2048×2524 透明底）。
- 对比图：`final/对比_原图_vs_新图.png`。

### 踩坑补充（抠透明底）
- gen_D 背景是均匀浅灰（≈244,244,246），**不是真棋盘格**（那是预览器叠加的）。
- **`ImageDraw.floodfill` 在本机 Pillow 12 上完全没生效**（返回后一个像素都没改）。
  可靠替代：S=8 粗网格保守池化（全格为浅色才算背景）→ 从四边种子做迭代膨胀求连通域 → 上采样 → 再按浅色掩码膨胀补边。
- `np.nonzero` 首尾元素 ≠ min/max，包围盒必须用 `.min()/.max()`。

### 已完成（10:51）
- **已替换 `assets/V7/login_shield_exact.png`**：211,992 字节（207 KB），417×514 RGBA，四角 alpha=0。
  `.meta` 未改动（原本就是 417×514），UUID 不变。git 状态为「已修改、未提交」。
- 原图未另做备份——该文件受 git 跟踪且替换前是干净状态，可用 `git checkout -- assets/V7/login_shield_exact.png` 还原。
- **中间产物已全部清理**：`art_sources/rename-BY/` 下只保留最终产物 `login_shield_exact.png`，
  已删除 gen_A~gen_E、generated、compose（含 debug、母版、被否的合成版、对比图）、compose_shield.py。

### 待办 / 下一步
- 游戏内其他带旧名的图片待逐个处理。约定：完成即清理中间产物，只留最终成品。

## mine_hero_exact 改名（第二张，已完成）

- 图：`assets/V7/mine_hero_exact.png`，1023×459 RGB**不透明**（无透明通道），原 846 KB。
  内容是完整场景大图：顶部中文健康提示条 + 喇叭图标、赌场休息室夜景、中央金色盾牌、右侧 CASINO 招牌。
- 图内旧名两处：盾牌中央 `8L`、盾牌上方 `BL POKER` → 改为 `BY` / `BY POKER`。
  `POKER GAME`、`CASINO`、四角星、黑桃、中文提示条均为通用内容，不动。
- **本次纯图生图效果很好，没走合成**：4.5 图生图（`2880x1292`，按原图 2.229 宽高比）一次成功——
  场景、中文提示条、CASINO 招牌基本零漂移，只有盾牌被重绘（略微放大）。
  → 结论：**场景类大图可以放心走纯图生图**，提示词里把要保留的元素逐条列全即可。
- 后处理：LANCZOS 缩回 1023×459 → RGB 6bit 量化 → optimize/compress_level=9 → **387 KB（−54%）**。
- `.meta` 未改动，尺寸一致（1023×459，trimX/Y=0），UUID 不变。
- 未提交（用户未要求）。
- 中间产物已清理，`art_sources/rename-BY/` 只留 `login_shield_exact.png` 与 `mine_hero_exact.png` 两个成品。

## nav_bar_exact 改名（第三张，已完成）

- 图：`assets/V7/nav_bar_exact.png`，941×153 RGBA，**带透明底**（左上、右上角 alpha=0），原 222 KB。
  内容：底部导航条，四个图标+中文标签「公告/客服/钱包/我的」，中央凸起的金色盾牌里是 `8L`。
- 只改盾牌里的 `8L → BY`；四个中文标签、四个图标、导航条本体与金边全部保留。
- 生成尺寸：宽高比 6.15 极宽，按 4.5 最小像素算得 **4762×775** —— 单边超过 4096 **服务端照样接受**，
  不用为了迁就 4096 去裁切或加白边。这条已补进 `art-text-swap` 技能。
- 透明底做法：先把原图压到白底做输入参考 → 生成时要求「纯白背景、条体位置尺寸不变」→
  生成后按「近白 + 与边缘连通」求背景连通域 → 反相得 alpha → 高斯模糊 1.2 抗锯齿。
  结果不透明占比 0.896，与原图完全一致；四角 alpha 0/0/255/255 也对上。
- 后处理：LANCZOS 缩回 941×153 → RGB 6bit 量化 → **100 KB（−55%）**。
- `.meta` 未改动，尺寸一致（941×153），UUID 不变。
- 中间产物已清理，`art_sources/rename-BY/` 现留三个成品。

## 三张完成情况
| 图 | 原体积 | 新体积 | 变化 |
|---|---|---|---|
| login_shield_exact | 351 KB | 207 KB | −41% |
| mine_hero_exact | 846 KB | 387 KB | −54% |
| nav_bar_exact | 222 KB | 100 KB | −55% |

已提交：`login_shield_exact`（commit 7e5eef9）。
待提交：`mine_hero_exact`、`nav_bar_exact` 及本次约定更新。

## announcement_shield_v8_exact 改名（第四张，已完成）

- 图：`assets/V7/announcement_shield_v8_exact.png`，340×407 RGBA 透明底，原 277 KB。
  V8 定稿版的盾牌徽章：顶部 `BL POKER`、中央 `8L`、底部 `POKER GAME`、金黑桃。
- 改成 `BY` / `BY POKER`，`POKER GAME` 与黑桃不动。生成尺寸 1755×2101（宽高比 0.835）。
- 白底抠透明流程跑通，不透明占比 0.679（原图 0.677），四角 alpha 全 0。
- 体积 **277 KB → 124 KB（−55%）**。`.meta` 未改动，尺寸一致，UUID 不变。
- 白底抠图的通用后处理脚本（粗网格连通 + 6bit 量化）在 /tmp 用完即删；
  逻辑已固化进 `art-text-swap` 技能，下次直接按技能里的代码走。
- 中间产物已清理。

### 四张完成情况
| 图 | 原 | 新 | 变化 |
|---|---|---|---|
| login_shield_exact | 351 KB | 207 KB | −41% |
| mine_hero_exact | 846 KB | 387 KB | −54% |
| nav_bar_exact | 222 KB | 100 KB | −55% |
| announcement_shield_v8_exact | 277 KB | 124 KB | −55% |

### 五张完成情况
| 图 | 原 | 新 | 变化 |
|---|---|---|---|
| login_shield_exact | 351 KB | 207 KB | −41% |
| mine_hero_exact | 846 KB | 387 KB | −54% |
| nav_bar_exact | 222 KB | 100 KB | −55% |
| announcement_shield_v8_exact | 277 KB | 124 KB | −55% |
| agent_v8_shield | 47 KB | 34 KB | −28% |

## agent_v8_shield 改名（第五张，已完成，11:40）
- 图：`assets/V7/agent_v8_shield.png`，141×164 RGBA 透明底，原 48,059 字节。
  与 `announcement_shield_v8_exact` 同一套 V8 金盾徽章：顶部 `BL POKER`、中央 `8L`、
  底部 `POKER GAME`、金色黑桃。改成 `BY` / `BY POKER`，`POKER GAME` 与黑桃不动。
- **一次生成即通过，没试错**：参考图先 3 倍 LANCZOS 上采样 + 轻微 USM 锐化再压白底
  （原图只有 141×164，直接喂给模型细节太少），生成尺寸 **1781×2071**（宽高比 0.860，总像素达标）。
- 提示词按 `art-text-swap` 技能四段式：保留清单（盾牌轮廓/金边层数/蓝盾纹理/顶部菱形/`POKER GAME`/黑桃）
  + 改动清单（`8L→BY`、`BL POKER→BY POKER`）+ 拼写断言（「不是数字 8 加字母 L」）
  + 白底与比例声明。
- **抠透明底新踩的坑（重要）**：白底抠完直接 unpremultiply，边缘 alpha 1–180 的像素 RGB 达 **(227,230,233) 接近纯白**，
  在深色背景上会泛一圈白边（原图对应位置是深蓝 `(40,85,111)`）。
  修法：缩到目标尺寸后加一步**色溢填充**——把 alpha<0.88 的像素 RGB 用迭代膨胀从最近实心像素取值
  （3×3 BoxBlur 加权求均值），填完边缘均值降到 `(137,154,164)`，白边消失。
  **这一步应补进 `art-text-swap` 技能的透明底流程。**
- 后处理：内容包围盒对齐到原图 `x 2..136 / y 2..162` → 预乘 alpha 后 LANCZOS 缩回 141×164
  → 色溢填充 → RGB 6bit 量化 → optimize/compress_level=9。
- 校验：尺寸 141×164 与 meta 一致；`.meta` 未改动、UUID 不变；四角 alpha 全 0；
  不透明占比 0.648（原 0.644）；alpha 重合度 0.984；三种底色（深蓝/品红/白）下合成均无白边。
- 成品留档 `art_sources/rename-BY/agent_v8_shield.png`；中间产物已清理。
- 未提交（用户未要求）。

## ImagesLuck/公告/1.png —— 8 个标题提亮（第 5 项，**已否决并回退**）

**结论：此路不通，不要再试。** 用户判定「效果不行」，要求回退且不再改。
- 时间线：11:27 完成并替换 → 11:30 用户回退。
- 已执行 `git checkout -- "assets/ImagesLuck/公告/1.png"`，恢复为 943,206 字节原版，
  `git status` 中已不再显示该文件改动。`.meta` 全程未动。
- 临时对比图 `/tmp/公告1_*.png` 一并删除。

被否的是**做法**，不是执行质量：技术指标全部达标（尺寸一致、alpha 差异 0 像素、
字形逐笔保留、体积 943,206 → 727,882 字节 −22.8%、变化严格落在 8 个标题矩形内）。
失败点是**观感**——把深青字身提成亮青蓝、描边推成近纯白之后，用户不认可这个效果。

**教训（重要）**：
1. 「保字形提亮」属于我自己选的路线，用户从没要过。用户此前已定规矩「图片都用生成」，
   本次因为「中文短标题生成会写错字」我自行改道——**这一步就该先问用户，而不是先做完再问。**
2. 下次遇到「文字看不清但内容不能变」，先问用户想要什么效果，不要自行定方案。
3. 唯一确认可用的改图路线仍是 **AI 图生图生成**。
4. 技术细节保留在 `art-text-swap` 技能的「路线 B（已否决，勿用）」里，仅作「别再重复踩」的记录。

**过程中的技术要点（留档，勿再套用）**：
- 原图文字结构：描边 `(193,218,232)` 约 2px、字身 `(25,100,138)` 不透明、
  面板底 `(17,25,57)` 且横向变化剧烈；看不清的根因是字身与底明度差太小。
- 中途踩坑：误把「已替换过的图」当输入源导致二次提亮，差点写回。
  → **改图脚本一律先 `git show HEAD:<path> > /tmp/原图.png` 导出原图再开工。**
- 另一个坑：降采样函数把 0–255 的 RGB 当 0–1 处理（`clip(x,0,1)*255`）会溢出成随机值，字形变黑变花。
- 面板顶部有与字形同样不透明的白色卡边 `(246,246,246)`，光靠 alpha 分不开，必须用目标矩形硬掩码收口。

---

## ImagesLuck/公告/1.png —— 8 个标题提亮（初版记录，已作废）

- 图：`assets/ImagesLuck/公告/1.png`，750×1903 RGBA，原 943,206 字节。内容是大厅公告面板长图。
- 需求：用户圈出 8 个标题（`三花10` `三花6` `休芒` `揉芒` `手手芒` `补芒` `没搭动` `拖滚`）
  说「看不清，重新生成修改一下，让效果更好看，**所有文字内容都不要变**」。
  顶部 banner「三花特辑」未圈，不动。
- **为什么没走 AI 生成**：这是中文短标题，生成极易写错字，与「文字内容不能变」直接冲突；
  且原图是「深青字身 + 冰蓝描边」的成套设计，重生成会破坏与正文/组框的一致性。
  故改为**对原有文字像素做提亮**——alpha 通道一字节不改，字形绝对不变。
- **原图文字结构（剖面实测）**：描边 `(193,218,232)` 每笔画各一圈、约 2px；
  字身 `(25,100,138)` 不透明；面板底 `(17,25,57)`，alpha 在 60–160 之间且**横向变化剧烈**。
  看不清的根因：字身明度（L≈82）与深蓝底（L≈29）差太小，只剩描边能辨认。
- **做法（可复用）**：
  1. 用 `ImageFilter.MedianFilter(61)` 估面板背景 alpha（核远大于笔画宽度，中值取不到字形），
     `cov = (a - bgA)/(255 - bgA)` 得字形覆盖率。
  2. `w = smoothstep(cov, 0.70, 0.88)`——**阈值要高**，这样背景估计有偏差时也不会误伤面板。
  3. 按亮度锚点曲线提亮：`(39→98) (82→164) (150→226) (200→255)`，
     即 `RGB *= L_out/L_in`。深青字身 → 亮青蓝，冰蓝描边 → 近纯白，**笔画间隙仍暗 → 笔画分离保住了**。
  4. **必须再用用户圈定的标题矩形做硬掩码**：面板顶部有一条白色卡边（`246,246,246` 不透明），
     alpha 与字形无法区分，不裁会把卡边也提亮。
  5. 6bit 量化 + `optimize=True, compress_level=9` → **727,882 字节（−22.8%，比原图还小）**。
- **校验结果**：尺寸 750×1903 一致；**alpha 差异 0 像素**；变化严格落在 8 个标题带内；
  `.meta` 未改动、UUID 不变。
- 教训：`先确认 SRC 指向原图`——第一次误把已替换过的图当输入，造成二次提亮，差点写回。
  改图脚本一律以 `git show HEAD:<path>` 导出的原图为准。
- 中间产物已清理，只留 `/tmp/公告1_标题_前后对比.png` 与 `/tmp/公告1_整图预览.png` 供查看。
- 未提交（用户未要求）。

## 公告图 2.png：表格「10%」→「20%」（同图字形移植，非遗 AI 重绘）
- 目标文件：`assets/ImagesLuck/公告/2.png`（750×1903 RGBA，原 928,096 字节）。
- **路线判定：不走 AI 图生图。** 该图是几百字中文说明文（代理红利规则），AI 重绘必错字，
  与用户「其余效果要一致」直接冲突。改用**同图字形移植**：把表格第 3 行「12%」里的现成 `2`
  搬到第 1 行「10%」的 `1` 位置——同一张图、同一字体、同一描边渐变，像素级一致。
- 关键测量（互相关测各行数字 x 位移，反推字距）：`60%` 相对 `10%` 位移 +5、`8%` 为 −4
  → 推出 `1` 的 advance=8、其余数字=18（所以「20%」比「10%」天然宽 ~10px）。
- 背景建模（本图重要特性）：**alpha 只随 x 变**（186−0.1333·(x−450)），**RGB 只随 y 变**（x=545 竖采样拟合）。
  两条 1D 曲线即可完美重建被擦掉的底板，横向无接缝。
- 掩码合成：`a = clip((alpha−bgA(x))/(255−bgA(x)),0,1)`，左右缘加 `clip((x−x0)/6,0,1)` 渐隐窗，
  挡住源字形与相邻「1」粘连的笔画（否则搬来一块白残影）。位移取 DX=−17、DY=−123（行 1↔行 3 的固定行距）。
- 验收：编辑框外**逐字节零改动**（改动 1018 像素，全部落在 x458–484 / y599–638）；
  尺寸一致；`.meta` 未动；体积 928,096 → 928,416（**+0.03%**，用 `optimize=True, compress_level=9`，
  刻意不做 6bit 量化，以保住编辑区外像素不变）。
- 中间产物（原图导出、多版草稿、对比图、脚本）已全部清理。
- 未提交（用户未要求）。技能 `art-text-swap` 已新增「路线 C：密集中文正文图短文本替换」。

## 全仓扫描：仍含旧名 8L / BL 的图（11:44–11:57）

产出清单：`reports/8l-old-name-remaining-2026-09-18.md`（58 张）。

### 方法（可复用）
1. **文件名命中**：`find assets -iname "*.png"` + 正则 — 只有 1 个（`ImagesLuck/公告/大厅公告底_8L.png`）。
   结论：**文件名几乎不可靠，必须扫图内像素。**
2. **图内文字**：本机 Swift 可用（`/usr/bin/swiftc`，Swift 6.3），写 `VNRecognizeTextRequest`
   （`recognitionLevel=.accurate`、`recognitionLanguages=["zh-Hans","en-US"]`、
   **`usesLanguageCorrection=false`** —— 开着会把 `8L` 纠成别的词）。
   纯标准库，2357 张图约 60 秒跑完。**这是本机唯一可用的 OCR 手段，pyobjc 没装 Vision。**
3. **补漏**：OCR 对小于 ~几十像素的正文/艺术字会漏。把「OCR 空结果」的 1348 张**放大到最长边约 1400px**
   （LANCZOS 上采样 + 压白底）再扫一遍，多捞回 7 张（`游戏内/LOGO`、`皇冠框`、`大图标`、`筹码`、
   `imagesKK/游戏大厅/图标`、`搓背1`、`牌背1`）。**两轮扫描是必须的。**
4. **判断「是否在用」**：从 `.meta` 取 **texture uuid 和 subMetas 里的 spriteFrame uuid**
   （prefab 里引用的是后者，只查前者会大面积漏判），反查
   `*.prefab / *.fire / *.ts / *.js / *.json / *.anim`。
   再加 `assets/resources/project.manifest` 作次级判据（含 1728/2372 张，即「在热更包内」）。

### 结果
- ① 确定在用 **22 张**（含 `V7/` 的 14 张 shield/hero/announcement 系列、`ImagesLuck/游戏内/LOGO`、
  `resources/zuotype` 牌背搓背、`resources/other/drh/room_ready_art` 等）
- ② 在热更包内但无直接引用 **5 张**（`ui_bigwin_tex.png` 大图里也有 8L，已分块确认）
- ③ 疑似未接入 **31 张**（`V7/followup_*` 母版系列、`ImagesLuck/` 老素材等）
- 另有 `ImagesLuck/公告/大厅公告底_8L.png` 文件名带旧名（图内 OCR 未读到 8L）。

### 两个坑
- **`grep` 用工具调用查 UUID 会漏**（实测 `panelMain.prefab` 里明明有 `76409889…` 却没被列出）。
  **引用核查一律用 Bash 里的 `grep -rl`**，别用封装的搜索工具。
- **并行 OCR 写同一个重定向文件会把行写花**（`xargs -P 4 ... > out` 实测产生非 UTF-8 的残行，
  后续 `open(...,encoding='utf-8')` 直接报错）。要并行就每进程写自己的文件再合并，或干脆串行（2357 张也就 1 分钟）。
- macOS 的 `xargs` **不支持 `-a`**，要用 `xargs ... < file`。

### 顺带核实
已改完的 5 张（`login_shield_exact` / `mine_hero_exact` / `nav_bar_exact` /
`announcement_shield_v8_exact` / `agent_v8_shield`）**全部确认被 prefab 引用**，改名有效。

### 注意
`assets/ImagesLuck/公告/2.png` 显示为已修改 —— 是**另一个并行任务**改的（`10%→20%`，改动 1018 像素，
落在 x458–484 / y599–638），非本次范围，原样保留未动。

## 批量改名：22 张「确定在用」的含 8L 图（12:10–13:00，已完成）

用户指令：「生成所有 **确定在使用的** 包含 8L 的图片，改成 BY」。

### 结果
**22 张全部替换回项目，合计 11,468 KB → 5,762 KB（−49.7%）**；尺寸全部一致；`.meta` 一张未动；OCR 复扫无残留 8L/BL。

四轮生成 + 后处理才收干净（过程比预期长，下面每条都是可复用的）：

| 轮次 | 内容 | 起因 |
|---|---|---|
| 1 | 22 张全量生成（`prep.py`/`run.py`/`post.py`） | 主批 |
| 2 | 7 张收紧提示词重生成（加 `CALM` 禁发光/提亮） | 被 AI「美颜」 |
| 3 | 4 张换正确提示词重生成 | **我的提示词写错**（见坑 1）+ 字体风格被换 |
| 4 | 2 张公告面板水印重生成 | 水印顶部小字 `BL POKER` 漏改 |

### 坑（都很值钱）
1. **提示词里描述原图内容时，必须是「亲眼看过原图的」描述。**
   我在公告面板提示词里写了「保留底部导航条『公告 客服 钱包 我的』」——那是 `announcement_menu_long_exact`
   的内容，我把它复制到了 `announcement_detail_header/latest_exact` 上（那两张根本没有导航条）。
   AI 老老实实**凭空补了一条导航栏**。→ 写保留清单前逐张看图，不要凭印象复制。
2. **`post.py` 取包围盒时，原图与生成图必须用同一个 alpha 阈值。**
   原先给原图用 `8/255`（含外发光）、生成图用 `0.35`（不含），结果生成内容被拉伸去填原图的光晕包围盒，
   **整只盾牌被放大**。改成两边都用 `0.5`。→ 任何 bbox 对齐，先检查两个阈值是不是同一个。
3. **AI 会偷偷加一圈淡光晕。** 22 张里 10 张的 `alpha>0` 覆盖率比原图高一大截
   （例：`shield_hd` 0.610→0.848）。修法：按原图覆盖率为目标，对生成 alpha 做阈值拟合
   `al = clip((al-t)/(1-t),0,1)`，二分求 t，**这一步同时把体积又降了一档**（`shield_hd` 1901→1120 KB）。
   → 阈值拟合要设上限：若拟合后覆盖率反低于原图，说明原图的光晕是**设计的一部分**，不能压。
4. **原图是扁平矢量图时，AI 重绘后 PNG 会暴涨。** `bigbig.png` 水平相邻像素相同率：原 0.856 → 生成 0.501；
   于是 44 KB → 112 KB。它是扁平色块图，AI 给它加了微纹理。
   修法：**生成侧高斯模糊 2.5 + BOX 下采样 + 目标尺寸中值滤波 3**（`fix.py`）→ 58 KB（+32.6%）。
   另一张 `牌背3` 同因 41→129 KB，同法 → 52 KB。
   → 判据就是「水平平坦度」，一测就知道是不是这类图。
5. **只贴 bbox 内的内容会裁掉光晕。** `秦_发现按钮` 原图的光晕有 14.5% 落在自己 bbox@0.5 之外。
   修法（`fix3.py`）：按 bbox@0.5 求非均匀缩放比，把**整张**生成图缩放后按偏移贴上去。
   附带的坑：贴图宽度要用**源图**宽 `gen.shape[1]`，我一开始写成目标宽 `W`，贴出来只有 17px。
6. **`xargs -a` 在 macOS 上不存在**；**并行写同一个重定向文件会把行写花**（老坑，又踩一次）。
7. 大图（`941×1672`）只改一个中心水印，AI 有大量空白可以乱填——**提示词必须显式写「原图哪里是空的，输出就必须还是空的」**
   （`NOADD` 常量）。

### 验收流程（建议固化成脚本）
1. 尺寸一致 / `.meta` 字节一致 / 体积同比；
2. **alpha 覆盖率**（`(a>0).mean()`）原 vs 新，偏差 >0.09 报警 → 抓光晕；
3. **结构漂移检测**：分块比「局部梯度能量」，找「原图平坦、新图有结构」的格子 → 抓 AI 凭空添加的元素
   （第 3 轮就是靠它确认公告栏被加了一条导航条，`drift2.py`）；
4. 逐张 OCR 复扫旧名。

### 其他
- **`assets/ImagesLuck/公告/2.png` 的改动不属于那批被否的「提亮」**，是另一条并行任务（`10%→20%` 内容修改），
  已确认保留、未动。
- 技能 `art-text-swap` 已补「批量改名」与「体积控制」小节。

## 公告图 2.png（续）：正文里同一描述的 10% 与算例结果全部同步（8 处）
- 接上一段：主人指示「同一个描述的都改」。除表格外，正文 5 处一并改：
  - `盟主：…下级红利总和的10%` → `20%`
  - `A代理是盟主则他获得盟主红利: 150 (A代理红利)*10%` → `*20%`
  - `B代理是盟主则他获得盟主红利: 150 (B代理红利)*10%` → `*20%`
  - `(A代理盟主红利分成比例) = 15` → `= 30`（150×20%）
  - `(B代理盟主红利分成比例) =15` → `=30`
- 做法仍是同图字形移植：`2` 取自正文 `8%= 20` 的 `2`；`3`、`0` 取自正文 `12%=30` 的 `30`。
- **踩过的坑（重要）**：擦除框第一次给了 `±4px` 余量，把相邻的 `的`、`*` 各啃掉一块，
  放大后肉眼可见缺口。改成「旧墨迹 ∪ 新墨迹」的紧包围盒（0 余量）后干净。
- **摆放规则**：新字形按「右墨迹边缘」对齐旧字形 → 与后一位数字的间隙自动等于原图自然字距
  （`2` 换 `1` 得 2px、`30` 换 `15` 得 2px，与原始排版一致）。
- 正文用小字字形，不能用表格大字；正文是纯白 `(238,248,253)`/A=255 不透明，底板半透明，
  掩码直接由 alpha 反推；底板用「数字区上下各 12 行干净像素」做按 x 的垂直线性插值重建。
- 验收：改动 1339 像素；逐个核对相邻字（`的`/`*`/`=`）区域改动像素均为 0；
  尺寸与 `.meta` 未变；体积 928,096 → 930,025（**+0.21%**）。中间产物已清理，未提交。
- 技能 `art-text-swap` 路线 C 已补「4b. 正文小字」小节（紧包围盒 + 右对齐 + 派生数字同步）。

## 返工：leaderboard_v8_hero_title / room_ready_art「中间不干净」（13:00–13:30）
主人反馈：改名后的这 2 张图「中间都不干净」（附黑底截图，各自圈出中间区）。要求重新生成。

### 定性
- `leaderboard_v8_hero_title`（262×65）：上一版 AI 把**「耀」画得笔画过粗**，缩到 54×50px 后
  笔画粘连成脏斑 → 中间发糊。
- `room_ready_art`（1024×238）：上一版 AI 往**盾牌菱形内部填了一块不透明白/米色底**，
  原图那里是**透明镂空**的 → 黑底上看就是一团白，脏。

### 试错结论（关键，别重复烧钱）
- **Seedream 无法忠实还原「耀」这种 20 画密集字**在 ~54×50px 的字号：
  共试 **7 轮 28 版**（A/B、H1-3、M1-3、N1-5、P1-3、Q1-3、S1-3），
  结构都能画对，但**笔画一律比原图粗**（原图暗占比 0.128 / 实心 0.5635；
  最好的一版实心也到 0.60），放大后「光」的丷被画成叉、羽/隹带脏斑。
  - 压粗细的措辞（「清秀、细笔画」）会把**深蓝描边一起压没**（N1 暗占比掉到 0.049）。
  - 参考图放大（NEAREST/LANCZOS/USM）、数笔画、当「不可修改素材」全都无效。
  - 把「8L」那一小块单独喂给 AI 让它换字（S1-3）更糟：AI 直接画成白底黑字。
- **纯 AI 在这两张图上已到天花板。**

### 最终做法（两张图都走这一条）
**「未改动的文字/图形一律保留原图原始像素，只把 AI 生成的新内容贴回去」**
- `leaderboard_v8_hero_title`：原图 x≥79 的「荣耀榜」**逐像素保留**，
  只把 AI 生成的 `BY`（取自 out4/hero_N1）贴到 x<79。
  实测贴回后整体观感一致：`BY` 笔画游程中位 13.0px / 均 18.97px，
  原图「荣耀榜」是 12.0 / 18.10（原「8L」本身更细，10.0），字重接得上。
- `room_ready_art`：用 R1 版（AI 的大号金色 BY + 盾牌），把**封闭在盾牌菱形内的那块白色**
  清掉（见下），得到 R1_clean。

### 新技术：「清除封闭的大块白色」（这次新增，很重要）
`post.py` 的抠白是**从画面四边连通**才开始抠（`flood` 种子只取边界）。
**被金框封闭的白色区域（盾牌菱形内部）永远抠不到**，会以 ~40% alpha 残留成奶白块。
补丁（`kill_big_white`）：
1. `soft = clip((mn-190)/50) * clip(1-(mx-mn)/40)` → 白色可能性；
2. `core = soft > 0.45`，用 `MinFilter(2k+1)`（k=5）**腐蚀**——字母上的小块白消失，
   菱形大白块存活 → 当种子；
3. 在 `core` 内**测地重建**（迭代膨胀 ∩ core）→ 再 `dilate(2) & (soft>0.30)`；
4. `al *= (1-grow)`。
k=5 时 R1 干净；**k 太大会把字母掏空**（R2/R3 被掏空成空心字，作废）。

### 验收
| 图 | 尺寸 | 体积 | 覆盖率 | OCR |
|---|---|---|---|---|
| leaderboard_v8_hero_title | 262×65 | 31,449 B（原 31,996，**98.3%**） | 0.5785（原 0.5385） | `BY荣耀榜` ✅ |
| room_ready_art | 1024×238 | 136,566 B（原 290,142，**47.1%**） | 0.2083（原 0.2026） | `BY游戏已就绪` ✅ |
- 两个 `.meta` md5 与替换前一致（未动）。中间产物 `/tmp/r2` 已全部删除。

### 新增客观指标（好用，值得固化）
- **中文区 IoU**：候选与原图「alpha>128」二值掩码的 IoU（区域限定到中文列范围）；
- **暗占比**：`(rgb.sum(2)<330) & (alpha>128)` 的比例 → 抓描边/笔画粗细；
- **横切游程中位数**：逐行统计连续实心像素长度取中位 → 直接量笔画宽度（比肉眼看可靠）。

---

## 桌内 UI 元素重新设计：个人金币底框 / pai_1 筹码 / btn_zhuang 庄标

**性质**：只出设计稿，**未动 `assets/`**。成品在 `design-previews/待确认/2026-09-18-金币筹码庄标/`。

### 交付
| 文件 | 4x | 1x（实装尺寸） |
|---|---|---|
| 个人金币（金币底框） | 556×140 / 94.9 KB | 139×35 / 6.5 KB（原 1,869 B） |
| pai_1（筹码） | 100×100 / 18.4 KB | 25×25 / 1.7 KB（原 1,847 B） |
| btn_zhuang（庄标） | 148×148 / 30.1 KB | 37×37 / 2.9 KB（原 3,094 B） |

风格：深海军蓝 #0B2545→#1B5E9E + 香槟金 #E8D9A0，对齐定稿 V8。
生成用 Seedream 4.5，`--image` 同时喂「双色调位置参考图 + 定稿菜单按钮截图」当风格锚点。

### 【本轮最重要的发现】Cocos 2.4.13 九宫格把 inset 当两种单位用
读了引擎源码 `CCSpriteFrame._calculateSlicedUV`（纹理像素切 UV）与
`sliced.js updateVerts`（节点单位算顶点，`xScale=min(1,nodeW/(insetL+insetR))`）。
⇒ **放大贴图必须同步放大节点尺寸**，否则 `insetL+insetR>nodeW` 触发 `xScale<1`，装饰件被压扁。

实测三张图的使用处（`panelGameView.prefab` / `drh8.fire` 各有 8 处）：
- `scoreBK` **150×32**，`_type=1` SLICED，inset 35/20 → **金币底框不能放大 4 倍**，只能保持 139×35。
- `pai_1` **25×25**，`_sizeMode=1` **TRIMMED** → 贴图变 100×100 会让节点也变 100×100，需先把 `_sizeMode` 改 `0`。
- `zhuang` **37×37** TRIMMED（16 处）；另有 `庄` 24×24 / 34×34 是 CUSTOM，本来就安全。

**结论：筹码 / 庄标走 4x + 把 TRIMMED 改 CUSTOM；金币底框保持 1x。** 已告知主人，等确认再回装。

### 其它坑
- **图生图会被原图的荧光青污染**：提示词写「不许出现青色」也没用，连跑 3 版都带青色。
  改成喂「深蓝↔香槟金双色调位置参考图」（只给结构不给颜色）→ 一版就干净。
- **金色抠图不能用 `alpha=1-min(rgb)/255`**：金色 min 通道 160 → 被算成 alpha 0.37。
  改用「与背景色差」+ 逐行实心区掩码。
- **配色跑偏要看 90 分位亮度，不是色相**：色相统计报「青色 0.02%」，但图偏亮很多
  （庄标蓝区 90 分位 216 vs 定稿菜单按钮 150）。压暗重生成后达标。
- 交付流程与完整代码要点已沉淀为新技能 `cocos-ui-element-reskin`。

### 回装已完成（主人 15:25 确认「可以 替换吧」）
改动 8 个文件，**未提交**（按项目约定等主人明确要求提交）：
- `assets/ImagesLuck/游戏内/`：个人金币.png / pai_1.png / btn_zhuang.png 三张贴图 + 对应 .meta
- `assets/Scenes/drh8.fire`、`assets/resources/UI/panelGameView.prefab`：各 16 处 `_sizeMode` 1→0

| 图 | 尺寸 | .meta 改动 | UUID |
|---|---|---|---|
| 个人金币 | 139×35（不变） | **仅 borderLeft 35→40** | b77fc4c8 **未变** |
| pai_1 | 25×25→100×100 | width/height/rawWidth/rawHeight | 346818b0 **未变** |
| btn_zhuang | 37×37→148×148 | 同上 | 98e14d21 **未变** |

UUID 全部不变 ⇒ Creator 里不用重挂引用。

**回装中发现的两个坑（都已修）：**
1. **`pai_1` 是 `scoreBK` 的子节点**，`_trs[0]=-55`，底框宽 150 锚点 0.5 ⇒ 筹码圆心在底框节点 **x=20**。
   新图徽章中心行扫描出的圆心 ≈21 ⇒ 对齐（差 1px）。**如果没对齐，筹码会偏在环的一侧。**
   注：Cocos 2.4 节点变换存在 `_trs`（`[x,y,z,qx,qy,qz,qw,sx,sy,sz]`），
   `_position` 为默认值 (0,0) 时**整个字段被省略**，直接读 `_position` 会读不到坐标。
2. 新徽章宽 40px > 原 `borderLeft=35` ⇒ 徽章尾部会被卷进可拉伸区、节点变宽时被拉糊。
   **把 borderLeft 改成 40** 修掉（`xScale = 150/(40+20) = 2.5 > 1`，未触发压扁）。

**顺带确认的**：`score` 是 `cc.Label` 居中（`_N$horizontalAlign=1`），文本实际从节点 x≈54 起，
不会撞到徽章（右边 39）。底框色彩偏亮的问题主人未要求改，保持原样。

**离线验证手法（值得复用）**：照抄 `sliced.js updateVerts` + `CCSpriteFrame._calculateSlicedUV`，
本地按九宫格把底框渲染出来，再把子节点（筹码）按 `_trs` 摆上、叠一个示例分数文本 →
一眼看出遮挡/偏移/拉伸变形。验 150×32 和 260×32 两个宽度。合成图存在
`design-previews/待确认/2026-09-18-金币筹码庄标/实机合成验证.png`。

**待办（未做，需主人触发）**：`library/` 缓存是旧的，要在 Creator 里重新导入；
`assets/resources/project.manifest` 需跑 `1生成热更新包.command` 重建（那是构建产物，不手工改）。

### 追加改动：去掉压在金币框上的筹码图标（主人 16:54 要求「只保留金币框」）
16 个 `pai_1` 节点（`scoreBK` 的子节点，`_trs=(-55,0)`，节点坐标 x=20）
**`_active: true -> false`**，`drh8.fire` 8 个 + `panelGameView.prefab` 8 个。
**不删节点**（删了会让序列化数组 `__id__` 全体错位），只停用 → 可一键回退。

确认过：`assets/` 下**没有任何脚本**引用 `pai_1` / `scoreBK` / `getChildByName`，也没有 `.active =` 赋值，
`pai_1` 节点上只挂了 `cc.Sprite` → 停用安全。

**后果（已告知主人）**：16 处 `pai_1` 就是筹码的全部使用处 ⇒ **筹码贴图现在游戏里完全不显示了**。
上一轮为它做的 100×100 放大 + 32 处 `_sizeMode` 改动目前是空转（留着不影响，日后想恢复筹码直接改回 `_active`）。
若确认永久不用，可以把 `pai_1.png` + `.meta` 回退到 25×25、并把 `_sizeMode` 改回 1。

验证图更新为 `design-previews/待确认/2026-09-18-金币筹码庄标/实机合成验证.png`（新版无筹码）。

**当前 assets/ 累计未提交改动（8 个文件，未 commit）**：
3 张 PNG + 3 个 .meta + `drh8.fire` + `panelGameView.prefab`。
`drh8.fire` / `panelGameView.prefab` 各含：16 处 `_sizeMode` 1→0 + 8 处 `pai_1` `_active` true→false。




### 追加改动：底框改成「纯框」+ 分数数字居中（主人 17:00 要求「只要一个纯的框 左边所有的都去掉」）

**1. 重新生成纯空白框**（Seedream 第 3 版 `seedream_20260918-165919_..._b33786.jpg`，唯一成功的一版）。
构图：对称胶囊、深海军蓝近均匀填充 (3,59,106) / 90 分位亮度 115、香槟金细描边 + 内侧亮金高光线、**左端零装饰**。
重排成 139×35（4x 母版 556×140，位深 7bit；1x 走 6bit）。

**2. 【重要教训】九宫格 `borderLeft` 必须 ≥ 圆角弧的水平跨度。**
第一版把 `borderLeft` 设成「行宽达最大值 99% 的位置」（=181px，31.8% 高）→ **偏小**：
圆角弧的真实水平跨度是**最顶一行的左端内缩量**（=245px，42.5% 高）。
跨度设小了，圆角弧的后半段就被甩进「可拉伸的中间列」，被水平压缩 0.588× ⇒ **两端看起来是尖的（梭形）**。
改成 15（=245×35/576）后弧线 1:1 还原。**判定口径：顶端 3 行 + 底端 3 行的内缩量取中位。**
（`CCSpriteFrame._calculateSlicedUV` 把 border 当纹理像素切 UV，`sliced.js updateVerts` 当节点单位算顶点 → 左列 1:1、中列拉伸。）

**3. 分数数字居中**：`score` 原来 `_trs[0]=3.579`，比 `scoreBK` 的 `-5.834` 右偏 **9.413px** —— 那是给旧徽章让位的。
纯框之后必须居中，**16 个 `score` 节点（两文件各 8）`_trs[0]` 3.579 → -5.834**。
确认过 `score` 是 `cc.Label` overflow=NONE（节点自适应文本宽、居中对齐）⇒ 改节点 x 即改文字位置。
文字颜色由 `panelGameView.ts:2117` 运行时给：赢 (255,112,117) 红 / 输 (189,216,76) 绿 / 零 (250,240,214) 米白，
三种在新深蓝底上都清楚。

**4. 修了一个上一轮漏掉的 meta 错误**：`btn_zhuang.png.meta` 顶层 `width/height` 还是 37×37
（`subMetas` 里已经是 148），已补成 148×148。**下次放大贴图记得三个地方都改**：
顶层 `width/height`、`subMetas.*.width/height`、`subMetas.*.rawWidth/rawHeight`。

**5. 体积**：`个人金币.png` 1869 → 4505 B（+2.6 KB，可接受）。
`pai_1.png` 1847 → 18888 B、`btn_zhuang.png` 3094 → 30851 B —— `pai_1` 现在是**停用状态**（16 个节点全 `_active:false`、
脚本零引用），属于纯死重；若主人确认永久不用可回退省 17 KB。**未回退，等主人定。**

**当前 assets/ 累计未提交改动（8 个文件）**：3 张 PNG + 3 个 .meta + `drh8.fire` + `panelGameView.prefab`。
两个场景文件各含：16 处 `_sizeMode` 1→0 + 8 处 `pai_1` `_active` true→false + 8 处 `score` `_trs[0]` 3.579→-5.834。

**待办（未做，需主人触发）**：`library/` 缓存是旧的，要在 Creator 里重新导入；
`assets/resources/project.manifest` 需跑 `1生成热更新包.command` 重建。

### 追加改动：筹码（pai_1）重做 —— 为 25~30px 小尺寸做可辨识设计（主人 17:14 要求）

主人截图反馈：筹码在真机上只有约 30px，糊成一团。**并据此恢复筹码显示**（撤回上一轮的 `_active:false`）。

**1. 【核心结论】小尺寸图标糊，不是分辨率问题，是「元素个数」问题。**
- 旧筹码：外金环 + 10 个边块 + 2 道内圈 + 黑桃 = 4~5 组元素，且灰白低对比。贴图做到 100×100（4 倍超采样）照样糊。
- 新筹码：**只有 3 组元素** —— 细金外圈 + 8 个粗金边块 + 一个大号实心金黑桃（约占直径 55%，顶端一点红）。
  深蓝底大片留白，哑光、不做强高光。同一 100×100 贴图，真机上立刻清楚。
- **判据**：把候选按真机像素（25/30/40px）**1:1 渲染出来看**，不要只看放大图。
  这轮生成了 3 版：A 铜钱（中心是一大团金 + 方孔，糊）、B 黑桃但外圈金太重、C 精简版 —— **C 明显最好**。
- 生成提示词里要把「只有这几个元素、不要内圈、不要细线、不要花纹」写成**否定清单**，否则模型一定加辅助圆环。

**2. 尺寸决策**：贴图保持 **100×100**（节点 25×25 的 4 倍），`.meta` 一行都不用改；
  节点仍是 25×25、`_sizeMode: 0`(CUSTOM) → 真机按 25 设计像素绘制。
  **糊的根因不是贴图小**，别去改尺寸或加 mipmap。

**3. 恢复显示**：16 个 `pai_1` 节点 `_active` 全部改回 `true`（`drh8.fire` 8 + `panelGameView.prefab` 8）。
  理由：主人这次是**看着游戏里的筹码**提的要求，最新要求优先于上一轮「左端全去掉」。

**当前 assets/ 累计未提交改动（8 个文件）**：
3 张 PNG（个人金币 / pai_1 / btn_zhuang）+ 3 个 .meta + `drh8.fire` + `panelGameView.prefab`。
两个场景文件**各只剩 24 处**改动：16 处 `_sizeMode` 1→0（pai_1 8 + zhuang 8）+ 8 处 `score` `_trs[0]` 3.579→-5.834。
（`pai_1` 的 `_active` 已复原，diff 里不再出现。）

**筹码体积**：1847 B（原 25×25）→ **19419 B**（100×100）。若主人最终不要筹码，回退可省 17 KB。

### 追加改动：改成竞品风格（主人 19:24 发的竞品截图「6S POKER」）

**1. 底框 `个人金币.png` → 黑色半透明胶囊。**
竞品玩家分数底是**纯黑半透明圆角胶囊、无任何描边**（顶部那条「0429824」才有金边，玩家底框没有）。
新图：`150×32`（改成与节点 1:1）、纯黑 `(0,0,0)`、**alpha 140（≈0.55）**、端点全圆（radius = 高/2 = 16）、
**无描边、无渐变、无高光**。`.meta`：尺寸三处改 150×32，`borderLeft/Right = 16`（= 圆角弧水平跨度）。体积 234 B。
透明的黑块用脚本按几何画（纯色块没有「生成内容」，AI 出图反而会带噪声和假渐变）。

**2. 筹码 `pai_1.png` → 纯红白 + BY。**
竞品筹码 = 红盘 + 一圈白块 + 中心白圆 + 内写红字。按主人要求写成 **BY**。
新版：红盘 `#D01E28`(198,47,49) + 8 个白边块 + 大号白心圆 + 粗体红字 **BY**，**全图只有红白两色，暗灰像素 0%**。
100×100、17.6 KB。节点仍是 25×25、`_sizeMode: 0`，真机按 25px 绘制（BY 在 20px 下仍可辨）。

**3.【新踩的坑】白底扁平图标抠圆：浅色元素贴到外轮廓时，不能用「与背景色差」求 alpha。**
本例 8 个白色边块正好长在红盘外缘 → 白对白，色差≈0 → 会被判成背景，边缘出现缺口和半透明。
而且 AI 出图常带**柔和投影**，投影会把 alpha bbox 撑大 → 圆心偏移、圆半径偏大 → 抠出来的圆整个偏掉
（本轮第一版就抠歪了，成品里出现一个灰盘 + 错位的筹码）。
**正确做法：用主色掩码定外圆** —— `(R - max(G,B)) > 60` 取红盘 bbox → 得圆心和半径 →
`alpha = clip(r + 0.5 - dist)` → 颜色对白底反预乘。白块贴边和投影两个问题一起解决。

**4. 提示词要写死否定清单**：要「只有红和白、绝对不能出现黑色灰色、不要渐变、不要高光、不要立体、不要描边」，
否则模型一定给红盘加暗红边缘、给白块加灰色立体明暗（第一版就是这样，白块发灰）。

**5. 主人明确：不要中间验证图**，直接交付最终的 2 张素材。已删掉 `design-previews/待确认/2026-09-18-金币筹码庄标/` 全部对比图。

**当前 assets/ 未提交改动**：`个人金币.png/.meta`、`pai_1.png`、`btn_zhuang.png/.meta`、`drh8.fire`、`panelGameView.prefab`。

### 收尾（19:45）
- 交付目录 `design-previews/待确认/2026-09-18-金币筹码庄标/` 已精简为**只剩 2 个文件**：
  `个人金币-底框.png`(150×32) + `pai_1-筹码.png`(100×100)，4x 放大预览版也一并删掉。
- `实机合成验证.png` / `预览对比.png` / `筹码尺寸对照.png` 已删；`/tmp/cp` 整个临时目录已删。
- 最终客观校验（口径修正后）：
  - `个人金币.png` 150×32 / 234 B，中心 `rgba(0,0,0,140)`，四角全透明，meta 三处尺寸 150×32，border L/R = 16。
  - `pai_1.png` 100×100 / 17.6 KB，**红 54.4% + 白 43.8% = 98.2%**，其余 1.8% 全是红白过渡抗锯齿；
    「无色相且暗」(mx<180 且 sat<40) 占比 **0.00%** —— 无黑无灰。
    30px 实机尺寸渲染肉眼可辨 "BY"。
  - `btn_zhuang.png` 148×148 / 30.1 KB，meta 三处一致。
- **指标口径踩坑**：`rgb.sum(2)<330` 会把红 `#D01E28`(198,47,49)=294 判成「暗」，得出 32.6% 的假黑。
  正确口径必须是 **`max(rgb)<180 且 (max-min)<40`**（同时限暗 + 无色相），否则红/深蓝全被算成黑。
- **prefab/fire diff 定型**（两文件各 24 行）：16 处 `_sizeMode` 1→0 + 8 处 `score._trs[0]` 3.579→-5.834；
  无任何 `_active` 改动。UUID 全部未变。

### 提交推送（20:01）
- 主人调整了场景（`drh8.fire` 里 coin 节点换成新筹码 spriteFrame、节点 20×20→35×35、位置与文本宽度同步微调）。
- 已全量提交并推送：commit `59406df` → `origin/main`（e2c8a63..59406df），13 文件 / +402 −172。
- 含用户未提交的场景调整 + 本轮素材与 prefab 改动 + 交付稿 + 项目记忆。

## 默认头像改 BY 圆形（20:00~20:20）

**需求**：`assets/resources/other/` 下的默认头像（`Default_Man_Head.png` / `head.png` / `默认头像.png`），
按正式头像的尺寸重做成「带项目 LOGO BY 的圆形默认头像，和 avatar 一样的」。

### 关键事实
- 三个文件**内容完全相同**（md5 `27efd7889d27`，各 20360 B，140×139 RGBA），原来画的是旧名 **8L 花边形徽章**。
- **只有 `默认头像.png` 的 spriteFrame uuid `3200ca5e-29f4-43ba-8407-fa039dbaed6e` 在实际使用**
  （被 9 个 prefab/场景引用）；`head` 与 `Default_Man_Head` 的 uuid **零引用**，是冗余副本（未删，待主人决定）。
- **所有引用处 `_sizeMode=0`**（CUSTOM），节点尺寸 77 / 90 / 99 / 104 / 133 / 168 —— 所以放大贴图安全，不会撑大节点。
- 正式头像：`assets/resources/avatars/` 共 100 张，**统一 256×256 RGBA**，
  主体圆 bbox **5~250 → 直径 246**，`trimType: "none"`。

### 做法
1. **AI 生成**（Seedream 4.5，`2048x2048`，参考图 = `art_sources/rename-BY/login_shield_exact.png`
   用来锁 BY 的字形/金色/深蓝，提示词明确「不要照搬盾牌外形」）：
   正圆金环 + 深海军蓝底 + 香槟金衬线粗体 BY。
   提示词否定清单：不要投影/外发光/光晕/光斑/运动模糊，背景必须纯白。
2. **定圆**：AI 把圆画得略超出画布（实测 R=1028 > 半画布 1024）。
   用中心行/中心列 + 两条带外弦（`half² + dy² = R²`）**三点交叉验证** R≈1028，圆心=(1023.5,1023.5)。
3. 居中 pad 到 2056（各边 4px 白）让圆正好内切 → LANCZOS 缩到 **246** →
   `alpha = clip(123 + 0.5 - dist)` 几何圆 → 对白底反预乘 `(a - 255(1-s))/s` → 贴到 256 画布 (5,5)。
4. **验收对照**：成品主体 bbox 也是 **5~250 / 直径 246**，与 `头像01` 逐像素同级一致。
5. 覆盖三个 PNG；三个 `.meta` 同步：顶层 `width/height` → 256、sub `width/height/rawWidth/rawHeight` → 256、
   **`trimType`: `auto` → `none`**（对齐正式头像；auto 会按透明边重裁，改变圆相对节点的显示比例），
   trimX/trimY/offsetX/offsetY 归零。UUID 全部未变。
- 体积 88 827 B（< 所有正式头像的 129~150 KB），未做位深量化（金环渐变会被量化出色带，不划算）。
- 交付预览：`design-previews/待确认/2026-09-18-默认头像BY/默认头像-BY-预览.png`。

## 三款亮色牌背重做（20:29~20:50）

**需求**：`assets/resources/pk2/` 的牌背（`bigbig`/`bigbig1`/`bigbig2`）重做 3 张亮色版，
样式参考 `bigbig1`(蓝) / `bigbig2`(红)，顺序 红 → 蓝 → 其他亮色。

### 关键事实
- 三张素材同为 **348×480 RGBA**。**只有 `bigbig.png` 被引用**（handbig / 丢牌对象 / 回顾对象 / panelGameView / drh8.fire 等几十处），
  `bigbig1` / `bigbig2` **零引用**。
- **显示尺寸极小**：`_sizeMode=0`，节点 36×47 / 50×70 / **70×90（最多）** / 90×113 / 139×175。
  ⇒ 细节密度必须克制，参考图那种细密菱形网点在 70px 下就是噪点，这正是主人说「要清晰」的根因。
- 参考图卡片几何（从 `bigbig1` 的 alpha 实测）：**卡片 bbox x17~331 y8~471 = 315×464**，
  顶行内缩 45 → **圆角半径 = 45/315 ≈ 14.3% 卡片宽**；奶白边框宽约 15px（≈5.2% 彩色区宽）。
- 旧图中心是历史英文名「Pluck Poker」（项目内零引用），新图按项目新名写 **BY**。

### 做法
1. **三张都用 Seedream 4.5 `1728x2384`（比例 0.7248 ≈ 348/480）生成**，提示词结构完全一致，只换颜色词。
   - 第 1 张（红）**无参考图**，纯文生图定构图；第 2、3 张**拿第 1 张成品当参考图**，
     提示词写「参考图只用于确定构图与花纹布局，颜色完全按文字描述来改」→ 三张系列一致性靠这个。
   - 否定清单（必须写）：**不要细密网格纹 / 不要点阵 / 不要繁复花纹（会被缩到 70×90 显示）**、
     不要投影 / 外发光 / 光晕 / 高光斑、卡片外必须纯白。
2. **亮色卡片的抠图不能用「非白 bbox」**：卡片外缘是奶白边框，白对白测不出边界。
   改用**彩色区域 bbox（`max-min > 40`）**做基准 —— 硬边界，稳定。
   再按参考图实测的比例外扩（左右 15/286、上下 14/436）得到卡片矩形，圆角用自建圆角矩形 SDF 掩码。
   **不要套参考图的 alpha**：两张图的白框宽度不同，套上去会切掉白框或露出一圈白背景。
3. 卡片统一缩放到 **315×464**（与参考图卡片逐像素一致），贴到 348×480 画布 (17,8)，横向压缩约 3%（对称花纹看不出来）。

### 体积（硬约束：不大于原图）
亮色渐变 + 白描线的 348×480 PNG 原样 **244 KB**，远超原图。
量化实测（红版）：7bit 188K / 6bit 136K / **5bit 89K** / 4bit 56K；高斯 0.5~1.2 单独用只省 5~15%，不划算。
**选 5bit（`rgb // 8 * 8`）**，视觉零差异（348→70 的下采样会把量化台阶完全抹平）。
最终：红 88 502 B、蓝 87 153 B、紫 91 380 B，**全部小于原图**（114 903 / 101 710 B）。

### 安装映射（按主人给的顺序 + 现有色系，两者一致）
- 红 → `bigbig2.png`（原就是红）
- 蓝 → `bigbig1.png`（原就是蓝）
- 其他亮色（紫罗兰）→ `bigbig.png`
- **meta 一律不动**（尺寸没变 348×480，UUID 不变）。
- 交付预览：`design-previews/待确认/2026-09-18-牌背亮色3款/牌背亮色3款-预览.png`。

### 提交推送（20:47）
- 已推送 `59406df..1d6b7c3`，含两个提交：`c5036c1`（头像，主人自建）+ `1d6b7c3`（三款牌背 + 场景）。
- 主人本轮又调了 `drh8.fire`：handcardlist 0×118→70×118、handbig x -105→0、btn_4 51×51→45×45、
  若干 Background 101×41→157×75 / checkmark 103×41→160×77、标题 200×40→150×42.84、说明 310×26→198×30.24。
- **`_trs` 的 z 缩放（索引 9）出现 1 → 0**，共 5 处。Cocos 2.x 的 2D 渲染只用 sx/sy，
  **sz=0 不影响渲染**，是编辑器在 2D 模式保存节点时的正常写法，别当成异常去"修"。
- Creator 重新导入后自动重算了 `bigbig.png.meta` / `bigbig2.png.meta` 的 trim 数据（17,8 / 315×464），
  与新图 alpha bbox 吻合；`bigbig1.png.meta` 当时还没被重新导入（trim 数据仍旧），下次导入会同步。
  **trim 数据是导入结果、不是配置，手工改没意义**。

## 游戏内操作按钮改亮金版（20:50~21:20）

**需求**：`assets/ImagesLuck/游戏内/` 的操作按钮「太深沉」，重做成「好看轻松」的一版。
涉及 11 张：`2/3/4.png`、`btn_da/btn_diu/btn_gen/btn_xiu.png`、`敲.png`、`e1/e2.png`、`回座.png`。

### 关键事实（决定设计边界）
- `2/3/4.png` **内容完全相同**（同 md5），是底池盘。
- **文字来源分两类，这是本次唯一的硬约束**：
  - `丢/休/敲/大/跟`、`e1/e2`、`回座` 上的字是**画在 PNG 里**的 → 底盘与字色可完全自由设计；
  - **底池盘的数字是运行时叠的白色 Label**（`底池1/num`，fontSize 30，color 省略=白，无描边）
    → 底盘中心必须留一块深色区，否则白字看不见。**这是唯一必须保留深色的地方。**
- 节点尺寸：`cmd1` 下 5 个动作键 103×103、底池 98×100、`cmd6` 下 e1/e2 103×103、`回坐` 147×57；
  `2/btn_da/回座` 是 `_sizeMode=1`(TRIMMED)，其余 `0`(CUSTOM)。
- 各图实测圆形几何（圆心 / 直径）：底池 (48,49)/93、大 (52,56)/102、丢 (52,58)/100、
  跟休敲 (52,57)/100、e1/e2 (52,57)/99；回座圆角矩形 143×52 圆心 (73,28)，**圆角 R=15**。

### 设计
**亮金色系**（项目主色 = 深海军蓝 + 香槟金）：
香槟金立体外环 + 均匀细密刻度线 + 亮面内盘（中心奶白 → 边缘浅金）+ 深海军蓝 `#1B3A5C` 粗体字。
底池盘例外：中心是一块深海军蓝宝石实心圆（约直径六成），承载运行时白数字。
旧版是深黑圆盘 + 白字，所以整体观感从「沉」变「亮」。

### 做法
1. Seedream 4.5 生成，`2048x2048`（回座用 `3264x1257` 保 2.6:1 比例，总像素仍 ≥ 3,686,400）。
   第 1 张「敲」**纯文生图**定造型，之后**每张都拿「敲」当参考图**，提示词固定句
   「参考图只用于确定造型/金色配色/外圈刻度/光影，文字换成 X」→ 系列一致性靠这个，11 张风格统一。
   中文单字（敲 14 画）在 103px 下 AI 都能还原准确。
2. **抠图 bbox 必须用饱和度掩码 `max-min > 25`**，不能用「非白 / 亮度 < 246」：
   亮色按钮外缘是浅色渐变，用非白判定会把背景渐变圈进来，
   实测圆形按钮 bbox 从 2048（满画布）缩到正确值 1952，回座从 2579×966 缩到 1838×728。
   **bbox 偏大 → 成品按钮偏小 + 四周带一圈白边**（第一版回座就踩了这个）。
3. **圆角半径反解**：圆角矩形在距顶 dy 处内缩 `ins`，满足 `(R-ins)² + (R-dy)² = R²`。
   生成图回座用 dy=8/ins=238、dy=32/ins=173 两组交叉解，都得到 **R≈308**（bbox 1838×728）→
   缩放到 143×52 时 R≈22。**别沿用原图的 R=15**，形状对不上就会露出白角。
4. 画布尺寸**全部保持不变** → **`.meta` 一个都不用改**（`trimType:auto` 会由 Creator 重新导入时重算）。
5. 体积：6bit 量化（`//4*4`）即可达标；回座因原图是纯深色（4943 B）压得极好，
   用 5bit（`//8*8`）→ 6266 B（+27%，绝对值仅 +1.3 KB）。
   **总计 173 147 → 142 255 B（−18%）**，没有一张超过原图体积。

### 结果
11 张全部覆盖替换，尺寸一致、UUID 未变。交付预览：
`design-previews/待确认/2026-09-18-游戏内按钮亮金/游戏内按钮-亮金版-预览.png`（含新旧对比 + 底池叠白数字验证）。

---

## 游戏内操作按钮 — 第二版「宝石徽章版」（11 张，覆盖第一版）

主人否定第一版（金色单色），明确要求：**丢/休/敲/跟 色调各自不同、更有棋牌游戏风格、文字用艺术字、
2/3/4 底池中间留出空白（要显示运行时文字）、跟 的文字靠下（上方要显示数字）**。

### 最终设计
统一用**金色立体金属外环（回纹 + 铆钉）+ 每键不同色的平整珐琅内盘 + 白色描边立体艺术字**。
色调分配：丢=大红色、休=宝蓝色、敲=琥珀金、大=紫水晶、跟=祖母绿、e1=青蓝、e2=橙红、
底池 2/3/4=深蓝（中心干净留空，承载运行时白数字）、回座=玫红（胶囊）。

### 关键新发现（已写入技能与 MEMORY.md）
1. **`底池1/2/3` 各有一个 `num` Label（'33'，sz=30，`_trs` y=−19.7）→ 在贴图底部**；
   `e1/e2` 各有一个 label（'5'/'0.1'，sz=23，y≈−33.6 → 贴图 y 67~85）。所以 **e1/e2 的数字在底部、
   文字必须留在中上部**；只有 `跟` 的数字（`num` '22'，sz=30，y=+15.8 → 贴图 y≈14~50）在上半部分。
2. **`跟` 文字下移的量化依据**：节点 103×103、贴图 105×115、`_sizeMode=0` →
   贴图坐标 = `(51.5 − 节点y) / (115/103)`。数字区占贴图 y 14~50，
   所以「跟」字必须落在 **y≥60**。实测成品白字落在 y[72,91]，与数字区留有 22px 净空。
3. **AI 图生图会自己加「球面高光带」**：第一轮用成品当参考图，竖排白色反光带被整组继承，
   盘面变成玻璃球、还压字。提示词必须显式写「盘面是平的，不要白色反光条/高光带/球面高光」，
   同时给两张参考图（原图定「平整」+ 定稿定金环样式）才稳。
4. **系列一致性只能靠「拿定稿当参考图」**：各自拿原图当参考 → 11 张金环各长各样（回纹/铆钉/宽度都不同）。
   改用定稿 `gen3/丢.jpg` 当参考图后整组统一。
5. **调色板量化完胜位深量化**（最重要的新手法）：
   位深要压到 4bit 才勉强不超原图（画质差、还可能banding）；改 `im.quantize(colors=256, method=Image.FASTOCTREE)`
   后 **丢 13 710 → 6 001 B（44%）**，平均像素差仅 1.88。回座 6 266 → 4 080 B（65%）。
   **总计 173 147 → 63 115 B（36.5%）**，比 v1 的 142 255 B 又降了 56%。
6. **圆形按钮的 alpha 直接用原图的 alpha 通道**（裁 bbox → 跟着 AI 图一起缩放 → 贴回）。
   比重新解析 AI 图轮廓稳，零白边、剪影与原图逐像素一致。

### 结果
11 张全部覆盖替换，画布尺寸未变 → `.meta` 一个都没改。
交付预览：`design-previews/待确认/2026-09-18-游戏内按钮宝石版/游戏内按钮-宝石版-预览.png`
（含放大设计稿 + 实机 1:1 叠运行时白数字验证 + 原版对照）。旧预览目录 `2026-09-18-游戏内按钮亮金/` 已删除。

## 3 款新牌背替换到 zuotype（21:30~21:45，已完成）

需求：把上一轮做的 3 款亮色牌背替换到 `assets/resources/zuotype/` 下的「搓背0-2 牌背0-2」共 6 个文件。

映射：`搓背0/牌背0` ← `pk2/bigbig2.png`(红)、`搓背1/牌背1` ← `bigbig1.png`(蓝)、`搓背2/牌背2` ← `bigbig.png`(紫)。

### 关键几何（决定「零改 meta」的关键）
- 目标画布 **348×447**（保持不变）；源图 `bigbig*.png` 画布 348×480，卡片 alpha bbox = 313×462。
- 目标卡片 alpha bbox：`搓背0/1/牌背0/1` = (4,3,329,427) → 326×425；`搓背2/牌背2` = (4,4,330,428) → 327×425。
- **策略：把新卡片 LANCZOS 缩放铺满原卡片的 alpha bbox**（不是等比留边）。因为 462/313=1.476 直接填 425/326=1.304，
  相当于旧图的 1.13 倍横向拉伸 —— 与旧图**同样的微小变形**，视觉一致。做方案对比后选它。
- 结果：PNG bbox 与 `.meta` sub 逐项吻合，`.meta` 一字未动（这批本来就是 `trimType:null`，**天然免疫自动重导入**）。
- 体积 **1 036 868 → 219 374 B（21.2%，省 817 494 B）**。
- 交付预览：`design-previews/待确认/2026-09-18-zuotype牌背替换/zuotype牌背替换-预览.png`。

### 顺带查清的引用关系
- **在用**：`搓背1`(69e162ec) → 手牌 BK0 88×112 ×4 + 搓牌窗口「遮罩」519×752；`牌背1/2/3` → 设置里 107×144。
- **未使用**：`搓背0`(92c1ebc8)、`搓背2`(d28907b0)、`搓背3`(8ba398c1)、`牌背0`(5826c0fa)
  在当前场景/预制体中无引用（只出现在 `art_sources/v8-repairs/*/before/` 与 `temp/BackupAssets/` 历史快照里）。

## 【重要修复】Creator 在跑时覆盖 PNG，`trimType:"auto"` 的 trim 会被重算（21:45）

**现象**：回滚过一轮的 `.meta` 又变脏了：`游戏内/*.png.meta` 的 subMetas 从
`trimX=0 / 98x100`（未裁剪）被改成 `trimX=2,trimY=3 / 93x93`。

**根因**：原图最外缘有一圈 `alpha=1~5` 的极淡像素撑着 trim 边界，所以原 trim=满画布；
新图 alpha 边界紧 → Creator 重导入就裁到真实 alpha bbox。`pgrep -fl CocosCreator` 确认 Creator（PID 3131，
`--path /Volumes/SSD/qing`）正在跑。`_sizeMode=0` 的精灵按 rect 铺满节点 → 图形凭空放大约 5%。

**根治（本轮采用）**：不靠回滚，而是**让重算结果恒等于原值**。
按原 `.meta` 的 trim 矩形给新图补回那圈像素：边界上 `alpha<=1` 的位置填 `alpha=2` + 不透明像素中位色。
- 补完**逐张验证** `alpha>0` / `alpha>=1` / `alpha>1` 三种判定的 bbox 全等于 `(trimX,trimY,width,height)` ✅
  （不同版本阈值口径可能不同，三种都过才稳；本机 `trimThreshold:1` 对应 `alpha>1`）。
- 再 `git checkout HEAD -- *.meta` 回滚，`git status` 只剩纯 PNG 变动。
- 11 张补边后体积**不升反降**（`btn_diu 6001→5708 B`），alpha=2 在深色 UI 上完全不可见。
- 备选方案：把 `trimType` 改成 `null`（彻底免疫，但会改 `.meta` 内容）——本项目要求 meta 一字不动，故选补边法。

**最终 git 状态**：`assets/ImagesLuck/游戏内/` 11 个纯 PNG + `assets/resources/zuotype/` 6 个纯 PNG，
`.meta` 全部干净。两批均**未提交**。等主人 Creator 验收后跑 `1生成热更新包.command`。

已写入技能 `cocos-ui-element-reskin` 新增 6.15 节 + 常见坑表 2 行。

## 分牌背景（牌位底座）重做（21:54~22:10，已完成）

需求：「他是一个用来放牌的底座，重新设计一下 不用太花哨，牌背大小差不大，用来放牌的」。

### 节点结构（panelGameView.prefab / drh8.fire 一致）
```
目标 (193x142.7)
├── 分牌背景      70x92  trs x=-39.234  scale=1.1  _sizeMode=0
├── 分牌背景 copy 70x92  trs x=+44.823  scale=1.1  _sizeMode=0   ← 同一个 spriteFrame
├── 扯1          70x90  trs x=-39.151  y=-0.77   （空节点，运行时填牌）
└── 扯2          70x90  trs x=+44.742  y=-0.77
```
实机显示 **77×101**（70×92 × scale 1.1）。两个节点共用 uuid `3848c8a5`，替换一次两处同时生效。
> 坑：prefab 里 spriteFrame 的引用字段是 **`__uuid__`**（不是 `_uuid`）；用错会「命中 0 个」误判成没引用。

### 设计
原图是深蓝**同心方框 + 8L 徽章**（旧 LOGO，太花哨）。新图改为：一整块深蓝宝石底 + 细金边 + 描边内侧一道青线，四角小切角，**中央完全留空**。画布 70×92 不变。

### 三轮出图记录（Seedream 4.5，2048×2692）
1. **纯文生图 v1**：形状/配色对了，但 AI 在**底部加了一大片白色亮斑 + 蓝色发光光晕**（老毛病）。
2. **图生图 v1b**（拿 v1 当参考图，只要求去亮斑 + 去四角铆钉）：亮斑去掉了，但主体被裁出画布。
3. **图生图 v1c**（拿 v1b 当参考图，只要求「完整出现在画面中央，四边留白，不许裁切」）→ **定稿**。
   另跑的 v4（纯文生图重出极简版）被 AI 画成「白卡里嵌一条蓝条」，废；v2「石碑」、v3 墨绿绒布也不合适。

### 关键结论
- **「去掉某处多余元素」比「重画一遍」稳**：图生图只描述要改的那一处 + 明确「其余原样保持」，一次到位。
- **主体尺寸要按原图 `alpha>128` 的 bbox 对齐**（原图 59×87 → 新图 64×88），不是按画布；
  否则生成图自带的白边会让底座显小一圈。（已写入技能 6.16）
- **白底抠图用「最大通道」软 alpha**：`clip((252 - rgb.max(2))/10, 0, 1)`；配合「逐行 min/max x」求 inside 硬掩码
  （圆角矩形每行是一段连续区间），比 floodfill 快几十倍。用 min 通道会把青色描边漏判、抠完留白边。
- **换图后 `.meta` 一字未动** ✓ —— 新图四周已按 6.15 补了 `alpha=2` 的极淡像素，`alpha>0/>=1/>1` 三种判定
  的 bbox 全等于 `(0,0,70,92)`，Creator 重导入后 trim 仍是 0,0 70×92。

### 结果
`assets/ImagesLuck/游戏内/分牌背景.png`：**3 581 B → 2 144 B（P256 调色板量化，59.9%）**；画布 70×92、`.meta` 不变。
交付预览：`design-previews/待确认/2026-09-18-分牌底座/分牌背景-新底座-预览.png`（放大 ×6 对照 + 实机 1:1 + 牌压上去的效果 + 左右真实间距）。

### 顺带
- `seedream.py --json` 输出里本地路径字段是 **`local_path`**，没有 `path`（已更新 seedream-image 技能）。
- 同目录还有 `确认分牌.png`（7 443 B）本次未动。

## 确认分牌 按钮重做（22:06~22:25，已完成）

需求：「确认分牌 按钮也改一下 重新设计一下」（紧接分牌底座）。

### 节点结构
```
cmd2 (625x368)
├── bk 401.99x368
├── 目标 193x142.7            ← 分牌底座
├── 手牌 283x118
├── 确定扯牌 147x57  scale=1.2  _sizeMode=1(TRIMMED)  ← 用 确认分牌.png
├── 扯牌进度 84x84
├── 牌型1/牌型2  Label sz=35（空串）
```
实机显示 **176×68**（147×57 × scale 1.2）。`确定扯牌` **没有子节点**，所以「确认分牌」四个字是**画在图里**的，直接重绘。
> 注意：`_sizeMode=1`(TRIMMED)，与分牌底座的 `sm=0` 不同 —— 贴图 rect 直接对应节点，四周透明边不占位。

### 设计
沿用上一轮按钮那一套：**金色立体金属外环 + 香槟金色珐琅内盘 + 深棕细描边字**。
拿 `回座.png`（同为 148×57 胶囊、同套金环）当参考图保系列一致；盘色从玫红换成香槟金，因为是「确认」主操作。

### 四轮迭代（关键教训，已写入技能 6.18）
1. 纯图生图（回座当参考）→ 字画对了但**撑满内盘、顶到金环**。
2. 加「缩小到三分之二」→ 没变小。加「缩小到 60%」→ 反而更大。
3. 「只占内盘宽度的**三分之一**左右，四周留出大量干净空白」→ **终于变小** ✓
   ⇒ **相对描述（"缩小一点"）AI 基本不理，绝对占比（"三分之一"）才有效**；
     而且参考图自带文字比例，AI 会连比例一起抄，压不住就用否定清单兜。
4. 选定后处理：抠白底 → 缩到宽 147 → 居中放 148×57 → 补极淡外圈。

### 【新坑】抠白底把按钮内部高光挖成黑洞（已写入技能 6.17）
第一版抠图后内盘上出现**黑色斑块**（4× 放大可见）。原因：软 alpha
`soft = clip((252 - rgb.max(2))/10)` 是**全图**算的，按钮**内部**的白色高光 `max≈252+` → alpha≈0 → 透明。
`dilate(inside,2)` 只护住边界外 2px，护不住内部。
**修正**：内部核心区强制不透明，软 alpha 只管边界几像素 ——
`core = ~dilate(~inside, 3)`（即腐蚀 3px），`al = where(core, 1.0, soft) * inside * 255`。
凡亮色/金属质感、内部带高光或渐变到近白的素材都必须这么写。

### 结果
`assets/ImagesLuck/游戏内/确认分牌.png`：**7 443 B → 4 577 B（61%）**；画布 148×57 不变，`.meta` 一字未动
（trim 仍 `0,0 147×57`，`alpha>0/>=1` bbox 均等于该矩形），UUID 不变。
交付预览：`design-previews/待确认/2026-09-18-确认分牌按钮/确认分牌-新按钮-预览.png`
（×3 放大对照 + 实机 1:1 + 与同套 9 个按钮并列）。

### 当前累计未提交
`assets/ImagesLuck/游戏内/` 13 个 PNG（11 按钮 + 分牌背景 + 确认分牌）+ `assets/resources/zuotype/` 6 个 PNG，`.meta` 全部干净。

## 按钮边缘「白锯齿」批量修复（22:15~22:35，已完成）

主人反馈：「确认分牌 按钮外面 还有一圈白色锯齿，图片没切干净吧」。

### 根因
白底抠图只处理了 **alpha**（透明多少），**没处理 RGB**。白底与前景的过渡像素 RGB 本身是「金色掺白」，
例如实测 `rgb=(253,251,234) sat=0.08`、`rgb=(204,198,181) sat=0.11`。
即使 alpha 给到 255，渲染出来仍是浅色 → 深色背景上就是一圈**白色锯齿**。
> 这与本轮之前那个「内部高光被挖成黑洞」是**两回事**：黑洞是 alpha 错，白边是 RGB 错。两个坑都写进技能（6.17 / 6.19）。

### 诊断判据（关键：sat 必须卡到 0.15）
```python
body  = al >= 200
band  = ((dilate(body,2) & ~body) | (body & ~erode(body,2))) & (al > 0)   # 只扫外沿 2px + 主体最外 2px
white = band & (mn > 170) & (sat < 0.15)
```
- `sat` 放到 **0.40** 时，圆钮全部「中招」（20%+），但放大目视**圆钮是干净的** → 假阳性。
  原因：金环高光是有饱和度的金色（sat≈0.33），被 0.40 的阈值圈进去了。
- 卡到 **0.15** 才是真·去饱和白边（sat≈0.08）。
> 教训：判「有没有白边」不能只看亮度，**必须同时限「去饱和」**；先用放大目视校准阈值，再批量跑。

### 修复
从「正常像素」迭代扩散颜色，**只替换判为白边的像素**（alpha 原样保留，保住抗锯齿）：
```python
good = (~white) & (al>0) & ((sat>0.30) | (mn<140))
for _ in range(12):                       # 四方向迭代膨胀
    ... col[take] = sc[take]
rgb[white] = col[white]
```

### 结果
| 文件 | 体积 | 白边像素 |
|---|---|---|
| 确认分牌 | 4577→4459 B | 361→0 |
| 回座 | 3854→3793 B | 340→0 |
| 敲 / 丢 / 休 / 跟 | 6279→6099 / 5708→5558 / 6175→5973 / 5802→5631 | 各 ~220→0 |
| 大 / e1 / e2 | 6364→6344 / 6179→5954 / 6317→6121 | →0 |
| 2 / 3 / 4 | 4478→4343 ×3 | 各 230→0 |

共 **12 张**（11 按钮 + 确认分牌）白边清零，体积全部不升反降；画布尺寸与 `.meta` 全部未动。
`分牌背景.png` 本身无白边（0%），未处理。
> 胶囊形（回座、确认分牌）白边最显眼（上下长直边），圆形钮的白边短、易被金环高光掩盖，
> 所以主人先发现的是确认分牌。

交付预览：`design-previews/待确认/2026-09-18-按钮边缘修复/按钮边缘白边修复-预览.png`

## 排查：收到全服通知 ",,,,文本" 时误播「喜金」音效（只读，未改代码）

- 消息链路：`Account.js onMessage` → `KBEngine.Event.fire("SystemInfo", strMsg)`；全项目只有 4 个 `register("SystemInfo")`：
  `panelGameView` / `panelCloudNotify` / `GameDataManager` / `RoomInviteManager`。
- **唯一音效出口**：`panelGameView.ts:2602 SystemInfo()` → 2629 `this.gameLogic.PlayAudio2("喜金")`（资源 `Audio/eff/喜金.wav` 存在）。
  同分支还会 `奖池.active=true` + `displayJC.playAnimation` + `showMsg(arrayAll[4])`。
  `panelCloudNotify` 只在含「抽中」时播「烟花」；`GameDataManager` 只弹面板；`RoomInviteManager` 按 `ROOM_INVITE|` 前缀先 return。
- **根因**：2624 前的过滤 `let strRoomID = getAccount().roomID; if(strRoomID == arrayAll[2])` ——
  既没校验 `arrayAll[2]` 非空，又用弱相等。**后台通知格式就是空房间号**：
  `XuanManager/server/internal/api/notification_carousel.go:342 notificationSystemContent() = ",,,," + content`
  ⇒ 每条「通知_所有玩家_信息」（含轮播）split(',') 后 `arrayAll[2] === ""`。
  于是当客户端 `roomID` 为 `""`（或数字 0，`0 == ""` 也为 true）时 `"" == ""` 成立 → 误判为本房间喜金播报。
- 客户端 `roomID` 为空的窗口（代码里 `account.roomID = ""`）：`panelGameView` 728/1541/1559/3345、
  `DrhLogicMgr` 475 —— 均是「先清空 roomID，再异步切场景」，面板未销毁期间收到通知就会响；新场景 `panelGameView` 创建后
  若服务端 roomID 属性尚未回填，同样是空窗（`GameDataManager.onEnterRoom` 只交给 QueueMatchManager 另存，不写 roomID）。
- 建议改法（未实施）：`if(strRoomID && strRoomID === arrayAll[2])`，或 `Number(arrayAll[2]) > 0 && Number(arrayAll[2]) === Number(strRoomID)`。

## room_ready_art 抠图杂边修复（22:19~22:35，已完成）

主人反馈：「room_ready_art 之前生成的这个图片也同样有这个问题 抠图不干净 显示不对」。

### 定位
- 文件：`assets/resources/other/drh/room_ready_art.png`，1024×238，136 566 B（已提交状态）
- 引用节点：`房间已就绪美术字` 440×102 `_sizeMode=0` scale=1（panelGameView + drh8.fire 各 1 个），父节点 `等待开局提示`
- 贴图/节点比例 4.3025 vs 4.3137，**无拉伸**；`trimType: none` → 天然免疫 trim 重算
- alpha bbox (25,36)-(998,217) = 974×182，四周留透明边

### 问题
和按钮那批同源：抠图只处理了 alpha，没处理 RGB —— 字的外沿 2px 内有一圈**去饱和的灰白像素**
（574 个，占外沿 14.9%），深色牌桌上就是一圈"没抠干净"的杂边。
> 剖面实测：`x=637 a=252 rgb=(236,228,212) sat=0.10` 紧接着 `x=638 rgb=(144,112,52) sat=0.64`。

### 关键差异（已写入技能 6.19）
这张图**不能**照搬按钮那套「外沿 2px + 主体最外 2px」的扫描范围 ——
「游戏已就绪」的**字面本身就是银白色**，把主体最外 2px 算进去会把字面的白也替换掉。
**只取主体之外的 2px**（`dilate(body,2) & ~body & (al>0)`），字面整块保留。
> 判据：放大 3 倍看「主体最外那一圈」是不是设计本身的一部分 —— 是就只修外沿。

### 结果
外沿近白像素 **574 → 0**；体积 **136 566 → 35 463 B（26%）**；
画布 1024×238、alpha bbox、`.meta`（`trimType:none` / UUID）全部不变；Creator 未改写。
交付预览：`design-previews/待确认/2026-09-18-room_ready_art修复/room_ready_art-杂边修复-预览.png`

### 累计未提交
`assets/ImagesLuck/游戏内/` 13 PNG + `assets/resources/zuotype/` 6 PNG + `assets/resources/other/drh/room_ready_art.png` + `assets/Scenes/drh8.fire`，`.meta` 全部干净。

## room_ready_art 重新生成 + 硬策略抠图（22:35~22:50，已完成 · 上一条修补方案被否决）

主人反馈（否决修补）：「还是不对 这样抠图 抠不干净 效果太难看了，直接重新生成把」。

### 根因（重要认知）
上一版把外沿近白像素从 574 修到 0、体积也降了、`.meta` 没动，**指标全过但用户仍判难看**。
原因不在「白」，在 **alpha 的外发光软渐变**——原生成图自带外发光，外沿天然是 alpha 从 255 平滑降到 0 的
**十几像素雾边**；6.19 只换 RGB、没动 alpha，装进游戏仍是一圈晕开的糊边。
⇒ 用户说「抠不干净」≈ **边界不锐利**，跟 RGB 颜色关系不大。

### 做法
1. **重新生成**（不是修补），3992×928 图生图 2 版：
   - **A 版定稿**：边缘极干净锐利 / 无外发光光晕白边 / 最外沿直接是金棕描边 / 纯白背景 / 主体完整居中不裁切
   - B 版（要求「沿最外沿画一圈金棕描边线」）→ AI 自己加了个方框，**废**
2. **硬策略抠图**：`hard = clip((255-mn)/30,0,1) > 0.55` 硬切 → `core = erode(hard,3)` →
   颜色四方向扩散 **14 轮** → **所有非 core 的可见像素 RGB 一律用内侧颜色**（不是外沿 2px）；
   alpha 只做 `GaussianBlur(0.6)` + `keep = dilate(hard,1)`，核心区强制 255。
   - 参数是调出来的：初版 10 轮 + 腐蚀 2px 时外沿仍剩 **492** 个近白像素
3. 按原图 `alpha>128` bbox `(25,36)-(999,218)` 对齐贴回 1024×238 画布

### 结果
- **136 566 → 34 023 B（24.9%）**
- 画布 1024×238；alpha bbox `>0` / `>128` 判定全等于原图；`.meta`（`trimType:null`、UUID `37e6d7c7…`、sub `a3d8b745…`）**零改动**，Creator 未重写
- 交付预览：`design-previews/待确认/2026-09-18-room_ready_art重做/room_ready_art-重新生成-预览.png`

### 已落盘
技能 `cocos-ui-element-reskin` 新增 **6.20**（含「6.19 修完还被否 → 走 6.20」的决策规则表：硬边 alpha 走 6.19 / 外发光软渐变走 6.20）+ 坑表 1 行。

### 顺带建议（主人尚未回应）
同一批（12:00 改名任务）的 `leaderboard_v8_hero_title`、`nav_bar_exact`、几张盾牌图大概率有同样的
**外发光软边**问题，可以用同一套 6.20 手法一次性重做。
> 后续：本节方案（外发光重生成版）随后被主人否决，见下一节「改成实心牌匾」，那份预览已删。

## room_ready_art 第三次：改成实心牌匾（22:52~23:10，已完成）

主人反馈：「不行，你这扣的乱七八糟，重新设计一下 避免抠图这么多的把，减少镂空。好看一点」

### 认知升级（关键）
**这是设计问题，不是抠图问题。** 原设计「银白字面 + 金棕描边 + 内部渐变反光 + 两侧 1~2px 细金线」
本身就带**成百上千处细缝、内腔和细线**：细缝在硬阈值下时断时续、闭合内腔抠成孔洞、
细线缩到实机不到 1px 必然断成一串点。**6.19 换 RGB / 6.20 硬切都只修边界那一圈，救不了形状本身。**

### 解法：把元素改成「一个饱满实体块」
| 原设计 | 改后 |
|---|---|
| 字浮在桌面上（银白面 + 金棕描边） | **深棕木纹实心横匾 + 亮金包边**，字画在匾上 |
| 左右各一条细金线装饰 | **删掉**（细线是「边缘乱」头号来源） |
| 镂空菱形徽章 + 内嵌宝石 | **实心圆形金章 + 实心字母 BY** |
| 透明孔洞：内腔 + 缝隙 + 细线间隙 | **透明孔洞 = 0** |

机制：**字画在实心底板上 → 字与底都不透明 → 不产生任何镂空**。底板本身也不能有镂空花纹。

### 出图（4 版，第 2 版定稿）
- A1（文生图）：深棕木牌 + 金边 + 金字 + 左端 BY 圆章 ✓ 方向对，但**字太满、上下贴边**
- **A2 定稿**：字高改成「占底板二分之一左右」→ 上下留出宽阔木纹区 ✓
- B（冷色备选）：深蓝宝石底 + 金字 → 偏塑料、和深蓝桌面不分家，**弃**
- A3：写「字高占五分之二」→ AI **反向理解成放大**并给字加了黑描边，**弃**（同 6.18：别贪小）

### 抠图两个新坑（已写入技能 6.21）
1. **在目标分辨率上抠图**，不要原分辨率抠完再缩：原图 4608×864 → 目标 974×182（缩 4.7 倍），
   先抠再缩会被 LANCZOS 重新掺进白像素。正确顺序 = **先 resize 到目标尺寸 → 再硬阈值 → 再扩散填色**。
   阈值 215 一刀切（金边 mn≈120 / 木纹 mn≈40 / 白底四角 mn≈247），之后开运算去孤立噪点。
2. **AI 牌匾常铺满整张画布**（只有四角因圆角露白）→ `alpha>128` 的 bbox **等于全画布**，
   **不要裁 bbox**（会切掉圆角），整张缩放到目标主体尺寸再贴到目标坐标。

### 结果
- **真孔洞 0 px**（判定法：从画布四边在 `alpha=0` 上连通膨胀，剩下的才是被包围的孔）
- **外沿白像素 0**；抗锯齿过渡带仅 937 px（1px 宽，正常）
- **136 566 → 30 108 B（22%）**（P256 量化；30 KB 已达标，不用再降色数）
- 画布 1024×238、alpha bbox `(25,36)-(999,218)`、`.meta`（`trimType:null`、UUID `37e6d7c7…`）**全部不变**
- 交付预览：`design-previews/待确认/2026-09-18-room_ready_art牌匾重设计/room_ready_art-牌匾重设计-预览.png`
  （上一个「重做」目录已删）

### 已落盘
技能 `cocos-ui-element-reskin` 新增 **6.21**（提示词骨架 + 两个抠图细节 + 真孔洞检测代码）+ 坑表 3 行。

## room_ready_art 第四次：同色系横幅（23:15~23:35，已完成 · 上一版实心木牌被否决）

主人贴了实机截图反馈：「这个设计放桌面太难看了，重新设计一版。要好看不突兀的」

### 根因（重要认知）
上一版做成**深棕木纹牌匾 + 粗金框**，装到**深墨绿绒布桌面**上：
- 棕色块 vs 绿色桌 = **跨色相撞色**
- 实心木纹 + 粗金框的质感又重又像"告示牌"，和游戏 UI（细线 + 半透明 + 圆角）完全不搭

**给底板选色时只想着"元素自己好不好看"，没看它要落在什么背景上。**

### 解法
1. **底板色 = 场景主色的同色系深一档**：从截图采样桌面是 `#003E2F`（深墨绿），底板用 **`#012B18`**。
   对比交给金边和金字，底板只负责"沉进去"。
2. **质感对齐场景 UI 语言**：游戏里的按钮全是「深色圆角 + 细金边 + 金色文字」→ 照这个做。
3. 细金线装饰、镂空徽章、BY 章**全部删掉**。

### 出图
- 先试纯文字（无底板）：5 个金色字贴绿底 → **太轻太单薄**，和下方房号信息（也是纯文字）混在一起，不够"设计感"
- 横幅版 3 版：P1 亮翡翠绿（撞色倾向）、P2 深蓝黑（和 UI 按钮一致但跨色相）、**P3 深墨绿 #012B18 定稿** ✓

### 结果
- **真孔洞 0 px**、外沿白像素 0、过渡带 936 px
- **136 566 → 33 790 B（−75%）**
- 画布 1024×238、alpha bbox `(25,36)-(999,218)`、`.meta`（`trimType:null`、UUID `37e6d7c7…`）**全部不变**
- 交付预览：`design-previews/待确认/2026-09-18-room_ready_art横幅重设计/`（旧的「牌匾重设计」目录已删）

### 方法改进（最有价值的一条）
**把新图贴回主人给的实机截图再出对比图**，而不是在纯色底上模拟。
- 自动定位旧元素：颜色掩码（旧版是棕色木牌）→ 先按行统计找主体行段 → 再在行段内按列统计找列段
- 缩放比 = 截图 bbox 宽 / 贴图主体宽；`paste` 位置 = `(bx0 - 主体左上x*scale, by0 - 主体左上y*scale)`
- 可信校验：截图 bbox 宽高比 5.30 ≈ 贴图主体 5.352 ✓
- **纯色底只能验"形状干净"，验不出撞色**；真实截图带桌布纹理、渐变、周边 UI，一眼看出是"融进去"还是"贴上去"

### 已落盘
技能 `cocos-ui-element-reskin` 新增 **6.22**（三条硬规则 + 截图定位/贴回代码 + 校验方法）+ 坑表 2 行。

## room_ready_art 撤销：退回 HEAD 原版（23:40~23:45，已结束）

主人：「算了 帮我退回 最开始的 8L 的版本把 这个图片」
（徽章里其实是 **BY**，不是 8L；主人指的是 HEAD 里那版「BY 游戏已就绪」原图）

### 操作
```
git checkout HEAD -- assets/resources/other/drh/room_ready_art.png
```
- 恢复后：1024×238 **RGBA**、136 566 B、alpha bbox `(25,36)-(999,218)` —— 与 HEAD 逐项一致
- `.meta` 本来就没动过（`trimType:null` / UUID `37e6d7c7…` / sub 1024×238），**不需要一起回滚**
- `git status` 里该文件已消失 ✓
- 该图相关的两个预览目录（牌匾重设计 / 横幅重设计）已按「中间产物不留」删掉

### 剩余未提交（20 个）
`assets/ImagesLuck/游戏内/` 13 PNG + `assets/resources/zuotype/` 6 PNG + `assets/Scenes/drh8.fire`

### 教训（重要，已写入技能 6.22 尾部的「停手信号」）
同一素材被**连续否定 3 次**后应停下问清方向（"要更轻"还是"干脆别改"），不要再自己一路迭代。
`room_ready_art` 走完「RGB 修补 → 重新生成硬抠 → 实心木牌 → 同色系横幅」**四轮后主人直接撤销，四轮全废**。
根因：一开始没确认「这个元素到底要不要动」——它只是一行提示文字，主人可能本来就没打算大改。

## 提交并推送（23:50）

`005268e` 游戏内按钮/图标重做 + zuotype 牌背替换 → `1d6b7c3..005268e main -> main`，工作区已干净。

本次提交内容（27 个文件）：
- `assets/ImagesLuck/游戏内/` 13 张（12 张按钮/图标宝石版 + 白边修复、分牌背景、确认分牌）
- `assets/resources/zuotype/` 6 张牌背（搓背0-2 / 牌背0-2）
- `assets/Scenes/drh8.fire`：`确定扯牌` 节点 147→148 宽、`_sizeMode` 1→2、Label fontSize 30→35（**主人自己在 Creator 里改的**）
- 5 份 `design-previews/待确认/` 预览 + 2 个 memory 文件
- `.meta` 全部干净；提交前删掉了项目根下 seedream 的临时产物 `outputs/`

## room_ready_art 最终定案：主人自己换的素材（23:55 已提交）

主人自己在 Creator 里替换了 `assets/resources/other/drh/room_ready_art.png`：
- 新素材 **868×290**（@2x）、144 450 B，是「金色立体浮雕字 BY 游戏已就绪 + 蓝宝石菱形徽章 + 两侧细金线」
- `.meta` 的 `width/height/rawWidth/rawHeight` 由 Creator **自动同步**成 868×290；**UUID 与 `trimType:null` 都没变** ✓
- `drh8.fire` 的 `房间已就绪美术字` 节点跟着改成 **434×145**（比例 2.993 = 868/290，正好 @2x 匹配），位置 y 495.65 → 506.809
- 另外若干 Label 的 `_spacingX` 0 → 2

### 可复用结论
- **在 Creator 里直接把新 PNG 覆盖上去**，只要文件名和路径不变，`.meta` 的 UUID 就是稳定的；
  Creator 会自己同步 `width/height/rawWidth/rawHeight` 四处 —— 不必手动改。
- 但节点尺寸**不会**自动跟着变（除非 `_sizeMode` 是 TRIMMED/RAW）：
  `_sizeMode=0(CUSTOM)` 时节点会按自己的 rect 把新贴图拉伸，比例不对就会变形。
  这次主人自己把节点 440×102 → 434×145 改对了，**换图后一定要核对"贴图比例 == 节点比例"**。
- 结论：这类"提示文字/装饰字"素材，主人最终还是选了自己那版华丽风格（金色浮雕 + 细线 + 徽章），
  前面 4 轮重设计全部作废 —— 印证了「同一素材连续被否 3 次就该停下问方向」。

`b7573f5` 替换 room_ready_art 为金色浮雕字新版 → `005268e..b7573f5 main -> main`，工作区干净。

## 网页版全屏 loading 盾徽：BL → BY（00:20~00:45）

主人反馈：「网页版 在加载资源的时候 全屏loading界面中 还有 8L 改一下 改成 BY」

### 定位
- 图是 **`build-templates/web-mobile/splash.png`**（256×256 RGBA，83,662 B），
  同时构建产物 `build/web-mobile/splash.e5061.png` 与它**逐字节相同**（md5 一致）
- 内容：蓝宝石盾徽 + 金色包边/月桂叶，三行文字「BL POKER」/「BL」/「POKER GAME」
  —— 中央那个艺术化的 **BL** 就是主人说的「8L」（B 的造型像 8）
- **之前批量改名 BL→BY 只扫了 `assets/`，所以漏掉了 `build-templates/`** ← 这是根因

### 做法（路线 A 整张 AI 重绘）
纯 LOGO/徽章类、无密集中文 → 走路线 A。生成尺寸 2048×2048（R=1）。
两版候选，选**候选 2**（盾牌轮廓有下尖、宽高比 0.890 最接近原图 0.892）。

### 踩到两个坑（都已写入技能 `art-text-swap`）
1. **AI 给盾徽自动加了一圈灰色外描边/阴影**：(212,212,212)、**alpha=255 的实心带**。
   用 `hard = mn<230` 会把它当前景 → 深色底上一圈灰白边。
   改成 `hard = (sat>0.18) | (mn<130)`（**彩色 or 暗色**）后：
   占位 0.5775→0.5047、bbox 比例 0.881→0.890，与原图（0.5094 / 0.892）几乎吻合。
   再在目标尺寸上补一刀 `kill=(al>0)&(mn>170)&(sat<0.12)`，
   外沿近白像素 **607 → 35**。
2. **LANCZOS 缩放前必须预乘 alpha**：bbox 裁切框外的白色会在缩放时混进边缘，
   导致反预乘后边缘发白。RGB 预乘后缩放、alpha 单独缩放，再反预乘。

### 结果
- 文字：**BY POKER / BY / POKER GAME** ✓
- 尺寸 256×256、alpha bbox `(24,12)-(231,244)` 与原图**完全一致**
- 体积 **83,662 → 60,860 B（−27%）**
- 已回写 `build-templates/web-mobile/splash.png`（git 跟踪）
  + `build/web-mobile/splash.e5061.png`（build 目录未纳入 git，改它让网页版立即生效）

### 交付
`design-previews/待确认/2026-09-18-网页loading盾徽改名/网页loading盾徽-BL改BY-预览.png`
（深色底 / 白底各一组，另加顶部小字与中央大字的 1.9× 放大对照）







## 网页版全屏 loading 的 `8L` → `BY`（改对了位置）

### 定位
用户第一次说「全屏loading界面中还有8L改一下」时，我**改错地方**：改了 `build-templates/web-mobile/splash.png`（盾徽 splash 图）。
用户第二次给截图（「正在进入牌桌 / 正在下载场景资源…」界面，左上 76×76 圆角方块里是 8L）后定位到真源头：

- **不是图片，是 DOM 文字**。Grep `正在进入牌桌|正在下载场景资源` → `assets/scripts/common/WebSceneLoader.ts:78`（标题 `sceneTitle()`，`drh8 → "正在进入牌桌"`）；
  Grep `WebLoadingManager` → `assets/scripts/common/WebLoadingManager.ts`，**第 255 行**内联 HTML：
  `'<div class="qing-mark">8L</div>' +`
- CSS 在 `ensureDOM()` 内联 style（232 / 247 行）：76×76、`border-radius:22px`、`font-size:28px`、`letter-spacing:1px`、浅青系描边+渐变底。

### 修改（三处）
1. `assets/scripts/common/WebLoadingManager.ts:255` `8L` → `BY`（源码，需重新构建才进发布包）
2. `build/web-mobile/assets/main/index.33c3e.js`（build 未纳入 git，改它让**网页版强制刷新即生效**）
3. `build/jsb-link/js backups (useful for debugging)/main.index.js`

⚠️ `index.33c3e.js` 里 `8L` 共 2 处，另一处是 uuid 片段 `"95deb362KlHwJUYKk8LMtGv"`，**不能动**。改完 `count(BY)==1 && count(8L)==1`。
`build-templates/` 全文扫描无 `8L` 残留（HTML 层只有这一处）。

### 交付
`design-previews/待确认/2026-09-18-网页loading标记改BY/网页loading标记-8L改BY-预览.html`（原始 CSS 复刻，左右对照）

### 教训
**用户说「某个界面里的文字」时，先分清是「贴图上的文字」还是「DOM/引擎文字」**。
网页版 loading 层全是 `WebLoadingManager` 注入的内联 HTML，不要默认去改图片。
