# 2026-09-21

## 调研：充值增加 USDT 的接入方案（只出方案，未改代码）

摸清现状（基于代码与文档核实）：

- 客户端充值页 `assets/scripts/UI/panelQianBao.ts`：`onButtonClick` 的 `确认充值` 分支第一行就是硬拦截
  （`showPanel("panelMsgView",...,"暂未开通，后续处理"); return;`），**整个充值业务当前未开通**，原下单代码在其下方保留未删。
- 原充值链路：`http://<支付域名>/api/Pay/createOrder?uuid&amount&paytype&realname`（或 `WEB_TX_IP`
  `154.37.155.17:803` 的 `/api/JsonPay/createOrder`），支付站是**独立服务，源码不在本仓库**，后台可换 `支付域名`。
- 通道配置全在游戏库哈希：`支付管理`（可用渠道名列表）、`支付配置_<通道>`（Base64 JSON：
  `needinfo/infolist/money/notify/bank/bOpenInput/inputrange`）、`支付配置_提现`、`提现类型`。
  老的客户端内「支付管理」面板在 `assets/scripts/UI/panelManager.ts`。
- 提现侧 USDT 已完整落地：TRC20 地址输入 + `提现预留_<uuid>_USDT` + 后台三开关与 `USDT汇率`（1 USDT = ? 元）。
  即 **USDT 在提现侧已有先例，充值侧是空白**。
- 上分能力现成：旧内部管理 8890 `GET /newmeng793afw/exchange`
  （`coin/coin2/num/guuid/work_order/sgin`，`sgin = md5(exchange2_server_sign + coin + guuid)`，
  文档记载密钥 `qwert12345` —— 该密钥是明文，接外网服务前必须换）；后台另有
  `POST /api/admin/players/{guuid}/balance-adjustments`。入库表 `third_cash_info`、`usr_cash_water`、`work_order` 唯一工单号。
- 关键判断：法币充值靠第三方支付站验到账，**USDT 没有第三方兜底，必须自己验链上到账**；
  TRON 转账**不支持备注字段**，订单匹配只能靠「金额唯一化尾数」「一人一地址」或「用户提交 TXID 人工核验」。

给出的两条路线：A 半人工核验（1–3 天，后台点通过走现有加减分链路）→ 先跑；B 链上自动监听
（TronGrid/网关 + 订单匹配 + 确认数 + 幂等入账，2–4 周）。落点四处：客户端 Prefab/脚本、游戏库哈希配置、
后台订单核验页、新增收款服务。风控点：TXID 幂等去重、伪造/少付冒充、黑 U 冻结、汇率锁定、加密货币收款合规需自行确认。

用户仅要方案，未授权改代码。

## 补充讨论：一单一地址 vs 固定地址+金额尾数

用户按其他平台经验指出，USDT 支付一般是**一单一收款地址**（按地址即可定位订单/付款人），质疑固定单地址方案区分不了订单。核实后的结论：

- 两种做法本质都是**系统侧「唯一标识 ↔ 订单」映射**：一单一地址的识别力来自派生逻辑，不是链上自带的能力。
- 真实差异在成本与运维。TRON 上派生地址**只有 USDT 没有 TRX**，归集前要先给该地址补 TRX 或租能量，
  每笔归集约 1–3 USDT 能量费（租能量可压到 1 USDT 以内）；TRC20 转账本身的费用由**付款方**承担
  （含新地址激活）。固定地址方案零链上成本，但要求用户原样按尾数付款，改金额就匹配不上。
- HD 派生（BIP39/BIP44）**只需备份一次助记词**，不是管 N 个私钥；地址不可复用，需地址池与过期地址处理。
- 第三条路：crypto 网关（Cryptomus / NOWPayments 类）自带一单一地址，成本含在 0.5–1% 手续费里，
  不用碰私钥与归集。
- 推荐口径：客单价 ≥ 100 USDT 走一单一地址（攒够批量归集摊薄成本）；小额多且不想碰钱包走网关。

## TRON 转账费基准（2026-09 联网核实，USDT 充值方案用）

- **TRON Proposal #104（2025-08-29 生效）**：能量单价从 210 sun 降到 **100 sun**（1 TRX = 1,000,000 sun），
  USDT-TRC20 转账费腰斩。查老文章的成本数字会偏高一倍。
- 单笔 USDT 转账能量：收款地址**已持有过 USDT** ≈ 65,000 → 烧 6.5 TRX；
  **从未持有过 USDT 的新地址** ≈ 130,000 → 烧 13 TRX（合约要新建 balance slot）。
  *对「一单一地址」是关键：每个派生地址都是新地址，用户付款那笔按高一档算（费由付款方承担）。*
- **租能量**（TronSave / TokenPocket 等，按 sun 单价订单簿）：比烧 TRX 省 **60–80%**，单笔约 4.7 TRX；
  最小起租 64,000 能量，租期 15 分钟–30 天（默认 3 天），有 Auto-Buy 自动续。
- **归集成本按「地址数」线性增长，不按金额**：100 笔归集 = 650 万能量 → 烧 650 TRX / 租 30sun 195 TRX /
  租 10sun 65 TRX，折每单约 $0.2–0.6（租能量）。批量归集**不减少总能量**，只减少操作频次与出错。
- 归集前派生地址必须有 TRX 或能量（地址里只有 USDT 转不动）：给该地址打 TRX，或把租到的能量**委托到该地址**。
- **收币本身免费**（receiving costs nothing）；平台只在出账/归集时付费，付款那笔的费由付款方承担。
- 他链成本量级：BSC ≈ $0.1–0.3、Polygon ≈ $0.01–0.1、Solana ≈ $0.01–0.05、ERC20 ≈ $0.35–1+。
- 参考：Tronsave 商户指南明确「address-per-customer 是 TRON 上最干净的对账模型，地址即发票号」，
  代价是 sweep 成本随地址数线性扩张；替代方案是「每渠道一个固定地址 + 按金额与时间对账」。

## 提交：我的页（panelMain）V8 改版

- **Prefab** `assets/resources/UI/panelMain.prefab`：金币框节点由隐藏改为启用并放大（210×44 → 262×59，
  位置 -85,15 → -50,58）；`gold` 文本改为 190×39、位置 (-33,58)、颜色 (246,237,146)；
  `复制ID` 图标缩到约 32.3×33、上移到 (269.4,119.6)；**新增节点**「V8ID前缀」「编辑资料」；
  **删除冗余节点**「头像收费提示 copy」「修改个人信息2 copy」。
- **脚本** `assets/scripts/UI/panelMain.ts`：`onButtonClick` 中「编辑资料」与「头像」共用同一分支，
  点「编辑资料」直接打开「修改个人信息2」面板。
- **素材** `assets/V7/`：新增 `mine_gold_frame_v8.png`(17KB)、`mine_copy_icon_v8.png`(1.2KB)、
  `mine_edit_icon_v8.png`(1.3KB)；替换 `mine_profile_exact.png`（495776 → 399164 字节）。
  **注意：三张新增图当前未被任何 prefab/场景引用（uuid 也不在热更清单里），尚未接入界面**。
- 热更包版本由前一次提交升到 **5.0.21**。

## 实名表单瘦身：只收姓名，银行/卡号挪到提现时填（panelQianBao）

- **脚本** `assets/scripts/UI/panelQianBao.ts`：实名资料回填后的「是否需补全」判定由
  「姓名 + 银行 + 卡号」三者齐全改为**仅看姓名**（`if(value(1))`）；「提交实名信息」分支里
  删掉「请选择银行」「请输入正确的银行卡号」两处前置拦截。交易密码设置/校验流程未动。
- **Prefab** `assets/resources/Prefabs/钱包.prefab`：删除「V8内容」节点；「V7实名表单底」
  高度 488.58 → 310.04 并上移（y 71.2 → 160.5）；「交易密码」「确认密码」「提交实名信息」
  「V7实名重要提示」四个节点随表单缩短整体上移。Creator 重存顺带规范化数字写法、去掉
  已废弃的 `_overflow` 字段（这类 diff 无实质影响）。
- **回归测试** `tools/tests/wallet_channel_selection_regression.js`：新增两组用例——空白银行/卡号
  也能提交实名（并保住密码不一致等前置校验）；仅姓名档案重开后不重复触发实名引导、提现页
  银行/卡号保持可填空白。原「资料不完整需补全」用例改为「无姓名才算不完整」。
  运行通过：9 序列勾选 + 16 交互 + 14 金额态 + 75 通道高度 + 15 银行可选分支布局 +
  22 提现/实名竞态 + 40 哈希切换/汇率；TypeScript 转译 0 错误。
- **校验**：钱包.prefab 的 `__id__` 引用 0 处越界，415 条 `_children` 全部指向 `cc.Node`。

## 「我的」界面游戏推广入口改为仅代理可见（panelMain）

- **需求**：非代理玩家不显示「游戏推广」入口，只有代理才显示。
- **实现** `assets/scripts/UI/panelMain.ts`：`RefreshAgentMenuVisibility()` 原来只管 `Main/我的/操作/代理`，
  现在同时管 `Main/我的/操作/推广二维码`，两者共用既有判定 `IsCurrentUserAgent()`
  （`level > 0` 或 `role` 含 盟主/合伙人/总裁/老板）；两处取值都补了判空。
  刷新时机沿用现有链路：`onLoad` 末尾主动调 `set_role`/`set_level`，加 KBEngine 的 `set_level`/`set_role` 事件。
  非代理时「操作」下的 Grid（cc.Layout）按已激活入口自动补位，无需手工重排。
  *旧注释里的 `level > 50` 推广门槛是被废弃的历史规则，未启用；没有别处再改这个按钮的 active。*
- **测试** `tools/tests/promotion_shared_regression.js`：新增 4 组断言（level=0/role 空 或「普通玩家」→ 隐藏，
  role=老板 或 level=3 → 显示；代理时点入口仍能打开共用推广页）。**17 个回归测试全绿**，
  TypeScript 转译 0 错误。
- **待验收**：需在编辑器/网页里用非代理账号进「我的」确认入口消失、代理账号确认仍在（我没跑 Creator）。

## 提交时的保留项（未提交，属主人在编辑器里的改动）

`assets/resources/UI/panelMain.prefab` 工作区有主人在 Creator 里改的**推广二维码页排版**：整页上移约
74–79px（V7推广说明 y196→251.644、二维码 y-30→25.644、信息卡 y-358→-290.843 等），
`V7推广链接` 宽度 440→505，文案「扫描二维码，加入 8L」→「加入 BY」，推广ID 占位改 `111111`、字号 26→43，
二维码底图 uuid 换成 `83eff8c9-d07c-5bba-81fb-c86d155d5785`。**与本次改动无关，保留未动、未提交。**
改这个 prefab 时 Creator（PID 72605）正开着并在持续写入 —— 动它前先确认已保存。

以上改动（推广入口可见性 + 推广页排版 + 回归用例）已按主人「提交并推送所有内容」的指示一并推送到 `main`。
提交前复跑 `promotion_shared_regression`（18 用例）与 `agent_v8_regression`（25 用例）均通过。

## 代理面板「我的玩家」列表：观战入口 + 头像（panelHongli）

- **脚本** `assets/scripts/UI/panelHongli.ts`
  - `setMyPlayerItem`：每行绑头像 `ImageManager.BindPlayerListAvatar(user_id, 头像/mask/img)`；
    新增「观战」按钮——`room_id` 必须匹配 `/^\d+$/` 且为安全正整数才 `active=true`，
    点击时若 `!GpsManager.IsGpsOpen()` 且 `ConfigManager.enalbe_gps == "True"` 则弹「未打开GPS不能进入房间！」并 return，
    否则 `showPanel("panelLoading")` + `reqEnterRoom(RoomType.Custom, roomID, '{{"special_rule": "观战"}}')`。
  - `setYejiItem` / `setMengzhuItem` 同样绑定头像；盟主行的「比例」在非盟主分支清空（避免复用行残留旧值）。
- **脚本** `assets/scripts/logic/ImageManager.ts`：新增 `BindPlayerListAvatar(userID, img)` ——
  列表复用 Sprite 时，先把该 Sprite 从 `mapID2ImageSave` 的所有待回包队列里摘掉，再查缓存/入队，
  **防止上一玩家的头像回包串到新行**。
- **Prefab**：`玩家对象/盟主对象/贡献对象` 三个行预制体新增 `头像/mask/img` 结构（玩家对象另加「观战」按钮），
  头像框换 `V7/player_info_v8_avatar_frame.png`、按钮底 `V7/agent_v8_button_base.png`（新增 uuid 均有对应资源）；
  `panelHongli.prefab` 新增「V8操作」节点。四个 prefab `children` 悬空引用均为 0。
- **测试**：`agent_v8_regression` 增观战可见性（12 种非法 room_id）/ 复用行重新绑定 / GPS 拦截 /
  头像过期回包移除 / 业绩与盟主行服务端取值等用例，全部通过。
- **热更包**：`assets/resources/{project,version}.manifest` 被 Creator 重新生成，版本 **5.0.21 → 5.0.22**，
  随本次改动一并提交（原样入库，不手改）。

## ⚠️ 3 个既有失败回归测试（非本次引入，别误判成本次回归）

- `buyin_v8_regression.js`：测试 mock 没有 `cc.isValid`，而 `BuyinDisplay.onLoad` 已在用 → `TypeError: cc.isValid is not a function`。
- `realtime_record_v8_regression.js`：`panelGameView.prefab` / `drh8.fire` 与
  `art_sources/v8-repairs/realtime-record/last-apply-before/` 快照基准不一致（断言报 `unrelated 1`）。
- `wallet_editbox_alignment_regression.js`：提现「金额/input」等标签的 `_N$overflow = 2`（SHRINK），测试期望 `1`（CLAMP）。
  已核对 `HEAD~1`/`HEAD`/工作区三者的 EditBox 标签 overflow 完全一致 → 早于 `eb7c537` 就已存在，不是钱包改版引入。
- **跑测试的正确姿势**：不要用 `node x.js | tail` 的退出码判断（管道的退出码恒为 0，会误判「全绿」）；
  要直接取 node 自身退出码，或看是否打印 `PASS` 行。今天第一次全量跑就因此错判过一次。
