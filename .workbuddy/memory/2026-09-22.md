# 2026-09-22 工作记忆

## 23:5x 奖池业绩功能提交并推送

主人要求「提交并推送所有内容」，把工作区里**上一轮奖池提留代理业绩任务**的全部改动（20 项，含 8 个未跟踪新文件）
分 4 个提交推上 `main`，`0d560bd..571b3be`：

- `f9d5801` 客户端新增奖池业绩入口与明细面板：「业绩比例」入口严格按 `client_prop === "True"` 显示（老板等级不豁免）、
  统一 `date=-1` 查昨日、`proxy_performance / all_performance` 为入口比例、服务端分页每页 10 条、按 `context` 关联回包
  忽略过期/撤权响应、分母为零显示 `0.00%`；新增 `assets/V7/agent_v8_home_jackpot.png`、
  `agent_v8_header_jackpot.png`（蓝金图标，未覆盖既有美术）。文件：`panelHongli.ts` / `panelHongli.prefab` / 4 个新图与 .meta。
- `7f26eb4` XuanManager 奖池业绩后台：`GET /api/game/jackpot/agents`、`PUT /api/game/jackpot/agents/{playerId}`
  （校验原状态 + 异步回读确认 + 审计）、`GET /api/game/jackpot/report`（默认北京时间昨天）；前端页面 `/game/jackpot`
  （`JackpotPerformancePage.tsx`）；迁移 `027_jackpot_performance.sql` 只注册菜单与权限，不改玩家开通状态。
- `0662eea` 回归用例与文档：`tools/tests/agent_v8_regression.js`（奖池权限/昨日命令/零分母/分页/过期回包/撤权/超时/
  畸形数据）、`agent_v8_layout_engine.js`（3–7 按钮组合）、`docs/jackpot-performance.md`、
  根目录 `奖池提留代理业绩-客户端接口文档.md`、`XuanManager/奖池提留代理业绩_新后台接口变更说明（临时）.md`。
- `571b3be` 热更新清单 5.0.25 → 5.0.26（5513 → 5519 项，新增 6 项 = 两张新图标及其 import）。

### 提交前的验证（本次实际做的）

- 用 managed node + workspace 的 typescript 跑：
  `TYPESCRIPT_PATH=~/.workbuddy/binaries/node/workspace/node_modules/typescript/lib/typescript.js \
   /Users/yy/.workbuddy/binaries/node/versions/22.22.2-3/bin/node tools/tests/agent_v8_regression.js`
  与 `... agent_v8_layout_engine.js` —— **全部 PASS**（回归脚本默认走 `~/.nvm/versions/node/v22.19.0/...` 的
  typescript，本机没那个路径，**必须显式给 TYPESCRIPT_PATH**）。
- **本机没有 `go`**（`command not found`），后台的 `go test ./...` / `go vet ./...` 这轮没复核，按文档记载视为已通过 ——
  待验项，不要当成本轮证据。
- `git push origin main` 一次成功（未走 443 绕过）；`git ls-remote` 与本地 HEAD 一致。
- `check_memory.py`：0 错误，2 个既有体积警告（`CURRENT.md` 16372B、`topics/v7-ui.md` 29874B）。

### 注意

- `assets/resources/*.manifest` 与 `assets/V7/*` 都是热更新产物，改动来自生成流程；本仓库里每次生成都会 bump 版本号。
- 后台已部署到正式 8891（含迁移 027），**客户端本轮没有构建/发布**，线上联调项见 `docs/jackpot-performance.md` 的待验清单。
