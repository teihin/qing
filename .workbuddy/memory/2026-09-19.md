# 2026-09-19

## 排查：结算界面收到全服通知误播「喜金」（承 9-18 同一问题的追加确认）

主人给出关键区分：**正常房间内不响，停在结算界面才响** → 证明命中的就是房间号那一步比较，不是"任何通知都响"。

**已用构建产物核对线上逻辑**（用户测的是构建版）：`build/web-mobile/assets/main/index.33c3e.js` 中
`t.prototype.SystemInfo` = `getAccount().roomID == n[2] && (…奖池 active、num=n[3]、showMsg(n[4])、
displayJC.playAnimation、gameLogic.PlayAudio2("喜金"))`，与 `assets/scripts/UI/panelGameView.ts:2602/2629` 完全一致，
部署版没有别的分支。注意：压缩后中文是 `\uXXXX`，**必须用 Python 解出真实字符再比对，直接 grep 中文搜不到**。

**为什么结算才响**：结算阶段 `account.roomID` 已不再是有效房号。本项目自己的记录：
- commit `436d2a3` 说明："避免结算阶段 account.roomID 被清空后丢失参考房间"；
- `panelGameView.ts` 3222 / 3269 注释、`QueueMatchManager.ts` 91、`panelRecordInfo.ts` 73 均提到"结算阶段被清空"；
- `docs/memory/topics/client-game-network.md:26`：「结算后即使 account.roomID 已清空，只要当前仍是 drh8，也必须走过渡场景」。

清空动作**不在客户端**（客户端只在退房回包 `panelGameView` 1559/1541、退出房间按钮 728、结算超时 3345、
俱乐部关闭 `DrhLogicMgr` 475、GPS 拦截 2595 时清）→ 是服务端在结算时改写该属性。
`DrhLogicMgr:574` 用 `strRoomID == "0"` 判断同一状态，说明该值很可能是**数字 0**（字符串 "0" 与 "" 弱相等为假，也就不会响）。

房间内是真房号 → `roomID == ""` 为假 → 不响；结算期为 `""` 或数字 `0` → 与空房间字段弱相等命中。

未改代码（主人只要求定位）。结论与排查流程已沉淀为技能 `kbe-message-sideeffect-triage`。

## 修复：panelGameView.SystemInfo 房间过滤（已改代码，未跑构建）

- 改动位置：`assets/scripts/UI/panelGameView.ts` 的 `SystemInfo()`，原 `let strRoomID = getAccount().roomID; if(strRoomID == arrayAll[2])`
  改为：取 `account` → `let nMsgRoomID = Number(arrayAll[2])` →
  `if(account != null && isFinite(nMsgRoomID) && nMsgRoomID > 0 && nMsgRoomID === Number(account.roomID))`。
  其余分支（奖池 active、num、showMsg、playAnimation、PlayAudio2("喜金")）一行未动。
- 注意：**不要用 `strRoomID && strRoomID === arrayAll[2]`** —— 挡不住数字 0；结算阶段的值很可能就是数字 0。
- 验证结果（均通过）：
  1. 条件对拍：只有「结算空串 + 空字段」「结算数字 0 + 空字段」两组由「响」变「不响」，
     「房间内 + 本房喜金播报」仍为「响」；字段缺失 / 别人房间 / 房间字段为 0 均保持「不响」。
  2. TS 语法 + ES5 转译对照：`git show HEAD:...` 基线版与工作区版诊断数**均为 0**（未引入新语法问题），
     转译后的 ES5 分支已打印核对。
  3. `git diff --check` 通过，diff 只落在该方法（+4/-2）。
- **未执行** Creator 编译、`1生成热更新包.command`、真机联调 → 线上生效状态待主人操作验证。
- 环境坑：`npm i typescript` 默认装到 **7.0.2（Go 原生版）**，不提供 `createSourceFile/transpileModule` 的 JS API，
  `require()` 报 MODULE_NOT_FOUND；语法检查必须 pin `typescript@5.6.3`，装在
  `/Users/yy/.workbuddy/binaries/node/workspace`（隔离工作区）。

## 提交并推送：修复已进主干

- commit `5a77532`「修复：全服通知在结算阶段误播喜金音效」→ 已推送 `origin/main`（`4acfd8a..5a77532`），
  推送后工作区与上游 0/0。
- **只提交了 `assets/scripts/UI/panelGameView.ts` 一个文件**（+6/-2），其它改动一律保留原样未提交。
- **未提交（非本次范围）**：`.workbuddy/memory/*`、`assets/Scenes/drh8.fire`、
  `assets/imagesKK/公用/2.png(+.meta)`、`assets/scripts/common/WebLoadingManager.ts`、
  `build-templates/web-mobile/splash.png`，以及两个未跟踪目录 `design-previews/待确认/2026-09-18-网页loading*/`。
- ⚠️ **MEMORY.md 有并发写入**：工作区版本比已提交版本多 29 行，多出的 3 段
  （盘点旧名要扫 build-templates / 抠白底 AI 会加灰色描边 / 贴图文字 vs DOM 引擎文字）
  来自另一会话的「网页版 loading 改名」任务，本轮没有一起提交。下次维护前先确认没有重复段落。

## 排查+修复：浏览器版 `getAccount().guuid` 未判空导致断线期间每 0.1s 抛异常

主人给的浏览器控制台截图里有两组错：① `WebSocket connection to 'wss://154-37-155-17.nip.io/ws/20013/' failed`
+ `onerror_before_onopen`；② `DrhPlayerLogic.ts:2254 Cannot read properties of undefined (reading 'guuid')`。

**① WSS 连不上 = 网络/服务端侧，不是代码。** 本机实测（13:39，node 22 全局 WebSocket）连
`wss://154-37-155-17.nip.io/ws/20013/` **3/3 全部 101 成功且服务端不主动断开** → 服务器与反代当时是通的。
探测口径：`GET /ws/20013/`（不带 upgrade）返回 **502**=反代后端不通、**404**=端口不在白名单（实测 `/ws/9999/` 404）。
注意本机 DNS 被本地代理 hijack 成 fake-IP（`154-37-155-17.nip.io → 198.18.0.65`），curl 走本机 HTTP 代理、node 直连，两条路都能通。

**② guuid 报错 = 真 bug，已修。** 链路：`reset()` 会置 `entity_id=0` + `entities={}`（kbengine.js 3122/3148/3158），
所以 `KBEngine.app.player()`（=3215）在**断线重连期间必返回 undefined** → `GameDataManager.getAccount()` 返回 undefined。
`TryRecoverMissingSelfActionUI` 第 2254 行原先直接 `.guuid` 未判空；异常发生在 `strCurCountDown` 自减**之前**，
所以倒计时永不归零 → 计时器永不 unschedule → 断线期间**每 0.1s 无限抛异常**（自愈要等重连成功）。
- 改法（`assets/scripts/logic/DrhPlayerLogic.ts`，+4/-1）：先取 `accountSelf = GameDataManager.getAccount()`，
  再用 `accountSelf == null || this.info.strUserID != accountSelf.guuid` 短路。
- 验证：typescript@5.6.3 `createSourceFile` 语法诊断 **HEAD 版与工作区版均为 0**；`git diff --check` 通过；影响面只此一处。
- 同文件另有 ~12 处 `getAccount().guuid` 未判空（214/246/700/738/920/1063/1175/1237/1539/3011/3133/3445），
  但都在**服务端消息回调**里（收到包说明实体还在），风险远低于定时器里的这一处 —— 本轮未动。
- 「中间产物不留」已执行：临时 wstest.mjs / tscheck.js / 基线导出全删。

## 提交并推送：commit `b59553f`（45 files，+371/-266）

主人：「提交并推送所有改动。」全量入库并已推 `origin/main`（`a7826ee..b59553f`），工作区与上游 0/0。

提交前做了 4 项预检（结论：1 项回归已修，其余全部通过）：
1. **音频替换 1:1**（10 个 mp3/wav → ogg）：按去扩展名主名双向比对 HEAD 与工作区，差集为空。
   本项目音效走 `cc.resources.load("Audio/eff/" + 名字)`，**不依赖扩展名**；旧 uuid 反查
   `*.prefab/*.fire/*.anim/*.json` **无任何引用** → 换 ogg 安全。
2. **全仓 uuid 唯一性**：`assets/` 下 5294 个 uuid（含 `subMetas.*.uuid`）全唯一，无重复。
3. **贴图 ↔ .meta 自洽**：发现 **2 处本次引入的回归**并修回 ——
   `回座.png.meta` 顶层仍是旧的 148×57（PNG 已放大到 296×114；HEAD 原本三者一致），
   `确认分牌.png.meta` 顶层被写成 **2021×778**（与任何真实尺寸都不符，HEAD 是 148×57）。
   同时验证 `offsetX/Y == (raw-w)/2 - trim` 成立、实测 alpha bbox 与 `subMetas` 的
   trim `(9,11,278,84)` / `(0,0,148,55)` **完全一致**（`trimType:auto` 下稳定，Creator 重导入不会漂）。
   `三花.png` 300×300、`trimType:custom`（不裁切）→ 一致。
4. `drh8.fire` 的节点尺寸/`_sizeMode`/换 uuid 改动属于上一轮换图任务的同步，未逐项复跑其验收。

**坑与经验**：
- 中文路径脚本必须 `git -c core.quotepath=false` + `ls-tree -z`，否则拿到 `\346\270\270` 转义名，
  uuid 提取全成 ERR（第一次就是这么被坑的）。
- **不要 `os.walk` 整个仓库**统计 uuid（`library/`+`build/` 巨大，进程被 SIGTERM/exit 137）；只走 `assets/`。
- 不要把十几个 uuid 拼成 shell grep 的 `-e a|b|c`（会挂）；反查用 Grep 工具。
- `git add -A` 后 git 会把 `.meta` 误判成 rename（如 `发牌.wav.meta -> 下注.ogg.meta (68%)`），
  纯属 heuristic，**以 uuid 唯一性结论为准**。
- 提交时本机 Creator 正在运行（`pgrep -fl CocosCreator` 命中），提交后复查 `git status` 仍干净 → 未被改写。
- 未执行 Creator 编译与 `1生成热更新包.command`，热更包待重建（已在提交说明中注明）。

预检流程已沉淀为技能 `cocos-asset-precommit-audit`（4 项检查 + 脚本口径 + 上述坑）。

## 记忆维护：MEMORY.md 从 20.7 KB 压到 3.7 KB

原文件超限被截断。已整份归档为 `.workbuddy/memory/ART-NOTES.md`，MEMORY.md 改成索引版：
只留通用约定 / 代码排查 / Cocos 硬约束 / 改图路线结论，细节指向 ART-NOTES.md 与三个技能。

### 追加：主人要求「提交并推送所有内容」，上一节列的保留项已全部提交

- commit `7ac118d`「网页版 loading 改名收尾、公用图与牌桌场景调整、项目记忆更新」，10 个文件 +230/-22：
  `WebLoadingManager.ts`（loading 标记 8L→BY）、`build-templates/web-mobile/splash.png`（盾徽改名版，83.7→59.5 KB）、
  `assets/imagesKK/公用/2.png(+.meta)`（497×1067 → 856×1837，trim 56/34）、`assets/Scenes/drh8.fire`
  （节点 497×1067→809×1760、scale 1→0.6、颜色改 (73,218,102)、Widget 偏移同步）、三个记忆文件、
  两个 `design-previews/待确认/2026-09-18-网页loading*/` 预览（HTML + PNG）。
- 已推送：`5a77532..7ac118d`；工作区干净，与上游 0/0。
- 该提交包含另一会话产生的改动，**未逐项复跑其验证流程**，提交前只核对了清单与全部文本差异（已在提交说明中注明）。

## 牌背选项编号错位（只排查，未改文件）

- 现象：游戏内「设置 → 牌背」三格与设计稿三款亮色牌背对不上，整 **错开一格**。
- 根因：`assets/resources/UI/panelGameView.prefab`（uuid `1c485fb2-99c7-499b-a5e1-395955406b76`）三个 Toggle 节点名 = `牌背1/2/3`，
  缩略图 `Background` 的 SpriteFrame 也各引用同名 PNG；`panelGameView.ts:605` 用 `name.replace("牌背","")` 取号 →
  存 localStorage「牌背」→ `UpdateBackImg()` 里 `LoadImg("zuotype/牌背"+N)`。同一模式还在 `DrhPlayerLogic.ts:2555/3064`、`panelRecordInfo.ts:747`。
- 而 09-18 定稿的三款亮色牌背实际落盘为 `牌背0`(红) / `牌背1`(蓝) / `牌背2`(紫)，`牌背3` 是旧深色款。
  → 界面三格实际是 蓝/紫/深，**红色（牌背0）没有任何入口**。
- 场景 `assets/Scenes/drh8.fire` 里的牌背节点是同一 prefab 的实例（PrefabInfo.asset 均指向 `1c485fb2…`），改动要 prefab + fire 两处同步。
- 附带发现：`搓背3.png` 仍是旧 **8L** LOGO（`牌背3.png` 已换 BY）；`搓背0/1/2` 与 `牌背0/1/2` **逐字节同源**（md5 完全相同）。
- 线上热更包 `hot-update-output/1.0.28` 里节点名同样是 1/2/3（无 0），确认不是构建差异。
- 状态：**已按主人选定方案 A 修复**（见下）。

### 修复：三项改成 0/1/2

- `assets/scripts/common/Tool.ts` 新增 `Tool.GetCardBackIndex()`：读 localStorage「牌背」，非 `0/1/2` 或为空一律归一到 `"0"` 并写回（迁移旧值 `"3"`）。
- 6 处读取点全部换成该方法：`panelGameView.ts:582/893/2425`、`DrhPlayerLogic.ts:2555/3064`、`panelRecordInfo.ts:747`。
- `DrhPlayerLogic.ts:3064` 的切牌动画分支按**界面格子位置整体平移**：0→`displayQP1`+`pai3`、1→`displayQP2`+`pai1`、2→`displayQP3`+`pai2`（与改版前行为等价，红色款不会没有切牌动画）。
- `panelGameView.prefab` + `drh8.fire` 文本级一次性替换：节点名 `牌背1/2/3`→`牌背0/1/2`；缩略图 SpriteFrame uuid 同步位移
  （`016a00cd→5826c0fa`、`ee4e3a6d→016a00cd`、`d629abe8→ee4e3a6d`，即 牌背1→0 / 牌背2→1 / 牌背3→2）。
  **必须一次性映射替换**，分步替换会连锁。已核对 JSON 合法、三项缩略图与节点名一一对应、.meta 未动。
- 四个 TS 文件用 `tsc` 解析器查语法，全部 OK。**未做 Creator 编译 / 未重建热更包**。

### 仍未修（主人未要求）

- `assets/ImagesLuck/动画/切/hand_tex.png`（09-03）里的三套切牌牌背切片**仍是旧 8L 款**（蓝银/深蓝/墨绿金），
  与新的红/蓝/紫三款对不上 —— 无论选哪款，切牌动画里出现的都是旧 8L 牌背。

### 搓背与牌背成对一致性核对（已补 搓背3）

- 唯一引用点 `panelGameView.ts:592` `LoadImg(cuo,"zuotype/搓背"+strName)`，与牌背共用同一编号 → 编号天然对齐，无需改。
- 核对结果（sha256 + alpha bbox）：`搓背0/1/2` 与 `牌背0/1/2` **逐字节完全相同**；
  **`搓背3` 原来不一致** —— 内容还是旧 8L 款（42297 B），而 `牌背3` 已是 BY 款（54143 B）。
- 处置：直接用 `牌背3.png` 覆盖 `搓背3.png`，现在四对全部逐字节一致。
  两图的 `.meta` 裁剪参数本就相同（`327×425 @ trimX4,trimY4`，raw 348×447），alpha bbox 实测均为 `(4,4,331,429)`，
  因此覆盖后 `trimType:"auto"` 重算结果不变，**`.meta` 未被动**（覆盖前后 sha 一致，git 只见 PNG 变更）。
- 结论：牌背 N ↔ 搓背 N 从此一一对应，`zuotype/` 下已无 8L 残留的牌背类资源。

### 切牌图集 hand_tex.png 切片重建（按编号 0/1/2）

- **权威映射**出自 `tools/generate_qin_drh8_atlases.py` 的 `replace_hand_cards()`：
  牌背1→(放牌3,下牌3,上牌3)、2→(放牌,下牌,上牌)、3→(放牌2,下牌2,上牌2)；角度 下牌 `+7°`、上牌 `-7°`、放牌 `0°`。
  该脚本头注释另记：「variant 1 -> 牌背0/1, 搓背0/1, pai3 slices」——8L 时代 **牌背0 与 牌背1 曾共用同一张图**，这正是"旧配置0"的来源。
- `hand_ske.json` 是**空动画**：pai1/pai2/pai3 的 slot 帧与 bone 帧数全为 0，且自 `Initial project import` 起从未修改。
  → 切片组**不随动画切换**，跑哪套动画显示的都是骨架默认 displayIndex（0）。所以三组切片是"按编号预置"，不是按动画选。
- 重建方式（复刻原工艺，未改 json 坐标）：源改用 `zuotype/牌背0/1/2.png` 先 crop 到 alpha bbox，
  再 resize 到 `133×183` base → `rotate(±7°)` → 居中贴回切片矩形（`card_for_hand_slice` 同款逻辑）；
  编号按"格子位置平移"：0→`*3` 组、1→无后缀组、2→`*2` 组；切片先清空再贴，避免旧像素残留。
- 校验：9 个矩形**之外零像素变化**（顶部手/底部手/放牌手三张逐像素未动）、图集仍 `1376×892`、
  alpha bbox 仍 `(1,0,1376,891)`，与 `.meta` 的 trim 完全一致 → **`.meta` 未被动**。
- 体积：691651 → 824037 字节（+19%，已做无损 `optimize`）。亮色 BY 图案比深色 8L 难压；
  **未做有损量化**，因为整图量化会连带改到三张手部切片的肤色像素。
- `tools/generate_qin_drh8_atlases.py` 头部已加「勿运行」警告：`build()` 仍会用 8L 母版重写
  `牌背0-3`/`搓背0-3` 并按旧 mapping 刷切片，**跑一次就把今天的改动全部打回 8L**；
  `--check-structure`（只读）仍可安全使用。

### 提交并推送：d3bc375

- commit `d3bc375`「牌背编号改为 0/1/2：对齐切牌切片与搓背，并入排队面板与桌布改动」，19 个文件。
- 已推送 `cabebde..d3bc375`（origin/main）；与上游 `0 0`；**提交前后工作区均干净，Creator 未改写文件**。
- 提交前跑了只读检查 `docs/memory/check_memory.py check`：0 错误、2 个既有超预算警告
  （`CURRENT.md` 10793B > 8192、`topics/v7-ui.md` 29874B > 16384），非本次引入，未处理。
- **本次一并提交了非本任务来源的改动**（已在提交说明中分条注明，未逐项复跑其验证流程）：
  - `panelGameView.prefab` 新增整套**排队撮合面板**节点（排队弹窗/排队人员列表/一键邀请/空状态等，节点 1212→1254）。
  - `zuotype/1~3` 由 jpg 换成 png（922×2048 RGBA，各约 1.9 MB），`.meta` 随扩展名重命名、UUID 保持，旧 jpg 删除。
- 仍未执行 Creator 构建、未重建热更新包；牌背/搓牌/切牌需登录态实测。

### 切牌动画去掉手（hand_tex.png 手部切片清空）

- 需求：3 个切牌动画只保留牌、去掉手。
- 做法：**只清空图集里三张手部切片的像素** —— 顶部手 `(1,1,548×676)`、底部手 `(551,1,402×665)`、
  放牌手 `(955,1,284×690)`；**不改 `hand_ske.json`**。
- 不动骨架的理由：骨架 `zOrder` 帧用的是**槽位索引**（`[1,3]`、`[1,3,3,0]`，槽位顺序为 放牌/放牌手/底部手/下牌/上牌/顶部手），
  删槽位必然要重映射索引，而 DragonBones zOrder 帧的成对语义无法在本地确证；清空切片同样达成"不显示手"，且完全可逆。
- 校验：九张牌切片**逐像素零变化**；图集仍 `1376×892`；alpha bbox 仍 `(1,0,1376,891)`，与 `.meta` 的
  `trimX=1/trimY=0/1375×891` 完全一致 → **`.meta` sha 未变**（`ff29386b…`）。
  上半部分残留的少量像素经查是牌切片自身的抗锯齿外溢（列块 x659-679 / 826-832 / 1241-1375），与手无关。
- 体积：824037 → **520561 字节**（比改切片前的 691651 还小 24%，大片透明区压缩率极高）。
- 提交：`7912745`「切牌图集去掉手部切片，同步 Creator 重算的切牌节点尺寸」，3 个文件；
  已推送 `d3bc375..7912745`，与上游 `0 0`。
- 同批带上的是 **Creator 对 `drh8.fire` 的自动同步**（非人工改动）：切牌1、切牌2 的 `_contentSize`
  由 `0×0` 更新为 DragonBones 包围盒 `799.7517×706.1138`（与 `hand_ske.json` 的 `aabb` 完全一致）；
  连带切牌2 的 Widget `_bottom` 由 `277.242` 重算为 `-75.8149`（切牌1 早为 `-74.0149`，
  **切牌3 仍是旧值 `279.713`，三者不一致**）。该改动未经人工核对视觉效果，**切牌位置可能偏移，待 Creator/真机确认**；
  如需回退可 `git checkout HEAD~1 -- assets/Scenes/drh8.fire`。

### 提交并推送：9521a11（并带出误提交 ad0e86f）

- 本批工作区改动**均来自编辑器与生成流程，非本会话直接修改**：
  - `assets/Scenes/drh8.fire`：新增「皮池」「芒果_100x60」两个 Sprite 节点（引用 `assets/V7/游戏内/` 新素材），
    节点数 1261→1263；其余差异为编辑器整体重写。
  - `assets/resources/UI/panelTalk.prefab`：节点结构与名称均未变，调整位置 + 一处 spriteFrame 由 `5620c0ae…` → `a0014597…`。
  - `assets/resources/project.manifest`、`version.manifest`：**热更新版本 `5.0.14` → `5.0.15`**（有人跑过 `1生成热更新包.command`）。
  - 新增 `assets/V7/游戏内/`（22 个 PNG+meta，448 KB）、`assets/V7/芒果背景框.png`(+meta，876 KB)。
- commit `9521a11`（29 文件），已推送 `143fedd..9521a11`；与上游 `0 0`；工作区干净。
- **同时带出一个此前未推送的误提交 `ad0e86f`，提交信息只有一个「1」**（只改 `drh8.fire`，3030+/2978-）。
  它现已进入远端历史。如要清掉需 `git rebase` 后 force push。
- `assets/V7/.DS_Store` 由 .gitignore 忽略，未入库。
- 复查未解决项：**切牌3 的 `_contentSize` 仍 `0×0`、Widget `_bottom` 仍 `279.713`**，
  与切牌1（`-74.0149`）、切牌2（`-75.8149`）不一致，至今未同步。
- 仍未执行 Creator 构建；热更清单已升版，**未核实是否已上传到热更新服务器**。

### 大厅「发现」入口行为调整（举报反馈 / 比赛场）

- 需求：大厅点「举报反馈」直接开客服界面；点「比赛场」弹「暂无比赛，敬请期待！」。
- 定位：两按钮在 `panelMain.prefab` 路径 `panelMain/Main/发现/` 下，Button 的 `clickEvents` 为空，靠 `UIViewBase.onLoad` 里 `getComponentsInChildren(cc.Button)` 统一注册 → `onButtonClickVirtual` → `panelMain.onButtonClick`。原 `onButtonClick` **无这两个分支**，点击无响应。
- 改动：`assets/scripts/UI/panelMain.ts` 在「管理」分支后新增两条 `else if`（共 +10 行）：
  - `举报反馈` → `showPanel("panelKefu", ShowPanelMode.Cover)`。注意 `panelKefu.start` 只识别 `客服`/`VIP充值`/`VIP充值2`，传其它值会让 `strUrl` 为空，所以**不传 strUserData**，默认走 `kefuUrl` + `general` 渠道。
  - `比赛场` → `showPanel("panelMsgView", ShowPanelMode.Cover, "暂无比赛，敬请期待！")`（项目统一提示弹窗）。
- Prefab 未改（按钮与事件注册已存在），无需动 `.meta`/UUID。
- 验证：用工作区 typescript 做 `createSourceFile` 解析，`parseDiagnostics = 0`；`git diff --stat` 只有该文件 +10 行。
- 未做：Creator 编译、构建、网页版/真机点击实测。
- 收尾：经主人明确授权提交并推送 —— `9146531`（脚本改动）+ `b028185`（记忆同步），
  已推送 `a50ab43..b028185`，与上游 `0 0`，工作区干净。

### 测试房「报奖」消息链路打通（19:43）

- 需求：主人在测试房 **152496** 内要看报奖（「大赢家」）动画，要求造一条该房间的中奖消息发出来。
- 链路（服务端系统通知 → 客户端动画）：
  - 服务端下发 KBEngine 事件 `SystemInfo`，payload 为 JSON `{"system_content":"..."}`。
  - `assets/scripts/UI/panelGameView.ts:2602 SystemInfo()` 按**英文逗号**切分：`arrayAll[2]`=房间号、`[3]`=金额、`[4]`=正文。
    只有 `Number(arrayAll[2]) > 0 && === Number(account.roomID)` 才播报奖：激活 `奖池` 节点 →
    `displayJC.playAnimation("newAnimation",1)`（DragonBones **`ui_bigwin`**，素材在 `assets/ImagesLuck/动画/奖池动画/`，
    旧版另存 `ImagesLuck/动画/报奖-old/`）+ `喜金` 音效 + `showMsg(arrayAll[4])` 走「提示/txt」。
  - 同事件还被 `panelCloudNotify`、`GameDataManager` 消费，把 `arrayAll[4]` 推**全服跑马灯**。
  - **后台管理系统的「全服公告」固定发 `,,,,<正文>`（房间段为空）→ 不会触发房间报奖动画**，只有手填房间号才行。
- 发送入口：服务器本机 KB 管理接口 `http://127.0.0.1:8890/hall/command`（iptables 阻断公网，须 SSH 上机 curl）。
  ```bash
  curl --get 'http://127.0.0.1:8890/hall/command' \
    --data-urlencode 'header=通知_所有玩家_信息' \
    --data-urlencode 'param={"system_content":"0,0,<房间号>,<金额>,<正文>","context":"<操作号>"}'
  ```
  SSH 通道本机已就绪：`ssh -p 2233 -i ~/.ssh/id_ed25519_newserver client_update@154.37.155.17`（可执行命令，非仅 SFTP）。
- 实测：`0,0,152496,88888,恭喜玩家勇夺大赢家 88888 金币` → `ret_code 512` 成功。
  房间在线状态用 `header=查询_大厅_所有房间`（`param={}`）核实，152496 当时为「等待中」。
- 坑：`system_content` 靠英文逗号分段，**正文里不能再出现英文逗号**；检索状态码 `512`=成功、`769`=失败、`1280`=已接收。

### 报奖动画贴图被换肤误改，从 nnnn2 回滚（19:52）

- 现象：报奖（大赢家）动画的美术不对。定位到 `assets/ImagesLuck/动画/奖池动画/ui_bigwin_tex.png`
  —— 现版本被换成了黑白「大赢家」深色条 + 白色线稿烟花，**原始版本是紫蓝霓虹「BIG WIN」+ 彩色烟花**（`报奖-old/ui_bigwin_tex.png` 同样是金→黑白线稿，也被误改）。
  两张图体积也随之暴涨（385KB→610KB、198KB→603KB）。
- 影响面核实：`panelGameView.prefab` 与 `Scenes/drh8.fire` 的「奖池」节点引用的是
  **`奖池动画/ui_bigwin_ske.json`（uuid `a3c32556-…`）**；`报奖-old`（`a2e6ea64-…`）**全仓无引用**，纯历史备份。
  两组的 `ske.json`/`tex.json` 与 nnnn2 **字节级相同**，只有 PNG 被改 → 只需还原 PNG，图集切片坐标天然对齐。
- 操作：`cp /Volumes/CCCC/nnnn2/assets/ImagesLuck/动画/<目录>/ui_bigwin_tex.png` 覆盖两张 PNG。
  `.meta` 的 trim/尺寸（trimX/trimY/width/height/rawWidth/rawHeight）与 nnnn2 **完全一致**，覆盖后 Creator 也**未改写 meta**，无需 `git checkout` 还原。
- **两个必踩的坑（下次直接照做）**：
  1. `cp -p` 会保留源文件 mtime（nnnn2 里是 20:17，比当时时间还晚）→ Creator 判定「没变化」**不重新导入**。
     **覆盖后必须 `touch` 目标 PNG**。
  2. Creator 窗口**不在前台时不会扫描资源**；`osascript` 走 `System Events` 会被拒（无辅助功能权限，-10004）。
     可用：`osascript -e 'tell application "CocosCreator" to activate'`（直接按应用名，退出码 0 即生效）。
- 导入生效的**唯一可靠判据**：`library/imports/<uuid前2位>/<uuid>.png` 的 md5 与 mtime。
  导入前它是**坏版**（md5 `6fffdaa9` = HEAD 版），激活 Creator 后 19:52:59 刷新为 `23b9e05d`＝nnnn2 好版。
- 最终：两张 PNG 逐字节与 nnnn2 一致，`git status` 仅这两处 PNG 变更（meta 零改动）。**未构建、未重新生成热更新包、未提交**。

### 合成 4 秒报奖中奖音效（20:30）

- 需求：给一段 4 秒的「中奖」音效，要配合报奖动画。先把**动画时长量化**了：
  `ui_bigwin_ske.json` → armature `ui_bigwin`，`frameRate 30`，动画名 **`newAnimation`（不是 duration 值）**，`duration=70 帧 → 2.33 秒`。
  所以 4 秒音效 = 动画 2.33s + 尾部余韵，爆点落在 1.4s 左右正好覆盖动画结束。
- 现有音频素材：`assets/resources/Audio/eff/喜金.ogg`（5.68s，328KB，`panelGameView` 报奖时 `PlayAudio2("喜金")`）、`烟花.mp3`（6.01s）。
  nnnn2 里同目录是 **wav**（`喜金.wav`），当前项目统一转成了 ogg；`find` 全盘没有别的"中奖/报奖"音效。
- 环境：本机**没有 ffmpeg**。往隔离 venv 装了 `soundfile 0.14`、`scipy 1.18`（可读 ogg、可写 ogg/vorbis）。
- 合成路线（`numpy`+`scipy`，脚本 `/tmp/mk_bigwin_sfx.py`）：
  0.00s 噪声上扫起势 → 0.10s 钟琴六音琶音 → 1.05s 蓄力上扫 → **1.38s 主爆点**（C5 和弦钟琴 + 低音铺底 + 高通钹 + 合成欢呼）
  → 2.55s 高频装饰琶音 → 3.35s 尾韵；整体过卷积混响，立体声用 8ms Haas 展宽，`tanh` 软限幅后归一化到峰值 0.94。
- 自检（可复用为音效验收口径）：分段 RMS 必须呈「起-落-爆-收」起伏（实测 -41/-17/-23/**-11.8**/-17.7/-24 dB）、
  峰值落在设计点（1.447s）、削波采样数 0、谱重心约 1kHz、首尾无爆音。
- 交付：`报奖中奖音效_4s.wav`（689KB）/ `报奖中奖音效_4s.ogg`（60KB），放在工作区根目录。**未入 assets、未替换「喜金」**。

### 报奖音效改走「网上找素材」（20:40）——合成版被主人否决

- 主人反馈：**「不好听，不要自己合成」**，要求网上找**带礼花声 + 欢呼声的 3 秒音效**。合成版文件已删除。
  → **教训：游戏音效不要用 numpy 从零合成波形，主人明确不接受。优先找真实录音素材。**
- 素材站实测（**下次直接用这份结论**）：
  | 站点 | 能否直连抓 | 直链形式 | 备注 |
  |---|---|---|---|
  | **Mixkit** | ✅ 最好用 | `https://assets.mixkit.co/active_storage/sfx/<id>/<id>-preview.mp3` | **免费商用无需署名**，preview 就是完整时长（非试听切片）|
  | free-sound-effects.net | ✅ | 页面 `data-download` / `<source src>` 里的 `https://free-sound-effects.net/mp3/03/free-sound-<id>.mp3` | 用 `<source src>` + 紧随的 `<h3>` 配对标题即可批量取 |
  | freesound.org | ⚠️ 半可用 | 搜索页可 curl，预览 `https://cdn.freesound.org/previews/<id前3位>/<id>_<user>-hq.mp3` | **只有低质预览**，正式用需登录下原文件；许可逐个文件不同 |
  | Pixabay | ❌ 抓不到 | — | curl 只回 5.7KB 空壳（反爬/需 JS）|
- 免费站上**没有**理想的「礼花+欢呼」单一现成短音效（逐条分析过 20+ 条）。最终用两段**真实录音对位混音**（不是波形合成）：
  - 礼花：Mixkit `1689` Multiple fireworks explosions（3.27s，最强爆点 0.25-0.75s）
  - 欢呼：Mixkit `462` Huge crowd cheering victory（10.06s，1.5-7.5s 是稳定大欢呼 RMS 0.27）
  - 礼花取 0-3.0s，欢呼取 1.4-4.4s 峰值×0.42、0.18s 渐入，软限幅后峰值 0.94。
- **「礼花/欢呼」的频谱判据（可复用）**：欢呼 = 中频 500-3k 占比 **>85%**、质心 ≈1000Hz；
  礼花 = 低频为主 + 强瞬态、质心 <900Hz；两者叠加后全程 RMS 应无断层（实测 0.16-0.28）。
- 交付：`报奖音效_礼花欢呼_3s.wav`（517KB）/ `.ogg`（41KB），工作区根目录。**未入 assets、未替换「喜金」**。
- **20:42 主人反馈「效果不好」→ 上述两个文件已删除**。结论：合成版与"真实录音对位混音版"两条路都没让主人满意，
  **音效这块不要再自己出方案，先问清他想要什么**（现成文件？还是指定参考音）。
  `assets/resources/Audio/eff/喜金.ogg` 全程未被改动。

### 提交并推送 4474d71（20:51）

- 主人后续自行把报奖音效换成了 `assets/resources/Audio/eff/喜金.mp3`（4.02s、44.1kHz、96.6KB，
  删除旧 `.ogg`+meta），并在 Creator 里调了 `assets/Scenes/drh8.fire`、新增
  `assets/V7/游戏内/游戏就绪_434x145.png`。本次「提交并推送所有改动」一并带上。
- 按 `cocos-asset-precommit-audit` 流程预检，**四项全过**：
  1. 音效按去扩展名主名双向比对：HEAD 16 个 == 工作区 16 个，无漏删漏补；换扩展名不影响
     `cc.loader.loadRes("Audio/eff/"+名字)` 的按名加载。
  2. 旧音效 uuid `a1546d6e-…` 反查：仅存在于构建产物 `assets/resources/project.manifest`，源码无引用。
  3. 两张还原贴图：meta 顶层宽高 == PNG 尺寸、`subMetas.rawWidth/rawHeight` == PNG 尺寸、
     实测 alpha bbox 与 `(trimX, trimY, width, height)` 精确吻合。
  4. `assets/` 下 5321 个 uuid 全部唯一，无重复。
- **新踩的坑：`cp -p` 不只带 mtime，还会带源文件的权限位** —— nnnn2 里那两张 PNG 是 `755`，
  覆盖后提交出现 `mode change 100644 => 100755`。收尾必须 `chmod 644` 再 amend。
- 提交 `4474d71`（10 文件），已推送 `638b45c..4474d71`，`main...origin/main` 无 ahead/behind，工作区干净。
- **未做：未跑 Creator 构建、未重建 `project.manifest` / 热更新包** —— 音效改了文件名，
  要在真机/网页版生效需要重跑 `1生成热更新包.command`。

### 下注数字「放大再缩小」动画（21:25）

- 需求：下注时筹码飞到下注区后，下面的下注数字加一个快速放大后再缩小的动画。
- 定位：`assets/scripts/logic/DrhPlayerLogic.ts` 的 `ThrowCoinAnimate()` —— 筹码实例从
  `PlayerInfo` 飞到 `goldshow/coin`（0.2s moveTo），下注数字是 `goldshow/count`（cc.Label）。
  prefab 实测：`count` 的 `_trs` scale = 1,1,1，组件只有 `cc.Label` + `cc.Widget`（无 Animation），
  所以 `stopAllActions()` 是安全的。
- 改动：新增私有方法 `PlayBeiCountPopAnimate()`，在 `ThrowCoinAnimate` 的 `move` 结束
  `callFunc` 里（与 `add.destroy()` 同帧）调用。序列
  `scaleTo(0.08, 1.35).easing(easeBackOut)` → `scaleTo(0.12, 1).easing(easeSineOut)`，
  跑前先 `stopAllActions()` + `scale = 1` 防连击累积。守卫用项目已有的 `CanRunRoomAnimation()`。
- 校验：无本地 tsc，用 managed node + 临时安装的 typescript 走 `ts.transpileModule` 做语法检查，
  0 诊断（脚本已删）。
- **未做/待确认**：未跑 Creator 编译与真机/网页验证；`ThrowGold2Mang()`（投钱入芒池）走的是
  另一条路径（飞向 `ExShow/芒果标记`），本次未加同类动画。
- 改动前已有未提交项：`assets/Scenes/drh8.fire`（主人自己在 Creator 里改的），本次未触碰、未提交。

### 丢牌飞牌加旋转（21:30）

- 需求：丢牌时牌往中间飞，加旋转效果（原本纯平移）。
- 定位：`assets/scripts/logic/DrhPlayerLogic.ts` → `ShowCurState()` 的 `bei_shu_type == "-7"`（丢牌）分支。
  实例化 `Prefabs/丢牌对象`，挂在 `PlayerInfo` 下，从 `PlayerInfo` 飞到桌面中间 `发牌点`，
  用 `cc.tween` 两段：0.3s 平移 → 0.3s opacity 0 → destroy。上方留有前人注释掉的
  `//let rotate = cc.rotateBy(0.3,180);`（想做没做）。
- 改动：第一段 `.to()` 的属性表里加 `angle:360`，与 position 同步插值 → 飞行途中绕自身中心转一圈。
- prefab 核对：`丢牌对象` 根节点 50×50、锚点 (0.5,0.5)（绕中心转），两张牌是其子节点会一起转。
- 校验：`ts.transpileModule` 语法检查 0 诊断（临时脚本已删）；`Node.angle` 在本项目别处已在用
  （`DrhLogicMgr` 聚光灯、`panelGameView` 圆环）。
- **未做/待确认**：未跑 Creator 编译、未真机/网页验证；转速（360°/0.3s）如觉得太快可调小角度。

### 丢牌旋转调慢（21:34）

- 主人反馈「飞的圈数太多了，转圈慢一点，可以只转一圈」—— 判定是 360°/0.3s 太快（1200°/s）视觉上糊成多圈，
  而不是真的转了多圈（`Node.angle` 走 `_eulerAngles.z` + `_fromEuler()`，0→360 就是平滑一圈，无规范化跳变）。
- 改法：位置节奏完全不动，把一圈摊到两段 ——
  飞行段 `.to(0.3,{position,angle:180})` + 淡出段 `.to(0.3,{opacity:0,angle:360})`，
  总时长仍 0.6s，转速降到 600°/s（一半）。
- 语法检查 0 诊断。**经验：这类"旋转看着像转了好几圈"的反馈，先怀疑转速太快而不是圈数算错。**

### 提交并推送 9a88334 / a12f7e8（21:46）

- `9a88334`：下注数字落币弹动 + 丢牌飞牌旋转 + `drh8.fire` 场景微调（2 文件，32+/13-）。
  - 场景改动（主人在 Creator 中调）逐节点核对过：`bg` / `bk` / `V8战绩底板` / `V8围观底板` opacity 255→168，
    `BG2` 255→170；`牌局回顾`、`文字牌谱` 两个 Toggle 及其 `Background`/`checkmark` 宽度 298→260。
  - 解析方式：`git show HEAD:<file>` 导出后用 python 逐索引比对，打印差异对象的 `_name` ——
    `.fire` 只改值不改结构时索引稳定，比读 diff 行号可靠。
- `a12f7e8`：记忆同步（2 文件）。
- 预检：无未跟踪文件；`check_memory.py` **0 错误 2 警告**（`CURRENT.md` 11KB、`topics/v7-ui.md` 29KB
  超预算，均为历史问题，非本次引入）；提交时 Creator 正开着本项目，**提交后工作区仍干净**。
- 已推送 `afc707a..a12f7e8`，`main...origin/main` 无 ahead/behind。
- **未做：未跑 Creator 构建、未重建热更新包、未真机/网页验证。**

### 游戏内「提示」弹入/收起动画（22:02）

- 需求：`panelGameView` 下的「提示」节点由"直接显示"改为"快速蹦出来 + 2 秒后消失"（主人已把提示图片换小）。
- 定位：`assets/scripts/UI/panelGameView.ts` 有**两组重复入口**都操作同一个 `panelGameView/提示` 节点 ——
  `showMsg()`/`delayCloseMsg()`（1709，服务端消息驱动）与 `ShowMsg()`/`DelayCloseMsg()`（1880，本地交互，
  且它 schedule 的是小写 `delayCloseMsg`）。prefab 里该节点 scale=1、带 `cc.Sprite`+`cc.Widget`、子节点 `txt`。
- 改动：新增私有 `PlayTipShowAnimate()`（stopAllActions → active=true → opacity=255 → scale=0.2 →
  `scaleTo(0.18,1).easing(easeBackOut)`）与 `PlayTipHideAnimate()`（`spawn(scaleTo(0.15,0.2).easing(easeBackIn),
  fadeTo(0.15,0))` → active=false 并复位 scale/opacity）；四个入口全部改为调用这两个方法，**2 秒 scheduleOnce 不变**。
- 回到原点的核对：场景 `drh8.fire` 里该实例节点 scale 仍是 `[1,1,1]`（size 518x100，Widget 对齐），
  **动画以 1 为终点是安全的**；确认无其它代码直接改 `提示.active`。
- 语法检查 0 诊断；`check_memory.py` 仍是 0 错误 2 警告（历史文档超预算）。
- **未做：未跑 Creator 编译/真机验证；`drh8.fire` 有主人在 Creator 中新保存的大量改动（约 3671 行），本次未提交。**

### 排查：提示节点「缩小了还占满屏」（22:15）

- 现象：主人在场景里把 `panelGameView/提示` 缩小后，运行时「弹出来还是左右占满屏幕」。
- 实测数据（当前工作区）：
  - `提示` 节点 Widget `_alignFlags=44`（**BOT|LEFT|RIGHT**）、`_left=115.35`、`_right=116.65`、`alignMode=1`、
    `_originalWidth=749` → **宽度恒等于 父宽750 − 115.35 − 116.65 = 518**，与图片无关，且随父节点宽度重算。
  - Sprite `_sizeMode=0`（**CUSTOM**）+ `_type=0`（SIMPLE）→ 底板图被**拉伸填满节点框**。
    底板图是 `assets/imagesKK/公用/tishibg.png`（**745×70**，工作区里没有任何图片文件被改过）。
  - `Canvas` 设计分辨率 750×1334、`_fitWidth=true` → 运行时父宽固定 750。
- 结论：**提示的显示大小由「节点框」决定，节点框又由 Widget 左右对齐的边距锁定** —— 所以在编辑器里拖 contentSize、
  或把图片换小，都不会生效（换小图只会被拉伸填满原框）。与上一轮加的弹入动画无关（节点 scale 恒为 1，动画只改 scale）。
- 正确改法（两处必须同时改，否则 Widget 会再次覆盖 size）：Sprite 的 Size Mode 改成 `RAW`，
  Widget 取消 LEFT|RIGHT 对齐、改「水平居中(CENTER)」并保留 BOT。
- **未做**：未在运行环境验证真机实际宽度；Creator 正开着本项目，脚本直接改 `.fire` 有覆盖未保存编辑的风险，
  故只给操作建议未动手。

### 修复：提示节点改了不生效（22:25）

- 主人给了两张截图：编辑器里「提示」是 518×100 居中，游戏内却是横贯满屏的条幅。
- **根因（已由代码确证）**：`UIManager.showPanel` 是 `cc.loader.loadRes("UI/" + strName)` + `cc.instantiate(prefab)`，
  **运行时加载的是 `assets/resources/UI/panelGameView.prefab`**，不是场景 `drh8.fire` 里的同名实例。
  prefab 里「提示」仍是 749×77 + Widget `L=-0.4397 / R=1.4397` → 宽度 = 750−(−0.44)−1.44 = **749（满宽）**，
  与游戏内截图完全吻合；主人在场景里改的 518 从没被加载。
- 改动（`assets/resources/UI/panelGameView.prefab`）：`提示` 节点 contentSize 749×77 → **518×49**，
  Widget `_left` → `115.35009462105529`、`_right` → `116.64990537894474`（宽度 = 750−115.35−116.65 = **518**）。
  - 高度取 49 的理由：底板图 `assets/imagesKK/公用/tishibg.png` 是 **745×70**，518 宽等比对应 48.7；
    原 100 高会把图纵向拉伸约 1.4 倍（Sprite 是 CUSTOM 模式，按节点框缩放整图）。
  - `_bottom`（585.0095504194121）与 Sprite `sizeMode`（CUSTOM）刻意未动。
- 已 `touch` 文件让 Creator 重新导入；**热更新包/构建未重打，真机与网页还看不到**。
- **教训**：`panelXxx` 面板改 prefab，不要改场景里的同名节点（已写进 `MEMORY.md`）。

### 提交并推送 1030312（23:20）

- 我改完 prefab 后，**主人在 Creator 里自己又完善了「提示」节点**：内容尺寸回到 `518×100`、
  底部位置用 `620.75`，并新增子节点 `bk` 作为底板（Sprite 九宫格 SLICED）；我改的
  Widget 左右边距 `115.35 / 116.65` 保留 → 运行时宽度仍是 **518** ✓。场景 `drh8.fire` 同步新增了 `bk`。
- **排查要点（重要）**：`9bbda31e-ad49-43c9-aaf2-f7d9896bac69` 在 `assets/` 里查不到任何 `.meta`，
  但**不是断链** —— 它是 Creator **内置** SpriteFrame：`library/imports/9b/9bbda31e-….json` 里写着
  `cc.SpriteFrame` / `content.name = "default_panel"`（20×20、capInsets 6,6,6,6）。
  **判据：assets 里找不到 uuid 时，读 `library/imports/<uuid前2位>/<uuid>.json`，
  有 `content.name` 且 `content.texture` 指向内置纹理的就是引擎内置资源**，不用报警。
- 同批提交：`个人金币.png.meta` 九宫格边框（borderTop 0→14 / borderBottom 0→13，主人改的）、
  prefab 新增 `芒果_100x60`（挂芒池）与 `皮池`（挂底分）。
- 提交 `1030312`（4 文件）；验证：prefab/场景 JSON 可解析、节点组件数逐项核对、ts 语法 0 诊断、无未跟踪文件。
- **未做：未跑 Creator 构建、未重打热更新包、未真机验证。**

## 排查：XuanManager 玩家管理「最近登录」多 8 小时、跨到次日（23:15）

- 现象：后台「玩家管理」列表里 19 号就有 20 号的登录时间。
- **根因：服务端 `DATE_ADD(..., INTERVAL 8 HOUR)` 是多余的，一次 +8 变成了两次。** 该转换层建立在
  「MySQL 存的是 UTC」这个**错误前提**上。实测这台机器：
  - 系统 TZ = `Asia/Shanghai`（`/etc/localtime -> ../usr/share/zoneinfo/Asia/Shanghai`）；
  - MySQL `@@global.time_zone / @@session.time_zone = SYSTEM`，`NOW()=2026-09-19 23:16:46`、
    `UTC_TIMESTAMP()=15:16:46` → `NOW()` / `CURRENT_TIMESTAMP` / `FROM_UNIXTIME()` **拿到的本来就是北京时间**；
  - `webcm.mgr_*` 的时间列全是 `datetime DEFAULT CURRENT_TIMESTAMP` → 存的也是北京时间
    （实测 admin999 `last_login_at=23:08:49`，正是他本人登录时刻）。
  - 实测对照：`FROM_UNIXTIME(lasttime)=2026-09-19 22:56:53`（真实），加 8 小时后 `2026-09-20 06:56:53`（面板值）。
    凡北京时间 16:00 之后登录的玩家，日期都会滚到第二天 —— 与主人看到的现象完全一致。
- 前端**不是**问题：线上产物 `/www/html/.xuanmanager/web/assets/index-Ch377lxc.js` 与本地 `web/dist` 同为
  2026-09-15 15:59 构建（481126 B），含 `Asia/Shanghai` + `hourCycle:"h23"`；`web/src/time.ts` 对无时区字符串
  按北京时间解释、对带偏移时间戳按真实时刻渲染，都是对的。所以它忠实显示了后端给的错误值。
- **受影响面（后端 21 处 `INTERVAL 8 HOUR`，线上二进制 `strings` 计数=21）**：
  - 显示类（`DATE_ADD(x, INTERVAL 8 HOUR)` 包一层）→ 全部多 8 小时：`players.go:90`（玩家管理最近登录）、
    `bans.go:105/175/328`、`anti_theft.go:76/77/78/276/277/278`、`users.go:48`（后台用户管理）、
    `dashboard.go:139`（最近操作）、`visibility.go:54`、`configuration.go:295`、
    `player_optimization.go:162/238/686`、`transactions.go:227`、`platform_revenue.go:619`。
  - 筛选类：`transactions.go:231/232` 把北京时间日期范围 `DATE_SUB(..., INTERVAL 8 HOUR)` 当 UTC 比，
    同样错；`dashboard.go:61` 用 `beijingDayStart.UTC().Format(...)` 去比 `mgr_audit_log.created_at`（已是北京时间）→ 今日审计窗口偏了 8 小时。
- **游戏业务字符串确认无需转换**（保持原值即可）：`usr_cash_water.date/time` 实测 `2026-09-19 23:01:49` 与当前北京时间一致；
  `third_marketing_info.date/time`（937177 = `2026-09-14 18:33:31`）与该玩家 `sm_reg_time`（`18:33:40`）仅差 9 秒
  → 也是北京时间，所以 `anti_theft.go:77` 对 `m.date/m.time` 加 8 小时同样错。
- 正确修法：删掉所有 `INTERVAL 8 HOUR` 包装（`mgr_` 与 `FROM_UNIXTIME` 都直接原值返回）；`time.Local` 建议固定成
  `Asia/Shanghai` 而不是依赖系统 TZ。**接口文档 §22、`XuanManager/README.md`、`docs/memory/topics/xuanmanager.md`
  里「mgr_ 与 FROM_UNIXTIME 是 UTC」的记载是错的，必须同步改，否则会再被加回去。**
- 只读探测口径（可复用）：`ssh -p 2233 -i ~/.ssh/id_ed25519_newserver client_update@154.37.155.17`，
  后台在 `/www/html/.xuanmanager/`（`runtime.env` 可读，进程属 `client_update`），
  二进制 `/www/html/.xuanmanager/bin/xuanmanager`（Sep 15 15:59，sha256 `311526687e00…`）。
- **本轮只排查、未改任何代码、未部署。**

## 修复并部署：XuanManager 时间多 8 小时（23:25）

主人选定「改代码 + 部署正式」。改动与验证见
`docs/memory/handoffs/2026-09-19-xuanmanager-timezone-fix.md`，要点：

- 后端 `server/internal/api/` 删掉全部 22 处 `DATE_ADD(…, INTERVAL 8 HOUR)`（players/bans/anti_theft/users/
  dashboard/visibility/configuration/player_optimization/transactions/platform_revenue），
  工作台今日审计日界改 `.In(dashboardLocation)`，`transactions.go` 日期筛选改成直接比北京时间，
  `config.go` 的 `mc.Loc` 由 `time.Local` 固定为 `FixedZone("Asia/Shanghai", 8*60*60)`。
  合法 `INTERVAL` 保留：锁定 15 分钟、刷新 60 秒、`INTERVAL 1 DAY`。
- **部署前做了一个关键校验：** 用未改动源码构建，得到与线上二进制**体积逐字节相同**（12118054 B），
  全量字符串集合只有链接期打包位置差异 → 证明本地 HEAD 与线上同源，部署不会带出别的改动。
  这套「二进制体积 + `strings` 集合比对」口径可复用于以后的 XuanManager 部署自检。
- `go vet` 0、`go test ./...` 全过。构建 `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -trimpath`
  （Go 在 `/opt/homebrew/opt/go/bin/go`，本机 PATH 里没有）。
- 部署：备份 `backups/xuanmanager.20260919-2325-timefix-rollback`（sha `311526687e00…`）→
  上传 `incoming-20260919-timefix/bin/xuanmanager`（sha `b05e6344…` 校验一致）→ `stop.sh` / `install` / `start.sh`
  → `/api/health` 200、日志 `23:25:34 XuanManager started`。只读回读：最近登录 `2026-09-19 22:56:53` 等全部 ≤ 现在，
  `mgr_user.last_login_at = 23:08:49` 与北京时间一致；`ss -ltnp` 确认只有 8891 跑 xuanmanager。
- 文档同步：接口文档 §22、`XuanManager/README.md`、`部署说明.md` 第 32 条、`topics/xuanmanager.md`、
  `docs/memory/CURRENT.md` + 新交接。`check_memory.py`：0 错误 2 警告（CURRENT.md 11KB、v7-ui.md 29KB 为历史问题）。
- **改动未提交**（主人只说改代码+部署）；未登录后台页面做视觉复核（无管理员凭据），需主人刷新页面确认。

## 新增并部署：玩家管理「登录日期范围」筛选（23:31）

- 后端 `players.go`：`playerFilters` 加 `LoginFrom/LoginTo`（query `loginFrom`/`loginTo`），
  校验扩到 4 个日期字段 + 两个「起不能晚于止」；`buildPlayerWhere` 加
  `k.lasttime >= UNIX_TIMESTAMP(CONCAT(?, ' 00:00:00'))` / `<= …' 23:59:59'`（会话时区 +08，日界即北京时间当天；
  `lasttime` 为 0/NULL 的未登录账号不落范围）。单测扩到 6 个（含新条件子句/参数顺序/倒序范围/非法格式）。
- 前端 `PlayersPage.tsx`：`PlayerFilters`/`emptyFilters` 加两键（`queryString` 按 `Object.entries` 自动带上，请求逻辑不用改），
  更多条件面板加「登录开始日期/登录结束日期」两个 `date` 输入并带提示。
- 验证：`gofmt`/`go vet`/`go test ./...` 全过；`eslint`/`tsc -b`/`vite build` 通过（JS hash `index-Ch377lxc` → `index-DpMwsw8F`，CSS hash 不变）；
  只读 SQL 对拍：新条件当天命中 **8 条**，与 `FROM_UNIXTIME(lasttime)` 落在同一北京日界的参照口径 **8 条**一致。
- 部署后端 + 前端（**功能改动必须两边一起上**）：备份 `backups/xuanmanager.20260919-2331-loginrange-rollback`
  与 `web.previous-login-range-20260919`；前端用「解包到暂存 → mv 旧 → mv 新」交换，避免解包失败打空线上目录；
  上线后 `/` 的 index.html 指向新 hash、该 JS `200 / 481547 B` 且含「登录开始日期」、`/api/health` 200。
- 坑：**macOS `tar` 会带 `._*` AppleDouble 文件**，发布后要 `find web -name "._*" -delete`。
- 文档：接口文档 §22 补登录日期范围的时区口径；新交接 `docs/memory/handoffs/2026-09-19-xuanmanager-player-login-range.md`。
  技能 `xuanmanager-timezone-and-deploy` 新增「5.1 前端改动要一起部署」章节。
- **改动未提交**；未登录页面点击验收，需主人刷新「玩家管理 → 更多条件」实测。
