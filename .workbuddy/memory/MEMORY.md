# MEMORY.md —— qing 项目长期约定

> 项目内的当前状态、决策与专题记录在 `docs/memory/`（入口见根 `AGENTS.md`）。
> 本文件只放跨任务复用的操作约定，不写具体进度。

## 交付与清理（2026-09-18 主人明确要求）
- **中间产物一律不留。** 任务完成、最终产物交付后，立即删除过程中生成的所有临时文件：
  试错版本、AI 生成的多版草稿、调试图、临时脚本、对比图。只保留最终成品。做完即删，不用再问。
- 只交付成品，不把中间态当结果拿出来。
- 过程要短：不要为了追求完美来回试错，先出可用结果。

## 美术改图（游戏改名 BL → BY）
- **硬约束：新图体积不能明显大于原图**，会直接影响安装包大小。原图 `assets/V7/login_shield_exact.png`
  为 417×514 / 351 KB；改名后成品 207 KB。
- **换图手法：** 覆盖同名 PNG 内容、保留 `.meta` 不动 → UUID 不变，Creator 里不用重挂引用。
  覆盖前确认 `.meta` 里的 `width/height/rawWidth/rawHeight` 与新图尺寸一致（不一致要改，否则显示异常）。
- **尺寸必须与原图一致**（本图 417×514）。缩回原尺寸后再压体积。
- **压体积有效手法（按性价比排序）：**
  1. RGB 每通道降到 6bit（`//4*4`）+ `optimize=True, compress_level=9` —— 视觉无差异，本图 398KB → 252KB。
  2. LANCZOS 从生成图缩回目标尺寸（本身就会掉一大截）。
  3. 轻高斯模糊 0.7 只省 ~10%，不划算，别用。
- **AI 图生图改文字改不了「效果不变」**：Seedream 会重画整只盾牌（轮廓、金边层数、纹样、甚至黑桃颜色）。
  Ark 生图 API 无 mask/inpaint 参数。要「只改字、其余像素不动」只能走像素级合成。
- **像素级合成若要做，文字掩码不能只用「金色」判定**（R>B+18）——旧字的暗部倒角笔画是深褐色，会漏掉留残影。
  必须改成「偏离平滑蓝底模型」的判定。
- **抠透明底：** 本机 Pillow 12 的 `ImageDraw.floodfill` 完全无效（调用后零像素变化）。可靠替代：
  S=8 粗网格保守池化（整格为浅色才算背景）→ 从四边种子迭代膨胀求连通域 → 上采样 → 再按浅色掩码膨胀补边。
- **`np.nonzero` 首尾元素 ≠ min/max**，取包围盒必须用 `.min()/.max()`（否则切片会切空）。

## 工具环境
- 图片处理用托管虚拟环境：`/Users/yy/.workbuddy/binaries/python/envs/default/bin/python`（已装 numpy 2.5.3 + Pillow 12.3.0）。
  系统 `/usr/bin/python3` 有 Pillow 但**没有 numpy**。
- Seedream 4.5 显式像素尺寸下限是 **≥ 3,686,400 px**（不是技能文档写的 1280×720）。
