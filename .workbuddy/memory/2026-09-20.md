# 2026-09-20

## 提交并推送玩家信息弹窗 V8-new（main）

- 提交 `f04a05b`「玩家信息弹窗 V8-new 实施，清理待确认设计目录」，89 文件、+4208/-1932；随后 `74bff76` 回填提交推送状态。均已推送 origin/main。
- 内容：`panelUserInfo.prefab`/`.ts` V8-new 换肤、`assets/V7/player_info_v8_*` 19 张 PNG+meta、
  `tools/extract_v8_player_info_assets.py`、`tools/migrate_v8_player_info.py`、
  `art_sources/player_info_v8*/`、`docs/memory/handoffs/2026-09-20-player-info-v8.md`，
  以及删除 `design-previews/待确认/` 历史设计目录（用户手动删，共 45 项）。
- **踩坑**：删除 `待确认/` 后，`design-previews/README.md` 出现 6 处死链，
  改写成文字说明后仍剩 1 处 —— 是**既有笔误**：链接写 `25-玩家赢分榜.png`，
  真实文件名为 `25-排行榜-玩家赢分榜.png`。已一并修正，`check_memory.py` 归零。
- 收尾：`docs/memory/CURRENT.md`、交接文档中的「未提交推送」已回填为 `f04a05b`；工作区干净。

## 修复「牌型提示」界面点击穿透（未提交）

- 现象：游戏内打开「牌型提示」后，点击界面会穿到后面的座位/操作区，界面自身不关。
- 根因：`牌型提示` 是 `panelGameView` 的子节点（根下第 21 个，全屏 750×1334，Widget 四边对齐），
  但节点上**没有任何触摸监听**（无 Button、无 BlockInputEvents、脚本也没注册），
  于是触摸命中直接落到下层 `UserInfo` 里的座位按钮 —— 那个 listener 会 swallow 掉这次触摸，
  连 `panelGameView.node` 的 `ON_POINT_DOWN`（TOUCH_END）都收不到，所以界面也不关。
- 机制（查 Creator 2.4.13 引擎源码确认）：触摸候选 listener 按**场景图 z-order 排序**派发
  （`CCEventManager._sortEventListenersOfSceneGraphPriorityDes`，降序、active 的在前），
  **与注册先后无关**。节点只要注册了任一 touch 监听就会进入候选并被 `swallowTouches` 吞掉，
  不会再传给下层。节点 `_onPostActivated(true)` 会 `eventManager.resumeTarget`，所以
  active=false 时注册的监听在激活后照常生效。
- 改法（`assets/scripts/UI/panelGameView.ts` `onLoad`，仿既有「奖池面板」写法）：
  给 `牌型提示` 注册 `TOUCH_END` → `active = false`。一次改动同时解决穿透 + 点击任意位置关闭。
  未动 prefab（纯交互逻辑，不需要 Creator 重新导入）。语法自检 0 诊断。
- 同类隐患（未处理，待主人决定）：`系统设置`/`牌局回顾`/`实时战绩`/`带入窗口` 也是全屏遮罩但只靠
  内部「关闭上层」按钮，点空白处同样会穿透到下层。

## 提交并推送（本次会话收尾）

- 提交 `2a35998`：桌内牌局设置 V8 换肤、桌内弹窗统一原版风格、修牌型提示点击穿透；
  146 文件 22199+/5142-，已推送 `main`（远端 `ls-remote` 与本地 SHA 一致）。
- 提交前预检结论：37 张 `assets/` 下 PNG 的顶层/子 meta 尺寸与 trim 全部自洽；
  `assets/` 5433 个 UUID 无重复；改动 prefab/scene 引用的 SpriteFrame 全部可解析
  （有 5 个 uuid 在任何 `.meta` 里都找不到定义，但 HEAD 中同样被 42 份 prefab/9 份 scene
  引用 → 引擎内置资源，非本次回归）。
- **坑：`git push origin main` 走 22 端口被中断**（`Connection closed by 20.205.243.166 port 22`），
  但 `nc -z` 显示 22 可连、且 443 到 `ssh.github.com` 正常。可用 443 绕过：
  `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_github_qing -o IdentitiesOnly=yes -p 443" \
   git push ssh://git@ssh.github.com:443/teihin/qing.git main`
  （remote 配置是 `github.com` 的 Host 段，`ssh.github.com` 不在其中，必须显式 `-i`，
  否则会 `Permission denied (publickey)`）。走显式 URL 推送后本地
  `refs/remotes/origin/main` 不会自动更新，用 `git update-ref` 对齐即可。

## 带入积分「余额不足」子弹窗统一风格

- `panelGameView.prefab` 与 `drh8.fire` 的 `带入窗口/余额不足提示` 改为原版温馨提示风格
  （复用 original_dialog 深蓝金边主框/标题/蓝金按钮；标题「温馨提示」，取消左、充值右，
  保留右上关闭）。**无新增贴图**，故无 .meta 需对拍。
- 结构对拍（`json.load` 两份 `.fire` 比 `_name` 与 `__type__` 计数）：节点 1305→1310，
  新增的 5 个节点全部落在 `panelGameView/带入窗口/余额不足提示` 子树内，无节点被删。
  **`drh8.fire` 的 6053 行 diff 绝大部分是 Creator 重新序列化的次序/浮点噪声**，
  别被行数吓到 —— 用节点名/组件类型计数比对才能看出真实改动量。
- 场景里那个 `New Sprite(Splash)` 是 Creator 默认名，本仓 `panelMain`/`钱包`/`panelCloudNotify`
  等既有 prefab 里本来就有，脚本未按名字引用，**不算问题，不用改名**。

## 提交并推送：赠送金币 / 房间邀请批准稿 + 闹钟图标

- 提交 `d8c6c4b`「桌内赠送金币与房间邀请面板批准稿实施，新增闹钟图标」，79 文件、+3377/-329，已推送 `origin/main`。
- 内容：`panelGivePad.prefab`/`.ts`、`panelRoomInvite.prefab`/`.ts` 按批准稿替换，
  `assets/V7/give_pad_approved_*` 12 张、`assets/V7/游戏内/room_invite_approved_*` 9 张 PNG+meta 入库，
  新增 `extract/apply/validate/render` 工具与 mock 回归用例、两份 handoff，`CURRENT.md` 同步；
  `assets/V7/游戏内/闹钟_170x170.png` 新增；随附 `drh8.fire`、`panelMain.prefab`、`DrhPlayerLogic.ts` 改动。
- **坑：`git push origin main` 走 22 端口再次被中断**（`Connection closed by 20.205.243.166 port 22`），
  沿用上次的 443 绕行：
  `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_github_qing -o IdentitiesOnly=yes -p 443" git push ssh://git@ssh.github.com:443/teihin/qing.git main`
  推送成功后 `ls-remote` 复核远端 SHA = 本地 `d8c6c4b`，再 `git update-ref refs/remotes/origin/main d8c6c4b` 对齐。
- 本次未做提交前资源预检（PNG↔meta 尺寸、UUID 唯一性、引用解析），仅核了待提交清单
  （79 文件、无 >20MB 大文件、无凭据类文件）。历次 handoff 提到的「Creator 最终重新导入待完成」仍未处理。

## 玩家信息 V8 图标缩尺寸（fishing / nice）
- 需求：`assets/V7/player_info_v8_icon_fishing.png`、`player_info_v8_icon_nice.png`（各 1254x1254、876/843KB）压到宽 200 以内、高等比 → 200x200。
- 做法：premultiply → 面积预滤波 → LANCZOS → RGB-only USM(75)，覆盖原 PNG；同时把 `.meta` 的**顶层 width/height + subMetas 的 width/height/rawWidth/rawHeight 共 6 处**改成 200。`trimType` 本来就是 `none`，无 trim 重算风险；**UUID 保持不变**，prefab 引用不受影响。
- **新坑（补充既有经验）**：改完 meta 后 `touch` PNG + `osascript -e 'tell application "CocosCreator" to activate'` 触发重导入 —— Creator 确实把 `library/imports/<uuid>.png` 从 876KB 更新成了 42KB（生效了），**但 `nice` 的 meta 顶层 width/height 被 Creator 回写成了旧值 1254**（同批的 fishing 没有），subMetas 却是新值 200。→ **重导入后必须再复查一次 meta 一致性**，不一致就补改顶层；改完 5 秒内没被再回写即稳定。
- 结果：fishing 42.9KB / nice 38.8KB（原 876/843KB），与同目录既有 icon 风格一致（bomb 98x118/18.6KB、like 102x106/14.9KB、gun 161x98/23KB…）。
- 备份：原 PNG 与 meta 各一份在 `/tmp/qing_icon_backup/`（**临时目录，仅本次保险，需长期保留请另存**）。

## 客服顶部 V8 + 回顾对象 UUID 错位（**已知未修，主人选择原样交付**）

- 本轮主改动：`panelKefu.prefab` 顶部换肤 —— 47 对象/16 节点不变，仅 1 个 `cc.Sprite` 换成
  `cc.Label`（原客服图标节点改浅金 40px 标题），组件计数 `Sprite 8→7 / Label 1→2`，与交接描述一致。
- 其余随行改动：`drh8.fire`（±15 行尺寸/透明度）、`panelMain.prefab`（±3 行标签宽高）、
  12 张 `give_pad_approved_*.png.meta`（**只少了文件末尾换行符**，Creator 改写的噪声，无尺寸变化）、
  `project.manifest` / `version.manifest`（热更清单 5.0.15 → 5.0.16）。
- **⚠️ 未修问题：`Prefabs/回顾对象` UUID 错位。**
  - `回顾对象.prefab`（正式文件，今天 13:48 改过）的 meta uuid 被换成了 `49e39c6c-…`。
  - 未跟踪的 `回顾对象---.prefab`（8 天前的备份，**内容与 HEAD 版逐字节相同**）的 meta 占着旧 uuid `ee24a2b5-…`。
  - 而 `drh8.fire`（5 处）、`panelGameView.prefab`（5 处）、`project.manifest` 引用的都还是
    **旧 uuid `ee24a2b5`** → 这些引用现在解析到的是那个备份文件（旧样式）；运行时
    `loadRes("Prefabs/回顾对象")` 走路径，拿到的是新样式。**场景内嵌与动态加载会不一致。**
  - 已把选项和后果说明给主人，主人明确选择「原样提交」。下次谁要清理这个 `---` 文件前，
    **必须先把 `回顾对象.prefab.meta` 的 uuid 改回 `ee24a2b5-8f16-41cc-875f-aa0779fa00f1`**，
    否则 drh8.fire / panelGameView.prefab / project.manifest 的引用会直接断掉。
- 判 UUID 错位的手法（可复用）：`git show HEAD:<prefab>` 与工作区两份分别 `json.load`，
  比 `__type__` 计数是否一致只能看出结构，**uuid 归属要看 `.meta` 的 diff**；
  再 `grep -c <旧uuid>` 场景/prefab 里还有多少处引用，就知道引用有没有跟着走。

## 修 Android Studio「Cannot create directory …/.gradle/8.9/fileHashes」

- 现象：AS 里 Gradle sync 失败，报 `Cannot create directory '/Volumes/SSD/qing/build/jsb-link/
  frameworks/runtime-src/proj.android-studio/.gradle/8.9/fileHashes'`。
  AS 日志 `~/Library/Logs/Google/AndroidStudio2024.3/idea.log` 14:02:16 实锤同一句。
- 根因：该工程的 `.gradle` 是**软链接** → `/private/tmp/qing-android-gradle-project-cache`。
  `/private/tmp` 会被 macOS 在**重启时清空**（当天 uptime 仅 5h50m），目标一没，软链接悬空，
  之后任何 mkdir 都失败。**每次重启必然复发**，不是偶发。
- 这套 hack 的出处：`gradlew` 第 164-168 行早前加的 `set -- --project-cache-dir /private/tmp/…`
  （注释写"项目在 exFAT 卷上"，但 `/Volumes/SSD` 现挂载为 **hfs**，注释已过时）。
  **AS 不走 gradlew**，它直接用 `<projectDir>/.gradle`，所以两边本是同一路径、一起挂。
- 根治：缓存位置换成持久目录 `/Users/yy/Library/Caches/qing-android-gradle-project-cache`；
  `rm .gradle` 后重新 `ln -s` 指过去，**同时**把 `gradlew` 的 `--project-cache-dir` 改成
  `"$HOME/Library/Caches/…"`，否则 AS 与命令行会分裂成两份缓存。`bash -n gradlew` 通过；
  `.idea/`、`tools/`、`*.command` 均无旧路径残留。AS 里点一次 Sync 即生效。
- 注意：`build/jsb-link/…` 整条路径**不被 git 跟踪**（`git ls-files` 返回空），改这里零 git 影响。
- 顺带发现（未处理）：同目录有两个 7/23 的 JVM 堆转储 `java_pid26794.hprof`(553MB)、
  `java_pid50462.hprof`(574MB)，合计约 1.1GB 占空间。

## 修 AS `:qing:packageRelease` 失败（真因：Gradle daemon 堆 512m 打爆 OOM）

- 现象：`.gradle` 悬空软链接修好后，AS 里 release 构建仍失败 ——
  `Execution failed for task ':qing:packageRelease'.` →
  `A failure occurred while executing ...PackageAndroidArtifact$IncrementalSplitterRunnable`。
- **真因不是代码/资源，是 daemon 堆不够**：daemon 用 Gradle 默认 `-Xmx512m`，release 打包
  （R8 + shrinkResources + 两个 ABI 的 APK 打包）把它撑爆。AGP 把 OOM 包成了那句看不出所以然的
  `IncrementalSplitterRunnable`。
- **铁证**：daemon 带 `-XX:+HeapDumpOnOutOfMemoryError`，OOM 时在**工程目录**落堆转储。故障当场多出
  `java_pid88442.hprof`（620MB，**14:07**，与失败同一分钟；88442 正是 AS 那个 daemon）。另有 7/23 的
  26794 / 50462 两份，且 `daemon-50462.out.log` 里 7/23 就记着**同样**的 packageRelease +
  IncrementalSplitterRunnable 失败 —— **反复发作的老毛病，不是新问题**。
- 判坑手法（可复用）：daemon 日志里**找不到** OOM 报错行（异常转发给客户端了），
  但 `<工程目录>/java_pid<daemonPid>.hprof` 的时间戳与失败时刻对齐即可定案；hprof 只在
  `+HeapDumpOnOutOfMemoryError` 下产生。
- 修法：`proj.android-studio/gradle.properties` 把注释掉的 jvmargs 换成生效的
  `org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8`
  （本机 16GB 内存）。验证：新 daemon 日志出现 `Starting build in new daemon [memory: 4 GiB]`。
- 排查中两条被证伪的线索（记下避免重踩）：① 曾疑心 AS 的 JBR 21 与 CLI 的 Corretto 19 有差异 ——
  实测两边都能成功打包，**不是 JDK 问题**；② `buildDir` 被重定向到
  `$TMPDIR/qing-android-gradle/<name>`（根 `build.gradle` 第 24 行），该目录同样会被系统清理，
  但 Gradle 会自行重建，不致命。
- 沙箱副作用：在沙箱里跑 `--rerun` 复现时，ndk-build 的 `rm` 被拒（`Operation not permitted`），
  导致 NDK obj 目录被清空 —— **下次 AS 构建会重新编译原生库，耗时较长**，属预期。
- 遗留：三个 hprof 共约 **1.75GB** 未删（等主人确认）。

## 核实「后续会不会复发」——发现真正的复发口子

- **结论**：重启引起的（fileHashes 悬空软链接）**不会**复发；OOM **不会**复发，
  **除非在 Cocos Creator 里重新构建原生 Android 工程**。
- **口子在这**：`proj.android-studio` 是 Creator 生成的，模板位于
  `/Applications/Cocos/Creator/2.4.13/.../cocos2d-x/templates/js-template-link/frameworks/runtime-src/proj.android-studio/`。
  逐项核对过该模板是**干净原版**：`gradlew` 无 `--project-cache-dir`（0 命中）、
  `app/build.gradle` 无 `cleanReleaseAppleDouble`（0 命中）、`gradle.properties` 里那行 jvmargs
  **是注释掉的** —— **OOM 的根子就在这行**。
  → 一旦重新构建原生工程：4G 堆配置、`.gradle` 软链接、`gradlew`/两个 `build.gradle` 的 hack **全部被覆盖**，
  **OOM 原样复发**。（附带一点好转：模板无软链接，所以重建后不会再报 fileHashes 悬空 —— 缓存落回
  `<projectDir>/.gradle`，且盘已是 HFS 不是 exFAT。）
- 另两个口径：`build-templates/` 里只有 `web-mobile/splash.png`，**没有 Android 模板**；
  四个 `.command` 脚本只做热更生成/上传，**不重建原生工程** → 主人日常流程短期碰不到这个口子。
- **持久化办法**：镜像到 `build-templates/<构建输出目录名>/`。命名约定由既有文件佐证：
  `build-templates/web-mobile/splash.png` ↔ 输出目录 `build/web-mobile`，故原生 link 构建应放
  `build-templates/jsb-link/frameworks/runtime-src/proj.android-studio/`。**软链接无法靠模板复制**。
- 残余风险（已告知）：第三方「清缓存」工具若清掉 `~/Library/Caches`，软链接目标会被删 → fileHashes 报错复发。
- 已同步进技能 `cocos-android-studio-build-triage`（新增「复发风险」一节）。

## 提交并推送全部变更（main → `24ea008`）

- 提交 `24ea008`「排行榜/银行对象 prefab 更新 + Android 构建排障记录」，6 文件 / +9665 -9610
  （绝大部分是 `排行榜.prefab` 重序列化）。推送 `9405003..24ea008` 成功，随后 `git update-ref`
  对齐本地 `origin/main`，工作区干净。
- **提交前核实了两处非本次改动**（主人 14:31~14:33 在 Creator 里的会话产物）：
  - `排行榜.prefab`：**有实质内容变化** —— 移除 5 个「匿名节点」（`_name` 为空）及其上的 5 个组件
    （`PageEx` `e5505828-65ef-49b7-99f9-76b48f5e8bc0` ×3、`scrollview2` `55af2b0a-…` ×2），
    顶层对象 623→618、节点名 455→450。**已提醒主人确认是否为有意删除。**
  - 两个 prefab 的 `cc.PrefabInfo.asset` 自引用由 `{"__uuid__": <自身uuid>}` 改成 `{"__id__": 0}`
    —— 指向文件内第 0 个对象（即 prefab 自身），**语义等价的 Creator 重序列化写法，不是损坏**。
  - `project.manifest` / `version.manifest` 随构建更新。
- **复核手法（可复用，抵得住「万行 diff」）**：`git show HEAD:<p>` 与工作区各 dump 成 JSON，比
  ①`__type__` 计数 ②`_name` 多重集 ③正则抽全部 uuid 集合 ④逐个主题（如 `PrefabInfo.asset`）看差异。
  三者一致 + 仅自引用写法变化 ⇒ 噪声；计数减少 ⇒ 真实删除，要如实报给主人。
  另：压缩 uuid 的前 5 位与 `.meta` 里完整 uuid 前 5 位相同，可据此反查脚本名。
- 推送仍用 443 绕行（22 端口偶发中断）：`GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_github_qing
  -o IdentitiesOnly=yes -p 443" git push ssh://git@ssh.github.com:443/teihin/qing.git main`。

## 提交并推送：赠送面板换标题图（main → `a9bf2ea`）

- 提交 `a9bf2ea`「大厅「赠送」面板换用赠送标题图、清理 V8 表单底纹节点，热更清单升至 5.0.20」，
  8 文件 / +2542 -2946，已推送（443 绕行），`ls-remote` 复核远端 = `a9bf2ea`，`update-ref` 对齐，
  工作区干净；Creator 在跑但未改写 `.meta`。
- 内容：`panelMain.prefab` 的「赠送」覆盖层移除 `V8表单背景`、`V8赠送延展底纹`（各含 1 个 `New Label`），
  新增 `赠送标题_941x412` 节点（引用 `assets/V7/赠送标题_941x412.png`，sub uuid `4d6d617d…`）；
  删除 `assets/V7/gift_hero.png`(750×323) 与 `gift_hero_exact.png`(941×412)；
  新增 `assets/V7/赠送标题_941x412.png`(941×412, 190KB)；热更清单 5.0.19 → 5.0.20。
- 预检（走技能 `cocos-asset-precommit-audit`）：被删 4 个旧 uuid 在 `assets/` 下 0 引用；
  新图 PNG = meta 顶层 = `subMeta.w/h` = `rawWidth/Height` = 941×412（`trimType: custom` 满足 w/h==raw）；
  全仓 5478 个 uuid 无重复；prefab 里 3 个在任何 meta 中都找不到的 uuid（`6f801092`/`a23235d1`/`eca5d2f2`）
  在 HEAD 同样被引用 → 引擎内置资源，非本次回归。
- **手法补充**：`git status --porcelain` 会把中文路径转义成 `\350\265…`，脚本必须加
  `git -c core.quotepath=false` —— 本次第一版预检脚本因此把「新增 PNG」漏判为空列表。
  量 prefab 真实改动量用「节点路径集合差集」（本次 +1/-4 个节点），
  而 5434 行 diff 基本都是 Creator 重序列化噪声。
- 附带定位：玩家资料卡（`panelUserInfo`）里点「赠送」→ 弹 `panelGivePad`（其中标题节点就叫
  `赠送金币`）；大厅「我的」页点「赠送」→ 只是 `panelMain` 内部覆盖层，不加载独立 prefab。

## XuanManager 玩家管理：注册/登录时间拆成两列（已部署 + 已提交）

- `web/src/pages/PlayersPage.tsx`：玩家列表表头 `注册 / 登录（北京时间）` 拆成
  `注册时间（北京时间）` + `最近登录（北京时间）` 两列，原单元格里"最近：xxx"的副标题改成独立 `<td>`。
- `web/src/styles.css`：`.player-table` 的 `min-width` 1120px → 1220px（多一列）。
- 已 `npm run lint` + `npm run build`，新产物 `dist/assets/index-DC2upvZY.js`（CSS `index-DGtSq5jZ.css`），
  校验 JS 内含新表头、不含旧表头。
- 已部署正式 8891（主人明确授权）：暂存解包 → `mv web web.previous-player-time-split-20260920` → 切换。
  只换前端静态目录，未重启后端。线上回读：`/api/health` 200，首页引用新 hash，JS 200 / 481,557 B，
  新表头命中、旧表头 0 命中，CSS 含 `player-table{min-width:1220px}`。**未登录页面做视觉复核。**
- 已推送到 origin/main（`a9bf2ea..ba38c31`）：源码提交 `7865427`（PlayersPage.tsx + styles.css + 本日志）、
  交接回填 `ba38c31`（走 443 绕行：`ssh://git@ssh.github.com:443/teihin/qing.git`，推送后 `git update-ref` 对齐本地 `origin/main`）。
- 记忆侧同步：`docs/memory/CURRENT.md` 的 XuanManager 章节补 09-20 一行（顺带压缩历史重复表述，
  把该文件从 16374/17033 拉回 16384 硬上限内，余量已不足 10 字节）
  + `docs/memory/handoffs/2026-09-20-xuanmanager-player-time-split.md`。

## 客服玩家端去掉「结束咨询」按钮（已改源码 + 已上线）

- 需求（主人原话）：玩家端界面上的「结束咨询」没啥用，去掉；关闭由外层 APP 负责。
- 定位结果：它**不在 Cocos 侧**，而是 H5 客服页 header 里的按钮 ——
  `ChatTool/web/src/PlayerApp.tsx` 第 260 行 `<button className="header-action" onClick={endConversation}>结束咨询</button>`；
  Cocos 的 `panelKefu.prefab` 里只有「关闭」和已被 `ConfigureTitle()` 置灰的「弹出」。
- 改动：① 删按钮；② 连带删 `endConversation()`（`tsconfig.app.json` 开了 `noUnusedLocals`，不删会编译失败）；
  ③ 删 `styles.css` 里 4 条 `.header-action` 规则（1 条基础 + 3 条媒体查询）。`header` 用
  `justify-content:space-between`，去掉右侧按钮后品牌自然靠左，无需补样式。
- 服务端 `POST /api/player/end` 未动（客服端结束会话、`conversation.status === 'closed'` 的
  「本次咨询已结束」提示与「评价本次服务」入口都不受影响）。
- 已跑 `tsc -b`（0 错误）+ `vite build`：产物 `dist/assets/index-CRicsnay.js` / `index-Bq9oCEHx.css`，
  校验产物内「结束咨询」0 命中。**dist 在 `.gitignore` 里（`ChatTool/.gitignore:7`），所以线上要生效
  必须重新上传 `web/dist/` 到 154.37.155.17（见 `ChatTool/docs/deployment.md` 构建和安装第 3 步）。
- **已按要求上线（主人授权「更新线上版本」）**：`scp -O` 上传 tar 到 `/www/html/.chattool/incoming/`
  → 解包到暂存目录 → 校验（新 hash 命中、产物内「结束咨询」0 命中）→
  `mv web web.previous-remove-end-consult-20260920-183002` → `mv` 新目录 → `chmod 2755`（父目录继承
  `updates` 组）。**只换静态目录，未重启 Go 服务**（`CHAT_STATIC_DIR=/www/html/.chattool/web` 路径未变）。
- 线上回读：`127.0.0.1:8893` 与经 Caddy 的 `http://154.37.155.17/chattool/` 首页均 200，新产物
  `index-CRicsnay.js` 240742 B / `index-Bq9oCEHx.css` 44346 B 与本地一致；JS 内「结束咨询」0 命中、
  「评价本次服务」1 命中（保留），CSS 内 `header-action` 0 命中；`/chattool/player` 200；
  `/chattool/api/health` 200。回滚：`rm -rf web && mv web.previous-remove-end-consult-20260920-183002 web`。
- 源码与本记录随本次提交一并入库。
- 连接口径：`client_update@154.37.155.17:2233`，key `~/.ssh/id_ed25519_newserver`；
  ChatTool 前端根即 `CHAT_STATIC_DIR`，备份目录沿用 `web.previous-<描述>-<时间戳>` 扁平命名。
- 已提交并推送 origin/main：`68a8d5d`（客服移除结束咨询，含本记录 + 交接文档）、
  `88f3889`（场景 `drh8.fire`「强制秀牌」「看剩余牌」X 坐标改为 ±56 对称）。
  推送走 443 绕行（`ssh://git@ssh.github.com:443/teihin/qing.git`）后 `git update-ref` 对齐本地 `origin/main`；
  远端 `refs/heads/main` 已确认为 `88f3889`。

## 修复「点延时后分牌中图标多上移一截」
- 现象：分牌/看牌阶段点头顶「分」(已分牌) 图标正常；点「延时」后该图标终点比正常高一段，压住上家牌。
- 根因（引擎级，非业务分支写错）：`PlayerInfo/animateOut` 的弹跳动画（0.3s，`moveTo(0,58)`+`easeBounceOut`）播放中，
  非动画分支在 `获取_全量_状态_事件`/`当前_玩家_消息` 里直接 `position = (0,58)`（`ShowCurState/ShowKanPaiZhong(…,false)`）。
  Creator 2.4 默认 `cc.macro.ENABLE_STACKABLE_ACTIONS=true`，`MoveBy.update` 会按 `target - _previousPosition`
  把这笔外部位移累加进 `_startPosition`，动画收尾 = 58 + (58 - 打断时的位置)。用引擎源码做数值复现：第 3 帧被打断 → 110.58（上限 116）。
- 改法（`assets/scripts/logic/DrhPlayerLogic.ts`）：新增 `StopAnimateOutAction/SetAnimateOutPos/PlayAnimateOutUp`
  三个私有方法；4 个动画分支共用 `PlayAnimateOutUp()`（每次新建 action，不再复用 `moveAction`），
  4 个非动画分支改走 `SetAnimateOutPos()`（先 `stopAllActions()` 再定位）；`start()` 里那份共享 action 创建删掉。
- 验证：`ts.transpileModule` 0 诊断；数值复现「修复后终值 58」。**待真机/真局验证**（需重新构建或热更客户端生效）。
- 已提交并推送 main：`9328eca`（本次改动 + 上述记忆同步）；推送直接走 `git push origin main` 成功，未用到 443 绕行。

## 提交并推送：玩家信息钓鱼/点赞图标 + 战绩对象/回顾面板样式（main）

- 提交 `fddf2f8`「玩家信息新增钓鱼/点赞图标，战绩对象与回顾面板样式调整」，8 文件 +2383/-2080，已推送 `main`；
  `ls-remote` 复核远端 SHA 与本地一致，`git update-ref refs/remotes/origin/main` 已对齐，工作区干净。
- 内容：`panelUserInfo.prefab` 新增「钓鱼图标」「Nice图标」两节点，引用新入库的
  `assets/V7/player_info_v8_icon_fishing.png` / `_nice.png`（各 200x200，来自 1254x1254 等比缩放）；
  `战绩玩家对象.prefab` 字号 26→33 + 正文 `LabelOutline` 描边、根节点高 67.38→100；
  `panelRecordInfo.prefab` 节点「平台」改名「line」、新增 708x64 节点、列表条 230→121.24。
- **本次补做了提交前资源预检**（上一条记录里缺的那项）：新增 PNG 实际尺寸与 `.meta` 顶层/subMetas
  三方一致（200x200，`trimType: none`）；两个 spriteFrame uuid（`f976fc0b…` / `de88f7b4…`）全仓唯一，
  在 `panelUserInfo.prefab` 中引用可解析。`git ls-files -s` 权限位均为 100644。
- **坑（复用上次）**：`git push origin main` 走 22 端口仍不可靠，**直接走 443 绕行更省事**，不必先试 22：
  `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_github_qing -o IdentitiesOnly=yes -p 443" git push ssh://git@ssh.github.com:443/teihin/qing.git main`
- 判别 prefab 真实改动量：**别读行数**。`panelRecordInfo.prefab` 2920 行 diff 里对象数/组件类型计数
  完全不变（455 对 455），真实改动只有几处尺寸/改名；`panelUserInfo` 则是真新增 2 节点（262→268，+2 Node/+2 Sprite/+2 PrefabInfo）。
  （提醒：比较 old/new 集合差集时别把 `old-new` 当成新增 —— 这次一开始就标反了。）
## 提交并推送：pk2 全量牌面换新配色 + 搓牌窗口手1/手2 置透明（main）

- 提交 `a42cd90`「pk2 牌面序列图全量换新配色，搓牌窗口手1/手2 置为透明」，275 文件 +482/-482，已推送；
  远端 `ls-remote` SHA 与本地一致，`origin/main` 已对齐。
- 内容：`assets/resources/pk2/` 164 张牌面序列图（`<类型>_<点数><a|b|d>`，由 `PKCardInfoScript.ts`
  与 `DrhPlayerLogic.ts` 按 `pk2/`+type+`_`+num+变体 动态加载）**全部替换**为深红+银灰新配色；
  `drh8.fire` 里 `panelGameView/搓牌窗口/牌/手1`、`手2` 的 `_opacity` 255→0（仅 2 行，`_active` 仍 true）。
- 提交前预检（全部逐张核过，0 异常）：**164 张尺寸与原图逐张一致**（无适配影响）；
  110 份 `.meta` 的顶层/subMetas/raw 尺寸与 PNG 实际尺寸三方自洽；**texture 与 spriteFrame uuid 全部未变**
  （prefab/scene 引用不受影响）；`trimType` 未变；总体积 8.4MB→8.6MB（+1.9%）。
- 这批 meta 的真实改动是 **trim 范围归零**（`trimX/trimY` 1→0、`width/height` 变为 raw 值）：
  新图四边补了 alpha 边界像素，Creator 重导入后 trim 失效 —— 正是 MEMORY 里那条
  「覆盖 PNG 会被 Creator 重算 trim、致 `_sizeMode=0` 元素放大」的规避做法的**结果特征**，
  看到 `trimX/trimY` 由 1 变 0 就该知道图被补过边。
- **坑（新，重要）：443 绕行也会断流。** 首次 `push`（275 文件、~9MB 二进制）直接
  `send-pack: unexpected disconnect while reading sideband packet` / `client_loop: send disconnect: Broken pipe`，
  远端未更新。**加 SSH 保活参数重试即成功**，以后大改动推送直接带参数，别先裸推：
  `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_github_qing -o IdentitiesOnly=yes -p 443 -o ServerAliveInterval=15 -o ServerAliveCountMax=10 -o TCPKeepAlive=yes" git push ssh://git@ssh.github.com:443/teihin/qing.git main`
- 待主人决定：`.git/objects/pack/tmp_pack_*` 有 6 个 **共 12GB** 的推送中断残留（9-09/9-10 留下，
  非本次），磁盘尚余 414GB，未清理。

## 客服站（ChatTool）玩家端品牌标识 8L → BY（未提交、未部署）

- 目录 `/Volumes/SSD/qing/ChatTool/web`。品牌字硬编码在 6 处 JSX + `index.html` 标题，无 CSS/资源承载：
  `src/PlayerApp.tsx`（错误页 brand-mark、页头 `player-brand` 在线客服旁 brand-mark-small）、
  `src/components.tsx`（`LoadingScreen`）、`src/main.tsx`（ErrorBoundary 兜底页）—— 以上为玩家端；
  `src/AgentApp.tsx` 4 处（登录展示区、移动端 mark、工作台顶栏、`EmptyState icon`）为客服端，
  同一 `brand-mark` 类，一并改成 BY 免留半截。
- `index.html`：`<title>BY 在线客服</title>`、description「BY 游戏在线客服中心」。
- 构建：`PATH=<managed node 22.22.2>/bin:$PATH npm run build`（= tsc -b + vite build）通过，
  产物 `dist/assets/index-ByFeOAMB.js`（旧 `index-CRicsnay.js` 已清）。
  校验：`dist` 内 `BY` 8 次、`8L` 0 次；源码文本 0 残留。
- **服务端不内嵌前端**：`server/internal/config/config.go` 的 `StaticDir` 默认 `../web/dist`，
  动态读盘，只重编 Go 二进制没用，**只把新 `web/dist` 同步上线即可**。
- 线上入口：`http://154.37.155.17/chattool/player`、`/chattool/agent`，Caddy 反代到 `127.0.0.1:8893`。
- 本地确认效果：`CHAT_PUBLIC_PATH=/chattool/ node node_modules/vite/bin/vite.js preview --host 127.0.0.1 --port 4173`
  → `http://127.0.0.1:4173/chattool/player`（无会话密文时停在错误页，仅能看到 BY 标识）。

### 已部署上线（20:22，未提交 git）

- **连接口径**（与游戏热更上传同一台机、同一账号）：`ssh -p 2233 -i ~/.ssh/id_ed25519_newserver client_update@154.37.155.17`，
  **`client_update` 同时是 `/www/html/.chattool` 的属主**，可直接改，无需 sudo。
- 线上静态目录 = `CHAT_STATIC_DIR` = `/www/html/.chattool/web`（**不是** `web/dist`）；服务进程
  `/www/html/.chattool/bin/chattool` 以 client_update 运行。**Go 动态读盘，传完静态文件不用重启服务**。
- 部署手法（可复用）：本地 `COPYFILE_DISABLE=1 tar czf` 打包 dist → scp 到 `.chattool/` →
  远端解到 `.web-new` → `cp -a web web.previous-brand-by-20260920` 备份 → `mv web .web-old`、`mv .web-new web`
  → `chmod 2755 web web/assets`（**别丢 setgid 2755**，原目录是 `drwxr-sr-x`）→ `chmod 644` 文件。
  本次备份落在 `/www/html/.chattool/web.previous-brand-by-20260920`（回滚就是把它换回 `web`）。
- 验证：线上 title「BY 在线客服」/ description 已 BY；`web/index.html`、两个 assets 的 **sha256 与本地 dist 逐个一致**；
  `/chattool/api/health` 与 `/chattool/player` 均 200。
- **坑（重要，别误判）**：请求已删除的旧资源 `…/assets/index-<旧hash>.js` 返回 **200 而不是 404** ——
  这是 SPA 兜底（回落 `index.html`，`Content-Type: text/html`、长度 608）。**判定资源是否更新要看
  Content-Type/Length 和 sha256，不能只看状态码**。（chattool 的 old js 已确认不存在，全盘 find 只剩备份里那份。）
- 源码与 dist 改动**仍是未提交状态**（`ChatTool/web/src/*.tsx`、`index.html`、`dist/`），按约定等主人明确要求再提交。
