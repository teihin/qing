# qing 项目入口

更新：2026-09-05。此文件只保留工作约束和读取导航；当前事实、进度与证据在下列文档。原完整记忆已归档，不能继续把历史条目追加到本入口。

## 开始任务

- 本次整理核实的检出为 `/Volumes/SSD/qing`，项目配置版本为 Cocos Creator **2.4.13**；实际运行实例按任务核对。每次先确认工作目录；Git 项目检查分支和未提交修改。旧 `/Volumes/CB/qing`、`/Volumes/CCCC/qing` 是历史路径，不能直接用于写操作。独立 worktree 以实际路径和分支为准。
- 先读 [当前状态](docs/memory/CURRENT.md)；按任务读 [决策](docs/memory/DECISIONS.md) 和 [专题索引](docs/memory/README.md)。长对话接续时若上下文缺失，重新读取相关项。无需每次通读归档。
- 用户最新明确要求决定目标；以当前代码和实际检查确认实现状态。发现文档冲突就说明差异、核实来源，不自行补造结论或重复执行历史命令。

## 共同约束

- 保留用户及其他任务的未提交修改；只修改本次范围，不全仓格式化或顺手重构。记忆维护不构成提交、推送、构建或发布授权；按当前任务实际授权执行。提交时标题和说明使用中文。
- Cocos 换肤必须直接写入正式 Prefab/Scene 和资源引用，使编辑器中可见；运行脚本负责业务/交互，不用它批量创建美术节点、重排换肤布局或覆盖 SpriteFrame/颜色。已有真实头像、动态数据显示不受此换肤限制误伤。
- 已采用 V7 的页面以对应模块最终确认稿为依据，参见 [V7 专题](docs/memory/topics/v7-ui.md)。不能用旧秦风/旧 8L 生成器覆盖这些页面，也不能据此自动重做尚未确认采用 V7 的牌桌或其他模块。
- 保留 `.meta`、UUID、业务节点名、事件和动态加载契约。可拉伸底板才用九宫格；盾牌、Logo、图标、美术字保持比例和独立高清资源。长屏页面按确认稿固定顶部/底部，中段伸缩或滚动；例外按具体页面记录。
- 服务器权威；金币、支付、提现、权限等不能只依赖客户端校验。协议、状态恢复、重连、切场景和异步回调改动需核对相应链路。
- `build/` 里有恢复的原生工程与产物，不当作普通缓存批量删除；`runtime-src.zip` 也不自动解压覆盖。不要因为归档记载过成功，就认定当前构建或线上环境已验证。
- 运行工具前核对写入副作用：`tools/validate_qin_drh8_skin.py` 会执行生成器并改写美术，不能当作只读检查。本文档检查仅用下方 `check_memory.py`。其他业务/美术工具按目标单独选择。
- 外部改 Prefab 后要确认 Creator 已导入；网页 Recompile 不等于 Prefab 已刷新。操作现有 Creator 按当前授权执行，避免重复实例或丢失未保存编辑。
- 静态/引用检查、编辑器可见、网页实际交互、Creator 构建、真机及线上发布分别记录；没做的明确标待验证。不记录凭据与私人敏感数据，不把单次关机等指令跨任务延续。

## 收尾与持续维护

- 重要决定或完成一个可验收阶段后，更新当前状态中对应模块及必要专题；决策变更注明范围、来源及替代关系。根入口目标 3–5 KiB，当前状态目标不超过 8 KiB，历史细节归档。
- 多任务各写 `docs/memory/handoffs/<任务名>.md`；共享状态更新前重新读取，避免覆盖他人。交接含目录/分支、目标、改动文件、验证结果、未完成项和下一步。
- 回复完成前核对记忆与实际结果一致，运行只读检查：`python3 docs/memory/check_memory.py check --root "/实际项目根目录"`。修复本次文档错误后说明更新位置和未验证项；结构通过不代表业务验收通过。
