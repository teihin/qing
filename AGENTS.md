# 项目记忆（Codex 自动读取）

最后更新：2026-08-15

本文件保存跨对话需要持续使用的项目事实与工作约束。涉及服务端行为、线上配置或原生包状态时，仍应在当前环境重新验证。

## 记忆维护规则

- 用户已要求持续维护本文件。以后每次处理本项目的需求、问题排查、代码修改或运行验证后，都应在任务结束前把新增的可复用信息（包括成功经验和踩坑教训）自动整理到本文件，无需再次询问。
- 优先记录：问题现象、根因、解决方案、涉及文件、验证结果、仍未解决的风险、关键技术决定和可复用操作经验。
- 已验证事实与推测必须分开；没有运行或联调验证的内容要明确标注为“待验证”。
- 新结论改变旧结论时，直接修正原条目并更新日期，避免不断追加互相冲突或重复的记录。
- 保持内容精炼、可检索；临时终端输出、一次性进度和无长期价值的细节不写入。
- 不记录密钥、密码、Token、验证码、签名信息、身份证、银行卡、手机号或其他敏感数据；只记录其所在模块、风险和处理状态。
- 每次实际更新本文件时，同步修改顶部“最后更新”日期，并在最终回复中简要说明新增了哪些记忆。
- 后续所有 Git 提交的标题和正文必须使用中文，并详细说明改动范围、关键行为、数据或权限影响以及验证结果；禁止只写英文标题或无法反映实际改动的简短描述。
- 本机 `git push` 若报 `Connection closed by 198.18.0.11 port 22`：这是本机代理把 github.com 解析为 Fake-IP（198.18.0.11），SSH 认证其实已成功、数据也已上传，只是代理在传输收尾清理连接时中断；`ssh -T git@github.com` 认证正常不能排除该问题，普通重试也可能继续报同样错误。改用 `GIT_SSH_COMMAND='ssh -o IPQoS=throughput -o ServerAliveInterval=15' git push origin main` 即可稳定推送，随后用 `git fetch origin main` 复核远端已包含该提交。此为本机开发环境经验，与仓库内容无关。

## 项目概况

- Cocos Creator 版本：`2.4.13`。
- 客户端类型：8 座四川扑克玩法“扯弦”，也叫“丁二黄”；使用标准扑克牌，不是牌九或骨牌游戏。
- 架构：服务端权威。客户端主要展示房间/玩家状态、发送操作指令和播放动画；胜负、牌型结算、下注合法性不能只依赖客户端。
- 设计分辨率：`960 x 640`，`fitHeight = true`。
- 正常启动场景是 `assets/Scenes/login.fire`；主要牌桌场景是 `assets/Scenes/drh8.fire`。
- `settings/project.json` 与 `settings/builder.json` 当前启动场景均为 `login`（UUID `5f94096e-b633-4077-9775-4f1a773f0fc0`），构建同时包含 `drh8`。修改或打包前仍需重新确认目标，不能仅凭历史记录推断。
- `assets/Scenes/login - 001.fire` 属于测试/备用场景。
- Android/iOS 原生工程目前封装在根目录 `runtime-src.zip`，不是普通活动源码目录。不要未经确认就解压覆盖或假定 Creator 构建会自动使用它。

### 2026-08-13 当前交接速览

- 活跃仓库是 `/Volumes/CCCC/qing`，主分支为 `main`，远端为 `git@github.com:teihin/qing.git`；2026-08-14网页版体验、支付战绩、WebHome及XuanManager相关全部工作区改动已提交并推送，功能基线为`9e4b293`。提交、构建或发布前仍需重新检查分支、工作区和远端，不能把该提交号当作永久最新版本。
- 当前客户端重点能力：大厅房间按“全/小/中/大”筛选并使用可下拉刷新的虚拟列表；排队撮合支持同房预坐和经 `roomTransition` 的异房快速切换；普通断网继续重连，服务端明确 `onKicked` 则停止重连并返回登录；赠送金币只允许大于0的整数。
- 牌桌下注聚光灯首次默认开启，可在牌局设置中关闭并保存本地选择；它只在发牌动画完成且当前下注玩家操作倒计时圈有效时显示，按头像世界坐标动态计算方向/长度并持续逆时针旋转。最新方向、末端和发牌门禁代码已完成构建验证，但真实连续下注、跳过、弃牌和收灯仍需在线牌局回归。
- 当前五套桌布为全屏绒布风格，牌背1/2为蓝银与墨绿金8L款，搓牌图及切牌图集已同步；运行资源、原图归档和生成母版均已入库。五桌面切换、两牌背选择、搓牌和DragonBones切牌动画仍需登录态逐项验证。
- 2026-08-15本地`assets/resources/project.manifest`与`version.manifest`已生成到`3.0.8`，`packageUrl`仍为`http://154.37.155.17/up`；该版本号只证明本地清单内容，不能自动视为公网已发布或旧客户端已升级。正式发布仍须按“最新Native构建→生成产物→上传暂存→服务器原子切换→远端版本/资源数/MD5抽检→旧客户端真机升级”逐层验收，并在发布后重新核对公网`/up`，不能沿用旧的3.0.6线上结论。
- 2026-08-14新增根目录`服务器完整安装与迁移手册.md`和`deploy/server/`基础设施模板，覆盖旧服root导出、新服务器用户/SSH/MySQL/KBEngine/Caddy/TLS/WSS/缓存/热更新解压、AudioServer、XuanManager、ChatTool、Web Mobile/WebHome、cron、防火墙、验收和回滚。现网只读基准确认为CentOS 7、Caddy 2.11.4、MySQL 5.7.44、SSH 2233、iptables开放80/443及TCP/UDP 20000～20099、MySQL绑定127.0.0.1、业务端口18080/8890受防火墙保护、8891/8893绑定loopback；`client_update`无权读取`/home/release/kbe`、root cron和MySQL授权，因此正式迁移前取得root导出包是不可跳过的硬门槛。现网基础Caddy曾对整个`/up/*`使用immutable，但热更新路径不带内容哈希；新模板已纠正为两个manifest no-store、其余`/up`资源no-cache，同时隐藏`.xuanmanager`、`.chattool`及Caddy辅助目录。模板已由现网同版本Caddy成功adapt并断言srv0=:443、srv1=:80、六个KB WSS路径和Audio/Xuan/Chat上游齐全；本轮未重载或修改现网Caddy。
- 当前最重要的验证边界：排队换房弱网/迟到回包、房间列表真实分页、聚光灯连续轮转、赠送整数软键盘、桌布/牌背动画、两台设备互踢与普通断网重连均未形成完整自动化或全量真机矩阵；静态检查和Creator构建通过不能替代这些联调。
- 2026-08-14 全服房间邀请客户端已实现。`assets/Scenes/drh8.fire`把120×114圆形“一键邀请”入口直接预制在`UserInfo/player0`本人头像上方，本人正式坐下、`round_count == 0`、`GameStatus == init`、未处于预坐且房间有空位时显示；入口视觉现直接使用`assets/动画/邀请/yaoqing`的Spine 3.8动画，图集已从竞品亮青风格调整为8L深蓝玻璃、银边和克制青色点光，旧代码呼吸光/旋转环与旧Label均停用，父节点Button继续负责点击、显隐和冷却。发送成功后入口在本玩家、本房间的180秒冷却期内完全隐藏，不显示倒计时；冷却结束且仍满足条件才重新出现。常驻`RoomInviteManager.ts`按账号和`roomType:roomID`保存绝对到期时间，所以玩家可在A/B/C房间分别邀请，离房、返回及客户端重启不会重置。点击时先落本地冷却，再调用`reqHallCommand({header: "通知_所有玩家_信息", system_content}, "room_invite")`；曾误用星号版时KB会返回769/`0x301 GC_FAILED`，服务端实际命令必须是英文下划线版本。
  当前发送协议升级为`ROOM_INVITE|V2|roomType|roomID|inviterID|编码后昵称|编码后底皮|最低带入|当前人数|最大人数|编码后文案`；客户端仍能解析V1和最初四段格式，但因旧格式缺少权威最低带入而不再展示，避免金币不足玩家收到无法进入的邀请。接收时使用账号`gold + gold2 / 100`与邀请携带的房间最低带入比较，余额不足直接忽略且不登记同房去重，充值达到要求后仍可接收该房间后续邀请；弹窗已显示后点击“前往”还会再次核验余额，防止期间消费导致无效换房。`GameDataManager`、`panelCloudNotify`和`panelGameView`先拦截邀请前缀，避免旧公告/奖池逻辑误解析；接收端按账号和房间持久化已接收记录，同房不同玩家只处理一次，不同房间分别排队，30秒过期且最多10条。`panelRoomInvite.prefab`直接预制8L风格房间号、邀请人、底皮、人数、忽略/前往和“本次登录不再弹出”，复选框默认不勾；该选项只在当前客户端会话生效，不写入`localStorage`，旧版本误保存的`suppressAll=true`会自动清除且不会丢失三分钟冷却及已接收房间记录。大厅可以接收，牌桌内仅没有座位的观战玩家可以接收，已坐下、已预坐或正在带入的玩家不会弹窗。PlayerList确认占座时会立即关闭已打开弹窗；点击“前往”前再次检查座位状态，首次预坐从请求发出时即拦截，旧玩家直接坐下到PlayerList回推之间另有10秒保护，确保快速点击也不会发送退房/换房请求。观战玩家在牌桌点“前往”时，先通过现有`roomTransition`流程安全离开当前房间，再以观战方式进入邀请房间；排队中会要求先取消排队。邀请到达后会输出一次性`[RoomInvite]`判定日志，便于区分余额不足、协议缺字段、会话屏蔽、同房去重、当前房间、坐下保护和实际弹窗入队；`UIManager.getInstance()`内两条高频场景调试日志已移除，避免邀请轮询放大控制台刷屏。Prefab/场景JSON引用、`git diff --check`和Cocos Creator 2.4.13隔离Web Mobile Debug＋MD5完整构建通过；最低带入条件为本次构建之后新增，未重新执行构建。大厅/牌桌观战、余额边界、已坐下快速点击、跨房失败回退仍需真实账号联调。
  已完成Prefab/场景JSON引用检查、`git diff --check`及Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建；正式`build/web-mobile`未覆盖、未上传。仍需两个以上真实账号联调服务端广播、同房多人去重、A/B/C独立冷却、重启恢复、GPS拦截和目标房间失效的服务端提示。
- 2026-08-14 牌桌右上“排队”入口改为`assets/动画/排队/pd`的Spine 3.8循环动画，竞品图集中的固定图标和文字已替换为无字8L深蓝银青底板，场景原Label仍在动画上层动态显示“排队/排队中”；旧父节点Sprite停用，但原Button、Widget、排队状态和切房协议均未改。该动画来自Unity：原30帧把亮弧内的大块低透明度深蓝矩形一起导出，Unity材质会弱化它，但Cocos Spine的additive混合会把矩形也叠加成“左右摆动”；`tools/style_queue_invite_spine.py`现按每帧attachment的atlas offset和JSON位置映射到统一218×86圆角外环遮罩，只保留沿外圈旋转的亮弧/柔光，保持atlas尺寸、区域坐标、30帧顺序和JSON时间轴不变。按钮底色本身也使用同轮廓的抗锯齿透明圆角遮罩，不再只是矩形底色上画圆角线；外侧深色阴影已删除，避免四角发方及出现黑边。脚本可从`HisImg/20260814-queue-invite-competitor-original/`原始备份幂等重建排队及邀请图集。两个Spine节点已直接序列化进`drh8.fire`，`panelGameView.ts`只负责启动/暂停动画并保留资源缺失时的旧视觉回退；场景引用、PNG尺寸/解码、`git diff --check`及Cocos Creator 2.4.13隔离Web Mobile Debug完整构建通过，正式构建未覆盖、未上传，仍需真实房间确认最终位置、缩放、点击热区和低端机帧率。
- `tools/validate_qin_drh8_skin.py`会主动重建并改写141张运行资源，不是只读检查；脏工作区禁止直接运行。优先使用JSON/Meta/尺寸/哈希检查和Creator构建，确需运行时先保存文件清单与哈希，并只撤销该脚本额外生成的内容。
- 2026-08-13 PWA发布预检：Creator使用`Web Mobile`、启动`login.fire`并包含`drh8.fire`和`roomTransition.fire`的场景选择正确，测试场景`login - 001.fire`应继续排除；正式竖屏网页建议把Web方向从`Auto`改为`Portrait`并开启`MD5 Cache`。当前`build/web-mobile`仍是普通Web构建，没有Web App Manifest和Service Worker。2026-08-14 KB主连接的浏览器分支已改为WSS，但多处支付、配置、客服等业务入口仍需分别核对HTTPS/WSS或同域TLS代理，PWA所需Manifest、图标、Service Worker和更新策略也尚未补齐；因此KB改为WSS不等于整个项目已完成HTTPS PWA改造。
- 2026-08-14 KB主连接WSS客户端已实现：`GameDef.ts`定义浏览器专用`wss://154-37-155-17.sslip.io/ws/`及20013、20015～20019白名单，`GameDataManager.initKBE()`仅在`cc.sys.isBrowser`时启用该代理；Android/iOS Native继续沿用`IS_USE_WSS=false`和`ws://154.37.155.17:端口`。`kbengine.js`在统一`getServerAddr()`中按端口生成路径，因此登录、注册、重置密码、LoginApp到BaseApp切换及BaseApp重连均覆盖，未知端口会拒绝且不会降级明文。Node实测网页六个地址精确匹配、20020被拦截、Native登录/Base仍为WS；Creator 2.4.13独立Web Mobile Debug＋MD5构建通过，浏览器启动到可见Canvas。公网TLS/Upgrade检查中20013、20015、20016返回101且证书校验正常；预留的20017、20018当前返回502，20019当前未完成握手，待对应BaseApp真正启用时再验证。正式`build/web-mobile`未覆盖、未上传，且尚未用真实账号完成LoginApp到BaseApp、进大厅/进房和断线重连联调。
- 2026-08-14 网页语音和玩家客服已统一使用同一个HTTPS域名：`GameDef.ts`以`WEB_PUBLIC_HOST=154-37-155-17.sslip.io`集中生成语音`https://.../audio`、语音流`wss://.../audio/v1/stream`和客服`https://.../chattool/player?d={info}`。`WebVoiceClient.resolveEndpoints()`在`cc.sys.isBrowser`时强制使用上述地址并忽略浏览器`localStorage.AudioServerBaseURL`中可能残留的旧明文IP；`panelKefu.ts`在浏览器中覆盖默认值、VIP地址和服务端哈希`客服2`，全部使用HTTPS玩家入口。Android/iOS Native继续使用原`http://154.37.155.17/audio`、`ws://154.37.155.17/audio/v1/stream`及动态客服配置。公网实测语音健康200、语音WSS 101、客服玩家页200且证书正常；Creator 2.4.13独立Web Mobile Debug＋MD5构建和浏览器启动通过。后续上传的正式网页已确认加载`settings.efd6e.js`和`assets/main/index.023f7.js`，包内包含当前Web录音、WSS上传和HTTP补传代码。2026-08-14约11:56的iPhone Web Clip测试连续5次在AudioServer形成`web_`请求，服务端成功生成约1.8～2.3秒的M4A；音量检测均有有效信号（峰值约-17.9～-0.6 dB），公网Range下载返回206，故该次现象已排除麦克风未录到、WSS/转码上传失败和文件下载接口失败。用户随后确认松手后能看到本人语音播放图标但马上消失且无声：播放图标只能由KB房间语音回推触发，证明`reqSay`和房间广播也已成功；立即消失则精确对应`HTMLAudioElement.play()` Promise拒绝后`WebVoiceClient.play()`触发`onPlaybackDone`，再由`DrhLogicMgr.OnPlayNextAudio()`立即关闭全部`TalkPad`。根因已收敛为iOS Web Clip自动播放许可，而非录制、上传、下载或游戏广播。当前`unlockPlayback()`解锁的是一次性静音`Audio`，实际语音使用另一个新建的`Audio(blobURL)`；安全修复只改浏览器播放端：复用同一个持久`HTMLAudioElement`完成首次用户手势静音解锁和后续语音播放，`NotAllowedError`时不要立即结束队列和隐藏图标，而是保留当前语音并显示一次“点击开启语音”兜底，用户点击同一音频对象重试，成功后恢复自动播放。网络/解码等真实失败仍按原逻辑跳过并继续队列；录音、上传、KB协议、Android/iOS Native路径均不改。上线前必须用iPhone Web Clip验证冷启动首次收听、本人发送回听、连续队列、切房和后台恢复。
- 2026-08-14 苹果网页语音播放修复已实施：`WebVoiceClient`仅在`cc.sys.isBrowser`且识别为iPhone/iPad/iPod时启用持久`HTMLAudioElement`，并兼容iPadOS桌面UA（`MacIntel`且有触点）；描述文件Web Clip与Safari均属于该分支。首次触摸会用同一对象播放有效的16kHz单声道短静音完成解锁，后续语音只替换该对象的Blob URL，不再新建Audio；若仍收到`NotAllowedError`，当前语音最长保留30秒且不立即触发`onPlaybackDone`/隐藏头像图标，`MobileManager`使用现有`panelMsgView`提示“点击确定”，确定按钮在同一用户手势中重试播放，取消或真实网络/解码失败才继续原队列。Android网页、电脑网页以及Android/iOS Native继续走原逻辑，录音、WSS/HTTP上传、下载缓存、`reqSay`和房间协议均未修改。静音WAV结构回读为RIFF/WAVE、16kHz、单声道、16位、320字节PCM；分支静态检查、`git diff --check`以及Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建通过，产物已包含苹果识别、同一Audio复用和拒绝兜底代码。正式`build/web-mobile`未覆盖、未上传；仍需描述文件安装的iPhone Web Clip验证首次收听、本人发送回听、连续队列、切房和后台恢复。
- 2026-08-14 “我的战绩”列表已增加每个房间该玩家自己的“带入”金币列：服务端`PlayerAllScore.all_remark`对应`usr_total_score.remark`拆分数组，索引1已由XuanManager同表解析交叉确认是该玩家本房累计带入（不是全房总带入）；例如`["0","200",...]`显示200。`panelRecordList.ts`同时兼容数组和旧逗号字符串，字段缺失、空值、非数字或负数统一显示`--`，避免异常消息导致界面崩溃；`panelRecordList.prefab`与`战绩对象.prefab`在不改变行高和页面纵向结构的前提下重排为“房间ID、带入、底皮、时间、输赢”五列，标题复用现有蓝白“带入”美术。Prefab JSON/引用、`git diff --check`及Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建通过；正式构建未覆盖、未上传，仍需登录后用真实战绩页确认长位数金币的最终观感。
- 2026-08-14 Web首次切Prefab/场景卡顿保护已实现：新增`WebLoadingManager.ts`，只在`!cc.sys.isNative && cc.sys.isBrowser`时创建位于GameCanvas之外、跨场景存活的8L网页进度层；150ms内完成的缓存加载不显示，Cocos提供`completedCount/totalCount`时显示真实资源项百分比，下载完成后的JSON解析、Prefab实例化和首帧阶段使用不定进度而不伪造90%～99%，并发任务按任务ID合并，失败保留旧界面并提供重试。`UIManager.showPanel()`在Web分支等新Prefab成功实例化后才关闭`CloseOther`旧界面，Native仍按原顺序先关闭旧窗口并使用原二参数`loadRes`、原实例化异常路径。新增`WebSceneLoader.ts`接管现有全部场景入口：Web先`preloadScene`显示真实进度，再`loadScene`，牌桌`panelGameView`等首屏窗口的任务与场景任务重叠时加载层不会中断；排队换房继续使用`switchGeneration/switchPhase`取消过期预载，Native仍直接调用原`cc.director.loadScene`且无DOM、预载或进度逻辑。排行榜、钱包、聊天记录、战绩/牌局回顾/牌谱、银行/活动记录以及通用列表Prefab已通过`loadBlockingRes`接入；头像、音效、牌桌金币、丢牌、道具和对象池等实时/后台资源保持静默，避免游戏中频繁全屏遮挡。Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建成功；本机禁用缓存并模拟慢网时约7.5秒捕捉到加载层正常显示真实86%，390×844视口下卡片宽约335px、完整居中无溢出，恢复网络后页面可继续进入登录界面，控制台未见本次功能异常。正式`build/web-mobile`未覆盖、未上传，真实账号的大厅→牌桌、退出牌桌、排队快速换房以及资源失败重试仍需发布前联调。
- 若暂不做PWA，可把普通`Web Mobile`构建部署后，用允许手动安装的`com.apple.webClip.managed` `.mobileconfig`在iPhone桌面创建全屏Web Clip；它不需要Manifest、Service Worker、MDM或Apple开发者账号，网页更新也无需重装描述文件。用户必须在Safari下载后8分钟内进入“设置→已下载描述文件”手动确认安装。Web Clip只是无地址栏的网页入口，不是IPA或离线包；HTTP页面可继续使用现有HTTP/WS游戏链路，但仍不能绕过公网网页麦克风等安全上下文限制，也不能保证浏览器本地设备ID在清理网站数据或删除入口后保持不变。
- 2026-08-13 根目录`WebHome/`已新增以手机访问为主的8L静态下载站：复用现有登录背景、Logo和App图标，提供Android/iPhone/iPad/桌面响应式页面，自动识别平台、Safari以及微信/QQ/微博/抖音/支付宝内置浏览器，Android走`downloads/8L.apk`，苹果走带安装步骤的`downloads/8L.mobileconfig`。`site-config.json`集中维护下载地址，苹果Web Clip现指向用户确认的`https://154-37-155-17.sslip.io/web-mobile/`；`scripts/generate_mobileconfig.py`会内嵌256×256图标并生成`FullScreen=true`、允许移除、稳定标识的`com.apple.webClip.managed`配置，`plutil -lint`和Payload回读通过。`Caddyfile.example`给出`/download/`部署及APK/mobileconfig MIME配置。320×568窄屏Android、375×667 iPhone安装弹层、390×844常规手机、430×932大屏Android和1440×900桌面实际浏览器渲染均无控制台错误或横向溢出；主要下载按钮高度70像素，苹果描述文件按钮52像素，短屏弹层支持内部滚动并保留安全区。当前仓库没有正式APK，因此发布前仍须把已签名真机验证包放到`WebHome/downloads/8L.apk`；本机网络受代理Fake-IP影响，目标域名HTTP可见跳转HTTPS但TLS握手未能完成，正式发布前必须从真实手机网络验证域名证书、游戏首页和描述文件安装。
- 2026-08-14 WebHome对外苹果文案已统一为“苹果签名版”，入口卡片、设备识别、主按钮、安装弹层、浏览器引导、三步说明、Toast、Meta描述以及mobileconfig的Payload名称/描述均不再使用“桌面版、桌面安装、桌面模式、返回桌面、主屏幕添加”等营销表述；系统设置的真实菜单名“已下载描述文件”继续保留，避免玩家无法找到安装入口。手机端下载布局改为单平台：Android只显示安卓主下载按钮，iPhone/iPad只显示苹果签名版主按钮，“其他平台”和双按钮区整块隐藏；电脑浏览器仍并排提供Android与苹果签名版选择。`site-config.json`和生成脚本已同步，`8L.mobileconfig`重新生成并通过`plutil -lint`，Payload仍保持`FullScreen=true`、`IgnoreManifestScope=true`及原网页地址。浏览器在390×844苹果/安卓和1440×900电脑三种模式实测无横向溢出、手机端无交叉平台文字、电脑端双入口正常，控制台无错误；已于2026-08-14正式发布并完成公网文件哈希回读。
- 2026-08-13 新增可双击的`4上传推广网站.command`及`tools/upload_promotion_site.py`，部署目录按用户最终口径固定为`/www/html/webhome`，公网配置为`https://154-37-155-17.sslip.io/webhome/`，与游戏`/www/html/web-mobile`隔离。脚本复用现有SSH配置，上传前重新生成并校验mobileconfig内部`https://154-37-155-17.sslip.io/web-mobile/`、内嵌图标和全屏/可移除字段；正式上传从Android正式构建目录读取唯一有效APK，要求体积不小于1MB、ZIP完整且含`AndroidManifest.xml`，不会误传缺包页面。公网页面、素材、site-config、APK和mobileconfig打ZIP并校验，文档、生成脚本和Caddy示例不进入公网包；使用rsync断点重试，服务器校验SHA-256/文件数/权限后原子切换`webhome`，失败恢复旧目录，成功保留`/www/html/.webhome.previous`。dry-run与正式发布均已验证；正式发布时会比较公网APK SHA-256，相同则只上传约1.6MB的网站文件并沿用原APK。
- 2026-08-14 WebHome苹果描述文件线上复核与MIME修复：本地`WebHome/site-config.json`、本地`downloads/8L.mobileconfig`、公网`/webhome/site-config.json`和公网实际下载的`/webhome/downloads/8L.mobileconfig`均把Web Clip指向`https://154-37-155-17.sslip.io/web-mobile/`；当前本地/公网描述文件SHA-256同为`4daf46cc31511e2786e5a7d34163c4fad6d965837d42e31b6a36fd9fac3066a1`，`plutil`格式、`FullScreen=true`、`IgnoreManifestScope=true`、`IsRemovable=true`和内嵌8L图标均正常。iPhone曾把描述文件作为文本打开，根因是公网响应缺少苹果MIME；现有`tools/webhome_caddy_headers/`及正式Caddy的`qing_webhome_mobileconfig_headers`路由大小写不敏感地匹配sslip正式域名下`/webhome/`任意层级、任意名称的`.mobileconfig`，只对2xx响应设置`Content-Type: application/x-apple-aspen-config`、不写死文件名的`Content-Disposition: attachment`以及`Cache-Control: no-store`。服务器配置保存在`/www/html/.webhome-caddy-headers/`，每分钟幂等任务`qing-webhome-mobileconfig-headers`在Caddy重启丢失管理API路由时自动恢复，不覆盖其他路由；`WebHome/Caddyfile.example`也已同步。不存在的mobileconfig仍为无特殊头404，WebHome JSON、`/web-mobile/`和ChatTool玩家页响应未受影响。已安装的旧描述文件不会被网页自动替换；只有Payload本身发生变化时才需要用户删除旧描述文件/旧图标后重新下载安装，普通下载站网页更新不需要重装。
- 2026-08-14 WebHome手机端发布后仍显示旧页面的根因已确认是缓存而非上传失败：修复前公网`index.html`、`app.js`、`styles.css`、`site-config.json`和mobileconfig的SHA-256均与本地一致，但HTML/JS/CSS响应没有`Cache-Control`且静态文件名不变，iOS会继续复用旧入口、脚本或样式。`tools/upload_promotion_site.py`现会在每次上传前按`styles.css`和`app.js`内容SHA-256自动刷新`index.html`中的12位`?v=`版本号；本次线上入口为`styles.css?v=1be49963613b`、`app.js?v=3cb59f2338f9`。正式Caddy新增`qing_webhome_entry_cache_headers`，仅对sslip域名下`/webhome`入口及HTML/JS/CSS/JSON的2xx响应设置`Cache-Control: no-store, max-age=0`、`Pragma: no-cache`、`Expires: 0`，图片和APK不受影响，404也不附加缓存头；苹果mobileconfig同样为`no-store`。路由配置已纳入原每分钟自动恢复脚本并部署到`/www/html/.webhome-caddy-headers/`。重新发布后公网`index.html`与本地SHA-256同为`c93bd0f11f65640cf9e5b76ba71207e4f7cccb1fcd053d846f8a66d67070d5a8`，下载站、`/web-mobile/`、ChatTool玩家页和XuanManager入口均返回200；旧手机缓存不会被服务器反向删除，首次可用带查询参数的下载站地址强制绕过一次，之后普通地址会按no-store获取新版本。
- 2026-08-13 HTTPS网页版首次进入出现“Cocos标题进度完成后长时间黑屏，稍后才显示登录页”，已确认不是废弃的`game.config`请求导致：`panelUpdate.ts`原本只拼接并打印该旧URL，实际XHR读取代码早已整段注释。真实原因是Cocos自带首段进度只覆盖启动场景；启动后`UIManager.showPanel("panelLogin")`才通过`cc.loader.loadRes`异步下载登录Prefab及依赖，启动页同时被关闭，下载期间便只剩黑画布。当前Web构建总计约65MB/1719文件，登录阶段包含数十个资源请求及10.8MB的`PingFF.ttf`，线上静态文件响应未设置`Cache-Control`，首次网络慢时更明显。`assets/scripts/UI/panelUpdate.ts`现仅对非Native增加第二段“正在加载登录资源”进度：保留更新页和现有大小进度条，使用`loadRes`进度回调显示0～100%，资源完成后再打开登录面板；失败时显示现有网络异常重试入口。原生AssetsManager/热更新路径未改，同时删除了旧`game.config`的误导打印。Creator 2.4.13已在独立临时目录完成Web Mobile Debug＋MD5构建，浏览器实际运行可进入登录页，构建脚本包含第二段进度；尚未覆盖正式`build/web-mobile`或上传线上，慢网下进度观感仍待正式网络验证。后续性能优化可另行压缩/拆分登录字体资源并为带MD5文件配置长期缓存，但不应把这两项与本次防黑屏显示混为一项上线。
- 2026-08-14 网页第二段进度原本已计算百分比但实际不可见，根因是`panelUpdate.prefab`的字段命名与节点位置不一致：脚本属性`byteLabel`绑定在已隐藏的“文件进度/文件计数”，而可见“大小进度”上方的“大小计数”实际绑定为`fileLabel`。`LoadWebLogin()`现只在Web分支把0～100%写入`fileLabel`，不修改Prefab和Native热更新路径。Creator 2.4.13独立Web Mobile Debug＋MD5构建通过，本机模拟慢网实测百分比居中显示在进度条上方，完成后正常进入登录页；正式`build/web-mobile`未覆盖且尚未上传。
- 2026-08-14 Web Mobile首段启动图已通过项目模板`build-templates/web-mobile/splash.png`从Cocos Creator默认标识替换为现有银蓝8L游戏Logo，不修改Native启动图、登录场景、进度条样式或第二段登录资源进度。为降低首次加载流量，模板图现由`WebHome/assets/8l-logo.png`的540×540全质量母版缩放并量化为256×256、64色索引PNG，体积由258,880字节降至7,332字节（减少97.2%）；全质量母版保持不变，可供以后重新生成。PNG解码、透明通道、居中构图和深色背景预览已验证。Cocos下次Web Mobile构建会为新内容生成新的MD5文件名；已有`build/web-mobile/splash.a956c.png`仍是旧产物，必须重新构建才会生效，当前正式构建未覆盖且尚未上传。
- 2026-08-14 iPhone/iPad网页顶部黑条根因是`UIManager.setPanelInfo()`过去只按`cc.sys.os == OS_IOS`把每个主Prefab的Widget顶部下移50游戏单位，Safari/Web Clip也会被识别为iOS，导致浏览器已经处理可视区域后又重复预留刘海空间。该偏移现仅在`cc.sys.isNative && OS_IOS`时启用；iOS网页、Android网页不再额外下移，原生iOS刘海适配保持不变。源码静态检查通过，仍需重新构建Web Mobile并在Safari普通标签页、桌面Web Clip各验证一次顶部贴合与底部操作区。
- 2026-08-13 用户用上传脚本发布上述新版后，原浏览器标签页报`settings.1714a.js` 404、严格MIME拒绝执行，随后因`_CCSettings`不存在报读取`debug`失败；这不是上次解压权限问题，上次特征为403。当前本地和线上新`index.html`实际引用`settings.7e0e8.js`与`main.ef113.js`；同一Edge标签页原页面仍使用旧`settings.1714a.js`与`main.fba5f.js`，地址加随机查询参数强制重新取入口后立即切换到新哈希文件，确认根因是旧`index.html`缓存与原子发布删除旧哈希文件的组合。Creator模板生成的`index.html`还保留2024年文件时间，不能依赖默认启发式缓存保证版本切换。正式Caddy已增加两条仅匹配HTTPS sslip域名和`/web-mobile`的缓存路由：`/web-mobile`、`/web-mobile/`、`/web-mobile/index.html`返回`Cache-Control: no-store, max-age=0`并带`Pragma: no-cache`、`Expires: 0`；带MD5的顶层文件以及`/web-mobile/src/*`、`/web-mobile/assets/*`只在2xx响应时返回`Cache-Control: public, max-age=31536000, immutable`，故不存在的哈希资源仍为无缓存头404。无参数正式地址已实测直接加载新`settings.7e0e8.js`与`main.ef113.js`；入口、顶层JS、嵌套JS和10.8MB字体响应头均符合预期，`/webhome/`未误加规则，XuanManager健康与ChatTool玩家页仍为200。由于当前Caddy由只读`/etc/caddy/Caddyfile`启动，管理API规则本身重启会丢失，已把路由与幂等安装脚本保存到仓库`tools/web_mobile_caddy_cache/`和服务器`/www/html/.web-mobile-caddy-cache/`，并新增每分钟crontab `qing-web-mobile-caddy-cache`自动恢复缺失路由；脚本只在路由不存在时插入，不覆盖其他站点。后续发布最好再为已打开的旧页面保留上一版哈希资源短暂兼容，但当前入口不缓存已避免新开/刷新页面继续拿旧HTML。2026-08-14线上再次实测：入口仍为`no-store`，当前`style-mobile.45d43.css`、`src/settings.cd421.js`和7,332字节的`splash.47442.png`均为一年`immutable`缓存，且`service-worker.js`为404，确认当前是浏览器HTTP缓存而非Service Worker/PWA离线缓存。

- 2026-08-14 Web Clip全屏保持修复：用户反馈通过描述文件安装的网页版会显示浏览器界面。当前线上`/webhome/downloads/8L.mobileconfig`与本地旧版均已验证`FullScreen=true`，线上`/web-mobile/`入口也已有`apple-mobile-web-app-capable=yes`且直接返回200，因此不是签名丢失全屏字段或网页缺少苹果声明；风险点是Payload原为`IgnoreManifestScope=false`，Apple官方定义该值会在导航离开Web Clip起始URL范围时重新显示Safari UI。`WebHome/scripts/generate_mobileconfig.py`现改为同时生成`FullScreen=true`和`IgnoreManifestScope=true`，使游戏进入同域客服、语音等其他路径时仍保持无地址栏模式；`tools/upload_promotion_site.py`新增该字段的强制上传前校验，本地`8L.mobileconfig`已重新生成，`plutil -lint`、Payload回读和上传校验通过，新SHA-256为`f176aea6f6a1182c38faf172e196c8670c4a1012c344a7f027d7556525bbb4f5`。正式线上仍是旧SHA-256 `475e572a66c292231d3ae3734c2ec8dde87b0aabd4079da5464232213d21dc36`且`IgnoreManifestScope=false`，本轮未上传；发布后iPhone必须先删除旧描述文件/旧桌面图标，再重新下载并安装验证。此修复隐藏Safari地址栏和工具栏，但iOS系统状态栏、刘海安全区及Home Indicator不保证可由Web Clip永久隐藏。

- 2026-08-14 苹果网页版麦克风常驻提示修复：`panelGameView`进房会调用`MobileManager.PrepareVoice()`，此前网页分支继续执行`WebVoiceClient.prepare()`→`WebVoiceRecorder.prepare()`→`getUserMedia()`，同时`WebVoiceRecorder.stop()`只关闭PCM回调、不停止`MediaStreamTrack`，导致iPhone即使未按语音键也持续显示右上角麦克风占用提示。`WebVoiceClient`现仅在已识别的iPhone/iPad/iPod及iPadOS桌面UA网页分支改变采集生命周期：进房`prepare()`只校验浏览器能力、HTTPS和语音地址，不申请麦克风；真正按住语音键时才创建采集流；松手约50ms尾音窗口后、取消、启动失败、切房或销毁时均执行录音停止并`shutdown()`采集链，从而停止所有音频轨道。已录PCM保存在上传Session，释放采集链不影响WSS/HTTP补传和播放；Android网页、电脑网页、Android/iOS Native以及KB语音协议保持原逻辑。`git diff --check`和Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建通过，正式构建未覆盖、未上传；仍需iPhone Web Clip验证进房无橙色麦克风提示、按住时出现、松手后及时消失，并连续发送多条语音确认每次重新采集稳定。
- 2026-08-14 苹果网页版首次录音授权弹窗保护：首次按住语音时，iOS权限弹窗会打断原触摸并先触发停止，授权成功后迟到的`getUserMedia`返回会因采集链已释放而报“麦克风预热已取消”。`WebVoiceClient.start()`现仅在本次Session已经收到停止且错误明确为“录音已取消/麦克风预热已取消”时静默结束，不再弹二次错误；用户拒绝权限、轨道不可用、网络或其他真实错误仍保留原提示。第一次授权按压不上传语音，授权后需重新按住；原生Android/iOS路径和非苹果网页行为不变。`git diff --check`及Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建通过，产物已确认包含双取消文案保护；正式`build/web-mobile`未覆盖、未上传，iPhone Web Clip真机复测待完成。

- 2026-08-14 iPhone Web Clip“全屏”边界已核实：用户删除旧图标并重新安装后，顶部时间、Wi-Fi和电量仍可见；这不是`FullScreen`或`IgnoreManifestScope`未生效，而是iOS系统状态栏。Apple Safari Web Content Guide明确说明Home Screen standalone模式隐藏浏览器URL栏和底部按钮栏后“Only a status bar appears at the top”；`apple-mobile-web-app-status-bar-style=black-translucent`只能改变状态栏外观/让内容延伸到其下方，不能隐藏时间、网络和电量。Apple的Web Clip文档所称全屏同样只保证无URL、搜索栏和书签；截至2026-06 WebKit的iPhone普通元素Fullscreen API请求仍未实现，Apple工程师也确认现有API没有受支持办法做到网页真正隐藏系统栏。因此不要再用mobileconfig、Manifest`display:fullscreen`、Meta或自动`requestFullscreen()`承诺隐藏iPhone状态栏；若业务必须连时间/Wi-Fi/电量一起隐藏，只能交付原生iOS壳/IPA并由`UIViewController`控制状态栏，属于不同发布和签名方案。现有`IgnoreManifestScope=true`仍用于跨路径时避免重新出现Safari浏览器UI，与系统状态栏无关。

- 2026-08-14 苹果安装版网页屏幕常亮保护：新增`assets/scripts/common/WebWakeLockManager.ts`，只在iPhone/iPad/iPod（含iPadOS桌面UA）且以主屏幕安装/standalone模式运行时申请Screen Wake Lock；普通Safari标签页、Android/电脑网页和Native客户端保持不变。初始化时先申请，若系统要求用户激活则由首次`touchend/click`补申请；页面进入后台时主动释放，回到前台时自动重新申请，组件销毁时移除监听并释放。该能力用于游戏可见期间阻止系统因无操作自动熄屏，但用户手动锁屏、系统省电/电量策略或不支持Wake Lock的旧iOS仍可中断，不能承诺任何系统状态下永久常亮。WebKit官方虽从iOS/iPadOS 16.4为主屏幕Web App加入该API，但相关兼容问题到18.4才标记修复，正式口径按iOS/iPadOS 18.4及以上验证；Creator 2.4.13独立Web Mobile Debug＋MD5构建通过，产物已确认包含常亮管理器，正式`build/web-mobile`未覆盖且尚未上传，仍需真机把自动锁定设为较短时间后验证前台不熄屏、切后台再返回可恢复。

- 2026-08-14 苹果网页版真实GPS提交修复：`assets/scripts/logic/GpsManager.ts`此前对所有浏览器固定返回非法测试值`131.61969,204.0761`（纬度和经度均超出合法范围），虽每分钟调用`reqSetProperty("gps", gps)`，但并非真实位置，服务端无法可靠使用。现仅iPhone/iPad/iPod及iPadOS桌面UA网页调用`navigator.geolocation.getCurrentPosition()`，要求HTTPS和用户定位授权，验证纬度`[-90,90]`、经度`[-180,180]`后按现有协议“纬度,经度”保留6位小数，并在首次成功、每60秒及页面回到前台时通过`reqSetProperty`提交；定位失败不再提交伪造坐标，拒绝权限时清空旧缓存。Android网页和电脑网页不申请定位、不提交GPS且保持不因GPS阻止进房；Android/iOS Native原生定位链路未改。首版已于13:35左右由用户上传，线上入口为`no-store`且当前`assets/main/index.6bcc9.js`已实测包含苹果UA判断、Geolocation和提交代码，不是漏传或旧入口缓存；玩家496702首次无授权弹窗时最近登录13:34，早于线上主资源修改时间13:35:36，直接原因是手机仍运行发布前的旧网页进程。该玩家彻底重开后最近登录更新到13:41，iPhone出现授权弹窗并同意，后台重新读取已得到合法WGS84坐标，确认“苹果识别→浏览器定位→`reqSetProperty`→KB写库”全链路成功。随后首次点击坐下仍提示未开GPS，是长时间关闭定位后的首次冷启动尚未返回坐标，不是系统开关或服务端失败；按用户最终口径，坐下检查不等待定位、不自动续做，检查当刻无合法GPS就保留原提示并结束本次操作，定位随后恢复后由玩家重新点击坐下。源码仅保留苹果网页首次有效`touchend/click`补申请和页面会话级合法坐标缓存，非网页版及原坐下流程不变；这部分尚未覆盖正式`build/web-mobile`或再次上传。
- 2026-08-14 玩家580629在苹果网页版无重复授权弹窗但进房提示GPS未开，同时用户确认该iPhone的苹果地图也无法定位。该现象表明当时系统定位链没有产出坐标，不是游戏漏弹授权：网站权限已经记为允许时本来就不会重复弹框，`getCurrentPosition`仍可能因系统定位服务、冷启动、室内信号、网络辅助定位或VPN环境返回不可用/超时。应先关闭VPN并确认苹果地图恢复蓝点，再彻底重开Web Clip；地图仍不能定位时检查“定位服务→Safari网站→使用App期间/精确位置”，必要时重启手机。游戏继续坚持无合法实时坐标就不放行，不使用历史缓存伪造定位；本次未修改代码。

## XuanManager 新后台管理系统

- 新后台的全部代码统一放在根目录 `XuanManager/`；服务端提供的数据接入规范为 `XuanManager/新后台管理系统接口文档.md`。当前技术栈为 Go 标准库后端加 React/Vite 前端：Go 便于在无 Node/Go 环境的 CentOS 7 游戏服务器上交付静态 Linux amd64 二进制，React 构建产物由同一个后端同源提供。
- 用户要求后续每次完成 XuanManager 功能更新时都同步更新正式版本，不能只停留在本地；除非用户明确说明“只改本地”。每次正式更新仍必须先完成本地检查和候选端口验证，核对上传哈希一致，保留可回滚副本，切换后验证正式健康、接口权限和静态资源，并停止候选进程。
- 新后台后端应部署在游戏服务器本机；XuanManager 自有 `mgr_` 表统一存放在本机 MySQL 的 `webcm` 数据库。游戏业务数据默认从 `kbedm` 只读查询；只有注册写入 `third_marketing_info`、长期公告写入 `usr_hash_info`、发牌优化事务更新三个白名单字段等用户明确批准的流程可以使用独立的服务器本机写连接。内部管理接口固定为 `http://127.0.0.1:8890`。浏览器前端不得直连数据库或透传旧管理接口，数据库和内部 HTTP 均不得为开发方便开放到公网。
- `webcm` 专用账号具备自有库的增删改查和常规 DDL，但没有 MySQL `REFERENCES` 权限；因此 `mgr_` 迁移不创建外键约束，关联合法性由服务端接口校验。真实账号密码仅保存在服务器权限受控的运行配置中，不写入代码或项目记忆。
- 2026-08-08 已通过现有 SSH 通道在服务器本机完成只读连通性验证：`GET /index` 返回 `index ok`；使用用户另行提供且未写入仓库的数据库账号成功连接 `kbedm`，`SELECT DATABASE()`、当前数据库身份查询、`tbl_Account` 存在性及单行只读查询均通过。后续配置必须使用服务器本机的环境变量或权限受控配置文件，不得把账号密码提交到源码、日志、审计记录或本文件。
- 2026-08-09 已完成一期后台自有权限系统：登录和改密、后台用户创建/编辑/启停/分配角色/重置密码、角色创建/编辑/权限矩阵、模块与操作权限管理、工作台和审计查询。后台用户与游戏玩家严格分离；菜单可见性只负责界面过滤，每个后端接口仍独立校验权限。超级管理员通过 `is_super` 服务端旁路动态拥有全部有效权限，系统角色不能被编辑或降权，超级管理员不能被停用或从用户管理重置密码，可在登录后自行安全改密。
- 2026-08-10 超级管理员隐身规则已在后端统一实施：非超级管理员的后台用户列表、分页总数、角色人数、工作台用户/审计统计、最近操作、完整审计查询及通知历史均排除超级管理员账号和其操作；公告、奖池、支付、活动、封号、客服加减分、玩家优化等业务页继续展示当前业务状态，但隐藏超级管理员的操作人、操作时间、原因和工单等审计归属，且不会回退到更早的普通管理员记录造成误归因。非超级管理员即使猜中超级管理员用户ID并调用编辑、停用或重置密码接口，也统一返回用户不存在；超级管理员本人仍能查看完整信息。审计可见性同时按 `operator_id` 和 `mgr_user` 目标ID/用户名过滤，覆盖超级管理员自己的登录成功/失败记录。
- 2026-08-11 XuanManager 后台密码规则的最终口径是“至少6位”：用户所说“大于6位”的实际期望包含6位密码，`123123` 必须能通过。规则按Unicode字符数校验，不再强制同时包含数字、字母或特殊字符；创建后台用户、管理员重置密码、用户自行改密及首次超级管理员初始化均复用同一校验。bcrypt 的72字节硬上限继续保留，5位密码和超过72字节的密码仍拒绝。创建用户、重置密码和改密弹窗提示均统一为“至少6位，不限制字符组合”。
- 2026-08-09 已新增“游戏管理 → 玩家管理”，原后台用户、角色权限、功能模块、操作审计统一归入“系统设置”。玩家接口仅用 `game.player.view` 权限读取 `kbedm.tbl_Account` 和 `kbe_accountinfos`，并自关联直属代理昵称；支持玩家ID、昵称、登录账号、代理ID、代理名字、等级、角色标记、房间ID、客户端状态/版本、余额区间和注册日期组合查询，默认分页且单页上限100。返回安全的账号、资产、代理、房间、登录和优化参数，不返回密码、GPS或IP。
- 2026-08-09 玩家详情已扩展为“基本资料、资金情况、房间战绩”三个页签。资金页复用 `GET /api/game/transactions` 和 `game.transaction.view`，显示当前金币、历史流入/流出、净变化、游戏净变化、道具支出及可分页的全部流水，并可带玩家ID跳转交易记录模块。房间页使用 `GET /api/game/players/{playerId}/rooms` 和 `game.room_record.view`，显示参与房间数、胜负、累计局数、带入、结算返还、总输赢及逐房记录，并可直接进入指定房间的完整牌局战绩；没有对应权限时页签禁用，不能借玩家查看权限绕过交易或房间权限。
- 2026-08-11 玩家管理已新增游戏玩家登录密码重置和超级管理员敏感信息查看。密码接口为 `POST /api/game/players/{playerId}/password`，独立权限 `game.player.reset_password` 由第18个迁移加入角色矩阵；密码要求6～32个字符、首尾无空格、显式确认，备注选填。后端调用本机8890 `异步_设置_玩家_属性` 修改 `userPWD`，接受512/1280后仍以 `sm_userPWD = MD5(?)` 循环回读，随后同步并回读 `third_marketing_info.player_wxpwd`；明文和摘要不进入响应、日志或审计。敏感信息接口为 `GET /api/game/players/{playerId}/sensitive`，基础要求玩家查看权限且后端固定校验 `is_super`，不进入普通角色权限矩阵；玩家列表不批量返回敏感字段，只有超级管理员打开详情才按需读取 `sm_ip/sm_gps`，响应 `Cache-Control: no-store`，每次成功查看写无具体值的审计。GPS按客户端“纬度,经度”解析，合法WGS84坐标在页面转换为高德URI要求的“经度,纬度”并打开地图；普通管理员直接请求返回403。正式库只读汇总确认21名玩家有IP、19条非零GPS且19条均符合两段数字格式，现有14个非空玩家密码摘要长度均为32；未读取或输出具体敏感值。
- 2026-08-11 玩家密码与定位版本已发布到正式纯HTTP地址。Go测试/vet、前端lint/生产构建、Linux amd64静态构建和 `git diff --check` 通过；候选8892健康、新迁移和权限记录、新路由未登录401、新JS/CSS及日志均验证正常。正式8891和Caddy健康200，两个新路由均由公网正确返回登录保护401，正式二进制SHA-256为 `d0311f8e3c0386ecb326e691543561b129fb8b690c3ed18be55360cb3dfd6871`，资源为 `index-D_y-1nHf.js`、`index-BpynXA6T.css`；候选8892已停止。上线验收没有重置任何真实玩家密码，也没有输出具体玩家IP或GPS。回滚文件为 `bin/xuanmanager.previous-player-security-20260811` 和 `web.previous-player-security-20260811`。
- 2026-08-09 已新增“游戏管理 → 代理管理”，只读权限为 `game.agent.view`，接口为 `GET /api/game/agents`、`GET /api/game/agents/{agentId}/relationship`、`GET /api/game/agents/{agentId}/children` 和 `GET /api/game/agents/{agentId}/bonuses`。支持按ID/名字/上级/身份/等级/盟主比例/成为代理日期筛选，逐级回溯BOSS、检测缺失上级/循环/双表冲突，查看直属或全部后代，以及查看代理累计总红利、已提取、剩余红利和逐笔来源。MySQL为5.7，不依赖递归CTE；全部后代由Go按 `upper_guuid` 索引分层读取，最多64层、20,000条并显式返回截断状态。
- 代理体系口径已结合真实表定义和客户端授权逻辑核对：BOSS由游戏角色“老板”识别；盟主由账号自身盟主字段或角色标记识别，不能只按数值等级猜测。`third_marketing_info.upper_guuid` 是直属上级，`upper98_guuid`、`upper99_guuid`、`upper100_guuid`、`upper101_guuid` 分别标注小盟主、大盟主、合伙人、总裁；注册关系的直属上级优先于账号表 `sm_agentID`，两者不一致时后台标记冲突。下级账号的 `sm_big_percent` 表示其获授盟主比例并约束继续向下授权上限，`sm_super_percent` 为合伙人比例；本期仅展示，不开放修改。
- 代理红利汇总的权威字段是 `tbl_Account.sm_all_hongli`、`sm_use_hongli` 和 `sm_hongli`，原始整数单位为百分之一红利，接口按除以100后的两位小数返回。逐笔红利来源以 `usr_income_info.proxy_guuid` 查询，`tax_number` 是该笔红利原始值，`winner_guuid/name` 和 `roomID` 说明来源玩家与房间，`remark3`、`remark4`、`remark5` 分别用于展示红利基数、比例和服务端收益层级。`usr_activity_info_sub_hongli` 的正向“红利”记录与 `usr_income_info` 一一对应，不能重复相加；只把负数或类型明确包含“提取”的记录作为提取明细。若账户累计提取大于可查明细，必须展示差额提示，不能伪造提取流水。
- 2026-08-09 真实数据联调确认当前有1个BOSS根节点、4个普通代理、尚无已授权盟主；4个代理均可完整回溯到BOSS。BOSS的直属下级共8个，正确拆分为4个代理和4名玩家。代理列表、类型筛选、链路详情、直属/全部下级、玩家筛选、比例字段和非法比例区间400均通过登录态API验证。
- 2026-08-09 代理红利真实数据联调确认当前BOSS账户累计总红利44.01、已提取0、剩余44.01；`usr_income_info` 有52笔来源且合计44.01，与账户汇总完全一致。登录态接口已验证分页、来源玩家/房间/日期筛选、空提取列表和非法日期400；每笔能自然显示来源玩家、房间、红利基数及20%/60%等实际比例。当前无红利提取样本，因此提取明细识别逻辑仍需在未来出现真实提取后复核。
- 2026-08-09 已新增“游戏管理 → 交易记录”，只读权限为 `game.transaction.view`，接口为 `GET /api/game/transactions`。接口要求精确玩家ID，并支持分类、收支方向、业务类型、关键字、日期和分页筛选；返回当前金币、流入、流出、净变化、游戏净输赢、道具支出以及逐笔前后余额。当前游戏仅使用金币，交易页和玩家详情均不展示钻石。
- 金币完整流水的权威来源为 `kbedm.usr_cash_water`；同结构的 `usr_cash_water1`、`usr_cash_water2` 及带 `sub_` 后缀的表属于空表、阶段表或辅助表，不与主表合并，避免重复统计。真实数据核对发现 `add_money` 在部分“芒皮/揍芒/休芒”等业务中符号不代表钱包方向，因此实际金币变化必须使用 `new_money - old_money`；`add_money` 只保留为原始业务金额。已核对主表各玩家最新余额与账号表当前金币一致，分类筛选和汇总均通过登录态真实数据验证。
- 2026-08-09 对照房间 `usr_cash_water` 与 `usr_total_score` 确认金币流水扩展参数不能不分类型直接拼接：`remark1` 是房间ID，`remark2` 是房间名，`remark3` 是第几局；`remark4` 在“打局/带入/结算”中记录该玩家本房间累计带入，在“芒皮”等牌桌动作中记录操作后的牌桌余额；“带入”的 `remark5` 是本次带入额。结算记录的 `remark5` 是服务端场景码，真实数据中 `13` 只出现在有有效局数和战绩的结算，`12` 出现在未形成战绩且金额为零的收回记录，但现有接口文档和客户端源码没有其官方枚举名称，不能把它当金币、局数或时长。交易列表和明细现已按 `option_type` 显示“房间、房间名称、第几局、累计带入、本次带入、操作后牌桌余额、消费项目、消费说明、结算类型”等业务标签；明细中的结算类型同时保留原始码供核对，消费说明会把服务器的“用X角-余Y分”转换为自然中文。
- 2026-08-09 已新增“游戏管理 → 房间战绩”，只读权限为 `game.room_record.view`。`GET /api/game/room-records` 不带 `roomId` 时返回已结算房间列表，支持房间号/房间名/参与玩家统一关键字、战绩日期和分页筛选；创建人不再作为筛选项，也不在列表、接口或详情中展示。带精确 `roomId` 时返回房间总览，逐局详情接口为 `GET /api/game/room-records/{roomId}/rounds/{round}`。前端采用“房间列表 → 房间详情”两级浏览：启用地九王时只显示“地九王”标签，未启用时留空，不显示“是/否”；点击房间后展示时间、参与玩家、累计带入、结算返还、最终输赢和全部局次。逐局详情展示庄闲身份、下注、芒果、输赢、两组牌面、发牌内容和按阶段整理的操作时间线，并可返回原列表。
- 房间战绩只读查询 `usr_total_score`、`usr_round_score`、`usr_paipu_log` 和 `usr_cash_water`。历史房间的地九王开关必须读取 `usr_total_score.remark` 的第 11 个逗号字段（索引 10）：“地方”或包含“地九王”为是，“标准”为否；`remark2` 第 3 项是奖池等数值，不能作为地九王布尔值。大厅实时房间压缩数组的地九王字段是另一套索引，不能混用。`usr_round_score.remark` 的牌局数据以 `@` 分段，已确认结果牌、下注、芒果、剩余芒果、庄闲身份、完整发牌、亮牌状态、奖池和补偿字段；牌面类型映射为 0方块、1梅花、2红桃、3黑桃、4大王，点数1/11/12/13分别为A/J/Q/K。`usr_paipu_log` 提供每步操作阶段、类型、金额、操作后余额和时间，不应再向运营人员显示无解释的原始数组或数字。
- 真实房间数据核对发现 `usr_total_score.score` 偶尔与最终结算不一致，例如某玩家原始战绩为 -250，但“结算返还－累计带入”为 -133，逐局分数合计也为 -133。因此房间玩家最终输赢优先采用结算返还减累计带入，缺少结算时才回退原始战绩；页面同时保留原始值并标记不一致，房间净输赢也按校正后分数汇总。
- 2026-08-09 已新增“系统配置”一级菜单，子模块为“游戏公告设置”“游戏通知发送”和“奖池设置”。公告权限为 `configuration.announcement.view/update`，接口为 `GET/PUT /api/configuration/announcement`；页面支持多行编辑、字符计数、玩家端预览、清空确认、修改元数据和重复键提示。2026-08-13 公告保存改为 `plain-text-preserved-v1`：前端与后端只把 CRLF/CR 统一为 LF，不再 `trim`，因此换行、空白行、段首缩进、连续空格、正文前后可见空白均会参与字符计数、编码、写库和回读；只有全空白输入按清空处理。编辑区与预览区共用“进入大厅弹窗/大厅公告详情页”选择，编辑框按对应客户端真实文字宽度、字号和行高进行1:1排版，预览区按相同比例缩略，因此两边的浏览器自动换行位置一致；窄屏编辑区保留固定客户端排版宽度并允许横向滑动，不随手机宽度重新折行。通知权限为 `configuration.notification.view/send`，接口为 `GET/POST /api/configuration/notifications`；发送必须显式提交 `confirm=true`，页面提供跑马灯预览、不可撤回提示和最近20条审计记录。奖池权限为 `configuration.reward_pool.view/update`，接口为 `GET/PUT /api/configuration/reward-pools`。
- 游戏长期公告使用 `kbedm.usr_hash_info` 的 `hash_key='系统公告'`，`hash_content` 必须严格兼容客户端 `btoa(encodeURIComponent(content))`，不能写明文。2026-08-09 实测该表引擎为 MyISAM，`ROLLBACK` 不会撤销写入，因此后端已改为保存前记录原行、写入后回读全部同名行、失败时按原始 ID 显式恢复；新插入分支按本次自增 ID 精确删除。联调中产生的唯一临时值已精确清理，正式核对时公告行数为0，不存在可见测试公告。
- 游戏即时通知由后端调用本机旧服务 `通知_所有玩家_信息`，旧服务只能配置为 `127.0.0.1/localhost:8890`；浏览器不能直连。历史实现曾使用 `####` 触发“系统广播”前缀和橙色背景，2026-08-13 已按下条新口径取消，不得恢复。
- 2026-08-13 游戏通知已扩展为“立即发送＋持久轮播”。轮播接口为 `GET/PUT /api/configuration/notification-carousel`：查看复用 `configuration.notification.view`，修改使用独立的 `configuration.notification.carousel.update`；支持最多50条正文的新增、删除、排序，设置开始时间、相邻两条间隔10秒～24小时和循环次数0～999。一次完整循环定义为从第一条播放到最后一条，0表示持续循环，指定轮数完成后服务端自动停用；发送失败不跳过当前条目，在下一个间隔重试。配置由 `mgr_notification_carousel_setting`、`mgr_notification_carousel_item` 持久化，迁移为 `020_notification_carousel.sql`。只有正式8891通过 `XUAN_NOTIFICATION_CAROUSEL_WORKER=true` 启动唯一工作器，本地和所有8892候选必须显式为false，避免重复全服发送。
- 2026-08-13 即时通知和轮播公告改为 `,,,,<正文>`，不再加 `####` 标记，现有客户端因此直接走普通通知的黑色半透明背景并只显示后台正文，不会出现“系统广播:”前缀；英文逗号仍转换为中文逗号以保护旧客户端的逗号分段协议。`panelCloudNotify.ts` 也已兼容清除历史 `####` 而不补前缀，并统一普通背景，但该客户端补丁尚未打热更新包；新后台发送链路在现有客户端上无需等待热更新即可生效。
- 2026-08-13 轮播公告版本已更新正式环境。候选8892强制 `XUAN_NOTIFICATION_CAROUSEL_WORKER=false`，迁移、首页、健康、新接口未登录401、静态资源哈希和“候选无工作器”均通过；正式运行配置已启用唯一工作器，公网首页/健康200，新接口未登录401，候选进程和8892端口均停止。上线后只读确认轮播保持初始关闭、间隔60秒、无开始时间、循环次数/完成轮数为0、内容0条、没有发送记录，因此未向玩家广播任何测试内容。正式二进制SHA-256为 `1e87f67ddcbec386ac1d68c5c1aa2e2d0ffb1a1e67466a356b8da8ec6f71365d`，资源为 `index-CZmsMnN2.js`、`index-CFddd6R0.css`；主要回滚文件为 `bin/xuanmanager.previous-notification-carousel-20260813`、`web.previous-notification-carousel-20260813`、`runtime.env.previous-notification-carousel-20260813`，最终权限归属修正的二进制回滚为 `bin/xuanmanager.previous-notification-carousel-visibility-20260813`。
- 奖池设置通过本机旧服务 `异步_获取_奖池_数据` 和 `异步_设置_奖池_数据` 管理9个已确认键：底皮0.1/0.3、0.2/0.5、1/3、2/5、5/10、10/20、20/40、50/100、100/200。修改请求必须提交全部目标值、页面加载时的全部基准值和 `confirm=true`；后端设置前重新读取并比较基准，发现并发变化或未知新皮池时拒绝写入，设置后最多8次回读，不一致则尝试恢复原金额并审计。金额限制为0至1万亿整数。2026-08-10 首次真实业务修改已在 `mgr_audit_log` 确认成功：目标值、修改前值和旧服务回读值完全一致，证明 XuanManager 写入链路生效。
- 2026-08-10 奖池显示异常只读排查确认存在两套口径：旧后台接口返回奖池原始整数“分”，游戏内 `查询_奖池_信息` 和界面使用“元”；更直接的“所有皮池都显示4641”来自 KB 房间 `PlayerList.total_reward_money`：真实底皮1/3房间的原始消息即为4641，而同一时刻按皮池查询底皮1/3为642、各皮池合计为4638；`DrhLogicMgr.OnUpdatePlayerList()` 只是把服务端字段原样写入顶部奖池条。已确认问题位于游戏服务生成或缓存 `total_reward_money` 的口径，它使用了全局/合计值而不是当前房间皮池值；服务端源码目录当前账号无读取权限，具体赋值行待有权限后核对。XuanManager 奖池页面现已统一按元输入和展示、允许两位小数，并在提交时精确乘以100换算为分；HTTP API、回读和审计继续保留整数分，避免破坏接口契约。
- 后台自有数据现已改用 `webcm`，所有新表使用 `mgr_` 前缀。2026-08-10 已确认其中存在 `mgr_schema_migration`、`mgr_user`、`mgr_role`、`mgr_module`、`mgr_permission`、`mgr_role_permission`、`mgr_session`、`mgr_audit_log` 八张表，九个迁移均已记录；用户指定的首个超级管理员已初始化为启用状态，实际账号密码未写入仓库或本文件。此前验证阶段在 `kbedm` 创建的 8 张同名前缀表仍保留但不再被生产服务使用，未经用户明确确认不得删除。
- 鉴权使用 bcrypt cost 12 密码摘要、随机会话令牌的 SHA-256 存储、HttpOnly SameSite 会话 Cookie、独立 CSRF Cookie/请求头校验、账号连续失败临时锁定、停用/改权/重置密码后清理旧会话以及关键操作审计；审计输入显式排除密码。登录、读取当前身份/全部权限和安全退出已在服务器本机完成真实闭环验证。
- 2026-08-12 后台账号已改为严格单点登录：登录事务先更新并锁定对应 `mgr_user` 行，使同账号并发登录串行执行，再删除该 `user_id` 的全部旧会话并插入当前会话；因此最后完成的登录生效，旧设备随后的受保护请求返回401。前端统一监听401并立即回到登录页，窗口重新获得焦点时主动检查，闲置页面也每60秒兜底检查；瞬时网络错误不会误退出。单次退出仍只删除当前会话，用户自行改密保留当前会话并清退其他会话，管理员重置密码、停用用户或变更角色权限继续清退全部相关会话。迁移 `019_single_session.sql` 已把正式库历史会话从22条清理为2条，对应2个账号，正式核对重复数为0。Go测试/vet、Linux amd64静态构建、前端lint/生产构建、候选8892和公网首页/健康/资源哈希/未登录401均通过；正式二进制SHA-256为 `a7da4e816bec9db33474940b44f9f564a20101286fe93d1dd4bee2487ac550c2`，资源为 `index-B9XEISNs.js`、`index-kY-_8Ouz.css`，候选8892已停止。回滚文件为 `bin/xuanmanager.previous-single-session-20260812` 和 `web.previous-single-session-20260812`。
- 2026-08-12 游戏账号多设备登录的客户端策略已区分“普通断网”和“服务端 `onKicked`”：连接中断、连接失败及心跳超时仍按原流程自动重连；收到 `onKicked(failedcode)` 时则设置独立的停止重连状态、使所有已排队的异步登录失效、停止心跳与超时检测、把提示暂存到常驻 `GameDataManager` 并直接重载 `login` 场景，`panelLogin` 挂载后弹出“当前登录连接已失效，请重新登录”。用户主动点击登录才解除该停止状态，因此两台新版客户端不会再因旧设备被踢后自动重连而循环互踢；账号密码本地缓存不会清除。实测发现服务器重启和其他设备登录都可能走 `onKicked`，且客户端收到消息时错误描述可能尚未导入，当前无法可靠区分原因，所以不再把所有 `onKicked` 误报为其他设备登录。`kbengine.js` 的 `Client_onKicked` 继续保证始终派发踢线事件；服务器重启与多设备登录的原因细分需要 KB 服务端提供稳定且不同的错误码后才能实现。普通断网自动重连、两台设备互踢、服务器重启后的重新登录及房间内踢线返回登录仍待真实联调。
- 前端采用深海蓝、翡翠青和白色高端运营中枢风格，已实现登录、工作台、后台用户、角色权限矩阵、模块/权限和操作审计页面，并带桌面、窄屏和手机布局。`npm run lint`、TypeScript 与 Vite 生产构建通过；Go `test`、`vet` 及 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64` 静态构建通过，Linux 产物确认是 x86-64 静态 ELF。
- 2026-08-09 XuanManager 已作为常驻服务部署到服务器私有目录 `/www/html/.xuanmanager`，使用用户级守护进程和 crontab 看门狗维持服务与 Caddy 路由。公网地址为 `http://154.37.155.17/xuanmanager/`；Caddy 剥离 `/xuanmanager` 前缀后反向代理到仅监听 `127.0.0.1:8891` 的 Go 服务，不直接开放 MySQL、旧 8890 或新 8891 端口。公网登录页、健康接口以及超级管理员登录、读取会话、退出均已实测返回 200。用户明确要求不使用 HTTPS，当前 `XUAN_COOKIE_SECURE=false`；公网 HTTP 会明文传输后台账号、密码和管理数据，应明确保留此风险，能限制固定来源 IP、可信内网或 VPN 时优先限制。
- 2026-08-11 公网地址必须带末尾斜杠：`http://154.37.155.17/xuanmanager/` 实测返回200，`/xuanmanager/api/health` 返回200且服务状态正常；不带斜杠的 `/xuanmanager` 当前由Caddy返回404。后续给用户提供访问地址时必须保留末尾 `/`；若要兼容无斜杠地址，需要单独调整Caddy增加重定向规则。
- 2026-08-09 本地前端开发入口为 `http://127.0.0.1:7458/`。`web/vite.config.ts` 支持用 `XUAN_DEV_API_TARGET` 和 `XUAN_DEV_API_PREFIX` 配置开发代理；当前不入库的 `web/.env.local` 将本地 `/api` 代理到公网 XuanManager 的 `/xuanmanager/api`，因此静态资源从本机加载、数据库和后端仍留在服务器。开发代理会把 `Origin` 改写为已配置的后端目标，否则本地页面的 PUT/POST 会被正式后端同源保护误拒绝；正式服务的 CSRF、会话和同源校验没有放宽。已重启本地 Vite 并实际验证登录200、未确认通知400、退出200。
- 2026-08-11 本地 `7458` Vite 进程停止后已重新以 `npm run dev` 启动，`http://127.0.0.1:7458/` 实测返回200；该进程状态属于当前时点，后续使用前仍需用本机访问重新确认。新打开的内置浏览器若显示登录界面，通常是因为没有登录会话，不代表本地服务或远程 API 故障。
- 2026-08-09 可读性检查确认一期界面字号偏小：原表格正文仅 10px、表头 9px，表单提示和权限说明最低 8px。`web/src/styles.css` 已统一提高字号与行高，正文/表格/表单/按钮主要文字调整到 13–16px，重要辅助文字不低于 12px；本地窄屏实际渲染、Vite 生产构建和远程静态资源均已验证并发布。
- 2026-08-11 手机端底部导航已改为可横向滑动的定宽菜单项，恢复图标下方的功能名称，并在路由切换后自动把当前菜单滚动到可见位置；底栏右侧新增固定“全部”入口，弹出的分组菜单只复用当前会话已授权的 `session.modules`，可直接进入任意有权限模块。弹层支持遮罩/关闭按钮/Escape关闭、打开时锁定页面滚动、横竖屏切出手机断点时自动关闭，并适配iPhone底部安全区。前端lint、TypeScript/Vite生产构建和diff检查通过；本轮只切换正式静态资源，未修改或重启后端，也未变更权限与数据。候选8892健康、JS/CSS 200、未登录401、上传哈希及隐藏AppleDouble文件清理均已验证；正式公网资源为 `index-Dm7VykNq.js` 和 `index-kY-_8Ouz.css`，首页、健康接口和静态资源均返回200，候选8892已停止，回滚目录为 `web.previous-mobile-navigation-20260811`。本次发布时只读确认正式二进制SHA-256为 `a5835144d7f23237db95fe25eb491624495c0ca8588f18d88dd5395a9f1803ed`，本轮保持不变。
- 2026-08-12 各查询页重复点击同一条件不刷新的根因是筛选状态或序列化查询字符串未变化时，React不会重新触发依赖它的加载Effect。`web/src/queryRefresh.ts` 现提供统一查询修订号；后台用户、操作审计、玩家、代理及下级/红利、交易、封号及历史、防盗号、房间战绩、玩家优化及历史的查询、应用条件和重置入口每次都会显式递增修订号，因此条件和页码完全相同时也会重新请求。`web/src/api.ts` 同时对管理接口GET请求默认使用 `cache: no-store`，避免浏览器缓存继续返回旧结果；本地7458登录页实测200且控制台无错误，前端lint/生产构建、Go全量测试/vet和diff检查通过。正式版本仅切换静态资源，后端进程、二进制、权限和数据库均未改动；候选8892健康、资源哈希和未登录玩家接口401通过，正式公网资源为 `index-m0Vz3ua2.js`、`index-C_jTe8l6.css`，首页/健康/资源/未登录401均通过，候选已停止，回滚目录为 `web.previous-query-refresh-20260812-2055`。本次首次使用SCP上传得到0字节文件并被哈希校验拦截，改用SFTP临时文件、校验一致后再改名成功；后续此账号发布文件应优先使用SFTP并继续执行上传前后SHA-256校验。
- 2026-08-12 工作台“今日新增玩家”的旧实现把中国运营日零点换算成UTC时间字符串后比较 `tbl_Account.sm_reg_time`，与玩家列表直接显示的数据库注册日期不一致，会把数据库前一日16:00后的注册记录算入当天。该指标现改为严格使用游戏库 `CURDATE()` 当天 `00:00–24:00` 的首次注册记录，页面说明改为“每日新注册玩家”；封号状态和 `game.player.unban` 审计不参与统计。真实只读交叉核对确认，一名当天注册后又解封的玩家只因首次注册进入新增数，不会因解封重复增加；发布验证期间又有新账号持续注册，因此绝对数量动态变化，但修正前窗口始终比数据库当天窗口多出那条前日记录。今日登录仍按中国运营日口径。新增SQL契约测试；Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建、候选8892、正式健康/资源哈希/未登录401均通过。正式二进制SHA-256为 `5424cc3f430353f66a67165fabb9560bdbb27d2746908cda89b9a23a03527001`，资源为 `index-rj4NLBxg.js`、`index-C_jTe8l6.css`，候选已停止；回滚文件为 `bin/xuanmanager.previous-dashboard-registration-count-20260812-2115` 和 `web.previous-dashboard-registration-count-20260812-2115`。
- 2026-08-09 玩家管理版本已发布到现有纯 HTTP 地址，本地 `7458` 前端也已重新启动并代理远程新版 API。已实际验证：迁移和菜单权限、超级管理员登录、玩家分页列表、等级筛选、非法余额区间返回400、退出登录，以及公网页面/JS/CSS/健康接口均正常。服务器保留部署前的二进制与静态目录作为回滚副本。
- 2026-08-09 代理管理版本已发布到同一纯HTTP地址，本地 `7458` 页面也已确认加载新模块并代理远程API。`npm run lint`、TypeScript/Vite生产构建、Go `test`/`vet`、Linux amd64静态构建和候选端口真实数据闭环均通过；正式公网首页、健康接口、新JS/CSS返回200，未登录代理接口返回401。服务器保留部署前的二进制 `xuanmanager.previous-agent-20260809` 与静态目录 `web.previous-agent-20260809` 作为回滚副本。
- 2026-08-09 代理红利版本已发布到同一纯HTTP地址，本地 `7458` 页面同步可用。正式版本登录态红利接口返回200，公网首页、新JS/CSS和健康接口返回200，未登录红利接口返回401；测试会话已注销，候选端口已停止。服务器保留部署前二进制 `xuanmanager.previous-agent-bonus-20260809` 与静态目录 `web.previous-agent-bonus-20260809` 作为回滚副本。
- 2026-08-09 已新增供游戏客户端注册页调用的公开接口 `POST /api/game/registrations`，规范请求字段为 `invitationCode`、`nickname`、`loginName`、`password`、`avatarIndex`，并兼容接口文档旧字段 `upper_guuid`、`player_wxid`、`player_wxname` 及头像别名 `photo`。邀请码必须为6位数字并命中代理/盟主候选账号；昵称为1～32位且只能使用中文、英文字母或数字；登录账号当前必须为6～16位英文字母或数字；当前源码允许头像编号为未补零的字符串1～100，旧客户端不传时默认1。登录账号同时检查 `third_marketing_info.player_wxid`、`tbl_Account.sm_wxID` 和 `kbe_accountinfos.accountName`，最终仍由 `player_wxid` 唯一索引处理并发冲突。注册主记录只写 `kbedm.third_marketing_info`；旧游戏要求的 MD5 密码兼容逻辑保留，但明文和摘要均不写日志、审计或响应。公开接口允许无凭据跨域 POST，按可信代理后的来源IP限制10分钟10次；浏览器走纯HTTP时密码仍会明文传输，属于用户已接受但应持续保留的风险。
- 2026-08-09 注册接口最初发布的是6位纯数字账号版本；同日已将英文账号与昵称字符限制调整重新发布到正式8891。新版通过 Go测试、vet、Linux amd64静态构建、上传前后SHA-256一致性、正式健康接口和进程校验；使用有效邀请码、6位英文数字账号和故意非法昵称的安全请求返回“昵称只能使用中文或英文字母”，证明英文账号已通过账号格式校验且请求在写库前结束。真实数据库已验证专用账号对 `kbedm` 仅有全库SELECT及对 `third_marketing_info` 的INSERT/UPDATE/DELETE，表上 `player_wxid` 为唯一索引；成功插入分支仍未用生产测试账号做端到端写入，待客户端首次真实注册时复核。旧注册版回滚二进制为 `xuanmanager.previous-registration-20260809`，本次发布前正式版回滚二进制为 `xuanmanager.previous-registration-account-rule-20260809`。
- 2026-08-12 注册昵称规则现已真正统一为1～32位中文、英文字母或ASCII数字，空格、下划线和其他符号仍拒绝。此前提交校验、Prefab占位文字、XuanManager接口和游戏内改名校验虽已允许数字，但 `panelLogin.onRegisterInputChanged()` 的实时 `text-changed` 过滤正则遗漏 `0-9`，导致注册页数字输入后立即被删除；现已补齐数字范围。游戏内修改昵称仍保留最长10位的原有限制，四条改名分支保持中文/英文/数字规则和提示。XuanManager昵称规则已于2026-08-11通过Go测试/vet、Linux静态构建、候选8892无写库验证并发布到正式8891；正式二进制SHA-256为 `aeae43f775878ecf95ca73e23baff822b28a666c82f3e4b8b6d7292d50d23578`，回滚文件为 `bin/xuanmanager.previous-registration-nickname-20260811`。本次仅修客户端实时输入过滤，Creator运行态输入仍待验证。
- 2026-08-11 公开注册接口已强制昵称和登录账号全局不可重复：昵称同时检查 `third_marketing_info.player_wxname` 与 `tbl_Account.sm_name`，登录账号同时检查 `third_marketing_info.player_wxid`、`tbl_Account.sm_wxID` 与 `kbe_accountinfos.accountName`，比较前统一 `TRIM+LOWER`，英文大小写不同也视为重复。昵称检查与注册写入在同一数据库连接中使用按规范化昵称生成的 MySQL `GET_LOCK` 命名锁串行化，避免两个并发注册同时通过；登录账号继续由 `third_marketing_info.player_wxid` 唯一索引兜底。重复昵称返回409 `NICKNAME_EXISTS/该昵称已被使用`，重复账号返回409 `LOGIN_NAME_EXISTS/该登录账号已被使用`，客户端现有注册错误弹窗会直接显示服务端文字。线上只读核对发现注册表已有1组历史重复昵称，本次未擅自改名或删除，仅阻止后续新增重复。Go测试/vet、Linux静态构建、候选8892两条无写库重复校验、正式8891健康/静态页、公网健康/页面均通过；正式二进制SHA-256为 `a5835144d7f23237db95fe25eb491624495c0ca8588f18d88dd5395a9f1803ed`，回滚文件为 `bin/xuanmanager.previous-registration-unique-20260811`，候选8892已停止。现有 `supervise.sh` 在子进程正常退出码为0时会一并退出，切换正式二进制后必须调用部署目录的 `start.sh` 重新拉起守护进程，不能只等待自动重启。
- 2026-08-09 注册头像版已发布到正式8891。由于 `third_marketing_info` 没有头像字段，接口成功插入后会按注册记录ID异步等待游戏服务补齐 `player_guuid`，最多重试2分钟，再调用8890内部命令 `异步_设置_玩家_属性` 把所选序号写入账号 `photo`；客户端同时按登录账号暂存所选头像，首次进入大厅时若服务端仍为空或为不同序号会主动 `reqSetProperty("photo", 序号)`，待服务端返回一致后清除暂存，避免创建账号与头像落库之间的时序造成默认头像覆盖。Go测试、vet、Linux amd64静态构建、远端哈希、进程和健康接口通过；正式接口用故意越界头像21验证返回400“头像编号必须为 1 到 20”，请求在写库前结束，没有创建测试账号。随后同工作区发布的奖励池版本继承了本次头像源码并成为当前正式二进制，头像接口复测仍生效；本次头像候选包被保留为 `xuanmanager.previous-reward-pools-20260809`，头像发布前的旧版为 `xuanmanager.previous-registration-avatar-20260809`。
- 2026-08-14 注册上级人数维护已实现并发布正式环境。用户确认邀请码是注册必填项，注册成功即完成代理绑定；“今日新增”只表示游戏数据库 `CURDATE()` 当天注册的玩家，不单独统计后续绑定事件。注册接口会从邀请码代理沿直属 `upper_guuid`（缺失时回退账号 `sm_agentID`）递归回溯到BOSS，最多64层并拒绝断链、无效上级和循环；旧表只提供三个字段，因此新记录继续兼容固化 `upper_guuid/upper2_guuid/upper3_guuid` 前三层，但人数维护保留完整上级链。累计/今日人数改为扫描注册表直属关系图、按任意深度遍历并精确重算，再用一条 `UPDATE ... JOIN` 同步完整链上所有具有 `third_marketing_info` 承载行的代理，失败时短时幂等重试，不使用可能重复的缓存 `+1`。代理列表与详情也使用同一任意深度关系聚合覆盖缓存，`today_lower_count`只认当天注册日期；BOSS 648425没有营销承载行，因此后台可实时显示其关系聚合值，但不得伪造BOSS注册记录。正式只读核对值仍为440554共5、182605共10、648425共17，当日均0；历史缓存未执行整表批量回填，新注册会刷新其完整上级链。Go全量测试/vet、Linux构建、MySQL `EXPLAIN`、8892候选和正式三类邀请码无写入409验证均通过，测试前后注册记录均为0；正式二进制SHA-256为 `f582410afb20086edd162f022357788d8c16c86258b78bf98dbafbb2d09b8eca`，公网首页/健康200，候选已停止。回滚文件为 `bin/xuanmanager.previous-registration-lower-counts-three-level-20260814`（上一版三层实现）和 `bin/xuanmanager.previous-registration-lower-counts-20260814`（本次功能前版本）。
- 2026-08-09 交易记录版本已发布到同一纯HTTP地址，本地 `7458` 页面同步可用。`npm run lint`、TypeScript/Vite生产构建、Go `test`/`vet`、Linux amd64静态构建、候选端口登录态真实金币流水和汇总验证均通过；正式公网首页与健康接口返回200，未登录交易接口返回401。服务器保留部署前的二进制 `xuanmanager.previous-transactions-20260809` 与静态目录 `web.previous-transactions-20260809` 作为回滚副本。
- 2026-08-09 交易关联说明可读化版本已同步本地与正式纯HTTP页面，前端 lint/生产构建、公网页面和新版JS/CSS、后端健康接口均验证通过；本次只更新静态页面，服务器保留 `web.previous-readable-transactions-20260809` 作为回滚副本。
- 2026-08-09 房间战绩两级列表及地九王标签版本已发布到同一纯HTTP地址，本地 `7458` 页面同步可用。真实数据联调时 `usr_total_score` 有7个房间、29条玩家总战绩，7个现存房间的规则字段均为“标准”，因此当前地九王单元格和详情标签位置均留空；只有规则为“地方”或包含“地九王”时才显示“地九王”标签，不再显示“是/否”。默认列表、房间号精确筛选、房间/玩家/日期查询SQL、分页汇总、房间详情、逐局牌面、操作日志和结算校正均通过。前端 lint/生产构建、正式公网首页、最终JS/CSS和健康接口均验证通过。正式页面继续不展示创建人和旧玩法文字；本次只更新静态页面，服务器保留 `web.previous-dijiu-label-20260809` 作为回滚副本。
- 2026-08-09 玩家档案版本已发布到同一纯HTTP地址，本地 `7458` 页面同步可用。真实玩家联调确认资金页能汇总177条金币流水，房间页能返回4个历史房间并正确计算逐房带入、返还、参与局数和输赢；其中原始战绩与结算不一致的房间继续显示校正提示。前端 lint/生产构建、Go `test`/`vet`、Linux amd64静态构建、正式页面/JS/CSS/健康接口均通过，未登录资金和玩家房间接口返回401。服务器保留部署前二进制 `xuanmanager.previous-player-profile-20260809` 与静态目录 `web.previous-player-profile-20260809` 作为回滚副本。
- 2026-08-09 系统配置版本已发布到同一纯HTTP地址，本地 `7458` 页面同步可用。Go测试/vet、Linux amd64静态构建、前端lint/生产构建、第7个迁移、3个模块、4个权限、超级管理员授权、候选8892和正式8891健康检查均通过；候选端口已关闭。浏览器实测桌面与800px窄屏无横向溢出，新页面辅助文字不低于12px，公告预览、通知逗号转换和二次确认按钮状态正常，控制台无警告或错误。公网最终JS/CSS和健康接口返回200，服务器保留 `xuanmanager.previous-system-configuration-20260809`、`web.previous-system-configuration-20260809` 和字号调整前静态目录作为回滚副本。
- 2026-08-09 奖池设置版本已发布到同一纯HTTP地址，本地 `7458` 页面同步可用。第8个迁移、奖池模块、2项权限和超级管理员授权均已核对；Go测试/vet、Linux amd64静态构建、前端lint/生产构建、候选8892读取与未确认400、正式8891/公网健康和最终JS/CSS均通过，候选端口已关闭。浏览器验证9个皮池、4200总额、临时修改差额、恢复、二次确认按钮、800px窄屏和控制台均正常，测试输入已恢复且没有点击保存。服务器保留 `xuanmanager.previous-reward-pools-20260809` 和 `web.previous-reward-pools-20260809` 作为回滚副本。
- 2026-08-10 奖池单位修正版已同步本地与正式纯HTTP页面。页面明确展示“元”，输入允许两位小数，示例与提示说明旧服务按“分”保存；本地真实数据验证原始64200分显示642元、输入642.01元显示单项差额+0.01元并能恢复，未勾确认时保存仍禁用，全程未提交任何奖池修改。前端lint/生产构建、公网页面/JS/CSS哈希和健康接口均通过，浏览器控制台无警告或错误；正式服务器保留 `web.previous-reward-unit-20260810` 回滚目录。
- 2026-08-10 已新增“游戏管理 → 封号管理”，权限为 `game.ban.view/create/remove`，接口为 `GET/POST /api/game/bans` 和 `POST /api/game/bans/{playerId}/unban`。游戏账号以 `tbl_Account.sm_client_status` 为权威封禁状态，非空即封号提示、清空即解封；写入只调用本机8890的 `异步_设置_玩家_属性` 修改 `client_status`，不直接更新游戏表，并在成功返回前循环回读校验。封号/解封均要求6位玩家ID、显式确认和操作审计。第9个迁移、模块、3项权限及超级管理员授权、Go测试/vet/Linux静态构建、前端lint/生产构建、候选8892、正式8891、公网页面/资源和本地7458登录态页面均已验证；发布时数据库当前已封账号数为0，QA只验证确认保护和只读链路，没有真实封禁玩家。候选目录已清理，正式服务器保留 `xuanmanager.previous-bans-20260810` 与 `web.previous-bans-20260810` 回滚备份。
- 2026-08-11 封号管理页面已扩展同页“封号历史”区，接口为 `GET /api/game/bans/history`，沿用 `game.ban.view` 权限，不新增表或迁移。历史以 `mgr_audit_log` 中 `game.player.ban`、`game.player.unban` 的成功和失败审计为权威来源，支持关键字、封号/解封、成功/失败和分页筛选；解封记录从 `before_json.clientStatus` 恢复当时封号原因，避免账号当前状态清空后丢失原因。接口使用既有超级管理员审计可见性条件，普通管理员看不到超级管理员执行的封号/解封记录；前端在封号或解封成功后自动刷新历史。Go测试/vet、Linux amd64静态构建、前端lint/生产构建和 `git diff --check` 已通过，QA没有执行真实封号或解封。首次SCP上传被截断且哈希不一致，未启用该候选；改用可续传的rsync后，远端候选哈希与本地完全一致。候选8892健康200、新历史路由未登录401、静态首页和新资源通过后已切换正式8891；正式二进制SHA-256为 `9dc3e3fd2c11c3fb7c7a80fb5ec2aaee8d5f66f0edf798c57db07e3d2928ccb7`，Caddy前缀下健康200，新历史接口已从404修正为权限保护401，正式资源为 `index-CJxCp81Z.js` 和 `index-De-Lzrwj.css`。候选8892已精确停止，回滚文件为 `bin/xuanmanager.previous-ban-history-20260811` 和 `web.previous-ban-history-20260811`。
- 2026-08-12 玩家加减分权限为 `game.player.balance_adjust`，接口为 `POST /api/game/players/{playerId}/balance-adjustments`，只操作整数金币，后端通过本机8890旧充值接口提交带签名工单；签名只保存在受控运行环境 `XUAN_GAME_EXCHANGE_SIGN`。玩家 `sm_roomID > 0` 时增加和扣减账号金币均允许，不再返回 `PLAYER_IN_ROOM`；该接口不会直接修改当前牌桌已经带入的积分，页面必须向操作员明确提示。操作仍要求2～120字原因、显式确认和页面基准余额，余额并发变化、扣减超过账号余额或单次超过1,000,000时拒绝；成功后回读 `sm_gold + sm_gold2/100` 并审计，不在源码、响应或审计中暴露签名。本版已通过Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建和diff检查；候选8892确认旧拦截字符串不存在、新提示已进入静态包、健康200及未登录401，正式8891和公网健康/首页/JS/CSS/权限保护均通过，未对真实玩家执行加减分。正式二进制SHA-256为 `fe6d54b357db6c1390e59a156d7c7cf3d664c391b5c3fc2fa36f4e9e53218d50`，资源为 `index-aptoA1GS.js`、`index-kY-_8Ouz.css`，候选8892已停止；回滚文件为 `bin/xuanmanager.previous-in-room-balance-20260812` 和 `web.previous-in-room-balance-20260812`。本地7458当时未运行，本次只发布正式版本。
- 2026-08-12 玩家管理的基本资料已精简：代理关系只展示直属代理，删除大代理、合伙人、总裁三个重复层级；注册与其他参数中删除优化1、优化2。`GET /api/game/players` 同步停止查询和返回这7组冗余字段，只保留 `agentId/agentName`，以减少列表载荷并防止前端再次显示无效的 `undefined`；直属代理ID/名字查询条件、完整代理管理和独立玩家优化模块均保持不变，权限与数据库没有变更。新增JSON载荷契约测试，Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建和diff检查通过；8892候选的健康、静态资源哈希、旧文案缺失和未登录401通过，正式登录态浏览器已真实加载9名玩家并确认详情只剩直属代理、注册时间、最近登录和备注。正式二进制SHA-256为 `d2c8cab88dcacd7dbc249d44fec8757774f2fa4122aff2a8ee762107c084e055`，资源为 `index-B-m8skxV.js`、`index-kY-_8Ouz.css`；公网首页、健康、资源哈希和权限保护通过，候选8892已停止。回滚文件为 `bin/xuanmanager.previous-player-profile-cleanup-20260812` 和 `web.previous-player-profile-cleanup-20260812`。浏览器若在发布前已打开SPA，必须刷新页面才能加载新资源，单纯切换Hash路由仍会继续使用旧内存包。
- 2026-08-11 房间维护查看权限为 `game.room_maintenance.view`，新实时列表接口为 `GET /api/admin/hall/rooms`，旧 `GET /api/game/room-maintenance` 保留兼容；后端按 `大厅所有房间列表Web接口文档.md` 调用本机8890命令 `查询_大厅_所有房间`，完成1基/0基页码转换、默认50/最大200、`ret_code=512` 校验、对象/数组结构校验、安全错误透传和 `Custom` 房间限制。接口保留玩法、规则、俱乐部和创建人等完整房间元数据，但当前紧凑页面只展示房间号、房间名、状态、局数、玩家/观战/等待带入人数、创建时间和备注，不展示玩家明细或牌局敏感数据；读取失败即清空列表，严禁回退到会残留幽灵房间的 `tbl_Account.sm_roomID`。解散权限仍为 `game.room_maintenance.dissolve/dissolve_all`，通过独立的 `异步_执行_房间_命令`、显式确认和审计执行，全部房间另要求超级管理员及 `ALL_ROOMS`。本版已通过Go测试/vet、Linux amd64静态构建、前端lint/生产构建并发布到正式纯HTTP地址，公网健康接口、首页、新JS/CSS及新路由未登录401均已验证，本地 `7458` 同步重新启动；服务器保留 `xuanmanager.previous-hall-rooms-20260811` 和 `web.previous-hall-rooms-20260811` 回滚副本。首次发布前正式KB命令曾返回 `ret_code=769`；2026-08-11 14:56 后登录态真实联调已确认生产KB成功返回12个实时 `Custom` 房间。首次接入的白屏根因是KB房间项按文档返回 `room_id`、`special_rule` 等下划线字段，而React页面误按 `roomId`、`specialRule` 读取并调用未定义值的 `length`；`RoomMaintenancePage.tsx` 现已在接口入口统一兼容两种命名并为数组/数字/文字提供安全默认值。白屏修复和紧凑卡片已随交易黑名单整体前端包同步到正式纯HTTP页面；遗留的 `candidates/hall-rooms-20260811/xuanmanager` 候选进程已确认精确路径并停止，8892端口随后由本次交易黑名单候选短暂复用并在发布后关闭。
- 2026-08-11 房间维护卡片按运营快速浏览需求改为紧凑布局：桌面宽屏三列、1180px以下两列、820px以下单列；卡片仅保留房间号、房间名、状态、局数、玩家、观战、等待带入、创建时间、备注及有权限时的解散按钮，不再展示玩法、规则、房间类型、俱乐部或创建人。间距、字号、边框、阴影和按钮均缩小，当前2560宽页面一屏可见约三排房间；12个真实房间稳定渲染，前端lint/生产构建和浏览器控制台检查通过。本地7458与正式纯HTTP静态页面均已同步，正式回滚目录为 `web.previous-transaction-blacklist-20260811`（该次整体前端发布同时包含本紧凑版）。
- 2026-08-11 已实现并发布“游戏管理 → 交易黑名单”，用于限制指定游戏玩家赠送金币，不影响账号登录或进入房间。游戏端权威配置为副本配置 `npc`（逗号分隔的6位玩家ID）和 `disable_cash_exchange`（`True/False`总开关）；旧管理页同区域的 `npc_win_rate` 是翻牌胜率，本模块不得读取或修改。后台接口为 `GET/POST /api/game/transaction-blacklist`、`PUT/DELETE /api/game/transaction-blacklist/{playerId}` 和 `PUT /api/game/transaction-blacklist/status`，权限拆分为 `game.transaction_blacklist.view/create/update/delete`。新增和替换会先核对 `tbl_Account` 玩家存在；所有写操作要求显式确认和页面版本摘要，调用本机8890的 `设置_副本_配置数据` 后循环回读，失败时尝试恢复原值并写审计；异常旧配置内容通过 `warnings` 暂停名单写入，避免自动规范化误删。页面支持总开关、按ID新增、替换、删除及本地搜索，并明确显示当前限制是否生效。Go测试/vet、Linux amd64静态构建、前端lint/生产构建均已通过；本机模拟接口确认菜单、状态卡、新增区、名单表格和修改确认弹窗正常渲染且无应用脚本错误。正式发布通过 `client_update@154.37.155.17:2233` 完成：候选8892健康、静态页200、新路由未登录401，正式8891重启后健康200，Caddy前缀路由的首页/健康/新JS `index-B7AeT6GV.js`/新CSS `index-BbdQUPpN.css`/新接口权限保护均通过；正式二进制SHA-256为 `aed629cc2bb12257f550c9f91695a5805a4dc8b90683298eb34d10bd3f3c6f27`，候选8892已停止。本轮只读验证，没有新增、修改、删除名单玩家，也没有切换名单总开关。服务器回滚文件为 `bin/xuanmanager.previous-transaction-blacklist-20260811` 和 `web.previous-transaction-blacklist-20260811`。本机浏览器自动化因安全策略拒绝访问127.0.0.1，故没有完成发布后的登录态页面复核；该限制不能通过其它浏览器控制方式绕过。
- 2026-08-10 支付通道配置权限为 `configuration.payment.view/update`，接口为 `GET/PUT /api/configuration/payments`。页面采用通道总览、单通道三段式配置、全局支付与提现设置、变更摘要和保存确认；管理客户端现有支付1～支付12、VIP充值、VIP充值2、自助充值、提现及数据库中未来新增的 `支付配置_` 键。底层继续使用 `usr_hash_info`：`支付管理` 为 `#` 分隔启用列表，`支付配置_<名称>` 为客户端 Base64 编码 JSON，包含 `needinfo/infolist/money/notify/bank/bOpenInput/inputrange`；支付域名、支行要求和支付宝/银联/USDT提现说明沿用独立键。批量写入使用修订哈希防并发覆盖、MyISAM原行快照、写后回读和显式恢复；更新存在但值未变化的行不得因 `RowsAffected=0` 误判为缺失并插入重复键。
- 2026-08-13 支付通道的银行列表已完整迁入网页版：`bank` 不是全局银行表，而是每个 `支付配置_<通道>` 自己的 `#` 分隔字段，客户端在充值资料选银行时读取当前充值通道配置，在银行卡提现和实名页读取“提现”通道配置；因此支付1～支付12、VIP充值、VIP充值2、自助充值和提现均应显示独立的可选银行编辑区。页面支持一键补齐旧 `panelManager.ts` 空列表时使用的14家常用银行（只追加缺项，不覆盖自定义项）、逐项新增/改名/删除、上下调整客户端展示顺序和放弃未保存修改；单通道最多50项，银行名最多40字，前后端均拦截空值、`#`、控制字符和重复项。保存仍复用整页修订冲突校验、MyISAM快照恢复、客户端Base64 JSON写入及回读，不新增后台专用银行表；审计摘要增加各通道银行数量。本版已通过Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建和diff检查，并发布到正式纯HTTP地址；候选8892健康、支付接口未登录401和新资源200后已关闭，正式8891及公网首页/健康/资源/401均通过，正式二进制SHA-256为 `e367b8880403360943edd5a7e2ae8710eecc5acb31b9cc16158c824851c21bed`，资源为 `index-CFdNiioV.js`、`index-L5rD8uma.css`，回滚文件为 `bin/xuanmanager.previous-payment-bank-management-20260813` 和 `web.previous-payment-bank-management-20260813`。验收没有保存或改变任何真实支付/银行配置；本地7458已启动，但浏览器无登录会话，未为视觉验收登录超级管理员以避免单点登录清退其他会话。
- 2026-08-10 活动管理权限为 `configuration.activity.view/update`，接口为 `GET/PUT /api/configuration/activities`。页面提供 `activity_on` 总开关、玩家手数榜/玩家赢分榜/代理红利榜三张配置卡、时间区间、领取开关、展示名次、奖励规则、玩家说明和 `activity3_list_power` 五档倍率。三套活动分别映射客户端已确认的 `activity3_*`、`activity2_*`、`activity_*` 老板配置键，三段玩家说明继续写 `usr_hash_info` 的“活动文本_”键；旧服务读取返回769表示键未设置，按空值处理。单项停止通过清空该活动时间区间实现，批量保存逐项设置并回读，失败时逆序恢复已改老板配置和活动文字。
- 2026-08-10 上述四项运营功能已发布到正式8891及公网纯HTTP路径，本地7458已重启并代理正式API。Go测试/vet、Linux amd64静态构建、前端lint/生产构建、第10个迁移、模块权限、候选8892三项登录态GET、四类未确认400、正式健康/静态资源/未登录401均通过；浏览器实测当前房间、支付配置、活动配置和加减分弹窗，桌面及800px窄屏无横向溢出，控制台无错误警告，全程没有实际加减分、解散房间或保存支付/活动配置。正式服务器保留 `xuanmanager.previous-operations-20260810` 与 `web.previous-operations-20260810` 回滚备份。
- 2026-08-10 房间错误数据源修正版已发布到正式8891、公网纯HTTP路径和本地7458。Go测试/vet、Linux amd64静态构建、前端lint/生产构建、候选8892启动与权限保护、正式健康/资源哈希/未登录401均通过；登录态页面确认不再出现历史房间，明确展示“实时列表暂不可用/错误历史数据已停用”，指定房间号能进入二次确认且未提交任何真实解散命令。正式服务器保留 `xuanmanager.previous-room-source-fix-20260810` 与 `web.previous-room-source-fix-20260810` 回滚备份。本条记录的是专用接口上线前的历史状态，已被2026-08-11新增的 `查询_大厅_所有房间` 接入方案取代；禁止数据库、历史战绩或客户端上报伪造房间列表的约束继续有效。
- 2026-08-10 已新增“游戏管理 → 玩家优化”，本游戏只实现客户端实际使用的“发牌优化”，不查询、展示或修改遗留的 `sm_optimize02_*` 胡牌优化字段。权限为 `game.player_optimization.view/create/update/delete`，接口为列表及单玩家 `GET`、集合 `POST`、单玩家 `PUT/DELETE`；查询支持玩家ID/账号/昵称、后台设置账号、历史游戏设置人、启用状态、概率范围和分页，展示 `sm_optimize01_man/count/chance` 和最近配置时间。`sm_optimize01_man=648425` 等值是游戏运行所需的BOSS标识，不代表后台操作人；当前列表优先以成功的 `mgr_audit_log.operator_name` 显示实际后台设置账号，没有后台审计的历史数据才回退显示游戏设置人。页面提供独立的新增、调整、删除入口，三类写操作均由对应角色权限决定，不再额外限制超级管理员；仍要求显式确认但不要求填写原因，调整和删除还提交页面基准值防止覆盖并发变化。写入使用本机游戏库事务，不调用8890在线属性接口。
- 玩家发牌优化属于高风险牌局参数，写操作必须具备对应的新增、调整或删除角色权限，并继续要求显式确认和必要的页面基准值。后端只允许参数化更新 `tbl_Account.sm_optimize01_man/count/chance` 三个白名单字段；目标玩家行使用 `FOR UPDATE` 锁定，新增验证尚未启用，调整和删除验证锁定后的旧值与页面基准完全一致，更新后在事务内回读一致才提交。删除在同一语句中归零次数/概率并清空设置人。操作成功后把操作人、操作前后值和可选原因写入 `mgr_audit_log`。
- 2026-08-10 玩家优化版本已发布到正式8891、公网纯HTTP路径和本地7458。第11个迁移、模块及2项权限、Go测试/vet、Linux amd64静态构建、前端lint/生产构建、候选8892启动和未登录401、正式健康/资源哈希均通过；登录态浏览器核对真实汇总、列表、概率条和配置弹窗，修改59次的临时输入可在填写原因并勾选确认后启用保存按钮，但未点击提交。发布后数据库再次确认线上仍为58次/100%、优化2仍为0，正式服务器保留 `xuanmanager.previous-player-optimization-20260810` 与 `web.previous-player-optimization-20260810` 回滚备份。
- 2026-08-10 玩家优化新增/删除补全版已发布到正式8891、公网纯HTTP路径和本地7458。第12个迁移、新增与删除权限、Go全量测试/vet/Linux静态构建、前端lint/生产构建、候选8892健康和四个未登录401、正式进程/公网页面/健康接口/最终JS与CSS均通过。登录态浏览器确认：顶部有独立新增入口，未启用玩家468554可按ID核对后进入新增表单；已启用玩家292989行内同时提供调整与红色删除入口，删除必须填写原因并勾选确认后才启用提交。QA未点击任何写入按钮，最终数据库仍为292989的58次/100%、468554未启用、优化2均为0。候选8892已关闭；服务器保留 `xuanmanager.previous-player-optimization-crud-20260810` 与 `web.previous-player-optimization-crud-20260810` 回滚备份。
- 2026-08-10 真实操作与失败排查确认：管理员把玩家292989从58次成功调整为38次，审计记录成功且当前回读仍为38次/100%；随后给玩家565923新增10次/100%时，8890通用属性调用虽返回成功，但数据库没有建立完整优化参数并最终恢复为0/0/空设置人。根因是该命令只修改在线玩家实体，不能作为后台发牌优化写入口。此前新增页面要求玩家在线、通过8890逐字段写入的实现已废止，不得恢复。
- 2026-08-10 玩家发牌优化已改为服务器本机直接数据库事务：后端新增独立的 `XUAN_GAME_DB_*` 连接，只允许本机 `127.0.0.1` 和 `kbedm`；新增、调整、删除在 InnoDB 事务中以 `SELECT ... FOR UPDATE` 锁定 `tbl_Account` 玩家行，校验页面基准后单次更新 `sm_optimize01_man/count/chance` 三个白名单字段，提交前回读一致，任一步失败自动回滚。该流程不依赖玩家在线状态；浏览器不接触游戏库凭据，成功操作继续写 `mgr_audit_log`，最近配置时间和实际后台设置账号均优先读取后台成功审计。新增、调整、删除三个操作的原因输入和必填校验均已移除；兼容请求中的非空原因仍会校验并审计。
- 2026-08-10 发牌优化直写修正版已发布到正式8891、公网纯HTTP路径和本地7458。Go全量测试、vet、Linux amd64静态构建、前端lint/生产构建、候选8892双数据库健康检查、本机事务 UPDATE 权限及 ROLLBACK 前后值一致均通过；登录态浏览器确认玩家565923可进入新增表单且明确提示离线可配置，292989调整弹窗没有原因字段并明确不依赖在线。QA没有提交真实优化变更，最终仍为292989的38次/100%、565923未启用。正式服务器保留 `xuanmanager.previous-player-optimization-direct-db-20260810`、`web.previous-player-optimization-direct-db-20260810` 和 `runtime.env.previous-player-optimization-direct-db-20260810` 回滚备份。
- 2026-08-10 玩家优化操作人显示及原因简化版已发布到正式8891、公网纯HTTP路径和本地7458。列表字段由“游戏设置人”改为“设置账号”：优先显示最近一次成功 create/update 审计的 `operator_name` 并标记“后台账号”，只有无后台审计的历史数据才显示游戏历史设置人；关键字筛选同步支持后台账号。新增、调整、删除三个弹窗均无原因输入，后端不再要求 `reason`，但兼容请求提供非空原因时仍校验并审计。Go测试/vet、Linux静态构建、前端lint/生产构建、候选8892健康/401和正式登录态页面均通过；浏览器确认292989与565923都显示 `admin999 / 后台账号`，新增和删除弹窗无原因字段，QA未提交写操作。正式服务器保留 `xuanmanager.previous-player-optimization-setter-account-20260810` 与 `web.previous-player-optimization-setter-account-20260810` 回滚备份。
- 2026-08-12 玩家优化页面已新增同页历史记录区，接口为 `GET /api/game/player-optimization/history`，复用 `game.player_optimization.view` 权限且不新增表或迁移。历史以 `mgr_audit_log` 的 create/update/delete 审计为权威来源，支持玩家/账号/操作人关键字、操作类型、成功/失败和分页筛选；响应只提供可读的修改前后次数、概率、操作人、时间、可选备注与结果，不向浏览器透传原始审计JSON。接口复用 `nonSuperAuditVisibilitySQL` 在服务端排除超级管理员操作，普通账号无法通过直接请求绕过；正式库只读核对现有6条玩家优化历史均由超级管理员执行，普通视角可见数为0，超级管理员视角为6。Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建和diff检查通过，候选8892健康、新接口未登录401、上传哈希一致后已发布正式8891；公网首页/健康/JS/CSS均为200，新接口未登录401，正式二进制SHA-256为 `0343b7a5b3086012773287444ca515ef8239f109f5e36aceb80abacbdedf135d`，资源为 `index-BcPPpCuK.js`、`index-C_jTe8l6.css`。候选8892已停止，回滚文件为 `bin/xuanmanager.previous-player-optimization-history-20260812` 和 `web.previous-player-optimization-history-20260812`；本轮没有执行真实玩家优化写操作。内置浏览器当时没有登录会话，为避免单点登录使用户现有会话失效，未重新登录做页面态验收；本地7458已重新启动。
- 2026-08-10 玩家加减分的客服维护原因以 `mgr_audit_log` 为权威来源，游戏主流水 `usr_cash_water` 的补分记录只保存调整金额与调整后余额，不保存原因，也不能把其 `remark1/remark2` 解释成房间号/房间名。交易接口现按玩家、操作前后余额、增减方向和30秒内时间窗口把金币流水与加减分审计一一匹配，返回 `maintenanceReason`、`maintenanceOperator`、`maintenanceWorkOrder`；交易列表、明细“客服维护信息”和玩家详情“资金情况”均直接展示，加减分表单也明确提示原因的三个可查位置。真实补分流水已在候选8892、正式8891和本地7458页面验证，原因、操作人、工单及服务记录金额/余额均正确；没有再次执行加减分。正式服务器保留 `xuanmanager.previous-adjustment-reason-20260810`、`web.previous-adjustment-reason-20260810` 和最终提示文案前的 `web.previous-adjustment-hint-20260810` 回滚备份。
- 2026-08-10 超级管理员隐身版已发布到正式8891、公网纯HTTP路径和本地7458开发入口。Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建、候选8892健康、正式本机/公网健康、最终JS哈希和未登录用户/审计接口401均通过；真实 `webcm` 只读校验确认当前只有1个超级管理员、暂无普通后台用户，普通视角用户数为0，35条总审计中仅3条非超级管理员相关记录可见，因此本轮无法进行已有普通账号的登录态页面验证。正式服务器保留 `xuanmanager.previous-super-visibility-20260810-154804` 与 `web.previous-super-visibility-20260810-154804` 回滚备份，候选8892已停止。
- 2026-08-11 后台密码规则最终纠正为“至少6位”并发布到正式8891、公网纯HTTP路径和本地7458；先前按字面理解为至少7位的中间版本已被完全替代。Go全量测试/vet确认 `123123`、6位纯字母/纯符号/中文均通过、5位和73字节拒绝；前端lint/生产构建、Linux amd64静态构建、候选8892、正式本机/公网健康、最终JS哈希及新旧提示文本检查均通过。没有创建测试账号或修改任何现有密码。正式服务器保留纠正前版本 `xuanmanager.previous-password-min6-20260810-160648` 与 `web.previous-password-min6-20260810-160648` 供回滚，候选8892已停止。
- 2026-08-11 工作台只保留能够准确计算的累计玩家、今日新增和今日登录：累计玩家读取 `tbl_Account`；今日新增按中国运营时间（UTC+8）边界查询 `sm_reg_time`；今日登录按同一边界查询 `kbe_accountinfos.lasttime` 并按账号计数。真实在线人数对照证明 `kbe_entitylog` 的6个账号实体包含历史残留，并不代表当前在线；8890的玩家属性查询也会为离线玩家返回持久化属性，无法判断在线会话。当前服务器账号无法访问能提供权威会话状态的KBEngine内部接口，因此当前在线、今日峰值和小时在线对比已从接口与页面删除，第16个迁移删除此前错误采样表 `mgr_game_online_hourly`。后续必须由游戏服务提供后台可访问的已认证在线账号接口后才能恢复。
- 2026-08-11 玩家优化写操作已取消超级管理员硬限制；`game.player_optimization.create/update/delete` 三项独立权限现在完全由角色权限矩阵决定，前端按钮和后端接口均按对应权限控制，显式确认、事务基准、防并发覆盖、白名单字段和审计仍保留。第15个迁移已同步更新三项权限说明。首次合并版本发布到正式8891、公网纯HTTP路径和本地7458；Go全量测试/vet、Linux静态构建、前端lint/生产构建、候选迁移、正式进程/本机与公网健康、最终JS/CSS、登录态工作台和权限矩阵均通过，浏览器控制台无错误警告，没有执行任何发牌优化写操作。正式服务器保留 `xuanmanager.previous-dashboard-metrics-20260811` 与 `web.previous-dashboard-metrics-20260811` 回滚备份。
- 2026-08-11 错误在线统计删除版已发布到正式8891、公网纯HTTP路径和本地7458。后端响应、定时采样器和前端在线卡片/曲线均已删除；第16个迁移已在 `webcm` 留档并确认 `mgr_game_online_hourly` 表数量为0。Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建、候选8892、正式进程/本机与公网健康均通过；登录态本地页面只显示今日新增、今日登录、累计玩家三项，DOM核对“当前在线玩家”“今日在线峰值”和小时曲线均不存在。替换前旧进程曾因候选迁移先删除采样表产生两轮预期警告，新进程在16:52:10启动后没有再出现该警告。正式服务器保留 `xuanmanager.previous-dashboard-online-removal-20260811` 与 `web.previous-dashboard-online-removal-20260811` 回滚备份。
- 2026-08-11 当前工作区的 XuanManager 后台、客户端防盗号、注册与100头像、原生 Android/iOS 设备桥接及配套工具/文档已整理为一次全量 Git 提交。提交前通过 Go 全量测试和 vet、Linux amd64 静态构建、前端 lint/生产构建、JavaScript/Python 脚本语法、三个 Cocos prefab JSON 解析及100个头像资源数量检查；`web/.env.local`、`web/dist`、`node_modules`、`.DS_Store` 和临时构建产物均保持忽略，没有提交数据库密码或运行密钥。
- 2026-08-12 全量 Git 整理范围包含 ChatTool 完整源码与部署示例、XuanManager 封号历史/实时房间/交易黑名单/玩家安全和注册唯一性、客户端客服接入/注册交互/版本拦截/“我的”代理入口与资金明细、2.0.5 热更新清单及发布工具修正。提交前已确认 ChatTool 的运行环境、上传目录、二进制、依赖和构建产物保持忽略，示例环境变量中的数据库密码、引导密码和玩家链接口令均为空；XuanManager 与 ChatTool 的 Go 全量测试/vet、两套 React 前端 lint/生产构建、100头像流程、Python发布脚本语法、Prefab JSON 和 `git diff --check` 均通过。Creator运行态、真实账号协议交互、正式服务部署与2.0.5热更新上传不属于本次Git推送验证，仍以各功能此前的独立联调/发布记录为准。
- 2026-08-09 修复 `deploy/centos7-user/supervise.sh` 的停止陷阱：旧写法在收到 TERM 后执行清理但继续主循环，导致 `stop.sh` 超时；现将 INT/TERM 单独路由到清理后退出。升级在线旧进程时已核对精确 PID，仅强制结束旧 Bash 守护外壳并向 Go 子进程发送 TERM，随后新版本正常启动。

## ChatTool 网页客服系统

- 2026-08-11 已在根目录 `ChatTool/` 实现独立的游戏在线客服项目，技术栈为 Go 标准库服务端、React/Vite 双端页面和 MySQL 5.7；默认只监听 `127.0.0.1:8893`，生产数据使用 `webcm` 的 `chat_` 前缀表且不创建外键，图片/视频/文件保存在权限受控的独立媒体目录，数据库只记录元数据和不可预测媒体ID。正式公网入口按IP子路径部署：玩家端为 `http://154.37.155.17/chattool/player`，客服工作台为 `http://154.37.155.17/chattool/agent`，Caddy剥离 `/chattool` 后转发到本机8893，8893和MySQL均不直接开放公网。
- 玩家手机H5已实现无客服在线提示和发送禁用、自动接入状态、文字/相册图片/现场拍摄/视频/PDF/文本/ZIP、输入中、媒体预览、结束咨询和五星评价；客服端已实现独立登录、主管/普通客服、在线/离开/离线、最少接待量与最久未分配优先的自动派单、接待上限、待接入/我的/全部/已结束队列、聊天、快捷回复、内部备注、转接、结束、玩家资料列、客服团队创建/启停/角色/容量及评价查看。客服主动离线、心跳超时或账号停用时，活动会话会重新入队并自动分配。
- 2026-08-11 用户明确取消KB授权和一次性票据链路，要求系统保持简单，只需让普通用户无法直接看懂入口中的玩家资料。当前接入改为：`panelKefu.ts` 直接读取当前Account的玩家ID、昵称、等级、平台、角色和房间，加入客服入口、服务通道和秒级时间戳后复用现有 `Tool.encrypt()` 生成AES-ECB/PKCS7十六进制密文，打开 `http://154.37.155.17/chattool/player?d=<密文>`；网页把密文提交到 `POST /api/player/session`，后端用 `CHAT_PLAYER_LINK_KEY` 兼容解密并校验默认15分钟有效期，成功后立即从地址栏移除密文。`ConfigManager.kefuUrl` 默认值已改为该模板；线上哈希配置 `客服2` 若覆盖默认值，也必须同步更新。游戏内“客服”传 `channel=general`，两个“VIP充值”入口均已改为同一ChatTool地址并传 `channel=vip_recharge`；本轮按用户要求只修改Cocos源码，不生成或发布客户端热更新包。
- 轻量加密只保证玩家ID、昵称等资料不以明文出现在URL中，不是可靠身份认证：口令随客户端发布，技术人员可以逆向后伪造资料。客服资料栏和元数据明确显示“游戏客户端轻量加密、未服务端认证”，不得向运营人员宣称这是权威游戏身份。`Tool.aesKeyBytes()` 和 `Tool.encrypt()` 已移除口令、派生键及密文的调试打印。ChatTool不再需要 `CHAT_INTERNAL_TOKEN`、`CHAT_TICKET_TTL`、`chat_player_ticket` 表或KB接入示例。
- ChatTool 客服密码仍使用bcrypt cost 12，会话仅存随机令牌SHA-256，并实施HttpOnly/SameSite Cookie、独立CSRF、同源校验、限流、CSP、媒体鉴权、文件头白名单、大小限制、审计和范围下载。公网纯HTTP部署时 `CHAT_COOKIE_SECURE=false`，Cookie路径限制为 `/chattool`；Caddy转发的客户端IP只在请求确实来自本机回环代理时才信任。实时通道采用SSE且消息以MySQL为权威；当前为单Go实例且没有内置恶意附件扫描，多实例前需增加Redis/NATS和共享对象存储，开放ZIP/PDF前应接入杀毒隔离或只开放图片和视频。
- 用户已明确确认ChatTool“不需要HTTPS，直接通过IP访问”。前端生产构建统一使用 `/chattool/` 的静态资源、API、SSE和媒体前缀，Caddy兼容无末尾斜杠308跳转。轻量直传版已通过CryptoJS已知密文兼容测试、Go `test -race`/`vet`、Linux amd64无CGO静态构建、React ESLint、TypeScript/Vite生产构建及三个Cocos修改文件的TypeScript语法转译。纯HTTP下客服密码、聊天内容和附件仍可能被链路窃听或篡改，此风险已保留在部署文档中。
- 2026-08-11 ChatTool 已正式部署到服务器私有目录 `/www/html/.chattool`，静态Linux amd64服务只监听 `127.0.0.1:8893`，复用服务器本机 `webcm` 账号并创建14张 `chat_` 表、记录4个迁移；运行配置权限为600，首次主管账号创建及公网登录闭环通过后已从运行环境删除一次性初始化密码。Caddy路由ID为 `qing_chattool_proxy` 和 `qing_chattool_redirect`，用户级crontab包含开机启动及每分钟路由看门狗。服务删除初始化密码后重启成功，本机/公网健康、`/chattool` 308、玩家页和客服页200、主管登录/身份、团队管理、在线/停止接待均已验证。
- 2026-08-11 线上 `kbedm.usr_hash_info` 原无“客服2”记录，现已新增且回读确认只有1行，值为 `http://154.37.155.17/chattool/player?d={info}`，因此现有客户端获取动态配置后会直接打开ChatTool。已发布旧客户端的加密资料只有 `vipid/phone/name`、没有时间戳；服务端当前兼容 `ts=0` 并在客服资料标记“旧客户端（无时间戳）”，新客户端继续执行15分钟有效期。旧链接无法限制重放，待新版客户端覆盖率足够后应删除兼容分支。
- 2026-08-11 游戏内真实点击客服首次显示“入口信息无效”的根因已经确认：当前测试客户端的AES密文和口令均正确，但 `JSON.stringify` 把玩家 `level` 发送为数字，服务端 `directPlayerPayload.Level` 原来只接受字符串，解密后的JSON反序列化失败并被统一映射成 `INVALID_PLAYER_DATA`。后端现用 `playerTextValue` 同时兼容字符串、数字和 `null`，入库前统一转为字符串；数字等级回归测试、Go `test -race`/`vet`、Linux amd64静态构建、正式二进制哈希及本机/公网健康均通过。线上真实密文在修复后先正确进入“入口信息已过期”分支；以同一真实资料刷新时间戳重新加密后，玩家页成功创建会话并进入无客服状态，证明游戏入口、解密和玩家会话闭环已通。无客服时能否发送的旧行为已于2026-08-12改为强制禁发。该修复只更新服务端，不需要发布Cocos热更新；文字/图片/视频的玩家与在线客服双端收发仍应在客服上线后完成首单验收。
- 2026-08-11 玩家和客服发送文字均失败的根因是纯HTTP IP页面没有浏览器安全上下文，`window.crypto.randomUUID()` 不存在；现统一使用 `createClientMessageID()`，安全上下文优先使用UUID，纯HTTP则回退到时间戳加随机串，仅作为服务端消息幂等键而非安全令牌。客服打开会话后白屏复现为详情/消息异步加载完成后React根节点被运行时异常清空；前端已增加会话列表与详情请求序号防止旧请求覆盖、API数组和玩家资料数据兜底、滚动异常隔离、日期/头像安全格式化和顶层错误边界。正式页面真实验证“待接入→立即接入”后持续稳定，玩家发送与客服回复各1条均成功实时到达、无错误提示且数据库只新增对应2条文本消息。
- 2026-08-11 客服工作台已新增玩家新消息提示音和“声音开/声音关”按钮，默认开启、浏览器本地记忆；开启按钮会播放试听音以解除浏览器自动播放限制。已分配会话只提醒当前客服，未分配会话的玩家新留言通过团队实时事件提醒所有在线客服，事件按消息ID去重。客服账号新增 `POST /api/agent/auth/password` 自助改密：必须验证当前密码，新密码使用至少6个Unicode字符、最多72字节且不能与旧密码相同，修改成功只保留当前会话、注销该账号其他会话并写不含密码的审计。创建客服、主管重置密码、首次主管初始化和客服自行改密统一复用同一规则；前端创建表单和改密弹窗均已同步为至少6位。
- 2026-08-11 ChatTool 发布停止链路已完善：`stop.sh` 同时以PID文件清理或进程消失为成功条件，并为旧版本保留20秒兼容窗口；新服务端在收到TERM后先关闭SSE事件中心，使玩家/客服长连接立即退出，再执行HTTP优雅关闭。正式环境快速停启实测停止0秒、总计1秒，不再出现新一轮 `context deadline exceeded`；此前超时日志属于升级旧二进制时的历史记录。2026-08-12 无客服禁发版发布后，当前正式二进制SHA-256为 `2c62402ef094abfc8180fb3177fe9fe66bbf0cd1b6fb6273fcad7fffe845d71e`，网页JS/CSS分别为 `index-l2DNZT_o.js` 和 `index-wypZBLr_.css`；功能发布前回滚副本为 `chattool.previous-online-send-gate-20260812` 与 `web.previous-online-send-gate-20260812`，中间补强前回滚副本为 `chattool.previous-online-send-gate-interim-20260812` 与 `web.previous-online-send-gate-interim-20260812`。
- 2026-08-11 ChatTool 自动派单失败根因已修复：`chat_message` 唯一键是 `(conversation_id,sender_type,client_message_id)`，欢迎消息之后的自动接入系统消息原来继续省略 `client_message_id`，两者都落成空串并使派单事务回滚。所有系统消息和媒体消息现均使用随机消息ID作为非空幂等键；普通文字消息缺失或携带超长客户端幂等键时也回退为服务端随机消息ID，彻底避免空串唯一键冲突。自动派单失败会记录带触发来源的服务端错误，不再静默忽略。正式环境已用现有会话真实执行“客服停止接待→玩家重新排队→客服上线”，未点击手动接入即自动恢复为活动会话，双方实时看到“客服已接入”系统消息，发布后日志无新错误。多个在线客服仍按当前活动会话最少、最久未分配、客服ID顺序只分给一人；本轮未创建额外正式客服做多人并发写入测试。
- 2026-08-11 双端输入状态已增加完整生命周期：只有活动会话且文本非空时才上报，持续输入每秒续期，停止输入、清空、失焦、发送、上传、切换会话/内部备注、离线和卸载时立即结束；接收端另有2.5秒兜底过期，停止事件丢失也不会永久显示。正式玩家页与客服页分别验证持续输入时提示出现、停止后自动消失，测试文本已清空。客服玩家资料栏新增“在游戏后台查看 / 处理”，打开 `http://154.37.155.17/xuanmanager/#/game/players?playerId=<玩家ID>`；XuanManager 玩家页会读取哈希参数并自动执行精确玩家ID查询，正式登录态已验证直接返回目标玩家及其权限允许的加减分、重置密码、详情入口，后台自身登录和接口RBAC没有绕过。XuanManager当前正式JS/CSS为 `index-CMGl9ZHa.js` 和 `index-BpynXA6T.css`，静态回滚目录为 `web.previous-chat-link-20260811`。
- 2026-08-11 ChatTool 已增加服务通道隔离，当前启用 `general/普通聊天客服` 和 `vip_recharge/VIP充值客服`。每个客服账号只能归属一个通道；队列、自动派单、我的/全部会话、未读数、接入、转接、媒体、输入状态和SSE事件都按通道过滤，主管的“全部会话”也只能查看本通道。自动派单仍按同通道客服的活动会话最少、最久未分配顺序只分给一人，无本通道在线客服时玩家端明确提示且禁止发送；不能让VIP队列阻塞普通队列。主管可在客服团队中为其他客服设置通道，客服有活动会话时禁止跨通道修改；正式库已确认两个通道、现有客服/会话归入普通通道且不存在跨通道分配。玩家会话按“玩家+通道”复用，并用独立随机会话引用选择HttpOnly Cookie，因此同一浏览器可同时打开普通与VIP页面而不会串会话。
- 2026-08-11 ChatTool 聊天内容默认只保留48小时：`CHAT_MESSAGE_RETENTION` 默认 `48h`，后台每5分钟永久删除到期文字、系统消息、内部备注和对应媒体文件；客服会话页也可二次确认后“一键清空记录”，双方实时同步为空。两种清理均保留会话状态、派单日志、评价、审计和仅客服可见的玩家备忘。正式发布前用户要求清空的既有玩家图片已按数据库媒体记录和受控路径精确永久删除；之后的新上传仍按48小时策略处理。
- 2026-08-11 客服聊天右栏已增加仅客服可见的玩家备忘列表，支持新增和删除，玩家端没有备忘接口或界面；生产验收新增的临时备忘已按唯一记录精确清理，原有备忘保留。客服标签红点已改为只显示真实未读消息数，选中并完成已读后“我的会话/全部会话”不再把活动会话数误当红点。客服可对活动或已结束会话执行内容清空，但普通客服不能清空未分配排队会话。
- 2026-08-11 用户要求把全部客服密码统一初始化为指定的6位临时密码；正式执行使用bcrypt cost 12摘要，明文未写入源码、迁移、审计或运行配置。事务共重置2个客服账号、注销2个旧登录会话、把全部客服设为离线并写入1条不含密码的批量重置审计；当时没有活动会话需要重新排队。最终只读核对为2个账号、1种相同有效摘要、0个会话、0个离线客服占用的活动会话和1条重置审计，本机及公网健康和新版页面“至少6位”文案均通过。所有客服必须使用新临时密码重新登录，之后可在工作台自行修改。
- 2026-08-12 ChatTool 客服账号已改为严格单点登录：登录事务先用 `SELECT ... FOR UPDATE` 锁定同一客服账号，使并发登录串行执行，再删除该账号全部旧会话并插入当前会话；数据库迁移 `005_agent_single_session.sql` 会保留每个客服最新历史会话并增加 `agent_id` 唯一索引，数据库层保证每个客服最多一条会话。后登录成功后向旧工作台推送 `session.replaced`，旧页面立即返回登录页并显示被顶下线提示；实时事件不可用时，前端也会统一监听客服受保护接口的401并退出。新登录不会把该客服置为离线或重新排队其会话。Go `test -race`/vet、Linux amd64静态构建、前端lint/生产构建和候选8894均通过；正式库已记录5个迁移，唯一索引1个、重复客服会话0个，发布时现存客服会话0个。正式本机及公网健康/客服页/新版JS均为200，未登录客服身份接口401，候选进程已停止；尚未使用真实客服账号执行双设备登录，以避免无授权登录和触发自动派单，待客服实际双设备操作验收。
- 2026-08-12 ChatTool 已按最终业务规则改为“必须有本通道客服在线才能发送”：玩家页无客服时显示“当前没有客服在线”，禁用文字框、发送按钮和图片/拍摄/视频/文件入口，客服上线后经SSE团队事件恢复，另有20秒状态轮询防止移动网络重连时漏事件。服务端文字事务和媒体上传落库事务会按通道二次确认至少一个客服满足启用、在线、心跳有效且登录会话未过期；无客服时统一返回409 `NO_AGENT_ONLINE`，不写消息、媒体或磁盘文件。自动派单、在线统计和转接目标也复用有效登录会话条件；客服会话自然过期时维护任务广播团队状态变化。Go `test -race`/vet、Linux amd64静态构建、前端lint/生产构建、两轮8894候选和正式切换均通过；正式本机及公网玩家页/健康/最终JS为200，未登录玩家身份接口401，最终上线时有效在线客服0、客服会话0、消息87、媒体3，发布前后消息与媒体数量不变，候选已停止。最终版本未创建迁移，正式库仍为5个迁移；当前新进程日志只有启动信息，日志中的三条优雅停止超时均为2026-08-11历史记录。
- 2026-08-13 玩家客服新增游戏内全屏半透明 WebView 模式，同时保留原独立网页协议和公网地址作为兼容能力，但游戏界面不再显示“浏览器打开”按钮。`panelKefu.ts` 在原 `cc.WebView` 上加载 `embed=game`，客户端与网页背景均设为透明，使牌桌可继续透出；内嵌页只提供文字、图片和视频，原独立网页仍保留完整入口。Android 项目自有 `QingChatWebViewBridge` 实现 `WebChromeClient.onShowFileChooser`，支持系统图片/视频选择器、取消与结果回传；iOS 桥负责把 `WKWebView` 设为透明，选择器继续使用系统 WebKit 能力并由同步脚本写入相册/相机用途说明。`tools/sync_android_chat_webview.py`、`tools/sync_ios_chat_webview.py` 用于 Creator 重建原生工程后的重复同步；iOS 设备/语音同步脚本也已改为自动发现当前唯一 `.xcodeproj`、修正历史 `8L*Bridge` 文件名并允许三类桥任意顺序重复执行。
- 游戏内 WebView 不依赖 SameSite Cookie：`POST /api/player/session` 在 `embedded=true` 时返回仅存 `sessionStorage` 的随机内嵌会话令牌，后续玩家接口通过 `X-Player-Embedded-Token` 发送；媒体显示先换取30分钟、限定媒体与会话的随机票据，图片和视频URL不携带完整玩家会话。内嵌页用2秒轮询替代无法附加请求头的原生 `EventSource`，普通独立网页仍使用Cookie与SSE。服务端只允许 `/player` 页面被嵌入，`/agent` 和API继续 `X-Frame-Options: DENY`/`frame-ancestors 'none'`；公网仍是用户明确要求的纯HTTP，不能提供链路机密性。
- 该版本已通过Go全量测试/vet、Linux amd64静态构建、React lint/TypeScript/Vite生产构建、Cocos Creator 2.4.13 Web Mobile构建、Android `compileDebugJavaWithJavac`、iOS无签名iphoneos整包编译和三类桥目标文件核对；内置浏览器确认半透明嵌入样式生效且不存在浏览器打开按钮。尚未在真实Android/iPhone上完成相册选择、视频选择、上传进度、播放与牌桌透出回归，客户端发布前必须由用户用各一台真机验证。ChatTool 服务端和网页已正式发布，当前二进制SHA-256为 `b3fa178abfbdad072abfaddff394e34fe29e917291a9461fada6566bfcdd5e60`，网页JS/CSS为 `index-CRPbYXyr.js`、`index-B7oJSgk3.css`；公网健康、玩家页允许嵌入、客服页禁止嵌入、未登录401和新资源200均回读通过，回滚副本为 `bin/chattool.previous-ingame-chat-20260813` 与 `web.previous-ingame-chat-20260813`。本轮未生成或发布Cocos客户端包。
- 2026-08-13 首次真机内嵌测试出现发送403：直接会话响应原本带有正确CSRF，但2秒状态轮询的 `/api/player/me` 因跨站WebView不发送SameSite Cookie而没有原始CSRF，前端随后把已有值覆盖为空，使文字、输入状态和媒体上传均被判为 `BAD_CSRF`。前端现只在响应确实携带CSRF时更新，并把内嵌CSRF与Bearer一起保存在当前WebView的 `sessionStorage`；服务端把通过 `X-Player-Embedded-Token` 成功认证的请求标记为请求头Bearer模式，该凭据不会被浏览器自动附带，因此写请求不再依赖Cookie-CSRF，普通Cookie模式仍强制同源和正确CSRF。单元测试、Go全量测试/vet、前端lint/生产构建通过；候选8894和正式8893均用立即删除的临时会话验证“Bearer+Origin:null→204、Cookie缺CSRF→403”，没有新增消息或保留测试会话。修复版正式二进制SHA-256为 `3211ace01248536672f2efa9acb978806971d73e7186e96f0208adfc33cfb9fd`，网页JS/CSS为 `index-v6TJPI4b.js`、`index-B7oJSgk3.css`，公网资源和健康均为200；回滚副本为 `bin/chattool.previous-embedded-403-fix-20260813` 与 `web.previous-embedded-403-fix-20260813`。
- 2026-08-13 ChatTool 玩家端整页随消息滚动的根因是玩家根容器只设置 `min-height`，消息区还保留250px最小高度，内容超出后由文档承接滚动；输入区的 `position: sticky` 也不能阻止顶部被卷走。玩家路由现统一锁定 `html/body/#root` 和 `.player-shell` 在可视高度内，顶部客服栏、服务状态和底部输入区设为固定Flex区，只有 `.player-messages` 使用 `min-height: 0`、纵向滚动、iOS惯性滚动和滚动边界隔离；内嵌WebView与独立网页同时生效。实际长消息手势验证中页面滚动值始终为0，消息区滚动位置正常变化，顶部与输入区坐标不变。用户随后要求再降低透明感，内嵌主遮罩由64%提高到72%不透明度，顶部由82%提高到88%，服务卡与输入区由90%提高到94%，仍保留牌桌透出；前端lint、TypeScript/Vite生产构建和公网验收通过。正式网页资源为 `index-ClyRbuNB.js`、`index-CAvNCWJe.css`，服务端二进制、数据库和Cocos包均未改变；切换前滚动版回滚目录为 `web.previous-player-opacity-20260813-1557`，再前一版为 `web.previous-player-scroll-20260813-1512`。安卓图片/视频选择依赖原生 `QingChatWebViewBridge.onShowFileChooser` 和 `AppActivity.onActivityResult`，当前生成工程已同步，未包含该桥的旧APK无法靠网页或热更新补齐，必须用当前源码重新构建发布安卓包；本轮按用户要求不代为发布Cocos/安卓包。
- 2026-08-13 多个客服窗口显示同一批会话并且玩家都显示“客服3”的生产排查确认不是自动派单给了三人：截图时正式库只有客服3持续心跳且有效在线，主管和客服2虽保留登录会话但已停止心跳并离线；主管先收到的8条活动会话在离线后重新排队，随后全部按规则分给唯一在线的客服3。根因是三个窗口使用同一浏览器配置文件，共享同一枚HttpOnly客服Cookie，最后登录客服3后所有窗口的请求实际都以客服3身份发送，但旧窗口仍显示首次加载的客服名称。客服端现为每个已登录窗口保存预期客服ID，所有受保护API通过 `X-Agent-Expected-ID`、SSE通过查询参数提交；服务端发现Cookie账号与预期账号不一致时统一返回401 `AGENT_ACCOUNT_SWITCHED`。同源窗口登录其他客服还会通过浏览器存储事件立即让旧窗口退出并明确提示使用独立浏览器配置文件、不同浏览器或不同设备；存储事件不可用时由请求身份校验兜底。该修复不改变自动派单、通道、会话归属或数据库结构；多人客服测试和正式使用不能在同一浏览器配置文件内混开账号。Go全量测试、竞态检查、vet、Linux amd64静态构建、前端lint/生产构建和本地双窗口切号实测通过；8894候选及正式8893的健康、页面、资源、未登录401和日志均通过，候选已关闭。正式二进制SHA-256为 `0e7ea710b0c212365609b9f5978166b5d32c239ef95c3f87cc43917458b36793`，网页JS/CSS为 `index-Jwe6Eeli.js`、`index-CAvNCWJe.css`；回滚副本为 `bin/chattool.previous-agent-identity-20260813` 和 `web.previous-agent-identity-20260813`，发布未迁移数据库也未重分配既有会话。
- 2026-08-13 两个有效在线客服仍把两个在线玩家都分给同一人的原因是上线时序和旧负载口径共同造成：客服主管先于小美约45秒上线，当时系统把7条历史排队会话一次性分给唯一在线的主管；小美随后上线时旧逻辑只处理排队队列，不会均衡已转为活动状态的会话，并且分流负载把很久没有心跳的旧玩家会话也持续计入。现以最近90秒仍有有效 `chat_player_session.last_seen_at` 的玩家作为实时分流负载和自动派单候选；客服最大接待量仍按全部活动会话校验，不会因实时口径绕过容量。客服登录、切在线、心跳、玩家创建/复用会话和维护周期都会执行同通道自动均衡，但只移动“最新派单为系统自动派单/系统均衡、玩家仍在线、当前派单后尚无客服回复”的会话；已回复、客服主动接入或人工转接的会话不会被搬动。正式切换后首次客服心跳自动把玩家237737从主管均衡给小美，玩家718968保留主管，两边实时负载均为1，写入系统 `transfer` 日志且没有修改聊天内容。Go全量测试、竞态检查、vet、Linux amd64静态构建、只读正式SQL预演、8894候选和正式公网健康/页面/401/日志均通过；候选已关闭，无数据库迁移和前端变化。正式二进制SHA-256为 `cdc9a6efc336ca0b96fd14a6c7210267adf98df993da47313128f73d91d60db8`，回滚文件为 `bin/chattool.previous-live-balance-20260813`。
- 2026-08-14 ChatTool游戏内玩家客服的手机可读性已二次放大：问题不是Viewport Meta或网页分辨率缺失，而是原页面字号和触控区仍不适合手机。`styles.css`现于1280px及以下启用仅限玩家端的醒目大字布局，覆盖Cocos WebView可能报告的宽CSS视口；玩家标题29px、服务状态25px、消息正文25px、输入框22px，头像、图片/视频入口及发送按钮同步扩大到58～84px。没有使用`zoom`或改变聊天协议，文档高度与宽度仍锁定视口，只有中间`.player-messages`滚动；真实浏览器390×844和1280×900计算样式均符合上述数值，页面本身`overflow:hidden`、消息区`overflow:auto`。网页版游戏首次打开客服空白、关闭重开才显示的根因是`panelKefu.ts`先给WebView赋URL、后绑定`loaded/error`，首次创建或缓存iframe时可能漏过加载事件；现改为先绑定事件和浏览器`message`监听、下一帧再导航，ChatTool玩家页完成首屏或错误页渲染后向父窗口发送`chattool:player-ready`，客户端同时校验来源域名与iframe窗口，8秒未就绪或首次初始化失败时自动带防缓存参数重试一次，Native仍沿用`loaded`完成标志。独立跨源iframe首开测试在5秒内收到`READY ok=true`，前端lint/TypeScript/Vite构建、Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建、8894候选以及正式HTTP/HTTPS入口、健康、新资源和未登录401均通过。正式ChatTool网页资源为`index-C2bkxB3x.js`和`index-pqCInQIj.css`，回滚目录为`web.previous-mobile-ready-20260814-1355`，正式服务端二进制仍为`cdc9a6efc336ca0b96fd14a6c7210267adf98df993da47313128f73d91d60db8`且数据库未变；本轮没有覆盖或发布Cocos正式网页版，首次打开修复需用户重新构建并发布Web Mobile后生效。
- 2026-08-14 Android内嵌客服出现字号过大、状态卡和输入区错位的根因是上一版`max-width:1280px`玩家大字规则同样命中了320～480 CSS像素的标准Android WebView；高DPR不是可靠分流依据。`styles.css`现按实际CSS视口分级：641～1280继续保留此前适配750设计宽的大字版，481～640使用中等字号，320～480及高度不超过520px的手机横屏使用紧凑可读版；窄屏标题20px、状态标题18px、正文17px、输入框16px，触控按钮保持44px以上。状态卡、提示条、媒体卡、输入栏和评价弹窗均同步限宽缩放，并为不支持CSS`max()`安全区写法的旧WebView增加普通padding/宽度回退；仍保持`100vh`到`100dvh`回退、页面根不滚动、只允许中间消息区滚动。实际浏览器以320×568、360×800、390×844、412×915、480×854、540×960、750×1334及844×390横屏逐项渲染，全部页面宽高与视口一致、横向溢出为0，长消息只在`.player-messages`内滚动；独立玩家页无运行错误。前端生产构建、8894候选、正式HTTP IP与HTTPS域名的健康/入口/新CSS均通过。正式网页现为`index-7TcoibTN.js`和`index-DACRYVtB.css`，同时保留上一版`index-CVqYnXsR.js`和`index-kehyjKPQ.css`避免旧入口缓存请求404，回滚目录为`web.previous-player-responsive-20260814`；服务端二进制仍为`75321df87720c870c0e8bbec474b3dcf250f4713a4f7eda789c8bf121a2e3e57`，数据库与Cocos源码/安装包均未改变，因此本次网页样式不需要重新发布Android或iOS包。
- 2026-08-14 ChatTool客服工作台已整体放大并改为“首条玩家消息后才入队”：工作台顶栏、会话列表、聊天正文、输入区、玩家资料、备忘与团队弹窗均提高到约13～23px字号和更大的触控区，桌面1440×900、1100×760、800×700以及窄屏600×700实测无横向溢出，1100宽仍保留310/540/250三栏且会话与消息只在各自中间区域滚动。玩家打开页面现在只创建不可见的内部会话，不再写“正在接入”欢迎消息，也不触发自动派单或在线负载均衡；第一条文字、图片、视频或文件成功落库时才把`queue_started_at`重置为实际入队时间并立即自动分配。自动派单、负载/容量、工作台数量、会话列表、详情权限和手动接入都要求存在`sender_type='player'`的消息，因此未发言会话不会显示、计数、占容量或被接入；无本通道客服在线时仍由原有服务端事务门禁拒绝第一条消息。玩家页在未发言时明确提示“请先发送您的问题”，390×844实测文档固定、仅消息区滚动。Go全量测试、竞态测试、vet、Linux amd64静态构建、前端lint/TypeScript/Vite构建、MySQL 5.7查询预演、8894候选和正式公网HTTP/HTTPS页面、健康、新资源及未登录401均通过。正式切换后一个维护周期确认未发言活动会话为0、4条未发言旧排队会话仍未分配；会话/消息/媒体数量保持23/194/4，无迁移和内容删除。正式二进制SHA-256为`75321df87720c870c0e8bbec474b3dcf250f4713a4f7eda789c8bf121a2e3e57`，网页JS/CSS为`index-CVqYnXsR.js`、`index-kehyjKPQ.css`，回滚副本为`bin/chattool.previous-first-player-message-20260814`与`web.previous-first-player-message-20260814`。

## 8L 换皮方向（丁二黄扑克风格，已确认并落地）

- 2026-08-09 客户已确认无扑克主视觉的 `8L` 深海蓝绿、铂金银灰高端会所版；项目必须完全去掉秦字、QIN、秦宫、玉玺、古代宫殿与牌九骨牌等旧元素。登录、大厅、牌桌及各弹层均以银色立体 `8L` 徽章、深海蓝宝石玻璃、黑曜石材质、翡翠青轮廓光和少量香槟金为统一基准。
- “不用红色或少用红色”只限界面背景、按钮、边框与装饰色；标准扑克牌必须保留真实花色，红桃、方块仍用标准红色，黑桃、梅花仍用黑色，不得为统一UI色板把扑克红色改成蓝绿色。
- 本轮客户确认范围是 `assets/resources/UI/panelLogin.prefab` 登录页和 `assets/resources/UI/panelMain.prefab` 中的“发现”大厅页，不是公告、我的或其他子页。两个Prefab当前根节点尺寸均为 `750 x 1334`；客户已否定近似比例的自由排版，后续效果图和实际换皮必须严格使用 `750 x 1334`，并保留现有节点位置、尺寸、高度、层级和交互，只重设视觉材质。
- `panelMain` 的房间列表不是固定“经典房/进阶房/高手房”大卡。`Main/发现/房间列表` 上的 `scrollview2.itemPrefab` 直接指向 `panelMain.prefab` 内部隐藏的 `房间对象` 节点；模板根尺寸为 733×146，其 `房间底框` 为 742×146，列表 `paddingTop=50`、`spaceY=10`。`ScrollItem.Refresh()` 从服务端压缩数组动态填入房间名、状态、剩余时间、底皮、人数、局时和地九王标记。重画大厅时必须按这一动态模板展示，不得自行改成其他房间卡结构。
- 2026-08-08 已核对公开扯旋规则和项目自身 `assets/resources/other/nameconfig.txt`：每位玩家最终使用 4 张牌，必须拆成两个 2 张牌组比较；项目牌型编码中 `丁皇|41:23` 对应大王＋红桃3，`天对|212:012` 对应红桃Q＋方块Q。后续宣传美术不得再画成德州扑克式任意五张扇牌；若展示一手牌，应明确为“2张＋2张”，且牌面必须能组成项目实际牌型。
- 2026-08-08 用户进一步否定在登录/大厅确认图主视觉中直接展示扑克，要求以高级感优先。最新方向完全移除牌面和牌背，以 `8L` 银色立体徽章作为唯一主角，搭配深海蓝宝石玻璃、黑曜石展台、铂金同心环、翡翠青轮廓光和极少香槟金，定位克制的高端私人会所，而非普通扑克扇牌海报。扑克规则仍适用于牌桌与未来玩法说明；若之后再次展示牌面，仍必须遵守上一条的4张、2＋2及项目牌型约束。
- 2026-08-09 客户确认图为 `design/8L-skin-concepts/8L-premium-no-cards-login-v3-750x1334.png` 与 `design/8L-skin-concepts/8L-premium-no-cards-lobby-v3-750x1334.png`，两张均已验证为 750×1334 RGB PNG。登录页保留账号、密码和登录按钮结构；大厅页采用生成主视觉与原布局确定性合成，快速加入、当时的1/2/5/10/20筛选、四条动态房间和底部五栏的原坐标与高度未移动，“发现”为选中态。此前所有秦风、牌九骨牌和直接展示扑克的确认图只作废稿保留；其中大厅数字筛选已于2026-08-12被恢复的“全/小/中/大”分类取代。
- 2026-08-09 已按审计清单替换 730 张运行界面图片，覆盖 `assets/ImagesLuck`、`assets/ImagesXYPK`、`assets/imagesKK` 以及仍被场景/Prefab引用的奖池、牌桌和公共 UI 图。标准扑克牌面、20张用户头像/表情/礼物内容、微信/支付宝品牌图标和二维码占位图不做风格篡改；只更新了两张牌背。生成入口为 `tools/generate_8l_full_skin.py`，固定目标清单为 `reports/8l-skin-runtime-targets.txt`。
- 原版 730 张图片已按原相对目录归档到 `HisImg/qing`，首版为 `8L-premium-v1`，底栏校正版为 `8L-premium-v2`，当前移除通用背景同心圆的版本为 `8L-premium-v3`。统一使用 `./tools/skin_version.sh list|verify|restore <版本>`；恢复只原子覆盖图片，不覆盖 `.meta`，执行前会校验归档哈希和当前 `.meta` 哈希。已实际完成相邻版本双向恢复闭环，各版工作区哈希均通过。
- 换皮保持所有 Prefab、场景、脚本、UUID、九宫格和透明裁剪契约不变。`tools/validate_8l_full_skin.py` 已确认 730 张尺寸/alpha边界/.meta 全部一致、无目标外图片变更、标准扑克牌面与支付品牌图标未改；Cocos Creator 2.4.13 Web Mobile Debug 构建通过，登录运行画布实测为 750×1334。
- `panelMain` 的动态大厅预览由 `tools/render_panel_main_discovery_preview.py` 在内存中克隆真实 `房间对象`，未修改 Prefab；四行仍是 733×146、156px 行距。`drh8` 主桌、20种隐藏弹层和12种操作态均完成 750×1334 离线回归，未发现未解析 Sprite、失败 Label、未支持组件或 SizeMode 错位。由于未使用线上账号，本轮未登录真实服务器核对服务端房间数据；动态预览只验证实际模板和字段绑定。
- 2026-08-09 用户用真实 756×1632 游戏截图对比确认稿，指出大厅底部切换条高度、中央选中按钮和四个入口图片不一致。现只重做 `panelMain/Down` 实际引用的6张图片：`操作台底板.png` 的153px画布保留但上方约61px透明、实际底栏约92px；`秦_发现按钮.png` 的209×146画布保留但主体缩为约142×113的铂金青色六边形；排行榜/公告/钱包/我的改为无实心底块的银白线性图标。所有 Down 节点、点击区域、位置和 Prefab 均未改，Creator 2.4.13 Web Mobile构建及 `.meta`/透明裁剪检查通过；最终预览为 `art_sources/8l/qa/panelMain-discovery-bottom-v2-750x1334.png`。
- 2026-08-13 游戏大厅房间筛选已从数字档位恢复为“全/小/中/大”，并在“大”右侧新增独立“空位”复选条件。分类继续把节点名原样作为 `查询_所有_自创_房间.fillter_01` 发送；空位勾选时发送 `is_have_site="1"`，取消时发送 `"0"`，两种状态都会重新请求第一页，并纳入分页筛选键防止旧分类回包串入。`panelMain.prefab` 直接保存全部控件：四个分类仍由 `过滤` 的 `ToggleContainer` 单选管理，“空位”Toggle嵌套在右侧 `空位条件` 包装节点内，使用现有蓝银圆形勾选/未选中美术，因此可与任一分类同时启用；历史不激活“20皮”节点已复用，不在运行时创建筛选UI。`tools/patch_lobby_empty_and_avatar_refresh.js` 可确定性重建，重复运行无重复节点；Prefab JSON/引用、协议静态检查、`git diff --check` 及750×1334真实大厅模板静态预览通过，服务端空位过滤和四类实际返回范围仍待登录真实大厅联调。
- 2026-08-12 大厅房间列表虚拟滚动已完成修复：`assets/scripts/common/scrollview2.ts` 改为基于 `ScrollView.getScrollOffset()` 计算绝对可见索引，保留固定小节点池并按数据索引取模复用节点；200条数据运行时只创建9个节点，首尾、空列表恢复和3条短列表均通过浏览器运行验证。列表统一使用顶部锚点和真实内容高度，并为短列表和空列表保留1像素不可察觉的滚动余量；大厅 content 上与虚拟定位冲突的 `cc.Layout` 已在Prefab关闭，运行时也会兼容关闭，因此任意房间条数都能弹性下拉并触发 `bounce-top` 刷新，不再补假房间、逐帧改写 `content.y`、布局间距或输出滚动日志。大厅房间列表的 `paddingTop` 已从50调整为10，消除分类栏与首条房间之间过大的视觉缝隙，不影响短列表下拉。`panelMain.ts` 的房间分页改为按滚动最大偏移量判断，增加串行请求、8秒超时、分类切换去串扰、总数/空页停止、房间ID去重与更新；`ScrollItem.ts` 增加异步状态图回调版本校验，避免节点复用后旧图片覆盖新房间；Prefab 恢复0.25秒回弹。Cocos Creator 2.4.13 Web Mobile Debug 构建、Prefab JSON、差异格式、200/200/50分页合并模型以及浏览器运行时的空列表/3条短列表下拉回弹、刷新事件和200条虚拟节点池验证均通过。尚未使用生产账号联调服务器真实分类分页响应；服务端响应未携带请求ID，因此客户端通过请求串行化降低乱序风险。
- 2026-08-12 已修复退出房间期间发牌动作报 `DrhPlayerLogic.GetArrayHandList` 读取未定义 `getComponentsInChildren` 并卡住的问题。根因是 `DrhLogicMgr.ThrowCard2Player()` 只查到了远端玩家的手牌节点但没有回填该玩家的 `transHand`，延迟动作随后进入 `AnimateMoveOneCard()` 时直接使用未初始化缓存；等待退出服务器回包期间仍在执行的发牌序列最容易触发。`DrhPlayerLogic.GetTransHand/GetArrayHandList` 现每次校验并安全回填当前打牌/观战手牌根，空节点或空组件数组直接返回；发牌入口、牌移动完成回调和异步搓牌图片回调均验证场景有效且不处于退出状态。`DrhLogicMgr.PrepareLeaveRoom()` 会在退出开始时停止根节点及全部玩家/牌节点动作、取消玩家延迟回调并忽略迟到的 `PlayerList` 与“状态鸡”；手动退出、返回大厅、GPS拒绝、战绩弹窗退出、解散关闭和场景销毁均接入清理。Cocos Creator 2.4.13 Web Mobile Debug 完整构建及浏览器运行复现通过：远端玩家在 `transHand` 未初始化时可正确取得手牌，退出中再次调用发牌动画被安全忽略。尚未使用在线牌桌账号实测服务器退出回包链路。
- 2026-08-13 已修复一次“登录/大厅切入drh8房间”红错：`Uncaught TypeError: Cannot read properties of null (reading 'push')`，栈顶为 `cc.Node.setParent(base-node.js:382)`。根因是初始导入即存在的 `UIManager.showPanel()` 场景竞态：调用时发起异步 `cc.loader.loadRes("UI/"+strName)`，约1秒场景切换后旧 `Canvas/Normal` 或 `Canvas/Top` 已销毁，迟到回调仍执行 `node.parent = optRoot`，Cocos内部遂对失效父节点的子数组调用`push`；异常还会使同名窗口残留“正在异步加载”状态。`assets/scripts/common/UIManager.ts` 现为每次异步窗口请求记录发起场景，回调成功后再次校验原场景仍是当前场景且原挂载根有效；不满足时直接丢弃旧场景结果，不把旧弹窗挂到新场景。加载成功、加载失败和旧场景丢弃三条路径都只清理与“窗口名＋发起场景”完全匹配的请求标记，旧回调不会清除新场景同名请求；`setPanelInfo()` 不再无条件清理其他请求的标记。同屏的“语音接入完成: Requested device not found”和黄色“Should not set alpha via color”均为独立警告，不是本次红错根因。独立TypeScript ES5转译、差异格式检查及现有Creator预览的Recompile均通过，实际生成的 `library/imports/50/50a003fb-b39a-49be-a409-2a79609cf035.js` 已包含场景/root二次校验，重编译后DevTools控制台为0条消息；尚未主动退出当前在线房间重新跑完整的登录→房间、房间→大厅和快速切场景人工矩阵。
- 2026-08-13 异地运行的多客户端录屏确认房间792768曾低概率出现单端异常；视频中的游戏客户端不在当前Mac本机运行，本机 `localhost:7456` 开发者控制台与该次故障无关，不能作为协议日志证据。轮到玩家“第三3”时头像决策倒计时正常从4秒走完，但该玩家自己的客户端始终没有手牌和操作按钮，超时后服务端正常推进到下一操作人；其他客户端没有同步卡住，因此不是服务端回合未推进。高置信结论限于“单个客户端的发牌/决策UI状态未完成或未自愈”，具体触发顺序尚未百分百坐实。源码存在与现象完全吻合的旧缺口：`发牌_结束_消息.pai_number` 只触发一次延迟发牌，`AnimateMoveOneCard()` 若当时 `持有_手牌_消息.now_pai` 尚未到齐便直接返回，后到手牌只写入 `handCardEx` 而不会补画；操作区又主要依赖牌移动结束回调 `DelayShowCmd2()`，所以会与牌一起缺失，而独立的 `is_action/proxy_countdown` 仍能显示倒计时。`MsgJieXi()` 还在更新 `is_action` 前先按 `role` 判断按钮，进一步放大同批状态切换竞态。Git追溯确认这些关键行为从初始导入提交 `29d6125` 就已存在，当前安全判定与退出保护只把原有提前返回前移，没有新增“缺牌后不补画”的行为；不是近期聚光灯、美术或客服改动新引入。当前未取得异地客户端该次协议原始时序日志，具体哪条消息晚到仍待加结构化日志或人工注入乱序复现。为降低临上线风险，`assets/scripts/logic/DrhPlayerLogic.ts` 只新增倒计时兜底而不重排消息或发牌主流程：仅限本人正在操作、房间和玩家均为running、处于下注/押牌决策且不是看牌/搓牌、倒计时已实际消耗5秒，并且手牌与对应操作区同时不可见时，每个操作回合最多介入一次；本地牌与按钮数据齐全便复用 `UpdateHandCard(false,false,0)` 和无动画 `ShowCmdPad()` 恢复，数据不齐则只请求一次现有全量状态。只要牌或按钮任一正常可见便完全不介入，操作人变化或操作结束会重置保护，不重放发牌动画、不提前翻牌、不修改协议和服务端。TypeScript ES5转译、11组条件分支矩阵、`git diff --check` 及 Creator 2.4.13 临时目录 Web Mobile Debug完整构建通过，构建未改写现有热更新manifest；异地真实牌局、迟到消息注入和触发后可操作性仍待验证，当前补丁尚未打热更新包。
- 2026-08-15 只读审计“服务器正确但仅某一手机出现牌数或牌面错误”后，确认客户端存在两条可独立复现现象的确定性路径。其一，`MsgJieXi()` 分别更新 `handCardEx` 与 `nHandCount`，而 `UpdateHandNoAnimate()` 按 `handCardEx` 画牌、却从 `nHandCount` 起隐藏剩余槽位；当新状态已有3张但旧计数仍为4时，第4个节点不会隐藏。该状态易被每局实际为空实现的 `ResetGameInfo()`、同座玩家 `BaseClone()` 保留旧游戏字段，以及无请求代次的多次全量状态与实时“状态鸡”交错放大。其二，`PKCardInfoScript.SetCardValue()` 的异步牌面资源回调不核对当前牌值或请求序号，旧牌图片若后返回会覆盖新牌图片，形成模型/服务器正确但单机显示旧牌面。另有高风险放大器：状态消息只按 `player_number` 路由而不校验房间、局次、玩家ID或消息序号；发牌延迟/动作没有房间局次及批次门禁且刷新时不取消旧翻牌动作；前台恢复可能重复注册无去重的“状态鸡”监听；`DrhNameManager`牌型兜底会改写传入的真实牌对象。上述均可因单机切后台、重连、全量刷新、缓存及帧率时序不同而只在一台设备暴露；本轮仅分析并用状态模型验证3张加旧计数4会留下第4槽，未修改业务代码，具体线上触发链仍需结构化日志或乱序注入验证。
- 2026-08-15 上述单机牌面异常的临上线最小风险修复方案已确定但尚未执行：第一层只在 `DrhPlayerLogic` 的无动画刷新、手牌复位和单张发牌入口统一计算“安全显示张数”，实际牌数组与服务端计数都有效时取两者较小值，只有实际牌未知的其他玩家才沿用计数，并将结果限制在0～4；所有超出安全张数的牌节点只做隐藏，既不改模型、不补牌，也不请求全量状态。第二层只给 `PKCardInfoScript.SetCardValue()` 增加递增加载序号及当前牌值/节点有效性校验，使旧异步图片回调自动失效。异常发生时可限频记录一次数组长度、计数、座位和丢弃旧图片日志。首轮不实现 `ResetGameInfo()`全字段重置、不改全量状态协议、不重排“状态鸡”、不重做发牌动画和事件注册，避免临上线扩大正常流程影响；验证至少覆盖3/4与4/3不一致、正常1→2→3→4、其他玩家仅有张数、旧图片后返回、切后台恢复和断线全量刷新。
- 2026-08-15 已由真实牌局验证并修复：本局进行中点击“站起围观”，排队按钮曾在`起立_事件`成功后立即出现，等其他玩家下一次打牌/状态刷新后又自动消失，根因是客户端把命令200错误等同于本人已离座。`panelGameView.ts`现新增统一`IsSelfActuallySeated()`，以当前PlayerList映射中0号本地视图的玩家ID是否仍等于账号ID为准；无有效牌桌、账号或玩家信息时按“仍坐下”失败关闭。起立成功回包不再强制`RefreshQueueEntryVisibility(false)`，入口显隐收到false时也会再次复核实际座位，true仍可供坐下/预坐成功提前隐藏。点击“排队”入口和最终提交申请均再次检查同一实际座位，牌桌状态失效时拒绝申请；战局详情的结算退房排队流程及服务端协议未改。四组状态模型验证通过：起立回包但本人仍在座位时隐藏、PlayerList确认真正离座后显示、坐下回包提前隐藏、不支持排队房间隐藏；`panelGameView.ts` TypeScript ES5转译语法、`git diff --check`以及Cocos Creator 2.4.13隔离Web Mobile Debug＋MD5完整构建通过，临时产物已确认包含全部实际座位复核逻辑，正式`build/web-mobile`未覆盖。全项目`tsc --noEmit`仍被历史Node类型、Cocos Vec2/Vec3及同仓Web JSX配置错误阻挡，与本次修改无关；真实牌局仍需复测起立瞬间、下一玩家操作、当前局结束后离座以及观战者正常排队。
- 2026-08-15 战绩详情已修复“排队成功后再次点击排队按钮只提示已经排队，无法查看状态或取消”的入口语义。`panelRecordInfo.ts`现在把`queueActive`优先解释为“查看现有排队”，不再套用新申请资格；状态变化会刷新入口，真正进入assigning/switching/pre_sitting时仍按原逻辑关闭战绩页。`panelGameView.CanOpenQueuePanelFromRecordInfo()`在非导航阶段允许已有队列直接打开顶层`panelQueueMatch`，保留已完成退房确认且不重复申请；弹窗继续用现有快照显示人数、顺位、名单和“取消排队”。`QueueMatchManager`另对申请回包中的`status=queued`及“已经排队/已在排队/正在排队/already queued”做窄范围权威恢复：本地转为queued并静默查询顺位，普通金币不足等失败不受影响，从而覆盖重连或迟到回包导致的本地状态缺失。已排队坐下显示、匹配切换拦截、首次结算申请和普通失败隔离条件模型、三个TS文件ES5转译、`git diff --check`及Cocos Creator 2.4.13隔离Web Mobile Debug＋MD5完整构建通过；临时构建误改的3.0.10 Manifest已准确还原，正式构建与服务器未改。真实客户端仍需验证排队成功→关闭弹窗→战绩页再次打开→查看顺位→取消排队。
- 2026-08-13 牌桌已新增可选“下注聚光灯”。`panelGameView.prefab` 与 `drh8.fire` 都包含编辑器可见的蓝青色半透明光柱节点及“牌局设置 → 下注聚光灯”开关，开关使用本地配置键 `下注聚光灯`，首次默认开启；玩家主动关闭后保存的本地值0继续生效，不会在下次启动被强制改回。素材和两份UI可由 `tools/generate_betting_spotlight.py`、`tools/patch_betting_spotlight_ui.js` 确定性重建。运行时只在房间为 `running`、服务端玩家 `is_action=True`、状态同时包含“决策”和“下注/押牌”，且该玩家现有 `PlayerInfo/lightBK` 操作倒计时圈确实可见时显示；倒计时圈是最终权威目标，避免同批消息中旧玩家尚未清除的 `is_action` 把光束错误指向上一位操作人。操作人切换时以牌桌中心为轴固定逆时针旋转0.28秒，整轮下注结束淡出。真机截图确认 Cocos 中锚点在素材底边，PNG底部对应牌桌中心、顶部对应玩家；光柱现为底部窄端位于牌桌中心、顶部宽端朝玩家，角度按局部正Y光轴和头像中心计算。第二轮真机截图暴露 Cocos Creator 2.4.13 的 `cc.rotateBy` 会在构造时反转传入角度，导致已算出的逆时针正角度沿相反方向旋转并经过错误的竖直位置；现改为传入负增量，使引擎内部最终按目标正角度持续逆时针旋转。每个座位都通过头像世界坐标计算独立距离，Y缩放以素材总高减去52像素末端渐隐区作为有效长度，因此实体光线精确到头像中心，剩余柔光越过头像后自然消失，不再因座位远近提前截断。宽端从原约206像素收窄到约98像素。每局在 `StartGame` 和“玩家_开局_事件”即把 `bettingSpotlightCardsDealt` 复位为否，发牌前与发牌过程中无条件隐藏；只有最后一张牌0.2秒客户端移动动画真正完成后才允许显示。断线重进时仅在全量状态已经包含手牌且处于下注决策状态时恢复“已发牌”门禁。连续服务端状态消息仍合并30毫秒，关掉设置或退出房间会立即停止动作并隐藏。Prefab/场景引用、首次默认开启、素材宽度/渐隐、四组跨座位旋转方向、三种目标距离末端位置、编译产物中的倒计时圈判定与发牌门禁，以及 Cocos Creator 2.4.13 Web Mobile Debug完整构建均通过；最终修正版仍需在真实在线牌局中复测连续下注、跳过、弃牌和收灯时机。
- 2026-08-13 已修复“聚光灯大部分牌局正确、偶尔整轮连续错位”的客户端竞态。根因是 `ShowBettingSpotlight()` 每次刷新都会先 `stopAllActions()`，同一操作玩家在0.28秒旋转期间收到第二条状态/倒计时消息后，第一次旋转停在中间角度，旧 `samePlayer` 分支又把剩余旋转强制清零；消息密集时会整轮连续积累错位。现缓存目标玩家、目标角度和目标长度：同一目标的重复刷新在停止动作之前直接返回，保留正在执行的旋转；只有玩家真正变化才重算逆时针动作；同一玩家若因布局变化导致目标坐标变化则直接校准，避免微小反向位移绕一整圈；每个旋转带动作序号，结束时再次把角度、长度和透明度精确吸附到本次目标，隐藏或新动作会使旧回调失效。头像世界坐标、中心点、长度和发牌门禁未改。Babel TypeScript解析、TypeScript ES5转译、重复目标不再触发停止动作、旧 `samePlayer` 强制清零移除及结束吸附静态回归断言通过；仍需在线牌局复测同一玩家密集消息、连续换人、跳过/弃牌和整轮收灯。
- 2026-08-13 牌桌左上角 `ConfigMain` 菜单已在“退出房间”正上方新增“联系客服”按钮，`panelGameView.prefab` 与 `drh8.fire` 均直接保存该按钮以便 Creator 预览和手工调整；点击后复用现有 `UIManager.showPanel("panelKefu", ShowPanelMode.Cover)` 打开游戏内客服覆盖层，并沿用菜单原有点击入口自动收起菜单。按钮使用新增 `assets/ImagesLuck/游戏内/联系客服.png`，`tools/create_room_customer_service_asset.py` 和 `tools/patch_room_customer_service_menu.js` 可确定性重建资源及两处布局；菜单高度由517增至582，退出按钮仅下移一行。PNG尺寸/Meta/UUID、场景与Prefab引用、按钮调用链、TypeScript语法、JSON引用完整性和静态菜单预览均通过，真实牌桌点击及客服网络交互仍待 Creator/真机验证。
- 2026-08-13 奖池弹窗的 `assets/ImagesLuck/奖池/比列.png` 已只恢复三行12张示例牌的正常象牙白牌面、红色花色和红色牌字，周边蓝银面板、概率圆环、文字和布局保持原8L美术不变。`tools/jackpot_card_colors.py` 固定12个牌面矩形并从原版归档逐像素恢复，`generate_8l_full_skin.py` 与 `generate_qin_drh8_skin.py` 已接入该保护，后续重建不会再次把牌面整体染蓝；已验证12个牌面区域与原版逐像素一致、区域外与现有蓝银版本逐像素一致，并通过实际奖池弹窗静态预览，Creator内动态打开仍待验证。
- 2026-08-13 当前随牌桌聚光灯与桌布/牌背资源提交的本地热更新清单最终版本为 `2.0.14`；Cocos/热更新工具在构建结束后会延迟递增版本并改写 `assets/resources/project.manifest`、`version.manifest`，提交前需等待并再次检查工作区。该提交只保存本地清单和资源摘要，不代表已上传热更新服务器。
- 2026-08-12 牌桌8个玩家头像上的决策亮框和黑色倒计时遮罩已恢复为圆形。两者均通过UUID `a97723cd-a490-419e-9817-9bdd4c61df72` 使用 `assets/ImagesLuck/游戏内/yuan.png` 的径向填充 Sprite；头像本身虽有圆形 Mask，但倒计时节点位于 Mask 外，不会继承头像裁剪。方形根因是 `tools/generate_8l_full_skin.py` 对该图返回无透明通道的纯色 RGB，后续 `enforce_alpha_contract()` 只保留裁剪边界而把边界内部填成矩形。现已从原版归档恢复185×186圆形透明遮罩，并把生成器改为中性白色RGBA且继承原alpha，使运行时代码仍可正常着色为黑/绿/橙/红；资源路径、UUID、尺寸和 `.meta` 均未改变。单资源重新生成、圆形alpha行宽检查、drh8场景与panelGameView Prefab共32处引用检查、JSON、`git diff --check` 和 Cocos Creator 2.4.13 Web Mobile Debug构建通过。
- 2026-08-13 玩家头像“留坐”状态遮罩已在 `drh8.fire` 与 `panelGameView.prefab` 的8个座位统一修正：`Head/留坐` 根节点及 `bk` 黑色圆底由80×80扩大为90×90，与头像外框尺寸一致；黑色圆底按最终确认保留原半透明效果，透明度为170，不使用完全不透明。文字、倒计时位置、SpriteFrame UUID及运行逻辑保持不变；`tools/patch_leave_seat_overlay_size.js` 可确定性重复应用并校验8个头像结构。两份JSON的16组尺寸、透明度170、CUSTOM尺寸模式、原UUID、脚本语法和差异格式均通过检查。修改发生时若Cocos仍打开旧 `drh8` 场景，Inspector可能继续显示内存中的80×80；用户重启Creator后已确认90×90正常，后续外部修改时仍应重新加载场景，避免旧内存覆盖磁盘内容。真实留座倒计时仍待在线牌桌验证。
- 2026-08-09 用户发现公告等多个主页下半部出现三层青色同心圆。根因是 `tools/generate_8l_full_skin.py` 的 `common_background()` 主动把圆叠加到通用背景，而 `panelMain` 根节点及我的、推广、公共弹层会复用这类背景；不是 Prefab 节点或业务数据。生成器现已删除该装饰，并只重建 `assets/ImagesLuck/公用/背景.png`、`assets/ImagesLuck/我的/我的背景.png`、`assets/ImagesXYPK/推广/背景.png`、`assets/imagesKK/公用/背景1.png` 四张图。公告真实 Prefab 合成预览 `art_sources/8l/qa/panelMain-notice-no-rings-750x1334.png` 已确认无圆，Creator构建、`.meta`、尺寸及 v2/v3 双向恢复均通过。
- 2026-08-13 大厅“游戏公告”不显示可见滚动条，但已支持手指上下拖动查看任意长度内容。进入大厅自动弹出的 `panelNotifyView.prefab` 已把原固定高度 Label 改为 Prefab 内真实 `ScrollView/Mask/Content/Label` 结构，文字使用 `RESIZE_HEIGHT` 自动换行增高；`panelMsgView.ts` 赋值后统一 CRLF/CR 为 LF，按自动换行高度与显式空白行数量的较大值刷新 Content 并回到顶部，因此尾部空白行也不会被吞掉。大厅公告页 `公告6` 原有无滚动条 ScrollView 同步由 `panelMain.ts` 刷新 Label/Layout/Content 高度，`公告8` 复用同一逻辑。三份脚本的 TypeScript 解析、Prefab JSON、层级/滚动方向/无可见滚动条/字号行高和运行脚本契约以及 `git diff --check` 已通过；完整通知美术校验仍被现有 `秦_通知弹窗底.png` 的97个强蓝/青像素规则拦截，与公告格式逻辑无关。客户端代码尚未打热更新包，也未在 Creator 运行窗口用线上长公告实际拖动回归。
- 2026-08-13 XuanManager 公告纯文本保真版本已更新正式环境：候选 `127.0.0.1:8892` 的首页、健康、未登录401、静态哈希和启动日志通过后切换正式8891；公网首页与健康200、公告接口未登录401、二进制及静态资源上传/正式哈希一致，候选进程和8892端口均已停止。正式二进制 SHA-256 为 `dea2f9387fa4ad65486e261277aa61a33c8fd9455ac1a8c19deaa7f5c0c61936`，资源为 `index-Ds-LxAjs.js`、`index-DTUuXfLS.css`；回滚文件为 `bin/xuanmanager.previous-announcement-format-20260813` 和 `web.previous-announcement-format-20260813`。本次发布未修改当前公告内容。
- 2026-08-13 公告1:1编辑区已作为纯前端更新发布正式环境：候选8892首页、健康、公告接口未登录401和静态哈希通过后，仅原子切换 `web` 目录，没有重启8891、修改数据库或公告内容。公网首页/健康200，正式资源为 `index-CarV0V_b.js`、`index-DSEGIWzU.css`，哈希与本地构建一致；候选进程和8892端口均已停止，回滚目录为 `web.previous-announcement-editor-wysiwyg-20260813`。
- 2026-08-13 头像库为100张：保留原1～20编号和UUID，新增21～100共80张256×256中国人社交头像；生成母版、总览及可重建脚本位于 `art_sources/avatars/social_avatars/` 和 `tools/import_social_avatar_sheets.js`。注册头像弹窗由 `panelLogin.prefab` 内20个固定按钮和真实“换一批头像”按钮组成，每次打开或刷新从100张中随机展示20张，注册选择免费。游戏内“我的→头像”资料页现也在 `panelMain.prefab` 内保存真实“刷新”按钮和常驻金色提示“更换头像每次收取10元”，刷新只重抽20个候选且免费；只有所选头像与当前头像不同时才弹出“更换头像将收取10元”确认，同头像或仅改昵称不收费。确认后发送大厅 `玩家_消费_命令`，`consume_type="换头像"`、`money=1000`（分，即10元），确认按钮在回包前禁用，只有服务端返回0x200后才设置 `photo/name` 并提示扣费成功；失败则不改头像并显示服务端原因，8秒超时不会按成功处理。`tools/patch_lobby_empty_and_avatar_refresh.js` 可重建Prefab控件，`tools/validate_local_avatar_flow.py` 已覆盖刷新按钮、10元提示和成功回包后更新约束；100份资源/Meta/UUID检查、750×1334头像选择静态预览和差异格式通过。XuanManager注册接口的 `avatarIndex` 范围已发布为1～100；本轮游戏内实际扣款、失败回包和余额刷新仍待真实账号联调。
- 2026-08-11 已修复新增头像21～100的批量裁剪错位。根因是4张ImageGen母版的分隔线、行高和外框不等宽，旧脚本按 `width/5`、`height/4` 平均切割，导致越靠下的行偏移越明显，第3批还会把带大段白色行间距的椭圆头像错误放大。`tools/import_social_avatar_sheets.js` 现按每批真实内容边界裁剪，第3批使用逐格边界并恢复为正方形；重建只覆盖PNG，已有 `.meta` 和UUID不再重写。编号总览增加1～100标签便于复核；本轮已重新生成80张资源，确认原1～20和全部Meta零改动，`tools/validate_local_avatar_flow.py` 验证100张尺寸、UUID及随机20流程通过。
- 2026-08-11 注册弹窗默认头像不再固定为头像1：每次点击“注册账号”打开资料弹窗时，`panelLogin.openRegisterPanel()` 都调用 `ImageManager.RandomAvatarIndex()` 从1～100重新随机一个默认头像；用户随后仍可打开头像选择弹窗手动更换，最终提交的仍是当前预览头像序号。初始化阶段保留头像1仅用于弹窗尚未打开时的安全占位，不影响每次打开时随机。
- 2026-08-11 注册表单的邀请码、昵称、登录账号、密码和确认密码未通过本地校验时，不再只更新资料框底部状态文字；`panelLogin.showRegisterError()` 会先收起当前输入框键盘，再用现有 `panelMsgView` 单确认按钮小弹窗显示具体错误原因，同时保留底部错误文字供关闭弹窗后核对。服务端注册错误和网络错误原本已经使用同一消息弹窗，现注册失败反馈形式统一。
- 2026-08-10 防盗号设备绑定只要求“同一手机卸载 App 后重新安装，设备 ID 保持不变”，不要求恢复出厂、刷机或换机后仍识别为原设备。Android 使用同一签名和系统用户下的 `ANDROID_ID`；iOS 首次生成随机 UUID 并保存到关闭同步的 `ThisDeviceOnly` Keychain 项；恢复出厂、刷机、系统用户变化、签名变化或 Keychain 被清除均按新设备处理。结合系统规模和维护成本，用户已确认本期不做设备 ID 加密、Hash或HMAC，`third_marketing_info.device_id` 明文保存并由 XuanManager 与 KB 按相同格式规则精确比较；数据库字段使用 `ascii_bin` 或SQL显式 `BINARY`，避免MySQL默认排序规则忽略大小写。普通接口、日志和审计仍不输出完整值，普通后台只显示掩码，高权限查看完整值必须审计且不可编辑。
- 2026-08-12 模拟器多开对防盗号的影响取决于虚拟设备实例，而不是窗口数量。当前 Android 桥接直接读取应用进程看到的 `ANDROID_ID`；同一 AVD、同一 Android 用户和同一签名 APK 通常得到相同值，独立新建的 AVD 通常得到不同值。第三方模拟器或同一 AVD 的快照/磁盘克隆可能复制相同标识，这是基于其克隆方式的推断，不能对所有模拟器作统一保证；若两个实例返回相同值，KB 会把它们视为同一绑定设备，防盗号无法区分，但账号单点登录仍会让最后一次登录生效。联调具体模拟器时应比较客户端原生桥接实际返回值的摘要，不应只按“多开窗口”推断。
- 防盗号主链路为“注册提交设备标识、KB 登录校验、Account 同步开关、设置页切换、后台解绑”。2026-08-10 客户端和 XuanManager 范围已完成本地实现：登录第三参数会携带版本、平台、设备 ID 和场景 JSON；无法识别设备时仍发送旧字符串“登陆”，保证未开启账号兼容，KB 对已开启账号负责返回终止型错误，客户端会停止自动重试。注册和大厅设置均使用 Prefab 固定节点，默认关闭；开启前明确提示本机绑定风险，最终状态只以 `Account.anti_theft_on` 属性同步为准，`kbengine.js` 已增加对应 setter 事件。KB 实际错误码、属性声明、开关命令和登录校验仍由 KB 团队实现并联调。
- KB 服务端责任边界：Loginapp/账号认证链路在密码验证成功后解析登录 `datas`，按平台和明文 `device_id` 精确比较并返回不会触发客户端重试的专用错误；Account 实体声明并同步 `anti_theft_on`，处理带路由参数的设置命令和原子状态切换；内部管理链路在解绑后同步在线实体并按需使旧会话失效。KB兼容旧客户端固定字符串“登陆”，但只能在防盗号关闭时放行；不能按老板角色做无审计的永久绕过。注册表单、设备标识获取和注册HTTP接口分别属于客户端与XuanManager。
- 供 KB 服务端修改和三方联调使用的完整文档为根目录 `防盗号功能接口文档-KB服务端修改版.md`，当前版本1.2。文档已改为明文 `device_id` 方案，并包含原生/Web设备来源、数据库字段、登录判断、专用错误、Account属性、`reqAccountCommand`双参数协议、原子开关、后台解绑、特殊账号约束、分阶段上线和验收清单；错误码实际数字、KB真实分发文件、结果事件和内部解绑命令准确名称仍需KB结合源码回填。
- 另有只供 KB 团队执行的独立文档 `防盗号功能-KB服务端改动说明.md`，不包含客户端设备ID实现、Prefab/UI、注册HTTP接口或XuanManager页面改动；只保留KB所需的数据库迁移、Loginapp输入与校验、专用错误码、Account属性、开关命令、原子并发、内部解绑、会话一致性、特殊账号、日志监控、上线顺序及KB验收/交付清单。后续发送给KB实施时优先使用该独立文档，整体文档继续用于三方联调。
- 网页版采用简化的“浏览器资料绑定”：首次访问用 `crypto.getRandomValues()` 生成随机 UUID，IndexedDB 主存储、localStorage 镜像，在同一来源和浏览器资料内复用，平台值为 `web`。清除网站数据/Cookie、无痕模式结束、更换浏览器、协议、域名、端口或设备后可能生成新 ID；已开启账号不会自动覆盖旧绑定，只能在原浏览器关闭或经客服身份核验后解绑。该方案只防简单密码盗用，不能宣称绑定物理设备或达到 WebAuthn 强认证。
- 客户端设备 ID 统一由 `assets/scripts/logic/DeviceIdentityManager.ts` 管理；Android/iOS 项目级源码分别位于 `native/android/device/` 和 `native/ios/device/`，Creator 重建原生工程后运行 `tools/sync_android_device.py`、`tools/sync_ios_device.py` 接回生成工程，不修改或覆盖根目录 `runtime-src.zip`。两套同步脚本连续执行两次均通过；Android桥接源码已用 android-35 `javac` 编译，iOS桥接源码已用 iphoneos SDK做 ARC 语法编译且 Xcode 工程可解析。
- XuanManager 新增 `game.anti_theft.view/unbind` 两项权限和 `/game/anti-theft` 菜单；`GET /api/game/anti-theft` 只返回脱敏设备 ID，支持状态、平台和账号筛选；`POST /api/game/anti-theft/{playerId}/unbind` 强制原因类型、2～120字身份核验说明和显式确认，只调用 KB 内部命令 `异步_解除_玩家_防盗号绑定` 并回读最终状态，不直接修改游戏表，审计不记录完整设备 ID。注册接口接收并校验防盗号设备字段；`third_marketing_info` 所需六个KB字段已在正式库核对存在。Go测试/vet、React lint/生产构建、Cocos Creator 2.4.13 Web Mobile构建均通过；750×1334浏览器实际检查注册弹窗、头像选择和防盗号开关无错位。KB实际登录错误码、开关回包和内部解绑命令仍需使用真实账号做端到端联调。
- 2026-08-10 防盗号版 XuanManager 已发布到正式 `127.0.0.1:8891` 和公网 `http://154.37.155.17/xuanmanager/`：迁移 `013_game_anti_theft.sql` 已应用，模块28及权限2801/2802已落库，正式二进制和前端哈希与本地候选包一致，本机/公网健康接口、新JS/CSS均返回200。此前开启防盗号注册返回 `INVALID_JSON / 请求内容格式不正确` 的问题已修复；公网使用格式正确的防盗号字段和故意无效邀请码验证，已进入业务校验并返回 `INVALID_INVITATION_CODE`，使用非法设备号则返回“设备标识格式不正确”，均在写库前结束。后台接口未登录返回401，列表SQL已用正式库只读 `EXPLAIN` 验证；未执行真实注册、真实解绑或修改玩家状态。回滚文件为 `bin/xuanmanager.previous-anti-theft-20260810` 与 `web.previous-anti-theft-20260810`，8892候选实例验证后已停止。
- 2026-08-13 正式环境首次实际点击客服解绑时，玩家 `472661`、`628099` 的多次请求均由 XuanManager 日志确认收到 KB `ret_code=769`，尚未进入状态回读。服务器本机8890健康，已注册的只读奖池命令返回512；使用不存在玩家探测 `异步_解除_玩家_防盗号绑定` 返回 `769 + 空对象`，与明确不存在的命令名返回完全一致。当前结论是正式 KB 尚未注册该建议命令名，或实际命令名与 XuanManager 约定不一致；不是后台原因字段、玩家绑定状态或回读超时。修复应由 KB 端实现/注册文档规定的原子解绑命令，或提供正式命令名和参数契约后同步 XuanManager，不能让后台绕过 KB 直接改表。
- 2026-08-10 防盗号跨浏览器复测状态：最初独立浏览器仍可进入大厅，KB调整后无痕浏览器已被20013登录链路拒绝，说明生产设备校验现已生效；但KB返回的防盗号数字错误码未出现在 Loginapp 下发的 `serverErrors` 表中，旧 `Client_onLoginFailed` 直接读取缺失项的 `.name` 后抛出异常，导致业务层收不到登录失败事件、loading不关闭并持续自动重试。客户端现已对未登记错误码使用 `UNKNOWN_SERVER_ERROR_<数字>` 安全日志并始终派发失败事件；业务层会把未登记错误作为终止型失败，关闭loading并只显示一次不含数字错误码的通用提示，实际数字仅保留在开发日志中。KB仍需把四个防盗号符号及描述正式加入实际 `server_errors.xml`（或项目对应错误配置）并重启/发布 Loginapp，使客户端能显示“当前设备不是绑定设备”等精确提示。
- 2026-08-11 防盗号开启/关闭确认框共用 `panelMsgView.prefab`；“取消”节点和按钮组件原本都存在，`panelMsgView.ts` 也会在传入确认回调时激活并分列按钮，但该节点的 `cc.Sprite.spriteFrame` 为空，因此运行时只有“确定”可见。现已在 Prefab 中引用项目已有的深蓝描边“取消”SpriteFrame `9cfb1894-48f6-40c0-a051-9bf7e8a8814b`；不改注册页或设置页布局，两个防盗号流程同时修复。
- 2026-08-11 注册登录账号最终规则为6～16位英文字母或数字：客户端注册正则、注册输入框上限和提示文字、登录页账号输入框上限、XuanManager公开注册接口、Go边界测试及接口文档保持一致。注册页防盗号开关下方常驻提示简化为“开启后仅当前设备可登录”，不再显示更换设备联系客服的说明；开启前二次确认弹窗仍保留完整风险提示。

## 启动与业务链路

```text
login.fire
  -> StartGameManager
  -> GameDataManager（持久化单例、KBE、心跳、重连、移动 SDK）
  -> panelUpdate（Web 跳过热更，Native 使用 AssetsManager）
  -> panelLogin
  -> panelMain（大厅）
  -> drh8.fire（牌桌）
```

- `assets/scripts/StartGameManager.ts`：启动入口。
- `assets/scripts/GameDataManager.ts`：全局会话、网络、登录状态、重连和场景跳转中心。
- `assets/scripts/common/UIManager.ts`：把 `assets/resources/UI/<面板名>.prefab` 动态加载到 `Canvas/Normal` 或 `Canvas/Top`。
- `assets/scripts/common/GameDef.ts`：服务器、状态、数据结构及公共常量。
- `assets/scripts/UI/panelMain.ts`：大厅、房间列表、钱包、个人中心和管理入口。
- `assets/scripts/UI/panelGameView.ts`：牌桌 UI 与玩家交互。
- `assets/scripts/logic/DrhLogicMgr.ts`：房间和牌局总状态。
- `assets/scripts/logic/DrhPlayerLogic.ts`：单座位状态、发牌、操作和结算表现。
- `assets/scripts/logic/DrhNameManager.ts`：牌型名称与点数。
- `assets/scripts/kbe_scripts/kbengine.js`：定制 KBEngine WebSocket 客户端。
- `assets/scripts/kbe_scripts/Account.js`：业务 RPC 包装及服务端事件转发。
- `assets/scripts/mobile/MobileManager.ts`：微信、语音、统计、GPS、图片和原生桥接。
- `assets/scripts/UI/panelUpdate.ts`：当前实际使用的热更新逻辑；`logic/UpdateManager.ts` 基本未使用。
- 2026-08-11 已静态确认 Native 启动强更流程：`StartGameManager` 打开 `panelUpdate`，其 `UpdateWebConfig()` 会延迟0.5秒调用 `checkUpdate()`；远端版本相对本地只增加补丁位（如1.0.28→1.0.29）时执行热更新，主版本或次版本提高（如1.0.28→2.0.1或1.1.0）时打开 `panelVertion`，不会进入 `panelLogin`。当前项目内及远端清单均为1.0.28，`.hot-update-config.json` 下一版本为1.0.29。发布原生不兼容版本时，新Android/iOS安装包必须内置与远端一致的2.0.1清单，否则新包也会被强更弹窗拦住；远端清单应在新版安装包就绪后最后切换。历史下载配置原本来自已停用的9000端点，当前不再依赖下载地址：`panelVertion.ts` 的“确定”和“取消”均直接调用 `cc.game.end()` 关闭客户端，不跳转网页；该脚本修改必须进入新版原生包或在切换大版本前先以普通补丁热更新到全部旧客户端，否则仍运行旧缓存脚本的客户端可能继续执行历史下载逻辑。网页版直接跳过Native热更检查，不受该强更版本影响。

## 网络与牌局协议

- 网络层使用 KBEngine/WebSocket 和全局字符串事件总线。
- `PlayerList` 是房间/玩家全量快照；`状态鸡` 是逐座位增量状态。
- 大量协议以手拼 JSON、中文命令字符串及 `reqHallCommand`、`reqGameCommand` 等接口传递。
- 2026-08-11 当前游戏内玩家“赠送金币”不是HTTP接口：`panelGivePad.ts` 通过Account实体RPC `reqAccountCommand(param, content)` 发送，`param` 是 `header=调用_方法_Exchange2`、`target_guuid`、`money_value`、`money_type=gold`、`user_pwd`、`client_version=2022032201` 组成的JSON字符串，`content` 固定为 `P@调用_方法_Exchange2`。金额在客户端不做乘除100或精度转换，直接以输入字符串传给KB服务端；赠送前用 `reqHallCommand` 的 `查询_用户_名字` 命令核对目标ID，结果由 `onExChange`/`onExChange2` 事件返回。
- 2026-08-12 游戏内所有调用 `调用_方法_Exchange2` 的赠送金币入口已统一为正整数金额：`panelMain` 的大厅赠送页、`panelGivePad` 最终确认弹窗和旧 `panelManager` 赠送页的金额 EditBox 均改用 `PHONE_NUMBER` 输入模式，Prefab 占位提示为“请输入整数”；提交前使用相同规则拒绝小数、负数、0、科学计数法、空格和其他非数字内容，并以“赠送金额只能输入大于0的整数！”弹窗提示，不向KB发送非法请求。大厅金额仍可留空后在最终确认弹窗输入；旧管理页“扣除”模式先校验用户输入的正整数，再沿用原逻辑添加负号。Prefab JSON、目标输入框路径、11组金额规则用例和 `git diff --check` 已通过，Creator真机软键盘与真实赠送回包仍待联调。
- 修改事件名、字段名、动作编码或中文命令前，必须同时核对服务端协议；本仓库不包含完整服务端实现。
- 下注动作中可见的特殊编码包括：跟 `-1`、敲 `-3`、休 `-5`、滚 `-6`、丢 `-7`。
- 房间外层状态大致为 `init -> ready -> running -> end`。

## 已确认的高优先级问题

以下来自 2026-07-22 的静态审查，尚未全部经过真实服务器和原生包运行验证。

### 安全与更新

- 热更新清单使用 HTTP，`panelUpdate.ts` 的资源校验回调无条件返回 `true`；更新包包含可执行脚本。优先改为可信 TLS、签名清单及真实完整性校验。
- 客户端源码中存在第三方服务凭据，微信换取 Token 也在客户端完成；不得在文档、日志或回复中复述具体值。应视为已暴露并安排轮换。
- 登录密码明文写入 `localStorage`。
- 钱包部分接口通过 HTTP 传递姓名、银行卡、手机号、身份证等敏感信息。
- Git 中存在签名文件，旧原生配置中还有明文签名信息。不要输出具体值；修复时不仅要删当前文件，还要评估轮换及清理历史。
- 当前 KBE 配置可见明文 WebSocket；支付、登录、更新和业务接口均需确认线上是否已统一使用 TLS。

### 确定性功能缺陷

- 以下活跃调用没有对应的 `assets/resources/UI/<名称>.prefab`，触发时会加载失败：
  - `panelCreateRoom`
  - `panelJiangli`
  - `修改预留信息`
  - `panelSJB`
  - `panelSJBWeb`
  - `panelTalkMsg`
  - `panelDrhClubEnd`
- `UIManager.showPanel()` 加载失败时不清理加载中标记，同名面板后续可能一直无法再次加载；并发加载也只共用一个字符串标记。
- 解散投票使用 `map[id] = value` 写入，却用 `Map.has/get` 读取，恢复投票状态失败。
- 玩家/座位映射更新前未完整清理，断线状态可能落到旧座位或被稀疏键判断漏掉。
- `DrhPlayerLogic` 在读取当前消息的 `is_action` 前先处理 `role`，部分操作界面可能使用上一条消息状态。
- 观战者刷新隐藏牌时索引未递增，会反复更新第 0 张牌。
- 牌型点数兜底会直接修改传入牌对象，存在污染真实手牌数据的风险。
- VIP 数据界面会把 `bIsVip` 强制设为 `true`；开通 VIP 与扣款又是两条独立请求，服务端必须保证权限、幂等和原子性。
- Android 首次申请定位权限存在 `locationManager` 空指针路径。
- Android Manifest 的微信回调 Activity 包名与实际 Java 类包名不一致。
- JS 调用的录音播放方法名是 `PlayRecord`，Android/iOS 实现的是 `PlayFile`。

### 构建与原生配置

- `settings/builder.json` 是横屏，但归档原生配置中存在竖屏设置，打包前需确定唯一基准。
- 旧 Android 配置为 SDK 28、仅 `armeabi-v7a`，并包含动态依赖、HTTP Maven、过宽或废弃权限。
- 2026-07-23 Cocos 将应用显示名从 `qing` 改为 `Qing` 后重生成 Android 工程，同时把 Gradle 模块名和 namespace 也改成了大写形式；显示名可以保留 `Qing`，但内部模块名与 namespace 已规范为小写 `qing` / `com.fireball.qing`。本次实际阻断构建的根因是旧 AGP 8.0.2 无法正确处理 API 35 的资源表，并叠加外置 exFAT 盘产生的 `._*` Gradle 缓存、无用 `instantapp` 模块和 NDK 版本漂移。当前生成工程已升级为 AGP 8.7.3 + Gradle 8.9 + Build Tools 35.0.1，固定 NDK 25.1.8937393，移除 `instantapp`，并把 Gradle 项目缓存与构建中间产物迁到本机临时盘；`:qing:processDebugResources` 与完整 `:qing:assembleDebug` 均已通过。Cocos 再次重生成工程可能覆盖这些生成目录修改，需复查或固化到构建模板。
- 2026-07-23 旧 Cocos Android `app/build.gradle` 通过 `variant.mergeAssets.doLast` 从 `runtime-src` 复制游戏资源，但没有声明这些目录为任务输入，Cocos 重构建后 Gradle 仍可能把 `mergeReleaseAssets` 判为 `UP-TO-DATE`，从而复用旧 APK 资源。当前生成工程已将 `mergeAssets` 设置为每次执行，重新构建后的 APK 内 `assets/main/index` 与 Cocos 最新输出 SHA-256 一致；实际 Release 输出因 exFAT 规避策略先生成在本机临时构建目录，再复制到 `proj.android-studio/app/release/qing-release.apk`。再次由 Cocos 覆盖原生工程后需确认该保护仍存在。
- Android Studio 的“Generate Signed APK”若把输出目录直接选为外置 exFAT 盘上的 `app/release`，macOS 可能同时产生 `._qing-release.apk`、`._output-metadata.json`；下一次出包在清理该目录时会报 `Unable to delete directory`。该隐藏副本可以安全删除，它不是 APK 内容。当前生成工程的 `app/build.gradle` 已加入 `cleanReleaseAppleDouble`，并作为 `preReleaseBuild` 前置任务，每次 Release 构建前自动清理 `app/release/._*`；任务顺序已通过 Gradle dry-run 验证。若 Android Studio 在启动 Gradle 前由 IDE 自身先清理目标目录，仍应改用本机 APFS 输出目录。
- iOS 配置包含旧 SDK 路径、任意网络加载和占位 Universal Link；发布前需重新核查。
- `runtime-src.zip` 内包含构建缓存和旧工程文件，不能直接当作干净、当前可发布的原生模板。
- 2026-07-23 Android Studio 首次显示 Release 成功时，实际生成的是 `instantapp/release/instantapp-release.apk`，其包名为 `org.cocos2dx.javascript`，不是主游戏预期的 `com.fireball.qing`。重新选择主模块签名后，正式包生成在 `build/jsb-link/frameworks/runtime-src/proj.android-studio/app/release/qing-release.apk`，已核对包名为 `com.fireball.qing`。签名打包时应选择 `qing`（部分 Android Studio 界面可能显示为 `app`）模块，不能选择 `instantapp`。普通 Gradle 任务的中间输出因根 Gradle 的 `buildDir` 重定向而位于系统临时目录；Android Studio 签名向导指定的最终 APK 则可能直接写入工程的 `app/release/`。
- 2026-07-23 新生成的 iOS 工程已从历史参考中最小迁移仍在使用的原生能力：`AppController` 提供按需申请“使用期间”定位、GPS 坐标读取、截图保存到相册以及剪贴板读写；`Info.plist` 只增加定位和“仅添加照片”用途说明。微信授权、TalkingData、GCloudVoice、旧头像相册/相机选择均未迁移；头像流程当前使用本地头像库，不需要相机或读取相册权限。
- iOS 工程已为定位补充 `CoreLocation` 链接。Creator 2.4.13 自带的旧胖包 `libwebp.a` 会被当前 Xcode 链接器以成员未按 8 字节对齐为由拒绝；已从其 arm64 slice 原样重新封装为项目内 `proj.ios_mac/ios/libs/libwebp.a`，并由 Debug/Release xcconfig 显式链接，不修改全局 Creator 安装。Release iphoneos arm64 的 Objective-C++ 编译和最终链接已成功。
- 受限执行环境无法访问 CoreSimulatorService 时，`actool`/`ibtool` 会误报 `iOS 26.5 Platform Not Installed`；这不是本机 Xcode 组件损坏。2026-07-23 已在系统权限环境确认 iOS 18.4、iOS 26.5 和 watchOS 26.5 Runtime 均已安装，完整 AppIcon 与启动图资源编译正常，无需重装平台组件。
- 2026-07-23 已用 `CODE_SIGNING_ALLOWED=NO` 成功生成完整未签名归档，包内为 arm64、Bundle ID `com.fireball.qing`，并确认 `code object is not signed at all`、`Assets.car` 与 `LaunchScreen.storyboardc` 均存在。最终交付文件位于 `build/ios-unsigned/qing-unsigned-full.xcarchive` 和 `qing-unsigned-full.ipa`，可交第三方重签；早先排除 AppIcon/启动图的 `qing-unsigned-check.*` 只保留为检查产物，不应再用于交付。
- 2026-07-23 当前 `build/jsb-link` Android Debug 工程已可成功编译。项目位于 exFAT 卷时，macOS 会把扩展属性写成 `._*` AppleDouble 文件，AGP 8 会将其误判为资源目录并在 `parseDebugLocalResources` 失败；当前工程在根 `build.gradle` 中把各模块 `buildDir` 定向到本机 APFS 临时目录规避，APK 再复制回 `app/build/outputs/apk/debug/qing-debug.apk`。同类伴生文件也可能污染 `.git`，可用 `dot_clean -m .git` 只清理 AppleDouble 元数据。
- 2026-07-23 直接执行Gradle `:qing:assembleDebug`只会重新编译当前生成工程的Java/Native并打包现有`build/jsb-link/src`与`assets`，不会把项目`assets/scripts`自动编译成最新Cocos运行资源。曾因此安装出“原生键盘补丁最新、但Cocos内容和panelUpdate仍旧”的Debug包，首次启动报旧的本地manifest解析失败；用户从Creator重新构建后制作的Release内容正常。验证原生改动前必须先由当前Creator实例重新构建Android，再执行Gradle打包并核对APK内`assets/main/index`与最新Creator输出一致。为绕过旧热更新页临时加入的原生输入框自动弹出测试钩子已完整撤销，未保留在工程。
- 当前 Android 工具链已升级为 AGP 8.7.3、Gradle 8.9、NDK 25.1.8937393、compile/target SDK 35 和 Build Tools 35.0.1。Java层最低API仍为16，但NDK 25会把Native最低平台提升为19并给出警告；后续正式发布前应统一最低API声明，不能继续把API 16设备视为已验证支持。
- 最新 Debug APK 已验证 ZIP 完整，且同时包含 `arm64-v8a` 与 `armeabi-v7a` 的 `libcocos2djs.so`；尚未做本轮语音版APK的安装、启动、登录和真机功能回归。
- 2026-07-23 已参考根目录`跨平台生成历史参考/build/jsb-link`把仍在使用的 Android 项目桥接移入当前`build/jsb-link`：`AppActivity.java`现提供常亮屏幕、运行时定位授权/更新与`GetCurGps`、兼容 Android 10 分区存储和 Android 9 及以下公共相册的`saveTextureToLocal`、以及转义后在 GL 线程回调 JS 的剪贴板读写。历史定位代码中`locationManager`未初始化即读取的空指针路径已消除，生命周期销毁时会注销定位监听。
- GCloudVoice、TalkingData和微信授权登录均未迁回：自研Android/iOS语音不使用腾讯JAR/AAR/SO、Framework或SDK初始化，只新增项目自有原生桥与系统麦克风权限；微信和统计入口继续为空操作。历史PictureSelector/相册选头像也未迁移，因为当前头像流程已经改成本地20张资源选择。
- 原生桥接整合后已执行不依赖旧缓存的`:qing:clean :qing:assembleDebug`并成功；APK ZIP校验通过、保留`arm64-v8a`和`armeabi-v7a`，实际权限为网络、精确/粗略定位以及仅限API 28以下的旧存储写权限。因Gradle `buildDir`已移到APFS临时目录，`app/instantapp build.gradle`的资源源目录必须用`projectDir/../../../..`定位`jsb-link`，并排除`._*`/`.DS_Store`，不能再从`buildDir`反推。GPS授权、关闭定位、剪贴板特殊字符、Android 9/10+保存相册和真机启动仍待设备回归。当前原生修改位于Creator生成的`build/jsb-link`，若日后删除该目录并“重新构建”，必须先迁入项目级原生构建模板或重新应用，不可假定会自动保留。
- Android权限兼容按系统版本分流：API 23以下定位无需运行时申请，API 23以上同时请求精确/粗略定位且任一获批即可工作，以兼容只授予大致位置；只使用前台定位，不申请后台定位。API 29以上保存本应用生成图片通过MediaStore的pending/publish流程，不申请读取相册、媒体位置或广泛存储权限；API 23～28仅在实际保存时请求`WRITE_EXTERNAL_STORAGE`并在授权后自动继续，Manifest将该权限限制到`maxSdkVersion=28`；API 22以下使用安装时权限。`INTERNET`和`ACCESS_NETWORK_STATE`属于安装时普通权限，不做运行时弹窗。该调整再次通过Android Debug完整编译，仍待Android 6/9/10/12/13+代表设备或模拟器分版本验证。
- 2026-07-23 Android 真机复现“键盘上方输入内容/完成栏瞬间消失”：系统输入法实际保持显示（`mInputShown=true`），Cocos 原生 `Cocos2dxEditBox` 也仍持有输入连接，但输入栏定位异常。根因是 Creator 2.4.13 的 `registKeyboardVisible()` 不适配新版 Android 全屏/inset 行为；与本轮合并的 `AppActivity` 定位、相册和剪贴板桥接无关。已排除：只改内部EditText的`topMargin`会压缩输入框；用当前根布局高度判断会在`ADJUST_RESIZE`动画中误判关闭；从被平移控件或窗口可视矩形反推高度会受系统平移影响并产生过量位移，真机曾记录输入栏跳到`y=104–238`。当前第五版全局2.4.13 `Cocos2dxEditBox.java`在Android 11及以上直接使用系统`WindowInsets.Type.ime()`给出的IME底部inset定位，不再反推键盘高度；若父布局已被`ADJUST_RESIZE`缩小则不重复平移，否则按IME inset整体上移“输入框+完成按钮”。Android 10及以下继续用稳定物理屏幕高度差兜底，不写死机型、分辨率或键盘高度。原版备份为同目录`Cocos2dxEditBox.java.qing-before-ime-fix-20260723`，第五版`:qing:assembleDebug`已成功并重新编译`libcocos2dx` Java；仍需正式签名包真机复测，并建议补测Android 10及以下、另一台正常resize设备、横屏和切换输入法。Creator升级或重装会覆盖该全局补丁。
- 后续真机日志进一步确认，“闪一下后关闭”的直接调用源不是inset定位：每次`onShown`后，`Cocos2dxGLSurfaceView`收到新的`ACTION_DOWN`，其2.4.13默认分支在`mStopHandleTouchAndKeyEvents`期间立即调用`Cocos2dxEditBox.complete()`，随后出现`HIDE_SOFT_INPUT`和JS `editing-did-ended`。现已最小修改全局`Cocos2dxGLSurfaceView.java`：编辑期间GL画布的单指/多指按下只拦截，不再隐式确认关闭；“完成”按钮、IME动作和系统返回键仍可结束输入。原版备份为同目录`Cocos2dxGLSurfaceView.java.qing-before-editbox-touch-fix-20260723`，`:libcocos2dx:compileDebugJavaWithJavac`已通过；待用户从Android Studio直接运行真机验证。

## 自研房间语音替换（服务端、Web、Android和iOS已实现）

- 2026-07-28 静态确认当前房间语音 UI 与消息链仍保留：`panelGameView.ts`在按下/松开时调用`MobileManager.StartRecord/StopRecord`，上传成功后原设计通过`reqSay("@@语音@@"+文件ID)`广播，`DrhLogicMgr`负责排队，`DrhPlayerLogic`触发下载与播放；因此可以保持现有界面和`@@语音@@`房间消息外壳不变，只替换媒体通道。
- 2026-07-28 Web客户端已接入：`WebVoiceRecorder.ts`优先使用`getUserMedia + AudioWorklet`、回退`ScriptProcessor`，连续重采样为16kHz单声道PCM16LE；`WebVoiceClient.ts`优先通过WS边录边传，松手发送尾包，WS失败时保留整段PCM并尝试HTTP补传；收到房间语音消息即并发预下载，轮到播放时复用同一下载或命中最多20条Blob URL缓存，再用`HTMLAudioElement`播放。`MobileManager.ts`保持原`StartRecord/StopRecord/DownLoadRecord`入口，上传完成仍通过`reqSay("@@语音@@"+voiceId)`广播；单条录音客户端上限9.8秒。
- 用户已明确确认安卓、苹果、网页所有平台均不需要语音令牌。统一流程是客户端直接上传语音，AudioServer返回`voiceId`文件名，客户端调用现有`reqSay("@@语音@@"+voiceId)`，KB只广播文件名；接收端用文件名直接下载播放。不得再增加`申请_语音_令牌`、`AudioToken`、Authorization或KB游戏服签发流程。
- Native客户端的默认明文入口是`http://154.37.155.17/audio`和`ws://154.37.155.17/audio/v1/stream`，仍允许受控环境用`localStorage.AudioServerBaseURL`覆盖。2026-08-14起浏览器固定改用`https://154-37-155-17.sslip.io/audio`和`wss://154-37-155-17.sslip.io/audio/v1/stream`，不再跟随页面来源，也不接受旧本地覆盖导致降级明文。
- Web端已用Cocos Creator 2.4.13命令行完成无令牌版`web-mobile` Debug构建，新增的录音、上传、预下载和播放模块均进入`assets/main/index.js`，客户端不再依赖任何KB新增接口。
- 2026-07-28 用户实测网页版每条语音开头会被截掉，根因是旧实现每次按下后才重新调用`getUserMedia`、创建`AudioContext`和处理节点，UI已显示录音但采集链路尚未就绪。`WebVoiceRecorder`现改为进入房间时预热并在录音间复用麦克风、AudioContext和处理节点，离开房间才完整释放；空闲时仅在内存保留约180ms滚动PCM前置缓冲，按下后先发送前置缓冲再发送实时音频。为给服务端10秒上限留余量，自动停止时间从9.8秒调整为9.6秒，最坏录音约9.78秒。Cocos Creator 2.4.13 Web Mobile构建及180ms/总时长边界检查已通过，实际语音开头效果待用户复测。
- 2026-07-28 网页版偶发HTTP补传`409 Conflict`已定位为同一请求ID的WS录音尚未完成清理时，客户端立即又发起HTTP补传。`WebVoiceClient`现会先向旧WS发送`cancel`、短暂等待`cancelled`并关闭连接，再对409、限流、常见5xx、网络失败和8秒超时使用同一请求ID做有界幂等重试；服务器无需修改或重新部署。每次按住现使用完全独立的请求ID、PCM、WS和控制响应状态，上一条正在补传时可继续录制下一条，不会覆盖文件名或音频缓存；最多保留6个并发待发送会话防止断网刷屏耗尽内存。
- 同次边界加固还包括：重复按下/重复松开只处理一次；不足300ms的快速点按静默丢弃；普通松手保留50ms尾音窗口，旧浏览器`ScriptProcessor`缓冲从4096降为2048样本；页面隐藏或`pagehide`时立即提交已录PCM；离开房间会中止录音、上传和播放且禁止旧语音广播到新房间；客户端硬限制PCM不超过9.8秒。Cocos Creator 2.4.13 `web-mobile` Debug构建已通过且上述逻辑已进入`assets/main/index.js`，AudioServer完整`go test ./...`也已通过；快速连点、断网恢复和切后台的真实浏览器交互仍需用户复测。
- 2026-07-28 用户实测切换房间后语音消息和播放气泡正常但听不到声音。Chronicle画面确认`reqSay`、`SayInfo`及播放UI均已触发且控制台无下载/自动播放错误；从公网AudioServer取回其中两条切房后M4A，时长为1.711秒和1.823秒，但平均与峰值均为`-91 dB`，文件本身是数字静音。因此问题在切房后的Web麦克风采集恢复，不在KB广播、下载或播放器。
- 上述切房静音已修复：`LeaveVoiceRoom`现只清理旧房间录音会话、上传、下载缓存和播放状态，健康麦克风链保留5秒供紧接着进入的新房间复用，超时或组件销毁才完整释放；新房间`prepare/start`会取消延迟释放。`WebVoiceRecorder`不再只看`readyState`，同时拒绝`muted`、`disabled`、已结束或收到`mute/ended`事件的轨道，并可在录音中原地重建采集链。`WebVoiceClient`检查PCM是否全零：开始600ms仍无任何非零样本时自动重建一次麦克风；松手时仍为全零则取消WS/HTTP上传、强制使输入链失效并提示“麦克风没有采集到声音”，不再生成和广播静音文件。Cocos Creator 2.4.13 `web-mobile` Debug构建已通过，修复逻辑已进入`assets/main/index.js`；真实切房后连续录音仍待用户复测。
- Android/iOS语音不能只靠Cocos TypeScript实现：Native JSB环境没有浏览器`getUserMedia/AudioWorklet/HTMLAudioElement`。三端继续共用Cocos层的按住/松开UI、房间队列、`voiceId`及`reqSay("@@语音@@"+voiceId)`逻辑；Android原生负责`AudioRecord`采集和`MediaPlayer`播放，iOS原生负责`AVAudioEngine`采集和`AVAudioPlayer`播放。PCM高频数据、边录边传和HTTP补传均留在各自原生层，只把开始、停止、返回`voiceId`及播放完成事件跨JSB桥传递。
- 2026-07-28 Android原生语音已实现：项目级主源码为`native/android/voice/QingVoiceBridge.java`，采用16kHz单声道PCM16LE；按下即通过HTTP或HTTPS chunked body边录边上传，松开只结束请求体并等待AudioServer编码，流式请求失败时保留最多9.8秒PCM并用同一requestId做有界HTTP补传，对409、限流、常见5xx和网络失败重试。实现不依赖第三方网络/语音SDK，也不使用令牌；进入房间且已有权限时会预创建`AudioRecord`以降低首音延迟，录音间自动再次预热。
- Android边界保护包括：重复按下/松开去重、权限弹窗期间松手取消本次录音、不足300ms静默丢弃、全零采样拒绝上传、9.6秒Cocos自动停止和9.8秒原生硬上限、每条录音独立会话编号、旧上传晚返回不影响下一条录音、切房取消旧录音/上传/下载/播放且禁止旧房广播。播放端收到文件名即预下载到应用缓存，最多保留20条M4A，轮到时使用`MediaPlayer`串行播放，成功或失败都会推进Cocos语音队列。
- `tools/sync_android_voice.py`会幂等把项目级Java源码复制到Creator生成工程，并只给`AppActivity`补初始化、权限回调和销毁清理，同时给Manifest补`RECORD_AUDIO`。Creator重建Android工程后必须再次运行此脚本；不能只依赖易被覆盖的`build/jsb-link`副本。
- Android验证已完成：Cocos Creator 2.4.13的Web Mobile与Android Debug/Release脚本构建通过，Java编译和双ABI`:qing:assembleDebug`通过；公网Caddy入口已用HTTP/1.1 chunked短PCM探针验证并按预期返回422且不落文件。最终Debug APK位于`build/jsb-link/frameworks/runtime-src/proj.android-studio/app/build/outputs/apk/debug/qing-debug.apk`，包名`com.fireball.qing`，包含`INTERNET`、`RECORD_AUDIO`、`QingVoiceBridge`及与最新Creator产物SHA-256一致的`assets/main/index.jsc`。2026-07-28用户反馈Android房间语音已测试通过；具体机型和HTTP/HTTPS覆盖矩阵未逐项记录。
- 2026-08-13 Android牌桌弹窗出现 `unexpected end of stream on com.android.okhttp.Address@...`。该文字是Android `HttpURLConnection` 底层OkHttp在HTTP响应完成前连接被对端或中间网络提前关闭的异常，不是牌局规则或KB长连接错误，`Address@...` 只是内部对象标识。结合现有回调链，原生 `QingVoiceBridge` 在流式上传失败并完成有界整段补传后，会把最后一次网络异常作为“语音录制失败”传给 `MobileManager.OnNativeVoiceFailed()`，而 `GetVoiceErrorText()` 对非权限错误直接原样弹窗；因此若发生在松开语音键后，该条语音大概率未发送，但牌局仍可继续。单张截图不能区分手机网络切换、Caddy/AudioServer主动断开或代理/上游超时；若重复发生，应按发生时间同时核对设备logcat和AudioServer/Caddy日志。当前只完成诊断，尚未把该类英文异常转换为中文提示。
- 2026-07-28 iOS原生语音已实现：项目级主源码为`native/ios/voice/QingVoiceBridge.h/.mm`，使用`AVAudioEngine + AVAudioConverter`采集16kHz单声道PCM16LE，`AVAudioSession`使用语音聊天模式并默认扬声器播放；进入房间且已有权限时预创建音频引擎以降低首音延迟。按下即使用Apple规定的`uploadTaskWithStreamedRequest`和`needNewBodyStream`向HTTP或HTTPS入口边录边传，并通过`Expect: 100-continue`避免服务器已拒绝时继续发送请求体；失败后保留最多9.8秒PCM并用相同requestId做有界整段HTTP补传。实现不用语音令牌、第三方SDK或无效证书信任绕过，HTTPS只接受系统信任的有效证书。
- iOS边界保护与Android一致：重复按松去重、权限弹窗期间松手取消、不足300ms或全零语音拒绝上传、9.6秒Cocos自动停止和9.8秒原生硬上限、每条录音独立会话、切房/退后台取消旧录音、待重试上传和播放并禁止旧房广播。2026-07-28复核又修正了音频回调恰在松手结束等待后才进入的极小并发窗口，以及旧房失败回调在切房后显示的问题。播放端按`voiceId`预下载M4A到应用Caches目录，原子写入并最多保留20条，`AVAudioPlayer`完成或失败均回调Cocos推进串行队列。
- `tools/sync_ios_voice.py`会幂等把项目级iOS源码复制到Creator生成工程，为`AppController`补初始化、退后台取消和进程结束清理，为`Info.plist`补`NSMicrophoneUsageDescription`，并把桥文件以ARC方式加入`Qing-mobile`源文件阶段。脚本同时把最低系统规范为当前Xcode支持的iOS 12.0，并确保Xcode构建项和Info.plist的Bundle ID均为`com.fireball.qing`；`settings/builder.json`的iOS包名也已同步。Creator重新生成iOS工程后必须再次运行该脚本，不能只依赖易被覆盖的`build/jsb-link`副本。
- iOS静态与构建验证已完成：Cocos Creator 2.4.13 Web Mobile和iOS Release脚本构建通过；当前Xcode/iphoneos arm64完整Release编译、链接、静态分析和未签名归档通过，`QingVoiceBridge`静态分析诊断为0。真实Foundation流式上传探针验证单次body stream回调、32000字节PCM、`Expect: 100-continue`和201响应均正确；真实`AVAudioConverter`探针验证48kHz双声道Float到16kHz单声道PCM16有有效信号；AudioServer全套Go测试通过。最终干净归档为`build/ios-unsigned/qing-ios-voice-unsigned-clean.xcarchive`，干净IPA为`build/ios-unsigned/qing-ios-voice-unsigned.ipa`，IPA SHA-256为`41f7d74c9b064ab44780224749a6102cfc4e396cb1820b3ecb22a5e8dbf53cc4`；归档和IPA内Bundle ID均为`com.fireball.qing`，最低iOS 12.0、Mach-O仅arm64、麦克风用途说明和HTTP兼容配置存在、`QingVoiceBridge`及反射选择器存在、`assets/main/index.jsc`与最新Creator产物SHA-256一致、两份产物均无AppleDouble/`.DS_Store`且IPA ZIP校验通过，应用未签名。Xcode直接向外置exFAT卷归档会在App内生成大量`._*`，需要复制时排除后再执行`dot_clean -m`。
- 当前没有可用iPhone，因此不能把“真实麦克风权限弹窗、物理麦克风首音、听筒/扬声器/蓝牙路由、电话或系统音频打断、真实蜂窝/Wi-Fi、签名安装和HTTP/HTTPS公网联调”标为已验证，也不能诚实承诺零真机问题；这些是最后仅能由iPhone确认的风险，不是本轮自动化验证已发现的代码错误。
- 公网浏览器只有在可信HTTPS页面中才能调用麦克风，并且HTTPS页面必须使用HTTPS/WSS语音入口。当前服务器只有HTTP/WS，所以标准公网网页版仍不能实际录音；localhost网页可以使用现有HTTP/WS联调。Android和iOS原生客户端均可直接使用当前HTTP入口，也都接受有效证书的HTTPS入口。
- 2026-07-29 已按Apple当前官方文档确认iOS可手动安装包含`com.apple.webClip.managed`载荷的`.mobileconfig`：普通个人iPhone不要求MDM、设备监管或用户批准的MDM，用户从网页下载后必须在8分钟内进入“设置→已下载描述文件”并确认安装；可配置自定义名称、图标和`FullScreen=true`，从桌面以无地址栏的Web App形式打开。安装动作最终由“设置”完成，不是Safari完成；Apple明确支持Safari网页下载和Mail附件两种手动分发方式，网站面向普通用户时仍应提示“请用Safari打开”，微信/QQ内置浏览器及其他第三方浏览器可能只预览或下载文件，不能作为未实测的稳定安装链路。它只安装Web Clip桌面入口，不是IPA或本地游戏包，删除描述文件也会移除对应入口。当前分发决策为优先采用PWA/Safari“添加到主屏幕并作为Web App打开”：步骤更少、没有描述文件安全警告，且能结合Manifest与Service Worker做缓存和版本更新；`mobileconfig`只作为需要固定图标、固定名称、强制全屏或受控用户安装引导时的可选入口，不作为普通玩家默认方案。Apple当前仍要求用户在Safari系统界面手动执行“分享/更多→添加到主屏幕→作为Web App打开→添加”，WebKit不提供自动或网站触发的安装提示，`beforeinstallprompt`支持请求已标记为不实现；网页按钮只能显示分步引导、检测是否已经以standalone运行，不能替用户完成安装。只有已受管设备可由MDM集中下发Web Clip，普通玩家若要求类似一键安装只能改走App Store/TestFlight等原生分发，不能继续称为PWA。Web Clip与PWA都不改变网页安全上下文，也不会授予麦克风权限或绕过混合内容规则，因此本项目HTTP页面无论使用哪种方式放到桌面，网页语音仍不能工作，生产方案依旧需要可信HTTPS页面和HTTPS/WSS语音入口。
- 2026-07-28 用户确认语音实现必须同时兼容带证书和不带证书的部署。服务端采用`plain/secure/proxy`三种传输配置：始终可提供HTTP/WS，可选直接加载证书提供HTTPS/WSS，或由外层反向代理终止TLS；客户端按环境使用HTTP(S)/WS(S)，不得因证书校验失败静默降级或信任无效证书。Android当前原生上传/下载同时接受HTTP和HTTPS；公网网页版录音仍必须处于可信HTTPS环境。
- 2026-07-28 已在根目录`AudioServer`实现第一版独立Go语音服务器：16kHz单声道PCM通过WS直接送入FFmpeg，不长期落盘；编码结果先写随机后缀的`.m4a.part`，完成后按日期和语音ID前缀分片原子移动为正式M4A。元数据当前使用同样分片的原子JSON文件索引，记录`voiceId`、请求ID、时长、大小、SHA-256、存储键和过期时间，不保存音频BLOB；默认保留7天，过期语音和未完成临时文件均自动清理。多实例部署前仍需把文件索引和本地磁盘适配为共享数据库/自建MinIO。
- `AudioServer`已实现HTTP/WS无证书监听、HTTPS/WSS直接证书监听及Nginx代理示例，两种监听可同时开启且不会自动降级；提供无令牌WS边录边传、无令牌HTTP PCM补传、无令牌文件下载、幂等请求ID、不可预测文件ID、ETag/Range、并发限制、健康检查和Prometheus文本指标。服务端内部`voice_id_secret`仅用于生成不可预测且可幂等重试的文件ID，不发送给客户端，也不是访问令牌。
- 服务端验证结果：无令牌版`go test -race ./...`、`go vet ./...`及Linux amd64无CGO构建均通过；Cocos Creator 2.4.13 Web Mobile构建也通过。
- 2026-07-28 已把AudioServer部署到现有热更新服务器`154.37.155.17`。服务器是CentOS 7 x86_64，`client_update`没有免密sudo且系统无FFmpeg，因此使用Linux amd64静态Go二进制和校验后的FFmpeg 7.0.2静态版，私有部署目录为`/www/html/.audio-server`且权限`0700`；AudioServer内部监听`18080`，公网不直接开放该端口，而是经现有Caddy提供`http://154.37.155.17/audio`和`ws://154.37.155.17/audio/v1/stream`。Caddy原配置已备份到部署私有目录，新增路由带唯一ID且不覆盖热更新路由。
- 当前服务器采用普通用户守护脚本，异常退出3秒后重启；`client_update`的crontab在服务器重启时启动服务，并每分钟只读确认Caddy语音路由，只有路由丢失时才重新插入。2026-07-28 无令牌版已原位更新并保留可回滚备份，公网WS已完成直接`start`、连续10个PCM帧、松手生成1秒M4A及无需Authorization下载闭环，测试音频和元数据随后已清理。HTTP补传集成测试通过；当前公网HTTP链路仍建议发送`Expect: 100-continue`，WebSocket主链路不受此问题影响。
- 当前公网入口仍是无证书HTTP/WS，Android和iOS均已按同一协议接入，但标准公网浏览器无法在此环境申请麦克风；要支持公网网页版录音仍需可信HTTPS/WSS。Android已由用户反馈真机测试通过；尚未完成生产负载测试、iOS签名包真机房间联调和三端完整覆盖矩阵联调。

## Cocos Creator 2.2.1 项目定制引擎审查

2026-07-22 已将 `/Applications/Cocos/Creator/2.2.1` 与 Cocos 官方 `2.2.1-release` 源码基线逐文件比较，并映射到 2.4.13。用户已确认本轮只迁移图片解密兼容功能，键盘、WebView、调试器和工程配置等其他差异均不动。

- JavaScript 引擎层没有项目定制逻辑。EditBox、事件、资源库、实例化、粒子等差异均为官方发行补丁、BOM 或版本元数据，不应迁移。
- 2.2.1 的 `cocos/platform/CCImage.cpp/.h` 有明确的 Native 加密 PNG 兼容补丁：识别项目旧格式文件头，重建 PNG 头尾，并用源码内固定参数循环 XOR 还原中间数据。该机制只是旧资源兼容/混淆，不应视为安全加密。
- 图片兼容功能已最小移植到 `/Applications/Cocos/Creator/2.4.13/CocosCreator.app/Contents/Resources/cocos2d-x/cocos/platform/CCImage.cpp`。只修改这一份 `.cpp`，未修改 `CCImage.h`、公开枚举或其他引擎文件，因此保留了 2.4.13 的 ASTC 等新增格式能力。原文件备份为同目录的 `CCImage.cpp.qing-before-image-crypto-20260722`。
- 2.4.13 实现是在 CCZ/GZip 解压后、正常格式检测前识别并恢复旧格式，再交给原生 PNG 解码器；同时增加空指针、长度溢出、分配失败和内存释放保护。它只提供运行时解密兼容，不会自动加密项目图片；当前仓库仍未发现正式加密器或发布期加密脚本。
- 兼容验证已通过：3 个真实项目 PNG 经旧格式往返后逐字节一致；2.4.13 的 `cocos2d::Image` 实际运行时分别解码 290×30 索引色、608×117 RGBA、85×85 RGBA 样本，普通图与加密图的尺寸、格式、数据长度和像素数据完全一致。11 组短输入和 6 组损坏输入在独立格式校验中均安全失败，命中旧格式标记的畸形/损坏输入在真实 Cocos 解码路径中也返回失败而未崩溃。
- 编译验证已通过：补丁源码分别用 macOS arm64、Android arm64、iOS arm64 的 C++11 环境成功生成目标文件；2.4.13 macOS 引擎静态库 arm64 和 x86_64 整体构建成功。Android/iOS 真机包尚未运行，发布前仍需清理原生缓存、重新编译并各做一次真机加载验证。
- 此功能只影响重新编译的 Native 包，不影响 Web、编辑器资源导入或未重编译的 Creator 内置 Simulator。Creator 升级或重装可能覆盖引擎补丁；热更资源若要使用旧格式，必须先加密资源，再生成 manifest/MD5。
- 当前项目 `assets` 下 1278 个 PNG 均为标准 PNG，工作区和 `runtime-src.zip` 未发现加密图片、配套加密器或加密脚本；本次移植的实际用途是兼容旧线上包或外部发布流程产生的旧格式资源。
- 2026-08-14 Web Mobile图片保护已实现，Native和项目源图不受影响：新增`tools/protect_web_images.js`、`web_image_loader.template.js`和`web_image_protection.json`，只处理最终`build/web-mobile/assets/<bundle>/native/`中的PNG/JPG/JPEG。标准图片使用AES-256-GCM转换为`QIMG2GCM + 12字节Nonce + 密文及认证标签`，扩展名和Creator资源URL保持不变；Nonce由相对路径和明文哈希确定性派生，同一资源重复执行密文稳定，兼容现有MD5 URL及一年immutable缓存。脚本会逐张往返校验、识别已加密文件并跳过二次加密，任一密文被篡改、密钥不匹配、格式异常或残留标准图片都会失败。运行时向`index.html`的`main.*.js`之后注入带内容哈希的加载器，Cocos启动前注册Web专用PNG/JPG/JPEG下载器；只拦截`assets/<bundle>/native/`，请求时附加稳定的`qimg=格式-密钥标识`查询参数，使首次从线上明文切换为密文时不会复用浏览器原有immutable明文缓存，再按ArrayBuffer下载并通过Web Crypto按需解密为临时Blob；外部图片仍走普通DOM Image，未加密标准图保留诊断兼容。iPhone/iPad网页首次下载并发最多4，避免密文、明文和解码纹理叠加造成内存峰值；纹理上传GPU后不增加持续渲染开销。`tools/upload_web_mobile.py`已在收集文件和打ZIP前自动执行保护与校验，并能从PATH、Homebrew或NVM定位Node；保护失败不会进入上传。当前未改写正式`build/web-mobile`，完整构建隔离副本实测1155张全部保护，原始42,157,813字节变为42,199,393字节（每张增加36字节），系统`file`已无法识别抽样PNG；重复保护前后聚合SHA-256一致，篡改单字节会被GCM认证拒绝。加密副本经本地HTTP和真实浏览器成功进入8L登录页，控制台无图片解密错误；同机同浏览器按启动后5秒和13秒截图对比，加密版与未加密版都在5秒仅显示背景、13秒完整显示登录控件，未观察到本机可见的额外延迟，但这不是目标iPhone性能结论。上传脚本dry-run生成1728文件、52.26MB ZIP并通过完整性校验，未连接服务器、未上传。该机制满足直接下载文件不可作为图片使用，但密钥和解码数据最终仍在浏览器，技术人员仍可能通过Hook或WebGL导出，不能当作不可破解的保密方案；已经缓存到用户浏览器中的历史明文响应无法远程删除，但新加载器不会再使用其旧缓存键。正式发布前仍需目标iPhone验证首次登录、进大厅、进牌桌、切场景和内存表现。
- 2026-08-14 后续Web发布固定操作顺序为：在Cocos Creator 2.4.13按现有设置选择`Web Mobile`、启动`login.fire`并正常构建到`build/web-mobile`，确认构建成功后直接双击根目录`3上传网页版.command`；上传脚本会在弹出正式上传确认前原地保护该构建的图片、注入加载器并完成全量校验，通过后才打ZIP和上传，用户不需要另跑加密命令。若在确认页取消，服务器不变，但本地`build/web-mobile`已经是可运行的密文构建；重复双击或失败重试不会二次加密。后续代码/美术有任何变化时直接由Creator重新构建覆盖该目录，再双击上传即可，不要在Creator构建前加密项目源图，也不要把该工具用于`build/jsb-link`、Native热更新或手机版。只做本地密文检查时可运行`node tools/protect_web_images.js --source-dir build/web-mobile`，校验已有密文可加`--verify-only`；密文网页必须通过localhost或HTTPS访问，不能以直接双击`index.html`作为有效验证。
- Android `Cocos2dxActivity` 曾把软键盘窗口策略从 `ADJUST_RESIZE` 改为 `ADJUST_NOTHING`。项目有大量 EditBox、`InputBK` 和 `EditBox2` 输入遮罩；2.4.13 仍默认 `ADJUST_RESIZE`。该项只在真机确认键盘导致布局缩放后迁移，优先放在项目 AppActivity/原生模板而非污染全局引擎。
- 2026-07-23 Android 原生 EditBox 的“输入内容+完成”栏真机表现为闪现后关闭。日志曾显示输入栏出现后，GLSurfaceView 收到一次 `ACTION_DOWN` 并调用 `Cocos2dxEditBox.complete()`，因此已修改 Creator 2.4.13 的 `Cocos2dxGLSurfaceView.java`：原生编辑期间送到 GL 画布的按下事件只拦截，不再隐式完成输入。排查确认 Android Studio 曾复用12:23的旧引擎class；禁用构建缓存重编后，14:00的实际runtime class已反编译确认两处`complete()`调用消失。随后真机日志与截图确认IME一直保持显示；窗口已使用`ADJUST_RESIZE`，再按929px键盘高度平移会重复避让并把输入栏推走，因此不再手工叠加键盘高度。取消平移后输入栏稳定显示但落在屏幕顶部；恢复可见区域底边定位后又因外层`WRAP_CONTENT + 底部对齐`与内部屏幕坐标混用而落到整屏底部、被键盘遮挡。当前`Cocos2dxEditBox.java`已把输入覆盖层改为`MATCH_PARENT`坐标容器，用`getWindowVisibleDisplayFrame().bottom - 容器屏幕Y - 输入栏高度`计算内部topMargin，使输入栏底边严格等于键盘上沿，并动态兼容刘海、导航模式和不同输入法。生成Android工程根`build.gradle`强制`libcocos2dx` JavaCompile每次执行；Java编译已通过，最终位置仍待用户从Android Studio再次运行验证。
- 2026-07-23 用户在上述坐标修正后报告“没有变化”，ADB核对发现手机包`lastUpdateTime=13:58:26`，早于修正后的`Cocos2dxEditBox.class`时间14:12:46，证明该次观察仍来自旧安装包而不是新坐标代码。已禁用构建缓存完整执行`:qing:assembleDebug`并成功，最新APK位于本机APFS临时构建目录，时间14:13:41；再次验证前必须由Android Studio普通Run重新安装，不能使用Apply Changes，并以手机包`lastUpdateTime`更新为准。
- 2026-07-23 删除重装后已由ADB确认主游戏新包首次安装时间14:15:51。MIUI/Android 11+沉浸式窗口中`getWindowVisibleDisplayFrame().bottom`可能仍返回整屏底边，不能作为IME上沿；当前定位已按版本分流：Android 11+使用`WindowManager.getCurrentWindowMetrics().bounds.bottom - WindowInsets.Type.ime().bottom`，旧系统使用可见窗口底边，再统一转换为全屏输入容器内坐标。完整`:qing:assembleDebug`已通过，仍待重新安装后的真机位置验证。
- 2026-07-23 安装时间14:19:41的新包仍被键盘遮挡；实时系统边界显示键盘上沿1471，而UI层级中输入覆盖层实际底边1679，说明依赖EditText首次`getHeight()`计算topMargin仍受首次布局高度为0影响。现改为不依赖控件高度：EditText固定`ALIGN_PARENT_BOTTOM`，动态设置`bottomMargin = 输入容器屏幕底边 - 键盘上沿`（该机实时差值208px），各设备按自身容器和IME边界计算。完整Debug构建已通过，待再次安装验证。
- 2026-07-23 动态bottomMargin版真机表现为输入栏先在正确位置闪现、随后跳到上方，证明系统初始`ADJUST_RESIZE`定位正确，但Insets回调发生了二次避让。根因是部分设备的`WindowMetrics`本身已经缩到键盘上沿，再减一次IME高度会得到过小坐标。Android 11+现先比较物理屏高、当前WindowMetrics底部和IME高度：窗口已明显缩小时直接用其底部，仍为全屏时才减IME高度；完整Debug构建已通过，待重新安装验证。
- 2026-07-23 上述WindowMetrics分流版仍出现“先在正确位置闪现、再跳到上方”，因此已撤销本轮对`Cocos2dxEditBox.java`的全部自定义Insets、平移、MATCH_PARENT和margin计算，恢复任务开始前备份的Cocos Creator 2.4.13原生EditBox布局。仅保留`Cocos2dxGLSurfaceView.java`中已由日志证实的修复：原生编辑期间GL画布ACTION_DOWN不再调用`Cocos2dxEditBox.complete()`。恢复后的完整Debug构建已通过，待真机重新安装验证；若仍异常，应只围绕原生布局采集日志，不再叠加自定义坐标策略。
- 2026-07-23 用户决定把通用键盘定位延后处理，当前先采用临时可见性方案：在恢复后的Cocos原生`setTopMargin()`计算上额外向上移动两个EditText自身高度，即从原生`topMargin - height`改为`topMargin - 3 * height`，不使用固定像素；GL画布不自动`complete()`的修复继续保留。`:libcocos2dx:compileDebugJavaWithJavac --no-build-cache`已通过，需重新构建并安装APK后验证，热更新不能下发该全局原生引擎修改。
- 2026-07-23 房间内“别人都是头像1、自己头像正确”已由真机日志定位：服务端`PlayerList`中的玩家对象没有下发`photo`字段，客户端只能先显示头像1并逐个发送`查询_用户_头像`；服务端对测试账号773818、959117实际均返回`photo:"1"`，因此显示层按返回值显示头像1，并非座位Sprite串用。自己头像正确是因为当前账号属性/本地缓存已有其数字头像。另观察到一次对`user_id:"init"`的无效头像查询，属于独立的空座位/座位刷新时序问题。要让他人显示所选头像，应优先确认服务端`reqSetProperty("photo", 序号)`是否写入`查询_用户_头像`读取的同一字段，并最好让`PlayerList`直接携带photo；客户端不能凭空还原服务端返回为1的头像。
- 2026-07-23 进一步结合新玩家坐下的浏览器日志确认客户端还有缓存短路：`PlayerList`不带photo时，`GetImageByName(id,"",img)`若命中`mapID2Avatar`（尤其以前缓存的头像1）会返回true，导致新座位不再发送`查询_用户_头像`。现已修改`DrhLogicMgr`：新绑定座位且PlayerList缺photo时无论缓存是否命中都刷新一次服务端头像；`ImageManager.AddWaitFreshImage2Catch`在已有缓存时保留当前显示，只在完全无缓存时先放头像1，且等待映射继续保证同一查询未返回前只发一次。`validate_local_avatar_flow.py`与`git diff --check`通过，待Creator/真机观察新玩家坐下日志应出现查询请求。
- 2026-07-23 房间头像偶发只显示默认1、重进恢复的根因是异步资源回调竞态。真机日志确认服务端已返回例如`photo:"10"`且无本地资源加载失败；同一座位Sprite会先等待头像1、随后等待真实头像，两个`loadRes`完成顺序不固定，旧头像1回调可能最后覆盖真实头像。`ImageManager`现为每个Sprite记录最新期望头像序号，缓存命中、成功回调和失败回退都仅在序号仍匹配时写入，过期回调直接忽略。头像流程验证与`git diff --check`通过，待Creator/真机做首次进入和连续换座验证。
- 2026-07-23 热更新“下载完成但重启后不生效”已定位并加入生成流程：`panelUpdate.ts`在`UPDATE_FINISHED`中已经把Manifest搜索路径置顶、保存到`HotUpdateSearchPaths`并调用`jsb.fileUtils.setSearchPaths()`，缺的是重启后在加载settings/引擎/bundle前恢复该值。`tools/generate_hot_update.py`现会在打热更包前幂等修补`build/jsb-link/main.js`顶部：Native环境读取全局`localStorage`、解析路径并调用`jsb.fileUtils.setSearchPaths()`；使用成对唯一标记防止重复插入，标记残缺、找不到`window.boot`或位置过晚时拒绝继续。该`main.js`不会进入只含src/assets的热更ZIP，只会由Android/iOS后续基础包携带，因此必须先发布一次包含补丁的新基础包，旧包不能靠热更新自举。临时文件首次/重复插入测试、真实当前main副本的`node --check`、Python语法和`git diff --check`均通过。
- 2026-08-13 已恢复客服内嵌 WebView，但没有照搬旧工程对全局文件访问、混合内容、禁缓存和返回键的宽泛修改。当前仅通过项目自有 Android `QingChatWebViewBridge` 为活动客服 WebView 安装最小文件选择能力，并由Cocos页面控制透明背景；Creator重新生成原生工程后必须运行 `tools/sync_android_chat_webview.py`，再做Java编译和Android真机图片/视频选择回归。
- iOS `CCDevice-ios.mm` 曾在读取电量前启用电量监控；项目牌桌确实显示电量，但 2.4.13 已内置相同处理，无需迁移。
- 2.2.1 `config.hpp` 强制启用 V8 Inspector 属于历史调试修改；2.4.13 已等效启用，不迁移旧 `#if 1`，发布前应另行评估是否关闭远程调试。
- 2026-07-23 按用户测试需求，当前生成工程的 `Classes/AppDelegate.cpp` 已取消 `COCOS2D_DEBUG` 条件，使 Debug 与 Release 都调用 `jsb_enable_debugger("0.0.0.0", 9527, false)`；iOS `Info.plist` 已增加本地网络用途说明。启用后的完整 Release 未签名归档编译成功，交付为 `build/ios-unsigned/qing-unsigned-debuggable.xcarchive/.ipa`。该端口暴露 V8 Inspector 和脚本执行能力，只限内部测试包；正式对外发布前必须恢复条件保护或关闭该调用。
- 旧 Xcode engine 工程有 arm64 和删项兼容修改，2.4.13 工程结构已变化，不能按旧工程覆盖或照搬删项；只在新生成项目中配置目标架构并实际编译验证。
- 旧 `CCImage的副本.cpp/.h` 与官方源码一致，只是修改前备份，不参与迁移。旧 Simulator 中的本项目资源属于预览缓存，不是引擎补丁。

## 已知未完成界面需求

根目录 `问题.txt` 记录了已有缺口，包括提取奖励按钮、经理页面分享文字/比例按钮、牌局实时统计、牌型提示、切牌动画、牌桌图片和奖池数字等。实施前应再次确认哪些仍是当前需求。

- 2026-07-23 静态定位钱包“银联选择金额后点充值一直转圈”：`panelQianBao.ts`读取金额Toggle的`node.name`作为订单`amount`，但支付配置刷新又把该名称写成`<金额>元`，请求可能提交非纯数字金额；显示`panelLoading`后仅在HTTP 200或XHR error回调中关闭。公共`Tool.HTTP_GET()`只对200调用成功回调，对4xx/5xx静默不处理，且未设置`xhr.timeout`，因此服务端拒绝、非200或长期无响应都会永久保留转圈遮罩。另有`arrayAll.Length`大小写错误，应为JavaScript的`length`。以上为代码静态结论，尚未抓到该次订单接口的实际HTTP状态，未修改业务代码。
- 2026-07-23 按用户要求临时停用钱包充值入口：`panelQianBao.ts`的“确认充值”按钮回调最前面直接显示“暂未开通，后续处理”并返回，不再创建订单或显示加载遮罩；提现功能未改。后续恢复充值时需先修复上一条记录的金额格式、非200处理和请求超时问题，再删除该临时拦截。
- 2026-08-10 已移除钱包在收到“更新支付配置”结果后自动请求 `JsonPay/getLastOrder` 的逻辑，切换/刷新支付配置不再自动查询玩家最后订单；“刷新订单”按钮的单次主动查询仍保留，创建订单返回的当前订单展示也未改。Creator 2.4.13 Web Mobile Debug实际构建通过，构建产物确认该接口只剩手动刷新入口1处。
- 2026-07-23 红利界面的“推广”页面已从高对比满屏金纹改为克制暖黑背景和单一中央邀请卡，新增标题、扫码说明及分享引导层级；保持原`背景.png`、`二维码框底.png`、`分享二维码.png`的尺寸、Meta和UUID不变。`panelHongli.prefab`仅把二维码Y从-14.654调整为80、分享按钮Y从-491.111调整为-340，节点名、动态二维码`Graphics`和分享事件不变。新增独立生成器`tools/generate_qin_promotion_skin.py`，并让`generate_qin_panel_main_remaining_skin.py`不再覆盖这三张推广专用资源。现有`validate_qin_hongli_skin.py`已通过58张运行PNG、Prefab结构、绑定、RGBA/Meta裁剪及无蓝青像素检查；Creator实际显示与二维码识别、分享点击仍待用户验证。

## 换皮美术方向

- 2026-07-22 开始进行整套游戏换皮，暂定新游戏名为“秦”，原游戏名为 `Battle`。
- 已确认主风格为黑金、高档、克制的东方帝王风；曾尝试“未来东方奢华”科技版，但用户否定该方向并确定回到第一版风格。
- 第一阶段从竖屏登录界面开始，采用秦代宫阙、黑曜石、暗金纹样与鎏金品牌字，保留账号、密码、登录按钮的原交互层级；品牌字已使用可控字体重绘为准确的“秦”。
- Creator 协作约束：如果项目已经在 Cocos Creator 中打开，不得再次启动新的 Creator 实例；后续换皮任务只修改美术文件及必要的序列化资源引用，Creator 的打开、预览、保存和运行操作全部交由用户完成。
- 效果图落地约束：用户确认后的效果图就是实际运行资源的视觉基准，后续切图和替换必须完整保留其构图、纹样、材质、层次与装饰细节，不得为了便于生成而自行简化；若现有节点尺寸或结构确实无法原样承载，必须先说明限制并确认处理方式，不能先做简化版。
- 登录换皮已生成并接入：新增 `秦_登录背景.png`（750×1334）、`秦_输入框.png`（573×86）和 `秦_清除.png`（45×45），原位替换登录专用的 `账号.png`、`密码.png`、`手机登陆.png`；`panelLogin.prefab` 保持节点结构和节点名不变，只更新对应 SpriteFrame 引用及输入/占位文字颜色。
- 登录背景、输入框和清除按钮原资源均被其他界面共用，因此本次采用新增登录专用资源并只重绑 `panelLogin`，没有覆盖 `背景.png`、`公用/输入框.png`、`CHACHA.png`。新 UUID 已被 Creator 资源库识别，静态尺寸和引用检查通过；实际运行显示与交互由用户在现有 Creator 实例中验证。
- 登录页顶部LOGO已从会被四边Widget非等比拉伸的`秦_登录背景.png`中完整分离：圆环、“秦”和`QIN`现合并为新增的400×400 RGBA资源`秦_登录LOGO.png`，`panelLogin`新增同级节点`登录LOGO`，Sprite使用SIMPLE/CUSTOM且trimmed=false，Widget只启用TOP+HORIZONTAL_CENTER（alignFlags=17、top=118），固定宽高与1:1比例；背景仍保持750×1334 RGB及原UUID，只在原LOGO区域补回宫阙暗纹。生成脚本`tools/generate_qin_login_skin.py`使用原始带环源图生成独立LOGO，并用`art_sources/login/qin_login_background_clean_source.png`局部修复无LOGO背景；输入框、按钮、业务节点与逻辑均未改。实际多设备显示仍由用户在现有Creator实例中验证。
- 2026-07-23 用户认可登录页整体构图但认为背景碎金、云纹和微小暗纹过多；现已新增`art_sources/login/qin_login_background_minimal_source.png`并将其设为登录生成器默认背景源。新版保留宫殿、阶梯、两侧建筑和地面秦纹法阵，明显减少随机亮点、云纹及重复墙纹，压暗外围烟雾和栏杆，扩大输入控件后的平静暗面；运行背景仍为750×1334 RGB、原文件名/Meta/UUID，LOGO和控件资源及Prefab布局未改。`qin_login_runtime_preview.png`已同步重建并完成视觉检查，Creator真机显示仍待用户验证。
- 2026-07-22 用户指出登录LOGO的宋体“秦”与最初确认效果图不一致；现已按用户提供的确认图改为横向展开、尖角收笔的定制金属“秦”字及独立`QIN`排版，继续使用原400×400透明资源、同一`.meta`/UUID、Prefab节点和Widget参数；自动裁剪边界已随新透明像素更新为`trimX=34、trimY=9、331×382`。最终源图保存为`art_sources/login/qin_login_logo_final_source.png`，生成脚本优先使用该源图，避免重建时退回系统字体；静态合成预览已更新，Creator实际显示仍待用户验证。
- 2026-08-12 项目内热更新清单已由生成流程更新到 `2.0.8`：`project.manifest` 共记录3690项资源，相比Git中的2.0.7清单仅变更 `assets/main/index.jsc` 和两份Native内嵌manifest；`version.manifest`同步为2.0.8。项目内清单与 `hot-update-output/2.0.8` 完全一致，清单全部资源的大小和MD5及 `ver_2_0_8.zip` 完整性已通过；生成器在清单计算完成后再把2.0.8 manifest同步进 `build/jsb-link` 的RawAsset，因此两份内嵌manifest的当前构建哈希按设计不等于更新包清单内记录的生成前哈希，不能据此误报包损坏。本记录只证明本地2.0.8包完整，不代表已上传或切换正式热更新服务器。
- 2026-07-23 已新增独立热更新生成器 `tools/generate_hot_update.py` 和双击入口 `生成热更新包.command`，替代旧 2.2.1 `hot-update-tools` 的生成/打包功能。脚本读取 Creator 2.4.13 Native 构建目录 `build/jsb-link` 的 `src/` 与 `assets/`，生成 MD5 清单、完整部署目录和 `ver_<版本号>.zip`；本次版本号、资源服务器根地址、是否同步项目内manifest、是否加密PNG均保存在Git忽略的`.hot-update-config.json`，每次交互运行都会显示全部当前值，直接回车沿用、输入新值修改，成功后自动递增版本最后一段。选择同步时覆盖 `assets/resources/project.manifest` 与 `version.manifest`。生成时可按当前 `CCImage.cpp` 已兼容的旧格式混淆输出包内PNG，默认启用，并实时显示当前张数/总张数、百分比和当前文件；只转换输出副本、不修改 `build/jsb-link` 或项目源图，清单大小与MD5按转换后的最终文件计算，已是旧格式的PNG不会重复转换。已对照旧版 `/Volumes/CB/nnnn2/tool.py`：标准PNG移除签名和IEND后转换，缺少标准IEND时按旧行为转换去掉签名后的全部数据，扩展名为PNG但无PNG签名时原样跳过并计数。脚本不自动构建 Creator、不上传服务器；生成前必须先确认 `build/jsb-link` 是最新构建，部署时客户端所需的 manifest、`src/`、`assets/` 必须在配置的服务器根地址可直接访问，不能只上传未解压 ZIP。最小夹具端到端测试、ZIP内容、图片往返、加密后MD5、版本递增与 `git diff --check` 已通过；完整当前构建尚未实际打包。
- 2026-08-13 生成2.0.18热更新包时曾把版本样式输入误填到“热更新服务器根地址”，导致 `.hot-update-config.json`、项目内两个Manifest、`build/jsb-link`内两个Native Manifest、`hot-update-output/2.0.18`及其ZIP短暂写成 `http://3.01`。已依据2.0.17及更早连续产物和Git历史恢复权威根地址 `http://154.37.155.17/up`，保留2.0.18版本与下次版本2.0.19不变；七份文本配置/Manifest已一致，重新封装 `hot-update-output/ver_2_0_18.zip` 后共3711项且ZIP内两份Manifest地址正确，不存在 `http://3.01`。本次只替换Manifest地址并重封ZIP，没有重新复制或修改资源、没有上传服务器；正式服务器只读检查因HTTP连接超时未取得结果，远端当前版本仍待网络恢复后确认。
- 本游戏热更新包通过 SFTP 上传到 `154.37.155.17:2233`，账号为 `client_update`，本机使用 `~/.ssh/id_ed25519_newserver`。SFTP登录后的受限根目录显示为`/`，上传更新内容时进入`/html`：`latest.json`放`/html/latest.json`，更新压缩包放`/html/packages/<包名>.zip`。2026-07-28 已重新验证同一端口和账号当前也可使用SSH Shell，语音服务器即通过该Shell完成原位更新、重启和健康检查；“只能SFTP、不能SSH Shell”的旧结论已失效。网页对应`http://154.37.155.17/latest.json`与`http://154.37.155.17/packages/<包名>.zip`。上传前仍需核对本次实际清单文件名、压缩包名和版本，不能把示例`xxx.zip`当成固定名称。
- 2026-07-23 `tools/upload_latest_hot_update.py` 与双击入口 `上传最新热更新.command` 已按服务器自动部署流程调整：自动选择 `hot-update-output` 中版本号最高且同时含manifest、`src/`、`assets/`的完整版本，直接使用生成脚本已经产出的 `ver_<版本号>.zip`，验证ZIP完整且不含 `.DS_Store`/`._*` 后，通过受限SFTP上传到默认 `/html/_incoming`，不再重复压缩。上传时先使用隐藏 `.uploading` 临时文件名，完整传输后原子改名为正式ZIP，避免服务器提前解压半包；服务器随后自动解压到服务端 `/www/html/up`，客户端通过 `http://154.37.155.17/up/...` 访问。上传后脚本会最多等待90秒并用防缓存参数轮询HTTP部署结果，只有远端`version.manifest`与`project.manifest`版本正确、完整资源清单与本地完全一致，且均匀抽检的5个远端资源大小和MD5全部一致，才报告上传及部署成功；超时或任何不一致均以错误退出。`generate_hot_update.py` 的ZIP步骤也已修正为排除macOS隐藏文件；旧上传配置若仍为 `/up` 或 `/_incoming` 会自动迁移为 `/html/_incoming`。新增校验已针对当前服务器1.0.10实际通过：远端项目清单3457项完全一致，5项资源大小和MD5一致。部署完成瞬间可能短暂返回404，脚本会继续等待而不会提前误判。
- 2026-08-11 热更新上传失败的实际原因是SFTP绝对路径已与旧配置不一致：`client_update` 登录后工作目录为 `/www`，真实接收目录是 `/www/html/_incoming`，旧 `/html/_incoming` 不存在。`tools/upload_latest_hot_update.py` 默认路径和旧配置迁移已改为 `/www/html/_incoming`，上传前使用SFTP `ls` 显式校验目录，不再忽略目录错误并继续传输。本机密钥权限600、SFTP登录、真实目录读取、2.0.1 ZIP结构均已验证；当时远程 `/up/version.manifest` 仍为1.0.28，本轮排查未重新上传或触发部署。
- 2026-07-23 当前双击入口文件名为 `1生成热更新包.command` 与 `2上传最新热更新.command`。两者执行成功或失败后都会显示对应结果并等待回车；在 macOS Terminal 中按回车后会延迟0.2秒自动关闭当前前台终端窗口，不再依赖Terminal的窗口关闭偏好。失败时保留原Python退出码。最初误用zsh只读特殊变量`status`导致脚本启动即在第5行退出，现已改为普通变量`result_code`；已用模拟Python失败返回码分别实际执行两个入口，确认提示、回车和退出码传递正常，语法与`git diff --check`也通过。
- 2026-08-13 根目录已新增Mac双击入口 `3上传网页版.command`，调用 `tools/upload_web_mobile.py` 将现有 `build/web-mobile` 内容直接打成无外层目录的ZIP并部署到固定的 `/www/html/web-mobile`。脚本只读取 `.hot-update-upload-config.json` 中现有的SSH服务器、端口、用户和密钥，不读取、不询问也不改写热更新资源根地址或远端上传目录；正式目标在代码中固定并二次校验，运行前只需确认一次，不会因误输入版本号而改坏连接配置。上传前要求 `index.html`、`main.*.js` 和 `src/settings.*.js` 齐全，排除`.DS_Store`/AppleDouble并拒绝符号链接，生成后逐项测试ZIP；服务器端先校验压缩包SHA-256，再解压到唯一临时目录并核对文件数与`index.html`哈希，全部通过后才切换正式目录。原正式目录会保留为 `/www/html/.web-mobile.previous`，任何解压、切换或切换后校验失败都会尝试恢复；进入远端部署脚本后的临时ZIP在成功或失败退出时均删除。首次真实上传使用一次性SCP时在0%出现 `timeout, server 154.37.155.17 not responding`/`lost connection`，当时上传进程已退出而非卡住；随后HTTP 80返回Caddy、SSH 2233在0.2秒内认证成功，证明是传输链路瞬时中断。服务器确认具备`/usr/bin/rsync`且磁盘剩余17GB后，上传阶段已改为 `rsync --partial --append`，单次运行最多自动重试4次并从已传位置续传，最终完整ZIP哈希仍是部署门禁。修正版真实上传51.40MB耗时约6秒并一次成功，远端部署回报1719个文件，`index.html` SHA-256为`10359c3e9648fe3783c1ed1d03e29df598cd9761ad0d7a99c393118ff5b26bf0`，与本地和公网HTTP下载完全一致；`http://154.37.155.17/web-mobile/index.html`返回200，远端临时ZIP已清除。部署前正式`web-mobile`目录不存在，因此本次没有`.web-mobile.previous`。`https://154-37-155-17.sslip.io/web-mobile/`仍为SSL连接超时，属于独立的HTTPS/Caddy/证书入口问题，不能把HTTP部署成功误报为HTTPS已可访问。
- 2026-08-13 首次网页版部署后公网只停留在Cocos启动图标，浏览器实际复现到Cocos `ERROR 4928`，目标为 `src/assets/scripts/UI/qrcode.c935a.js`。该文件没有漏传：本地与远端均为14055字节且SHA-256同为`edc04ede82f6cbd5f988ba0c3a620e0a0b5b64c768f8a15ff56f6952f7f86b2f`；真正原因是Creator构建文件权限不统一且ZIP保留了Unix权限，`qrcode.c935a.js`远端为`600`，Caddy用户无权读取，公网连续5次请求均返回403。当前`build/web-mobile`共1719个文件，其中404个为600、596个为700，合计1000个文件没有other-read权限，因此不能只修这一份脚本。正式 `/www/html/web-mobile` 已原地规范为目录755、文件644，修复后不可读文件和不可进入目录均为0，文件总数仍为1719且首页哈希保持不变；`qrcode`、settings及internal/resources/main三个入口包均由403恢复为200，`qrcode`公网下载哈希与本地一致。`tools/upload_web_mobile.py` 已在每次解压到临时目录后、切换正式目录前统一设置目录755和文件644，并验证所有文件具备other-read、所有目录具备other-read/execute，任何失败仍触发现有回滚门禁。全新浏览器以防缓存参数打开正式HTTP地址后成功加载`login.fire`并显示8L登录界面，原`ERROR 4928`未再出现；控制台“初始化了一次KB”是项目主动以error级别输出的旧调试日志，不是本次故障。
- 2026-08-13 `https://154-37-155-17.sslip.io/web-mobile/` 公网无法访问已定位为服务器上游443入站被拦截，不是域名、证书、Caddy路由或网页目录问题。服务器侧公共DNS把该域名正确解析为`154.37.155.17`，Caddy进程正常并监听`:::80`和`:::443`，服务器本机以域名/SNI访问公网IP的HTTPS返回200；Let's Encrypt证书CN匹配该域名，有效期为2026-08-13至2026-11-11。服务器`firewalld`未运行且当前账号环境没有iptables命令。外部公共节点对HTTP 80的西班牙、伊朗、意大利三地检测均返回200，而对TCP 443的以色列、伊朗、美国三地检测均连接超时，证明443在到达Caddy之前被云厂商安全组、机房防火墙或上游网络ACL丢弃。`http://154-37-155-17.sslip.io/web-mobile` 也不能绕过：Caddy明确返回308并跳转到同域HTTPS，荷兰、瑞典、土耳其三地均确认HTTP域名解析到`154.37.155.17`且收到308，随后仍会卡在443；不建议为绕过端口问题关闭自动HTTPS，因为网页版麦克风等浏览器能力需要可信HTTPS。当前Mac同时开启Clash TUN/Fake-IP，本地域名被映射为`198.18.1.67`并经`utun4`路由，会干扰本机诊断，但外部节点结果证明它不是唯一根因。修复需要在服务器供应商控制台或联系机房放行入站TCP 443（IPv4来源通常为`0.0.0.0/0`，若启用IPv6再加`::/0`）；放行后必须用外部节点和真实手机网络复测HTTPS，不能只以服务器本机curl为准。本轮仅只读诊断，未修改Caddy、系统防火墙或供应商安全组。
- 2026-07-23 Android 真机首次验证1.0.6时卡在热更新页；ADB日志明确显示尚未请求远端即报“解析本地 manifest 文件失败”。服务器地址不是根因：同一手机用curl访问 `http://154.37.155.17/up/version.manifest` 返回200，Manifest为1.0.6，应用也已声明INTERNET并允许明文HTTP；Release APK内两份RawAsset manifest内容完整。根因是`panelUpdate.ts`把`cc.RawAsset`对象本身传给`jsb.AssetsManager.loadLocalManifest()`，Cocos 2.4.13应传`nativeUrl`字符串。现已增加`getLocalManifestUrl()`，优先取`nativeUrl`/`url`，并修正检查与更新两个加载入口。该源码修复尚需在现有Creator实例中重新构建Android、在Studio重新出包并真机复测；不要仅清缓存后继续测试旧APK。
- 2026-07-23 上述修复重新出包后的第二次真机验证已能解析Manifest并完成版本比较：日志显示本地1.0.6、远端1.0.6，正确进入`ALREADY_UP_TO_DATE`，因此进度条为0是“无需下载”而非网络卡住。后续阻塞来自首次安装`tempver == null`被旧逻辑误判为缓存过旧，删除Remote后强制`cc.game.restart()`；`panelUpdate.ts`现已把首次安装单独处理为记录`tempver=local`并直接打开`panelLogin`，只有真实的非空旧版本才删除缓存并重启。2026-07-23 对安装新包后升级到1.0.10的真机日志进一步确认：资源下载完成，实际卡在`cc.game.restart()`销毁常驻`GameDataManager`时，`onDestroy`触发`logout`，而KBEngine消息表已开始清理，导致`Bundle.newMessage(undefined)`连续抛出`Cannot read property 'length' of undefined`并阻断重启。现已移除`GameDataManager.onDestroy()`中的网络登出（Native重启会关闭旧连接），并在`kbengine.js`的`logout()`增加消息定义缺失保护；JS语法和diff检查通过。当前手机强制重启应用后已识别缓存本地1.0.10与远端1.0.10一致，并成功加载`panelLogin`且未再出现该异常，说明已下载资源完整可用；源码修复仍需随下一个更高版本热更新包真机验证“一次更新即可自动重启进入登录页”。
- 2026-07-23 Android真机更新到1.0.18后进一步确认：更新文件、`Remote`搜索路径和`main.js`启动恢复均已生效，新版`panelMain`导入文件也确实下载到手机；完全杀进程冷启动后，`jsb.fileUtils`能把新配置和新版Prefab正确解析到`Remote`，但运行时`resources` Bundle仍持有安装包旧Prefab映射`2444b`，而远端新配置映射为`9b875`，实际大厅节点树中也没有新版测试文字。最终根因不是单纯软重启缓存，而是Creator 2.4 Native内置Bundle加载器用相对`assets/resources`初始化时没有按预期采用热更新目录。生成器现除恢复搜索路径外，还会在`main.js`中提取热更新根目录，并用绝对`<Remote>/assets/resources`加载内置`resources` Bundle；补丁可重复执行且Python/JS语法及幂等检查通过。Android更新成功后仍通过`AppActivity.RestartApplication()`启动新进程，确保重新初始化Bundle；`:qing:compileDebugJavaWithJavac`已通过。上述`main.js`和Java都属于基础原生包内容，需重新构建安装APK后再制作更高版本热更新验证，不能只上传热更新ZIP。重新生成原生工程时仍需保留或重新应用。
- 2026-07-23 首版绝对Bundle路径修复真机出现黑屏，日志显示基础包`settings.bundleVers.resources`要求`config.3127e.json`，而热更新目录的实际配置哈希不同，直接沿用基础包哈希会反复读取不存在的文件。现已改为启动时枚举`Remote/assets/resources/config.<hash>.json`，仅在同哈希`index.<hash>.js/.jsc`同时存在时启用该热更新Bundle，并同步覆盖本次启动的`settings.bundleVers.resources`；未找到完整配对时不设置热更新根目录，自动回退APK内置`resources`，避免首次安装或不完整缓存黑屏。当前黑屏手机上已用V8验证可正确识别实际`config.8d00d.json`及其`index.8d00d.jsc`，Python/JS语法、补丁幂等和diff检查通过；仍需重新构建安装基础APK后做启动及下一版热更新验证。
- 2026-07-23 经官方Creator 2.4文档复核后，已放弃上述“绝对Bundle路径+枚举config哈希”的试验方案：它混用了传统`jsb.AssetsManager/searchPaths`与2.4 Bundle MD5版本体系，真机已证明会因基础包与远端`bundleVers`不一致而黑屏，不作为正式方案。当前恢复官方传统流程：`main.js`只在所有脚本加载前恢复`HotUpdateSearchPaths`，`panelUpdate.ts`更新成功后使用`cc.game.restart()`，Android临时原生强制重启方法已删除。生成器新增官方兼容性门禁，若检测到`settings.<hash>.js`或`assets/*/config.<hash>.json`则拒绝生成，并提示在Creator构建面板关闭MD5 Cache后重新构建；禁止为此直接删除整个`build/jsb-link`，以免丢失现有原生定制。当前旧构建确实被门禁拦截（可见`settings.405b2.js`及三个哈希Bundle配置），Python/JS语法、Android Java编译和diff检查通过；下一步需由用户在现有Creator实例确认MD5 Cache关闭并重新构建，再验证稳定文件名、生成热更包和真机A→B更新。
- 2026-07-23 用户关闭MD5 Cache重新构建后，官方兼容性门禁通过：产物已稳定为`src/settings.jsc`以及`assets/{internal,main,resources}/config.json`、`index.jsc`。Creator重构建会覆盖`main.js`补丁，生成器已成功自动恢复官方搜索路径代码。无MD5产物同时让两份RawAsset清单文件名从`UUID.<hash>.manifest`变为`UUID.manifest`，生成器现兼容两种命名。1.0.21官方结构测试包已在本地生成，清单含3457项，明确包含稳定的`src/settings.jsc`、`assets/resources/config.json`和`index.jsc`；ZIP共3459项、无`._*`/`.DS_Store`且完整性测试通过，项目内及Native构建清单已同步，下次版本为1.0.22。尚未上传服务器；应先从当前已补丁和同步清单的Native工程重新出基础APK并安装，再发布更高版本做A→B真机更新，不能把同一1.0.21同时作为基础包和“新版本”验证。
- 2026-07-23 1.0.21基础APK干净安装后升级1.0.22，资源下载完成但更新页卡住。日志显示基础APK中的`panelUpdate`仍是已撤销的原生强制重启版本，调用已不存在的`AppActivity.RestartApplication()`触发`JavaScriptJavaBridge::CallInfo isn't valid`，且旧代码无论反射是否真正成功都会立即`return`，没有执行回退`cc.game.restart()`。1.0.21与1.0.22清单差异仅有新版`panelMain` Prefab和两份Manifest，`assets/main/index.jsc`完全未变化，证明Creator构建复用了旧脚本产物而非热更下载失败。ADB系统级强制重启后已恢复`Remote`搜索路径并识别当前为最新版本，无配置加载错误。当前`panelUpdate.ts`官方重启日志增加唯一`v2`标记；生成器新增脚本新鲜度门禁，若`assets/main/index.jsc`修改时间早于任一`assets/scripts/*.ts`则拒绝生成，当前旧构建已正确被拦截。需等待Creator完成脚本编译并重新构建，确认`assets/main/index.jsc`变化后再生成下一版本。
- 2026-07-23 用户再次修改大厅文字并重构建后，官方兼容性和脚本新鲜度门禁均通过：构建后的`panelMain`导入JSON含测试文字，`assets/main/index.jsc`时间晚于`panelUpdate.ts`。1.0.23相对1.0.22恰好变化4项：`assets/main/index.jsc`、`panelMain`导入JSON及两份RawAsset Manifest，ZIP完整性通过。真机成功下载并将本地版本更新为1.0.23，但该次更新仍由进程启动时已加载的1.0.22旧脚本收尾，旧脚本调用已删除的`AppActivity.RestartApplication()`失败后停在结束页；刚下载的新版`index.jsc`不可能反向替换当前正在执行的JS。ADB强制停止并重启一次后，客户端识别本地/远端均为1.0.23并正常进入大厅，证明更新文件与搜索路径有效。修改更新器自身时必须把当前更新视为一次迁移版本并允许一次外部重启；真正验证新版`cc.game.restart()`需从已运行1.0.23脚本的客户端继续升级到1.0.24或更高版本。
- 2026-08-12 `1生成热更新包.command` 再次运行失败的原因不是入口权限或脚本语法，而是脚本新鲜度门禁正确拦截了旧 Native 构建：热更新生成器固定读取 `build/jsb-link`，其中 `assets/main/index.jsc` 仍为15:08。23:24那次构建使用 `web-mobile` 并输出到 `/private/tmp/qing-round-countdown-qa/web-mobile`，没有更新Native；用户随后虽切回Android/Link，但把Creator“发布路径”误填为 `build/jsb-link`，Creator会自动追加平台目录，导致23:53的新Native实际生成在 `build/jsb-link/jsb-link`。发布路径应填写父目录 `build`（或绝对路径 `/Volumes/CCCC/qing/build`），最终产物才会落到生成器预期的 `build/jsb-link`。Web构建和嵌套Native构建均未损坏旧目录，标题“8L”也不是故障原因；不能复制Web版`index.js`替代、不能让生成器自动猜测嵌套目录，也不得删除门禁或伪造时间绕过。重新按正确父目录构建前仍需保留现有原生定制，不能手动删除整个旧 `build/jsb-link`。
- 登录换皮已以提交 `c0859bc` 推送到 `origin/main`；提交正文使用中文详细记录了资源、结构、验证结果和待验证风险。
- 共享弹窗背景 `assets/imagesKK/公用/框.png` 已经用户确认并替换为黑曜石、暗金多层边框、秦纹标题栏和底部秦印收口风格。图片保持635×680 RGBA，`.meta`、纹理UUID、SpriteFrame UUID及九宫格边距左83/右81/上106/下42均未改变；12个场景或Prefab共34处原引用继续生效。PNG、尺寸、透明通道和引用数静态检查通过，Creator实际显示仍由用户验证。
- 同目录4张原蓝色主操作按钮已同步为黑曜石暗金风：`充值.png`（154×54，1处引用）、`确定.png`（295×85，47处引用）、`赠送按钮.png`（305×85，1处引用）、`赠送按钮1.png`（258×84，1处引用）。均保持原文件名、尺寸、RGBA、`.meta`和UUID；箭头/返回/开关图标及原本米金色的`取消.png`不在本轮范围。静态图片检查通过，缩放显示和点击区域仍待用户在Creator中验证。
- `panelMain` 游戏大厅“发现”页首版效果已获用户确认并完成原位换皮：保留顶部“游戏大厅/客服”、快速加入及1/2/5/10/20档位、底部五项导航的现有结构；顶部仍严格使用750×92窄标题带和128×39中央标题，没有扩大节点或装饰占用范围。
- 发现页基础美术已原位覆盖31张现有PNG，包含大厅背景、共用顶部、客服、快速加入、房间卡、档位、底部导航和房间状态；共用图片按用户要求直接覆盖，因此`顶部.png`等其他引用位置会同步换皮。所有基础图片保持原文件名、像素尺寸、RGBA/RGB模式、自动裁剪边界、`.meta`和UUID。
- 用户随后明确取消大厅LOGO和底部“发现”按钮的DragonBones动画。`panelMain`保留原`LOGO`与`Down/发现/New Node`节点，但动画组件已改为普通`cc.Sprite`：顶部绑定`秦_大厅主视觉.png`（672×349），保持用户已保存的y=400.27/Widget top=92.225；底部绑定`秦_发现按钮.png`（209×146），为补偿旧骨骼原点将节点y从-77.97改为-4.295。两张静态图均使用CUSTOM尺寸、trim=false；对应动画图集不再被`panelMain`运行时引用。
- `地九王`不是旧游戏品牌名，而是本游戏的一种特殊规则；任何换皮都必须保留“地九王”原文字义，不得改成“秦牌九”等品牌字样。发现页房间卡的`地九王.png`已恢复为黑金“地九王”，倒计时Label（示例“剩余:30:00”）颜色已由蓝色改为暖金RGB(232,193,111)，规则逻辑和动态时间内容未改。
- 大厅换皮可由`tools/generate_qin_hall_skin.py`从`art_sources/hall/`源图确定性重建，当前脚本输出31张基础图片和2张静态主视觉图片；静态合成预览为`art_sources/hall/qin_hall_runtime_preview.png`。两张静态图尺寸、RGBA、透明裁剪、UUID及Prefab引用检查通过，实际显示与点击仍由用户在现有Creator实例中验证。
- `panelMain.prefab`剩余内嵌页面与弹窗已在2026-07-22完成补充换皮：按SpriteFrame UUID审计了127张直接引用图片，重点原位重绘公告/惩罚列表、加入房间数字键盘、推广二维码、设置、修改个人信息、修改登录/交易密码、赠送/受赠记录、资金明细等旧蓝色组件，共由`tools/generate_qin_panel_main_remaining_skin.py`确定性生成72张目标资源；共享`公用/输入框.png`、`公用/取消.png`和`登陆/CHACHA.png`也同步统一为黑曜石暗金风，因此其其他引用位置会一并变化。
- `panelMain`补充换皮使用`art_sources/panel_main_remaining/qin_obsidian_lacquer_source.png`作为克制黑曜石/黑漆母材质，禁止重新引入蓝青霓虹和密集纹样；公告1至5的既有中文说明及牌面示意通过本地色彩映射保留内容，只替换旧蓝青/紫色界面色，不使用AI重写正文。总览预览为`art_sources/panel_main_remaining/qin_panel_main_remaining_preview.jpg`。全部127张Prefab引用图片尺寸与`.meta`一致、没有修改任何UUID，`panelMain.prefab`保留原节点/Widget/按钮和业务结构，仅把7处亮青色动态文字改为暖金、2处蓝灰占位色改为灰金；Prefab JSON解析和`git diff --check`通过，Creator实际弹窗逐项显示与交互仍待用户验证。
- 用户反馈公告背景的石纹颗粒和四角云纹过于复杂；`公告底.png`、`弹窗公告底.png`、`透明底框.png`及`惩罚标题框.png`已改为无纹样的纯净暖黑漆渐变，前三者只保留大面积留白与细暖金边，标题框也取消角花。生成脚本已用`clean_lacquer()`固定该简洁方向，后续重建不得恢复整面石纹或四角装饰；对比预览为`art_sources/panel_main_remaining/qin_announcement_clean_backgrounds_preview.jpg`。
- `panelNotifyView`、`panelNotifyViewCZ`、`panelNotifyViewHD`三个同构公告弹窗已在2026-07-23统一完成专用黑金换皮：新增633×880 RGBA的`assets/ImagesLuck/公告/秦_通知弹窗底.png`并只重绑这三个Prefab，避免被`generate_qin_panel_main_remaining_skin.py`后续重建共享`弹窗公告底.png`时覆盖。新背景按真实700×880显示区域组织为清爽标题栏、纯净正文阅读区和底部操作区，只保留细金边、单枚秦印与极少量收口线；`最新公告/充值公告/活动公告`三张旧蓝青标题原位改为暖金字。三个Prefab的动态正文均从纯白改为象牙金RGB(232,215,180)，共享黑金`确定.png`继续复用且未覆盖。
- 三个公告Prefab原右上163×191关闭热区只有名为`确定`的Button而无可见图标；现保留该节点、名称、尺寸和点击逻辑不动，在`bk`末尾新增45×45的`关闭图标`视觉节点并复用现有金色`公用/btn_4.png`。`panelMsgView.ts`、Base64解码、`bk/msg`硬编码路径、服务器动态正文、字号/换行/收缩行为及确定/取消关闭规则均未修改。`tools/generate_qin_notify_view_skin.py`使用`art_sources/notify/qin_notify_lacquer_source.png`确定性生成4张专用运行图片和两张预览，`tools/patch_qin_notify_view_prefabs.py`可重复应用三份Prefab绑定，`tools/validate_qin_notify_view_skin.py`执行只读全量校验；双遍哈希、PNG尺寸/RGBA/Meta透明裁剪、强蓝青为0、关键节点/UUID/标题状态/共享资源保护和运行契约均已通过，Creator中的服务器长公告缩放及三个入口点击仍待用户在现有实例中验证。
- 2026-08-14 大厅首次进入的“最新公告”弹窗已按当前8L牌桌深蓝黑、银蓝金属风重新设计，并与充值/活动公告拆分资源：新增633×880的`assets/ImagesLuck/公告/大厅公告底_8L.png`，只把`panelNotifyView.prefab`的`bk`重绑到新SpriteFrame；`panelNotifyViewCZ`、`panelNotifyViewHD`继续使用原`秦_通知弹窗底.png`，因此充值/活动公告不受影响。新底图使用厚实银蓝外框、独立标题徽章、暗纹牌桌绒面正文舱和雕刻式底部操作区，160色调色板压缩后约371KB；“最新公告”标题由y=379.431微调到362并从1.4倍放大到1.65倍以居中匹配新标题牌。正文ScrollView、无可见滚动条、服务器动态文字、关闭热区、确认/取消按钮名及`panelMsgView.ts`均未修改。Prefab/Meta断言、JSON解析、`git diff --check`、Creator资源导入和Creator 2.4.13实际Prefab叠加预览通过；正式构建、上传及真实长公告拖动仍待发布流程验证。
- `assets/resources/Prefabs/排行榜.prefab`已按同一黑金秦风完成美术原位替换：排行榜目录16张专用PNG全部覆盖，准确保留“排行榜”“开始时间/结束时间”“玩家手数排行榜”“名次/玩家信息/手数/奖励”及`1皮/2皮/5皮/10皮/20皮`规则文字；节点、Toggle、动态Label、服务端字段和两个排行榜条目模板均未改动。
- 排行榜同时覆盖12张其引用的共用PNG，包括通用背景、垫底、表格标题头、金币、列表分割线、返回、四个分页箭头、栏目标题框和提示底框；这些图片会按用户确认的共用资源原则同步影响其他界面。`tools/generate_qin_ranking_skin.py`可从`art_sources/ranking/qin_ranking_trophy_source.png`确定性重建28张运行图片，静态合成预览为`art_sources/ranking/qin_ranking_runtime_preview.png`。28张图片的尺寸、RGBA/RGB模式和自动裁剪范围均与现有`.meta`一致，强蓝色像素检查为0，`.meta`/UUID未修改；Creator实际布局和交互仍由用户在现有实例中验证。
- `assets/resources/zuotype/1.jpg`至`5.jpg`五张竖屏牌桌背景已按用户确认的候选图原位替换，依次为克制黑金、皇家深蓝、经典翡翠绿、低饱和酒红和暖灰胡桃木风格。五图均保持原文件名、750×1334 JPEG规格及原`.meta`/UUID，中央牌面操作区无文字和品牌标记；静态格式和画面检查通过，Creator中的实际遮挡、缩放与牌局UI叠加效果仍待用户在现有实例中验证。
- 2026-07-23 真机反馈牌局设置中的5个桌面预览与点击后实际背景不一致。根因是`drh8.fire`内嵌的`panelGameView/桌面1`至`桌面5/Background`仍绑定`ImagesLuck/游戏内/额外/1.png`至`5.png`旧缩略图，而`UpdateTableImg()`实际按序加载`resources/zuotype/1.jpg`至`5.jpg`；运行时使用场景内嵌实例，不会读取同名Prefab。现已把`drh8.fire`内5个预览Sprite直接重绑到对应真实桌面SpriteFrame UUID，并同步更新`panelGameView.prefab`保持两处一致；均使用CUSTOM模式及原95×175节点尺寸，Toggle名称、索引、本地存储和切换逻辑未改。两个序列化文件的JSON、1→1至5→5映射和`git diff --check`已通过，Creator与真机显示仍待验证。
- `panelMain/Main/我的`主页面已完成黑金秦风换皮：原位覆盖18张“我的”专用PNG及2张共享头像PNG，包含页面标题、头像框、默认头像、上传按钮、金币框、VIP垫/三种VIP卡、五个隐藏统计标题和六个功能选择条；六个入口继续准确保留“战绩、代理、交易明细、赠送/受赠、个人数据、设置”文字及原业务节点名、顺序、SpriteFrame UUID和点击逻辑。
- 2026-08-11 “我的”页代理入口已改为默认隐藏，并在 `level` 或 `role` 属性到达/变化时按“等级大于0，或角色包含盟主、合伙人、总裁、老板”动态显示；点击入口也复用同一判断，不能只靠按钮隐藏。线上只读统计确认当前5名普通玩家等级均为0，两名代理身份玩家等级为98/99。修改或初始化交易密码收到服务端成功回调后，会同时清空两个面板共5个密码输入框并把本地初始化状态切换为已设置，失败时保留输入便于修正。资金明细表头已在 `panelMain.prefab` 中直接改为可预览的“说明、收/支、流向后、时间”四列，`资金明细对象.prefab` 同步显示四列并启用原隐藏的 `now` 余额；收支不再使用符号含义不稳定的 `add_money`，统一按 `new_money-old_money` 计算，收入显示红色 `+`，支出显示绿色负数，流向后显示 `new_money`。TypeScript语法、两个Prefab JSON/对象引用、列坐标、代理默认隐藏、头像流程和 `git diff --check` 已通过，Creator运行态的真实账号切换、交易密码成功回调及真实流水显示仍待交互验证。
- 2026-08-12 首次坐下已按KB新协议从一次性“座位+带入”改为两步：原补芒预检查保留，通过后客户端先向 `reqRoomCommand` 发送 `header=预坐_事件, site=<实际座位号>`，不提前显示带入窗口；`DrhLogicMgr` 完成下一份PlayerList的玩家绑定和座位显示后通知 `panelGameView`，只有确认本人出现在预定座位才自动复用“补充钵钵”界面。确认带入发送不含 `site` 的 `header=坐下_事件, money, ready=1, type=直接, club_id`，第二参数仍为 `坐下_事件1`；已经正式在座的普通补分继续使用 `type=申请`。预坐状态分为等待PlayerList、选择带入和提交中，重复点击会被拦截；选择阶段关闭主带入框、关闭余额不足层、点击牌桌遮罩或打开会统一收起界面的入口都会主动发送 `起立_事件`，服务端超时后PlayerList移除本人时客户端也会关闭弹窗并清理状态。服务端字段 `a_h_m_w` 对应的 `arraySited` 快速坐下分支保持原样：命中列表仍直接发送仅含 `header=坐下_事件, site` 的请求，不预坐、不选分、不传 `money`；客户端仍不自行验证剩余分，资格由KB生成该列表并二次校验。预坐回包日志中的 `onRoomCommand302` 是十六进制错误码 `0x302`，现已映射为“带入金币不足！”模态弹窗；同一错误随后再由 `PromptInfo` 返回时会短时去重，服务端历史JSON中的尾逗号及逗号后空白也可安全解析，失败只清理本地预坐状态而不会多发起立。TypeScript转译语法、关键协议静态断言和 `git diff --check` 已通过；真实KB的PlayerList留座显示、取消/超时释放及正式带入扣款仍待联调。
- 2026-08-12 房间内排队撮合与快速换房客户端已按《排队撮合功能接口文档.md》实现。新增常驻 `QueueMatchManager` 统一持有申请、取消、查询、`QueueMatchInfo` 推送、跨场景目标房/座位/`queue_id` 和切换代次；`panelMain`、`panelGameView`、`GameDataManager` 的进房导航回包会在撮合切换期间让协调器独占消费。同房匹配直接排队预坐；大厅收到匹配直接进入目标房；身在其他房间时，离房成功后先进入极轻的 `roomTransition`，待旧 `drh8` 完成销毁再进目标房并加载全新 `drh8`。进入目标房成功但排队预坐失败时，玩家留在该新房且继续保持排队，不重复抢同一座位，等待服务端下一次分配；下一次同房分配直接预坐，异房分配再执行轻量场景切换。排队预坐一经服务端确认成功立即结束撮合，随后PlayerList占位确认、15秒带入选择和普通坐下继续复用原流程。
- 2026-08-15 结算后排队日志只读复核确认：跨房流程中的 `queue_pre_site` 确实早于新 `drh8` 的 `LoadScene` 日志，但它只会在常驻 `QueueMatchManager` 已收到目标房 `onEnterRoom(0x200, targetRoomID)` 后发送，并非尚未进入目标房就预坐；管理器跨场景保存预约和预坐回包，新牌桌创建时通过 `onRoomViewReady()`重放预约，`DrhLogicMgr`再主动请求目标房PlayerList。该顺序用于先锁定短时分配的座位，不能仅因画面尚未切完判定为过早。本次截图中目标房PlayerList已出现本人15秒`site_countdown`，未见预坐失败证据。仍有待验证边界：网页冷启动资源预载若超过预坐成功后的20秒本地预约有效期或服务端15秒带入窗口，可能在新界面出现前失去带入流程；简单把预坐延后到场景完成又可能错过分配窗口，应另行以预加载/服务端时限协同解决。截图中同一`queue_id`在`left_seconds`递减期间反复`reqEnterRoom`不是重复预坐；当前客户端只有入房失败、房间号不符或换房对账后才会回到查询并再次进入，截图缺少对应`onEnterRoom`错误码，暂不能确定具体原因。
- 2026-08-12 房间牌桌右上“排队”入口和完整弹窗已直接加入 `drh8.fire`，层级为 `RoomFrame/排队` 与 `panelGameView/排队弹窗`；标题、紧凑状态区、四列表头、名单滚动视口、空状态、完整玩家行模板、申请/取消按钮和关闭按钮均为 Creator 可直接选择调试的场景节点，脚本运行时只克隆场景内模板并填充服务端数据，不生成整套界面。入口只在 `room_mode=积分房卡房` 且 `play_mode=传销扯旋` 的首期支持房型显示。名单参考其他游戏的“表头+玩家卡片”信息结构，但美术继续复用本项目的蓝青框体、字体、关闭图标、按钮和本地头像资源；四列显示玩家信息（头像、昵称、ID及本人标识）、已排队时长、底皮和服务端状态，本人行使用项目青色高亮。弹窗高度已由760增至1100，名单视口高度由340增至636，按每行106像素可一次完整显示6名玩家；表头、列表、申请按钮和温馨提示的纵向区间已重新布局并通过静态无重叠检查，空名单/加载/错误状态独立居中显示，超过6人后通过 ScrollView 滚动。弹窗根节点自带 `cc.BlockInputEvents`，每次打开会按 `cc.view.getVisibleSize()` 将根节点和遮罩扩展到当前设备可视区域并额外覆盖边缘，长屏手机不会露出或点击到后方牌桌。快速换房保留 `drh8.fire autoReleaseAssets=false` 以复用静态资源，同时在旧场景销毁时注销KB和 `cc.game.EVENT_HIDE/SHOW` 监听、停止计时与动作、清理语音房和 `ImageManager` 的失效场景Sprite引用，并在牌桌动态资源回调中检查节点有效性，避免旧场景异步回调污染新场景。
- 2026-08-12 `roomTransition.fire` 快速换房过渡页采用极简结构，场景中只保留可直接调试的蓝色机械大厅全屏背景和居中的旋转Loading图标；不显示提示面板、标题、房间号或说明文字，也不在运行时创建UI。背景复用登录阶段已加载的资源，`QueueMatchManager` 只按可视区等比放大背景以覆盖长屏，不新增美术下载成本。
- 2026-08-12 快速换房防卡保护已限定在常驻 `QueueMatchManager` 的排队导航阶段，不修改普通进房和全局重连策略。收到分配后按服务端 `left_seconds/timeout` 加2秒宽限设置与 `switchGeneration` 绑定的看门狗；离房、过渡、进房或预坐无回包时先调用 `查询_排队_撮合` 对账，`assigning` 才按当前账号 `roomID` 恢复导航，`queued/none/failed` 则按账号当前房间回到全新 `drh8` 或大厅。切换期间断线会废弃旧命令和旧场景回调，重连成功后立即对账；连续30秒未重连或对账查询再超时会退出过渡页。客户端记录已发送预坐的 `queue_id`，对账时不会重复发送同一座位的预坐请求，避免违反服务端禁止自重试规则。
- 2026-08-12 更新后的 `查询_排队_列表` 已接入：请求上下文固定 `p@queue_match_list`，按参考房间请求第0页30人；回包按外层 `context=queue_match_list` 与内层 `result` 独立路由，不会被申请/取消/单人查询误消费。弹窗打开时请求一次，申请或取消完成后按需刷新，显示排行、昵称、玩家ID、排队时长、底皮、`排队中/入座中` 和本人标记；名单只用于展示，不触发切房。列表超时和服务端 `failed` 会显示明确状态。Cocos Creator 2.4.13 `web-mobile` 先前实际构建通过，产物包含 `login/drh8/roomTransition`、新版请求逻辑及名单ScrollView/Mask/Content节点。2026-08-12 同房有空位的真实申请确认回包顺序可能为 `QueueMatchInfo matched → PlayerList占位 → onRoomCommand200(queue_pre_site)`；原实现由 `panelGameView.OnPlayerListUpdatedForPreSit()` 提前清空管理器预约，导致后到的成功回包执行 `copyReservation(null)`。现已为预约增加 `playerListConfirmed`，PlayerList先到时只标记确认，成功回包使用稳定预约引用、派发完成后才按双确认状态清理；`panelGameView` 同时禁止迟到成功事件把 `selecting/submitting` 倒退为 `waiting`，也不会在玩家已主动取消带入后重新武装。排队弹窗现会在状态进入 `assigning/switching/pre_sitting` 时立即关闭，覆盖同房预坐和异房切换，`QueuePreSitAccepted` 再兜底关闭一次；申请或取消只有收到服务端 `accepted/cancelled` 成功状态后才调度刷新当前参考房间名单，失败回包不会用额外查询覆盖错误信息。两个TS文件的ES5语法转译、竞态与弹窗/刷新关键静态断言和 `git diff --check` 通过；用户明确本次只要求修改代码，Creator构建和真实重复申请由用户操作验证。控制台随后出现的 `entity(...) not found` 暂无证据证明是申请失败根因，应与已修复的空引用分开处理。弱网慢回包、断线重连、30秒兜底、迟到回包及连续多次换房仍待联调。
- 2026-08-12 排队弹窗打开期间会每5秒静默调用一次 `查询_排队_列表` 对账真实名单，关闭弹窗或服务器开始分配座位时立即停止定时任务；静默刷新不会清空当前卡片、显示加载层或因单次超时覆盖上一次成功名单。名单成功回包会保存每人的 `queue_seconds`（缺少时从 `queue_time` 解析）及本地接收时间，弹窗每秒按“服务端秒数+回包后经过秒数”更新所有玩家行的已排队时长，因此服务器两次刷新之间也会连续走秒；下一次成功名单回包会重新校准。相关两个TS文件已通过ES5语法转译和 `git diff --check`，Creator运行态与真实5秒轮询仍待用户联调。
- 2026-08-12 牌桌右上排队入口现以服务端 `PlayerList` 中本人是否占有座位为显示依据：场景加载后先隐藏，首个名单确认本人未坐下且房型支持时才显示，避免断线重回已坐座位时短暂出现“排队中”；预坐成功、普通坐下成功或名单确认本人已在座位时立即隐藏入口，同时关闭排队弹窗并停止名单定时刷新。服务端确认起立或后续名单确认本人不在座位后，支持排队的房间才恢复入口。该修改已通过ES5语法转译和 `git diff --check`，Creator运行态待验证。
- 2026-08-13 排队预坐后取消带入曾出现右上角仍显示“排队中”。根因是真实回包顺序可以为 `PlayerList占座 → 用户取消带入 → onRoomCommand(queue_pre_site)`，旧实现在 PlayerList 先到时只记录占座、仍等命令回包结束排队，起立后入口又只恢复显隐而不重绘文字。当前规则已修正为：目标房 PlayerList 确认本人出现在预定座位即作为预坐成功的权威事实，`QueueMatchManager` 立即清除本地排队展示和快速换房阶段，同时保留预约以消费迟到回包；右上角入口每次重新显示都从常驻快照重绘“排队/排队中”。这一当前规则取代了上述 2026-08-12 “PlayerList先到时只标记确认”的旧策略。两个TS文件的Babel TypeScript语法解析和定向 `git diff --check` 通过；Creator构建按用户要求未执行，真实取消带入回归待用户联调。
- 2026-08-12 排队名单玩家对象新增 `online` 显示支持：客户端将数字/字符串 `1` 识别为在线、`0` 识别为离线，缺失或未知值显示“未知”；`drh8.fire` 的玩家行模板已直接加入独立“在线状态”节点，状态列上方保留“排队中/入座中”等业务状态，下方以项目青绿、灰蓝、金色分别显示在线、离线、未知。接口兼容 `status=queued/waiting/assigning/matched/seating` 英文状态码、数字 `bottom` 自动补“皮”，并继续优先使用 `queue_seconds`；缺少该字段时，`queue_time` 既支持旧的“分钟:秒”，也支持秒/毫秒Unix入队时间戳。场景引用、两个TS文件ES5语法转译和 `git diff --check` 已通过，真实 `online` 0/1与时间戳回包显示待联调。
- 2026-08-12 牌桌座位头像下方的 `PlayerInfo/offline` 图标与排队列表新增的 `online` 字段无关。它由牌桌 `PlayerList.Players[]` 的两条兼容条件共同控制：`status="offline"` 会映射为 `PlayerState.offline` 并显示；其他状态下若 `client_death.toString()` 精确等于 `"True"` 也会显示，否则隐藏。解析位于 `DrhLogicMgr.OnUpdatePlayerList()`，最终显隐位于 `DrhPlayerLogic.ShowHideOffline()`。
- 2026-08-12 排队跨房进入已有玩家的目标房后，真实复现过本人座位错误显示离线图标。同期画面与控制台证据确认账号237737的最新 `PlayerList` 为 `status="ready"`、`client_death="False"`，但离线Sprite仍处于显示状态，因此已排除PlayerList和排队名单 `online` 字段。`ClientDeath`/`EntitiesEnabled` 是服务端经账号实体通用 `onMessage/onPush` 下发、再由 `Account.js` 转发为客户端KBEngine事件，并非牌桌本地推算。根因是旧实现只按事件 `number` 查座位映射，忽略玩家 `id` 和房间上下文，跨房迟到消息可直接切换新房同座位UI且不更新模型。服务端现已为两类事件补充 `room_id`；客户端已实施最小兼容保护：只有事件 `room_id` 同时匹配账号当前房间和已验收PlayerList房间、`number` 命中当前全量重建的Map、`id` 匹配该座位玩家，且可选 `roomType` 为 `Custom` 时才更新 `strDeadState` 与图标；旧格式、解析失败、字段缺失、跨房、错座和错玩家消息全部忽略，本人收到的 `ClientDeath` 也不再把当前连接标为离线。迟到的旧房PlayerList不会覆盖已验收房间上下文；未主动触发全量查询，避免异常增量消息形成刷新风暴。`git diff --check` 已通过，按用户要求未执行Creator编译构建，真实断线/重连/跨房联调待用户验证。
- 2026-08-15 对跨房`PlayerList`重新审计后确认，现有保护只保证房间号不匹配时不会写入`strMsgRoomID`、不会重建`Players`/座位映射及后续牌桌UI，并非整包完全忽略。`DrhLogicMgr.OnUpdatePlayerList()`在房间号校验之前已经写入`strGameState`、创建者/俱乐部信息、开局/结束时间、`arraySited`、`arrayHisIn`、`game_end_time`，且`total_reward_money`会立即刷新奖池；因此在当前房间收到其他房间或缺失`room_id`的合法PlayerList时会“半处理后返回”，可能短暂影响解散按钮、退出条件、邀请入口、实时战绩时间和奖池，直到本房下一条PlayerList纠正。当前只做只读诊断，尚未修改代码；最小安全修复应在JSON解析后用局部变量先读取`room_id`与`GameStatus`，对当前非0房间不匹配消息立即返回，再提交任何成员/UI变更，同时单独保留并收紧`roomID==0 && end`的历史结算分支。
- 2026-08-13 再次多客户端联调后确认，账号237737经排队进入已经开局的房间时，头像上的蓝黑状态牌实际是“观战”，不是“离线”。目标房PlayerList明确返回该玩家 `status="init"`、`client_death="False"`，控制台搜索也没有对应 `ClientDeath`；`DrhPlayerLogic.UpdateUserInfo()` 在房间 `GameStatus="running"` 且玩家状态为 `init` 时会显示 `PlayerInfo/扩展状态` 并加载 `resources/other/观战.png`。该资源与 `resources/other/离线.png` 使用几乎相同的蓝黑圆角框，小尺寸下容易误认；所有客户端一致显示是正常的服务端状态展示，不是共享离线故障。此前关于“服务端误广播新房ClientDeath”的推测已被本次现场证据否定；现有ClientDeath房间/玩家/座位校验保留，不需要为本次现象再改业务代码。
- 2026-08-13 已修复本机牌桌0号显示槽低概率把其他留座玩家旁边的“回坐”按钮一并显示，并修复首次补丁造成的网页崩溃回归。原显示问题来自0号数组槽虽固定标为 `PlayerPos.self`，但本人不在座时可绑定其他玩家；旧代码只在账号匹配时设true，不匹配时没有关闭。首次补丁误把 `PlayerInfo/回坐` 改成所有玩家刷新时无条件访问，而场景/Prefab的8个玩家节点中只有0号拥有该子节点，另外7个返回null；真实PlayerList持续刷新遂累计约639条 `Cannot set properties of null (setting 'active')`，调用链为 `UpdateUserInfo → UpdateAllUserShowInfo → OnUpdatePlayerList`，最终导致Edge“此页存在问题/错误代码15”和高CPU。最终安全实现只在 `playerPos == PlayerPos.self` 时获取“回坐”节点，并保留null保护：留座中按玩家ID幂等赋值，本人true、0号槽绑定他人false；非留座也只在0号槽关闭。其他7个玩家节点完全不访问该子节点；点击端玩家ID保护、服务端协议、座位轮转、留座倒计时和正常回座流程均未改。16组初始状态/留座/槽位/账号矩阵0失败、场景结构检查、独立TypeScript转译、`git diff --check`通过；现有Creator实例经本地预览“Recompile”生成ES5产物，确认两处分支均含0号槽与null保护，Edge重新加载到登录页正常。真实登录牌桌PlayerList和多客户端座位重绑仍待联调确认。
- 旧默认头像中的`BATTLE`品牌已移除；初版无文字秦将头像因整体过黑被用户否定，2026-07-22 已改为明亮亲和的棋牌游戏人物：暖红金云纹背景、清晰暖肤色、象牙与红金衣领、轻量秦式发冠，不使用盔甲、阴影遮脸、文字或外部品牌。共享`resources/other/默认头像.png`会同步影响31处/10个文件，`ImagesLuck/公用/头像2.png`仍作为金色头像框同步影响26处/7个文件；昵称、ID、金币、VIP到期时间及隐藏统计数据仍由原动态Label显示，未修改数据字段、显隐逻辑或头像层级。
- 20张玩家头像库位于`assets/resources/avatars/头像01.png`至`头像20.png`：包含男女、青年/中年/老人及文士、侠客、商贾、将领、乐师、长者等不同气质，统一为明亮暖色、清晰五官、圆形金边的秦风棋牌游戏插画。每张为256×256 RGBA，拥有独立Texture/SpriteFrame UUID和Creator 2.4.13 `.meta`；母版与总览位于`art_sources/avatars/`，`tools/generate_qin_avatar_library.py`可确定性重新裁切和打包。
- 头像母版的外框和分隔线并非严格等宽网格，旧脚本按整图宽高等分后再取方形会让20张金色圆框相对256画布产生不同方向的偏移。2026-07-23 已逐张拟合并固定真实圆框中心，生成器现以每个圆心裁256×256，使原图圆框、透明圆形Mask和运行时选中圈同心；20张运行PNG及`qin_avatar_library_preview.png`、`qin_avatar_picker_runtime_preview.png`均已重建，既有`.meta`不再重写且UUID保持不变。`tools/validate_qin_avatar_alignment.py`逐张拟合输出圆框，验证19张正好位于(128,128)、头像01仅纵向偏1像素；Creator实际显示仍待验证。
- 2026-07-23 已把头像流程接入本地资源；2026-08-10扩展后，账号`photo`字段只保存未补零的字符串序号`"1"`至`"100"`，本账号字段首次为空时在大厅初始化随机选择1～100并回写；非空但不是有效序号的旧网址、文件名或越界值会迁移为`"1"`，查询到的其他旧账号无效值也只显示头像1。所有保存入口都会再次规范化序号。
- “我的”页原`ImagesLuck/我的/点击上传.png`入口文案为“修改个人资料”。打开`修改个人信息2`（以及兼容的首次资料面板）后，`panelMain.ts`使用5列×4行共20个固定运行时槽位，每次打开和点击“换一批头像”都会从100张中重新随机绑定；当前选择显示金色外圈与勾选标记，点击头像只更新顶部预览和隐藏序号，确认修改时才按是否真正换头像决定是否弹出10元消费确认。原“选择头像”图片保留为列表标题，不再使用系统相册。
- `ImageManager.ts`仅用`cc.loader.loadRes("avatars/头像XX")`加载和缓存本地SpriteFrame，大厅、牌桌座位/观战、玩家资料、赠送、战绩详情及牌局回顾等现有头像入口均复用该逻辑；旧图片HTTP下载、可写目录缓存、头像上传、系统相册回调和头像服务器常量已移除。`查询_用户_头像`仍保留，但只查询很小的`photo`字段；若`PlayerList`直接包含`photo`则牌桌立即使用该序号。`tools/validate_local_avatar_flow.py`当前验证100张256×256资源、200个唯一纹理/帧UUID、随机20帮助方法、序号写入、旧值回退及无网络头像代码；Creator真实账号的付费扣款、多客户端同步和Native运行仍待验证。
- Creator预览曾因快速编译仍引用已注册的旧`ImageManager`构造器，导致新加的`ImageManager.IsAvatarIndex`静态成员在运行时为`undefined`。头像数量、校验、规范化、随机和资源路径现均改为`ImageManager.getInstance()`返回组件上的实例成员，`panelMain`、`DrhLogicMgr`及`ImageManager`内部已清除这些新增静态调用；修改后首次验证需刷新或重新运行浏览器预览，让Creator同时重编译`ImageManager.ts`与调用方。
- “我的”换皮可由`tools/generate_qin_mine_skin.py`从`art_sources/mine/qin_default_avatar_source.png`确定性重建，静态合成预览为`art_sources/mine/qin_mine_runtime_preview.png`。20张运行图片的尺寸、`.meta`宽高/裁剪范围和UUID均保持不变，强蓝/青像素检查为0；独立点击子面板`panelRecordList`和`panelHongli`现已分别完成换皮，`panelVipInfo`及其专用图片仍未修改。Creator实际布局、动态数据和点击交互仍由用户在现有实例中验证。
- `panelRecordList`战绩界面已完成黑金秦风换皮：原位覆盖`assets/ImagesLuck/战绩`目录19张PNG及共享`assets/ImagesLuck/公用/我的战绩框.png`，包含标题、统计框、今天/昨天/前一天选中与未选中态、列表栏目文字及房间/底皮/时间/输赢条目图标；准确保留原业务文字、Toggle状态、分页按钮、节点名和查询逻辑。
- 战绩动态文字已同步匹配黑金风：`panelRecordList.prefab`的总局数、总手数和页码改为暖金；`战绩对象.prefab`的房间号为暖金、底皮/时间为象牙金、默认输赢为铜红；`panelRecordList.ts`仍按正负分支动态着色，但纯红/纯绿分别收敛为铜红RGB(196,86,66)和沉稳绿RGB(92,156,111)，没有修改字段、分页、Toggle或点击逻辑。生成脚本`tools/generate_qin_record_list_skin.py`使用已确认的完整战绩框源图`art_sources/record_list/qin_record_header_final_source.png`确定性重建20张运行图片，静态预览为`art_sources/record_list/qin_record_list_runtime_preview.png`；全部图片保持原尺寸、模式、自动裁剪边界、`.meta`和UUID，3张索引色图片也已固定透明裁剪边界，强蓝/青像素检查为0，Prefab JSON校验通过。共享背景、顶部、表格标题头、分割线、返回及分页箭头继续复用此前完成的黑金资源；Creator实际布局、Toggle切换、动态列表和点击进入详情仍待用户在现有实例中验证。
- 用户在Creator预览中反馈战绩统计框上半部留白过大、右上徽章过小且失衡，并明确要求实际资源不得简化效果图。`我的战绩框.png`现已直接按确认稿完整保留大型秦卷轴、圆形回纹、月桂、云纹、两侧精细装饰线、黑曜石纹理、分隔线和多层圆角金框，只去除预览棋盘格并适配为原612×279 RGBA；为避免独立统计条遮挡徽章底部，`局数手数`及两个动态数字节点整体下移20像素进入效果图预留的下方区域，节点名、引用和数据逻辑不变。
- 2026-08-15 `panelRecordInfo`战局详情按当前8L品牌重新设计，旧秦代黑金/青铜方向不再是本页现行基准。当前采用深海军蓝玻璃、铂银金属字、克制青色能量线，并让土豪/MVP/大鱼/劳模分别使用香槟金、铂银青、湖蓝青、青铜银专色；四种荣誉必须同属一套银青系统但保留明显辨识度，禁止重新变成四个相同圆框或普通平面文字。完整AI方向稿为`art_sources/record_info/8l_record_info_ai_direction.png`，由实际运行PNG合成的验收预览为`art_sources/record_info/8l_record_info_runtime_preview.png`。
- 页面层级已进一步确认：`panelRecordInfo`主页面只显示房间概况、荣誉玩家和可点击的战绩明细列表；点击某条战绩后才激活Prefab内默认隐藏的全屏`牌局回顾`子层，子层内部再用Toggle切换“牌局回顾/文字牌谱”。这两个页签不得画在主页面效果图中；子层视觉参考单独保存为`art_sources/record_info/qin_record_info_child_page_reference.png`。
- 2026-08-15用户明确允许调整位置并要求最终效果与AI方向稿一致，因此主页面不再沿用旧几何：`tools/layout_record_info_8l.py`把顶部“战局详情”放大到2.08倍、“牌局回顾”入口放大到1.65倍，荣誉主框下移，土豪/MVP/大鱼/劳模改为同一水平线，四项根节点使用同一圆形人物框但保留四种不同荣誉铭牌；原来透明度为0的“劳模”已恢复为255。房间信息、汇总条和战绩列表依次下移，列表可视区仍由原`ScrollViewEx`和Widget管理。所有脚本依赖的节点名称、层级、按钮和动态字段保持不变，隐藏子层内部“牌局回顾/文字牌谱”Toggle结构未改。
- 当前权威美术入口为`tools/style_record_info_8l.py`：它确定性重建`assets/ImagesLuck/战绩详情/`历史20张PNG、独占本页的`assets/ImagesLuck/公用/皇冠框.png`，并新增153×153的`荣誉头像框.png`；主框顶部直接复用正式`WebHome/assets/8l-logo.png`盾徽，四个荣誉根Sprite只在本Prefab内切换到新头像框UUID，真实玩家头像仍由原Mask/ImageManager加载。历史PNG首次运行备份到`HisImg/20260815-record-info-before-redesign/`且原`.meta`逐字节保持不变，新头像框使用Creator实际导入生成的UUID。`generate_8l_full_skin.py`已排除该专用目录和皇冠框，避免全量通用调色破坏专色和文字高光。Prefab引用、Python编译、`git diff --check`以及Cocos Creator 2.4.13隔离Web Mobile Debug＋MD5完整构建均通过；真实头像、长昵称、极长数值和隐藏子页仍需登录态视觉回归。
- 2026-08-15 战局详情已接入牌桌现有排队能力。用户在Creator中手工摆放的`panelRecordInfo/排行/排队`节点保持219×83、位置(-208.621,143.195)，其子节点`pd`保持218×86、位置(-14.162,10.3)、0.8倍循环Spine动画；功能代码不得重新生成或覆盖该几何。入口只在当前场景为受支持的`drh8`牌桌、房间引用有效且未处于换房流程时显示：观战玩家可用，已坐下玩家仅在`strGameState == "end"`时可用，历史战绩页及牌局进行中的已坐玩家隐藏。排队窗口现由`tools/extract_queue_match_prefab.js`从`drh8.fire/排队弹窗`确定性提取为`assets/resources/UI/panelQueueMatch.prefab`，`panelQueueMatch.ts`通过`ShowPanelMode.Top`显示并复用`QueueMatchManager`的状态、名单、申请/取消和跨房协议；战局详情在打开及等待排队期间保持显示，只在真正进入`assigning/switching/pre_sitting`时关闭，牌桌原排队入口也统一打开该Prefab。实测服务端不接受战局结束后用`起立_事件`释放状态，因此从结算战绩页申请排队时不再依据PlayerList座位显示区分坐下或观战：常驻`QueueMatchManager`会在全局及自身的所有成功`onEnterRoom`回包、牌桌创建、通过当前房间校验的PlayerList以及结算弹窗携带的房间号四层节点按账号缓存最后进入的房间号；结算页优先读取当前`account.roomID`，若它被旧结算逻辑提前清空则恢复该进房缓存，并以牌桌PlayerList房间号作最终兜底。首次结算排队无论本人当时显示为坐下还是观战、无论`account.roomID`是否已被清空，都必须先完整调用`reqStopGame()+reqLeaveRoom()`，收到成功退房回包后才清空本地房间号并以保存的原房间号调用`申请_排队_撮合`；只有本次退房成功已经确认后的取消/重新申请才允许直接复用原房间号。缓存按账号隔离，踢线时清理，不能把上个账号的房间带给新账号。等待期间保留静态战绩页，列表刷新也继续使用保存的房间号；匹配成功由常驻管理器正常拉入目标房间。结算主页面的`关闭`和牌局回顾子页此前遗漏的`关闭上上层`现统一走正常退房及场景导航，战绩面板保留到场景切走，避免关掉战绩后仍露出`drh8`空桌；即使当前`account.roomID`为空，只要尚未收到本次退房成功确认也继续发送正常退出房间协议，正在退房则记录回大厅意图并在回包/10秒超时后切大厅，已经退房则直接回大厅且排队状态继续由常驻管理器保持，若匹配已进入离房/进房阶段则禁止并发触发回大厅。大厅历史战绩中的子页返回仍只关闭子页，不会误退大厅。Prefab可重复生成哈希一致、内部引用/结构、手工按钮几何、五份相关TypeScript语法解析、流程静态断言及`git diff --check`通过；当前Creator实例已把Prefab导入到`library/imports`且脚本组件UUID识别正常，最后一轮进房缓存、退房/返回保护未覆盖正式构建。真实账号仍需验证结算观战玩家退房后排队、牌局回顾返回、等待时返回大厅、同房/异房匹配和匹配后自动进房。
- 2026-08-15 修复结算页排队匹配后的旧场景复用：结算页为了继续展示战绩会先退出服务端房间但暂留旧`drh8`，此时`account.roomID`为空。旧`QueueMatchManager.handleMatched()`把它误判为已在大厅，直接`reqEnterRoom(target)`；成功回包时又因场景名仍是`drh8`跳过`loadScene`，导致只关闭战绩、新房PlayerList进入旧牌桌且旧场景资源不释放。现在匹配成功发现`roomID<=0`但当前场景仍为`drh8`时，强制先经统一`beginTargetRoomTransition()`加载轻量`roomTransition`并释放旧场景引用，完成后才请求进入目标房间，成功后再重新加载全新`drh8`；原本仍属于房间的匹配继续先正常退房，再复用同一过渡方法。该顺序是结算排队换房的强约束，不能因服务端房间号已清空而跳过轻量场景。
- `assets/resources/Prefabs/钱包.prefab`及钱包内部动态条目`交易查询对象.prefab`、`银行对象.prefab`已完成黑金秦风换皮，覆盖充值、提现、记录、充值信息、实名认证、订单详情和选择银行等全部内嵌状态；原位重绘51张现有运行图片，并新增214×58的`钱包/支付宝提现.png`，补齐原Prefab缺失的支付宝提现背景/选中态。充值渠道中的银联、支付宝、微信、VIP及通用选择Tab按用户要求保留原图，不参与本轮重绘。
- 2026-08-14 充值支付通道已纠正品牌图片与选中态：`钱包.prefab`的支付1～3绑定支付宝蓝色图，支付4～5绑定恢复正常红蓝青配色的银联图，支付6～7绑定微信绿色图；公共`钱包/选择.png`改为透明中心的蓝银高光边框，选中时只增强轮廓、不再整块覆盖品牌图。节点尺寸、Toggle结构、通道名称和支付业务逻辑均未改；`generate_8l_full_skin.py`已把银联图与公共选中框加入保护清单，避免后续全局换色再次染成统一蓝色。Prefab JSON、SpriteFrame UUID映射、四张图片238×121尺寸/RGBA及`git diff --check`已通过，Creator实际充值页选中切换仍待运行态确认。
- 2026-08-14 支付通道图标已改为后台逐通道配置：`支付配置_<通道>`的客户端Base64 JSON新增兼容字段`icon`，允许`default/alipay/unionpay/wechat/other`；旧配置缺失字段时保持Prefab默认图，不会在后台升级时批量改变线上显示。XuanManager支付页为每个通道提供五种带配色预览的单选项，Go后端统一校验、写入、回读修订和审计图标映射；客户端收到`支付管理`后为每个可见Toggle查询对应配置并动态替换Background SpriteFrame，选中通道的普通配置回包也会再次同步图标，配置缺失/解析失败/未知类型均回退该节点原图。支付宝、银联、微信继续使用现有正常品牌色，新增238×121的蓝银中性`钱包/其他支付.png`及可重复生成脚本；四个SpriteFrame通过`panelQianBao`序列化属性预载，不依赖运行时网络图片。XuanManager Go全量测试/vet、Linux amd64静态构建、前端lint/生产构建、客户端TS转译、Prefab图标绑定及`git diff --check`通过；后台已发布正式纯HTTP地址，正式二进制SHA-256为`0165eda4e232a9f9995a21b4f08bd3b43eafb71919e72413e46dc180bcb07177`，资源为`index-czNXj1Ov.js`和`index-CnzuPC3_.css`，内网/公网健康与静态资源200、支付接口未登录401，8892候选已停止，回滚文件为`bin/xuanmanager.previous-payment-icons-20260814033033`和`web.previous-payment-icons-20260814033033`。发布和验证没有保存或改变任何真实支付配置；真实KB哈希回包和Creator充值页动态切换仍待联调。
- 2026-08-14 客户端钱包支付通道默认图调整：`assets/resources/Prefabs/钱包.prefab`中的`支付1`、`支付2`、`支付3`三个Background已从支付宝SpriteFrame改为现有银联`钱包/3-1.png`；节点顺序、尺寸、点击逻辑和支付配置协议不变。`panelQianBao`会在加载时捕获这三个新默认图，因此后台字段缺失、`icon=default`、配置解析失败或未知类型时均显示银联；后台明确配置`alipay/wechat/other/unionpay`仍按配置覆盖。Prefab JSON/节点绑定回读、`git diff --check`及Cocos Creator 2.4.13独立Web Mobile Debug＋MD5构建通过；正式构建目录未覆盖，运行态钱包及后台真实配置覆盖仍待验证。
- 2026-08-14 XuanManager支付配置出现“回读校验失败，已尝试恢复”的根因已修复。正式只读核对发现16个默认通道中仅`支付1`已有`支付配置_`记录、其余15个尚未初始化且不存在重复键；网页整体保存会首次写入全部通道，旧读取把未配置通道的`infoFields`返回为`null`，写后回读则变成`[]`，完整JSON摘要错误地把两个等价空值判为不同，因此日志出现`verify payment configuration error:null restoreError:null`并触发恢复。后端现把查询和请求中的空资料字段统一规范为非空空数组，回读校验按逐字段业务值比较；失败日志只记录`channels[索引].字段名`等路径，不记录域名、说明、银行或配置正文。新增空集合回读和字段级诊断测试；Go全量测试/vet、Linux构建、8892候选健康/鉴权/无轮播、正式内外网健康与哈希均通过，验证期间支付配置键仍为1且无重复，未保存或改变真实支付配置。正式二进制SHA-256为`b7f3c3e0d43ef21fe1ef67ccb9cd12cc1db42bc4e86daf1423982c0dac1cf291`，候选已停止，回滚文件为`bin/xuanmanager.previous-payment-readback-fix-20260814`。
- 钱包新增支付宝提现SpriteFrame只绑定到原Toggle的Background和checkMark，节点名、Toggle target/checkMark引用、初始选中状态及脚本路径均保持不变；为使原零宽节点可显示和点击，仅把`支付宝提现`父节点宽度由0修正为201，并将其两层图片恢复为与银行卡/USDT相同的214×58布局。动态金额、输入值、提示、实名、订单和两个列表模板文字统一为暖金、象牙金、灰金与克制铜红，没有修改字符串、服务端字段或业务分支。
- 钱包换皮可由`tools/generate_qin_wallet_skin.py`从`art_sources/wallet/qin_wallet_emblem_source.png`确定性重建，六状态静态预览为`art_sources/wallet/qin_wallet_runtime_preview.png`。52张输出图片的尺寸和透明裁剪范围均与`.meta`一致，既有`.meta`/UUID未改，新增资源使用独立UUID，强蓝/青像素检查为0；共用`公用/btn_4.png`会按共享覆盖原则同步影响8个文件中的18处关闭按钮。外部`panelKefu`、`panelMsgView`和`panelLoading`未纳入，Creator中的服务端动态渠道、三种提现方式、EditBox、列表和点击交互仍待用户在现有实例中验证。
- `assets/Scenes/drh8.fire`牌桌已按当前用户手调布局完成整套黑金秦风美术替换；运行资源现由核心生成器130项与牌背/图集生成器12项共同构建，去重后共141张PNG。范围包含主操作、玩家框、动态状态、语音、设置、带入、实时战绩、牌型提示、奖池、举报、转盘、牌背/搓背、切牌图集、爆奖图集和新增动作状态`resources/other/drh/滚.png`。动态文字严格保留“大、跟、敲、休、丢、分、滚、搓牌中、地九王”等原业务含义。
- 2026-08-13 五张可选桌面已从带实体椭圆桌子的同构背景改为类似用户视频的全屏绒布牌桌，均保持750×1334及原文件名、`.meta`和UUID；用户反馈首版深翡翠桌布略显脏后，最终顺序调整为`zuotype/1～5.jpg`依次为孔雀青、皇家蓝、曜石蓝灰、香槟棕和重新生成的干净深翡翠绿。新第5款只保留均匀细腻的绒布纹理、中央柔光和边缘暗角，首版斑驳深翡翠不再用于运行资源；只有美术颜色/纹理和选项顺序变化，座位、按钮、文字和交互布局未改。牌背选项1/2同步换成蓝银冰青与墨绿金色两款Art Deco设计，中央保留8L徽标；`牌背1/2.png`、`搓背1/2.png`以及DragonBones `ImagesLuck/动画/切/hand_tex.png`内第1/2套上牌、下牌、放牌切片已同步，选项3和旧配置0保持原资源。原图及调整前五桌面完整归档于`HisImg/20260813-fullscreen-tables-cardbacks/`，视频参考、生成母版和最终总览位于`art_sources/8l/table-cardback-20260813/`。尺寸/透明通道、图集1376×892切片坐标及场景Sprite解析均通过；`tools/render_drh8_scene_preview.py`从真实`drh8.fire`渲染750×1334牌桌时70个活动Sprite全部解析，确认新默认孔雀青桌布全屏且布局不变。浏览器只验证了登录预览可启动，未使用真实账号进入牌桌，因此五桌面切换、两牌背选择、搓牌和DragonBones切牌动画仍待登录态实际点击验收。
- 2026-08-13 搓牌/切牌DragonBones图集 `assets/ImagesLuck/动画/切/hand_tex.png` 的“顶部手、底部手、放牌手”三个人手切片已从 `HisImg/qing` 原版按切片矩形逐像素恢复为正常肤色和原红色指甲，不再使用8L全局换色产生的蓝灰皮肤；九个现有8L蓝银/墨绿牌背切片、1376×892尺寸、透明通道、`hand_tex.json`、`.meta`、UUID和骨骼动画数据均未改。同步更新生成母版 `art_sources/8l/table-cardback-20260813/qing_hand_tex_new.png`；`tools/generate_8l_full_skin.py` 已增加人手原色保护，`tools/restore_natural_hand_tex.py` 可只恢复三个人手切片并验证其他像素零变化，重复执行变化像素为0。原图切片alpha一致、运行图与母版SHA-256一致、Python语法、`git diff --check`及Creator 2.4.13临时目录Web Mobile Debug完整构建通过；真实搓牌/切牌动画播放仍待登录牌桌确认。
- 2026-08-13 玩家观战状态的实际运行资源是 `assets/resources/other/观战.png`，`drh8.fire` 与 `panelGameView.prefab` 的8个 `PlayerInfo/扩展状态` 节点均复用其同一SpriteFrame UUID。该资源已由原蓝色/橙色效果改为非蓝色的曜石黑＋香槟金高对比铭牌，使用暖金文字明确显示“观战中”；保持66×60透明画布，并把有效牌面限制在底部62×32安全区，因此无需改座位节点、位置、尺寸、场景、Prefab、`.meta`或UUID。`tools/generate_qin_drh8_panel_fix.py` 的 `draw_observer_badge()` 已同步生成该黑金牌，`tools/generate_8l_full_skin.py` 对该资源增加原样保护，后续全局8L换色不会再次染蓝。Creator尺寸模式牌桌预览确认铭牌覆盖头像下部且不遮昵称/金币栏，资源边界、Meta哈希、Python语法及`git diff --check`通过；因当前项目已有Creator实例打开，按协作约束未另启实例重复构建，真实在线观战状态仍待登录牌桌确认。
- 2026-08-14 未开局房间中央的纯文字“房间已就绪，等待开局”已替换为用户确认的金银立体“8L 游戏已就绪”美术字。运行资源为 `assets/resources/other/drh/room_ready_art.png`，1024×238 RGBA真透明画布，使用银白字、淡金8L徽标和少量蓝色宝石点缀，放在深蓝、墨绿、酒红等桌布上均保持清晰；确认Creator完全退出后，`drh8.fire`与`panelGameView.prefab`均把原“开局倒计时”Label原位直接改为440×102 Sprite、绑定同一SpriteFrame并重命名为“房间已就绪美术字”，不再使用运行时动态加载兼容层。下方“满N人准备后自动开局”文字及首局少于4人时显示提示组的条件不变；`DrhLogicMgr.ts`已移除`closeroom_countdown`消费、每秒调度和旧倒计时渲染。原ImageGen透明预览实际为无Alpha棋盘格，正式资源通过纯绿底、抠像和绿色溢出抑制生成，并以Alpha/深蓝合成复核避免白格、绿底和绿边；场景/Prefab/Meta JSON、UUID引用、`git diff --check`及Creator 2.4.13隔离Web Mobile Debug＋MD5构建通过，构建产物直接包含场景美术字节点及原始RGBA图片，真实房间不同桌布观感仍待登录后确认。
- 2026-08-15 “8L 游戏已就绪”美术字已增加克制的循环呼吸效果：`panelGameView.ts`在牌桌界面加载时只绑定现有`等待开局提示/房间已就绪美术字`节点，以1.2秒正弦缓动从原比例1.0放大到1.025、透明度238渐亮到255，再用1.2秒恢复，完整周期2.4秒；不改变位置、节点尺寸、父提示组显隐条件、下方自动开局文字或任何开局协议。绑定前会停止该美术字自身旧动作并复位比例，父提示组隐藏时Cocos动作随节点暂停、重新显示后继续，场景销毁时随节点清理。`drh8.fire`和`panelGameView.prefab`的同名节点均确认使用0.5居中锚点，场景/Prefab JSON回读、TypeScript构建、`git diff --check`以及Creator 2.4.13隔离Web Mobile Debug＋MD5完整构建通过；正式`build/web-mobile`、热更新Manifest及服务器均未改，仍需真实房间确认不同桌布上的最终呼吸观感。
- drh8换皮由`tools/generate_qin_drh8_skin.py`、`tools/generate_qin_drh8_atlases.py`确定性重建；`tools/generate_qin_drh8_panel_fix.py`已作为核心生成器最后一道必跑修复，专门重绘74张易变形的弹层/按钮/选择框/滑条/动作资源，后续不得绕过该步骤单独保留宽泛生成器的旧结果。`tools/validate_qin_drh8_skin.py`已验证141张PNG尺寸、模式和透明裁剪与`.meta`一致，两次构建哈希一致，场景/Prefab前后哈希一致；新增下注聚光灯后的预览成功解析70个活动Sprite和34个Label且未解析为0。
- 2026-08-13 注意：`tools/validate_qin_drh8_skin.py`不是只读校验，它会先执行两轮完整换皮生成器并改写141张运行资源及预览图；工作区存在用户手调桌布、牌背或其他未提交美术时禁止直接运行。提交前应优先使用JSON/Meta/尺寸检查和Creator构建；确需运行时必须先保存脏文件清单与哈希，并在结束后只恢复校验器额外生成的文件，避免覆盖用户美术。
- 2026-07-23 Creator截图确认的变形根因是部分`cc.Sprite`使用RAW/TRIMMED尺寸模式：Creator预加载会按原图/裁剪尺寸重置节点大小，不能只依据场景序列化的`contentSize`判断运行效果。修复时在原尺寸透明画布内限定实际可见安全区，并保留极低透明度裁剪锚点；`tools/render_drh8_scene_preview.py`的`--simulate-creator-size-mode`模式用于复现这条运行路径。
- `tools/render_drh8_panel_previews.py`覆盖20个弹层状态，`tools/render_drh8_action_previews.py`覆盖12个动态动作状态；两套都必须同时跑普通尺寸和`--simulate-creator-size-mode`尺寸。当前模拟Creator预览中实时战绩、带入/余额不足、牌局回顾、文字牌谱、设置、举报、奖池及主操作/分牌/预操作/坐下/观战/语音均无解析或隔离错误。为避免房号、局内信息和分牌倒计时互相遮挡，场景与`panelGameView.prefab`仅同步调整了这些显示节点的位置/字号，并修正“解散房间”Sprite绑定及少量蓝青/纯红动态文字色；按钮逻辑、脚本、协议和用户手调的其他布局未改。Creator中的DragonBones播放、真实动态列表数据、点击热区和多桌布显示仍待用户在现有实例中验证。
- 2026-07-23 已对旧品牌标题做全项目审计：运行文本、场景、Prefab、脚本、配置和文件名中没有`Battle/BATTLE/LUCK POKER/POKER STAR`；macOS Vision OCR扫描`assets`下1316张图片后只确认两张残留旧图——`assets/ImagesLuck/登陆/背景.png`含`BATTLE`，`assets/imagesKK/游戏大厅/帽子.png`含`LUCKY POKER`。两张图的Texture/SpriteFrame UUID均没有场景或Prefab引用，脚本也没有动态路径加载，当前页面不会显示，但文件仍在`assets`，正式清理时应替换或删除，不能仅以“未引用”作为最终换皮完成标准。`art_sources`下109张图片没有识别到旧品牌标题。
- 同次审计发现根目录归档`runtime-src.zip`仍保留Native旧品牌：iOS `CFBundleDisplayName`和Android `app_name`均为`Battle`，Android/iOS图标目录共24张真实PNG，其主图明确使用完整`BATTLE`图标；此外归档中构建缓存路径仍含小写`battle`。该ZIP当前被Git忽略且不是活动Native源码，不影响Web/Creator预览，但若未来解压复用打包，必须先统一替换应用名、全套图标并清理旧构建缓存。`assets/ImagesLuck/动画/报奖-old/`另保留无引用的`BIG WIN`备用图集；它不是项目标题，但属于未清理旧风格资源。
- 当前实际生成的`build/jsb-link`原生工程没有`Battle`文本：Android `app_name`为`qing`，iOS显示名使用当前`PRODUCT_NAME`。2026-07-23 已把原Cocos默认图标替换为正式“秦”品牌D版图标：Android `mipmap-mdpi`至`mipmap-xxxhdpi`共5张，iOS AppIcon共21张并补齐已绑定的1024×1024商店图标；全部为不含透明通道的RGB PNG。由于`build/*`被Git忽略，正式母图和可重复生成脚本已单独保存在受版本控制的`art_sources/app_icon/`与`tools/generate_qin_app_icons.py`，Creator重新构建原生工程后需要复跑脚本。

- `assets/resources/UI/panelGivePad.prefab`赠送确认弹窗已完成黑金秦风换皮：原位重绘仅本页引用的`赠送金币.png`、`名字垫底.png`、`金额.png`、`交易密码.png`和`赠送按钮1.png`，形成鎏金标题、秦印玩家信息条、象牙金表单文字及强调型主按钮；共享的弹窗框、输入框、头像/头像框、透明EditBox底图和取消按钮均未覆盖。Prefab仅统一昵称、ID、输入值和占位文字的暖金/灰金配色，并把原本位于弹窗外的固定金额Label从`(-604.72,-170.626)`校正到输入值区域`(3.617,-1.454)`，同时调整到输入框之后绘制，避免被黑色输入底遮挡；脚本依赖的节点路径、按钮名、圆形Mask及赠送业务逻辑未改。`tools/generate_qin_give_pad_skin.py`可确定性重建5张运行图片，真实资源按Prefab坐标合成的预览为`art_sources/give_pad/qin_give_pad_runtime_preview.png`；尺寸、RGBA、`.meta`/UUID、强蓝像素、Prefab JSON、SpriteFrame解析及重复生成哈希检查通过，从玩家资料进入的手输金额模式和大厅预填金额模式仍待用户在现有Creator实例中实际验证。
- `assets/resources/UI/panelUserInfo.prefab`玩家资料弹窗已完成清爽黑金秦风换皮：新增623×880的专用`assets/ImagesLuck/互动/用户信息框.png`并只重绑本页，原位重绘玩家信息标题、开通VIP、语音回放、赠送、语音聊天、九宫统计框、六个道具卡底框及九张统计标题，共17张本页UI图片。上半区使用大面积暖黑漆留白和细金边组织头像/身份/操作，统计区改为克制三行三列信息卡；头像、头像框、充值按钮和已换皮开关继续复用现有资源。
- 用户明确要求`panelUserInfo`的道具图片不用改；本轮已对亲嘴、鸡、啤酒、拇指、炸弹、枪及5张隐藏道具图做SHA-256保护校验，11张均保持逐字节不变，只替换其外层`表情框.png`。Prefab除专用大框SpriteFrame绑定与动态Label暖金/象牙金/铜红色板外，没有修改任何节点名、层级、尺寸、坐标、Button、Toggle或`panelUserInfo.ts`业务逻辑。`tools/generate_qin_user_info_skin.py`可确定性重建17张运行PNG和`art_sources/user_info/qin_user_info_runtime_preview.png`，`tools/patch_qin_user_info_prefab.py`可重复应用绑定，`tools/validate_qin_user_info_skin.py`已验证尺寸、RGBA、透明裁剪、UUID、道具哈希、关键布局及交互契约；Creator实际头像数据、VIP/赠送/语音和道具点击仍待用户在现有实例中验证。
- `assets/resources/UI/panelHongli.prefab`及其运行时加载的`玩家对象`、`盟主对象`、`贡献对象`、`总业绩对象`、`红利提取记录对象`五个条目Prefab已完成清爽黑金秦风换皮：原位重绘代理目录57张既有专用PNG，并新增82×27的`ImagesLuck/代理/盟主徽标.png`，覆盖主页面余额/统计/六个入口、玩家/盟主/业绩/提取记录子页、比例输入与授权/设置按钮。整体使用暖黑漆、细金边、象牙字和克制铜红，不再使用旧蓝青霓虹或西式皇冠；“提升.png”继续显示实际业务文案“设置”，“总业绩”按钮文字也已按节点语义纠正。所有动态Label统一为暖金、象牙、灰金、铜红和沉稳成功绿；推广二维码全屏层从旧`BATTLE`登录背景重绑到现有黑金推广背景；原本SpriteFrame为空的`盟主对象/type`已绑定新增盟主徽标。节点名、尺寸、Widget、Toggle、分页、EditBox、二维码Graphics、按钮父子层级、脚本和服务端字段均未修改。
- `tools/generate_qin_hongli_skin.py`可确定性重建58张运行PNG和`art_sources/hongli/`两张静态总览，`tools/patch_qin_hongli_prefabs.py`可重复应用文字色板与两项SpriteFrame绑定，`tools/validate_qin_hongli_skin.py`执行只读全量检查；本机可用`PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3.13 tools/generate_qin_hongli_skin.py`复跑。双遍生成哈希一致，58张PNG的尺寸、RGBA和透明裁剪与`.meta`一致，强蓝/青像素为0；`panelHongli`与5个条目Prefab JSON、关键路径、动态文字色板、推广背景/盟主徽标UUID及11张共享九宫格保护均验证通过。Creator中的26个页面/弹窗状态、动态列表、分页、二维码和代理授权/设置交互仍待用户在现有Creator实例中逐项验证。
- 2026-07-23 应用图标首轮四方向提案保存在`art_sources/app_icon/qin_app_icon_concepts_v1.png`，用户已选择D“咸阳宫门”。最终满画布无圆角源图为`qin_app_icon_d_source.png`，1024×1024 RGB母版为`qin_app_icon_d_master_1024.png`：前景为已确认的定制金属“秦”字圆形金印，背景为暖金暮光下的秦宫城门，并以两侧朱砂战旗和底部小朱印收口；不含`QIN/BATTLE/POKER`等额外文字。`tools/generate_qin_app_icons.py`可从固定源图确定性重建Android/iOS全套图标，双遍生成哈希一致；实际设备桌面遮罩和安装显示仍待Android/iOS真机验证。

## 资源体积与未使用审计

- 2026-07-29 已新增只读审计工具`tools/audit_cocos_unused_assets.py`，入口固定为`login.fire`、`panelMain.prefab`、`drh8.fire`和`panelGameView.prefab`，会递归跟踪标准/压缩UUID、DragonBones图集图片、可达脚本导入与项目现有动态加载约定，并把`build/jsb-link/assets`逻辑字节反向归属到源UUID。不要用外置exFAT卷上的`du`占用判断包体；当前构建资源逻辑体积为49.38 MiB，而目录分配占用会被128 KiB分配单元严重放大。
- 本轮没有删除或移动资源。审计报告为`reports/cocos-unused-assets-audit-2026-07-29.md`，完整候选为`reports/cocos-unused-assets-candidates-2026-07-29.csv`，动态保留清单为`reports/cocos-dynamic-assets-retained-2026-07-29.csv`。当前发现102个已进入构建的疑似未用资源，构建归属体积3.12 MiB；其中旧`表情`/`表情---`两套Prefab、两套旧DragonBones表情资源及已注释的`表情声音`共90个、约3.07 MiB，是主要候选。另有282个、6.88 MiB源码残留未独立进入当前资源构建，不得直接当作APK/IPA节省量。
- 动态加载保护已明确覆盖`表情2/*`、`pk2/*`、`zuotype/*`、本地头像、道具及其音效、牌局动作音效、`other/<服务端状态值>`、`other/drh/*`、`other/牌谱/*`、动态UI/条目Prefab、KBEngine实体类及Cocos全局脚本；这些不能仅因场景/Prefab无直接UUID引用而删除。真正清理时应先移到项目外隔离目录，重新构建并比较APK/IPA，再回归大厅全部入口、牌桌、战绩/牌谱、钱包/管理、表情/道具/音效和三端语音。
- 2026-07-29 `assets/font/PingFF.ttf`源文件为10.34 MiB，在当前Android APK和iOS IPA中压缩后分别占约6.72 MiB和6.71 MiB；其UUID直接绑定于56个场景/Prefab中的620个`cc.Label`，其中当前审计可达的47个文件含587个Label，大厅、牌桌和主要动态条目均大量使用，不能按“少量旧界面字体”删除。所有绑定共用同一字体UUID，因此可以保留文件名和`.meta`原位换字库，无需逐个修改Label。临时HarfBuzz测算显示：仅保留项目源码当前出现的900个汉字会缩至约192 KiB、ZIP约131 KiB，但会遗漏服务端公告、聊天、玩家名等动态字符；保留GB2312常用字符集会缩至约1.54 MiB、ZIP约1.02 MiB，理论上可比当前包内字体减少约5.7 MiB，但繁体、罕见姓名字和Emoji仍可能缺字。正式替换前必须确定动态文本字符范围，优先考虑“静态UI使用子集字体、任意动态文本使用系统字体”或接受明确字符集约束，并做三端显示、对齐和缺字回归。
- 2026-07-29 当前Android Release APK内图片共1072项、压缩后31.73 MiB，是字体之外最大的资源类别；其中PNG 1066项/30.05 MiB、JPG 6项/1.68 MiB。Android包将这些已压缩图片按原字节存储，图片文件每减少1 MiB，APK通常会直接减少接近1 MiB；当前iOS IPA图片压缩后为31.54 MiB。对`build/jsb-link/assets`内1061张PNG做不落盘的Pillow最高级无损重编码测算，并只采用体积变小结果，可从29.91 MiB降至28.84 MiB，理论节省1.06 MiB（3.56%）；全部图片重新解码为RGBA逐像素比对均无差异。传统PNG无损优化有收益但幅度有限，JPG无损优化通常只涉及元数据和Huffman表，收益更小；若要再明显减包，需要评估大背景/牌面/动画图集的尺寸、透明区域、WebP或有损但视觉无差别的编码，不能再称为严格无损。正式执行时只能优化`assets`源图并保留`.meta`/UUID，不能直接改`build`生成物；仅在新文件更小时替换，之后必须重新构建、生成热更新清单/MD5，并回归Web、Android、iOS的颜色、透明度、图集切片和旧PNG混淆输出。
- 2026-07-29 用户已把项目所在`/Volumes/CB`从exFAT重新格式化为普通APFS；当前挂载为`/dev/disk4s1`、大小1 TB、不区分大小写、未加密、可读写。全盘扫描已确认AppleDouble `._*`为0，过去约13万个伴生文件导致的Creator/Gradle隐藏资源和清理失败问题已从文件系统层消除。盘内仍有`.DS_Store`、`.Spotlight-V100`和`.fseventsd`，这些是macOS正常Finder/索引/文件事件数据，不应和`._*`混为一谈，也不要按“所有点文件”统删；`.git`、`.gitignore`等项目自身点文件必须保留。此前为规避exFAT而把Gradle中间产物迁到本机APFS临时目录的配置可以继续使用，若未来希望简化则需单独评估并重新跑完整Android构建，不能因换盘直接删除。

## 修改项目时的约束

- 当前工作区在 2026-07-22 已存在大量用户未提交修改。每次只检查和修改目标文件，不做全仓格式化，不清理或覆盖无关变化。
- `library/`、`temp/`、`local/` 等为 Creator 生成内容，除非任务明确要求，不把它们当源代码修改。
- 保留 `.meta` 文件和 UUID；移动、替换资源时必须同步核对场景/Prefab 序列化引用。
- 大型核心文件是既有“上帝类”，改动时优先小范围修复并验证状态恢复、重连、切场景和异步回调，不要顺手进行大面积重构。
- 金币、VIP、支付、提现、短信、后台管理、付费看牌等客户端校验均不可信；相关修改必须要求服务端重复校验并保证原子性。
- 不在提交、日志、测试输出或对话中暴露密钥、密码、签名信息、Token、身份证、银行卡或手机号。
- 目前没有完整自动化测试和本仓库内服务端。静态检查通过不等于功能已验证；高风险改动至少需要 Creator 2.4.13 运行、真实协议联调和对应平台原生测试。

## GitHub 与 SSH

- XuanManager 正式服务器 SSH 部署连接固定使用 `client_update@154.37.155.17:2233`，本机身份文件为 `~/.ssh/id_ed25519_newserver`，建议命令：`ssh -i ~/.ssh/id_ed25519_newserver -p 2233 -o IdentitiesOnly=yes client_update@154.37.155.17`。2026-08-11 已实测登录成功，远端身份为 `uid=1000(client_update)`、主目录/落点为 `/www`，并具备 `/www/html/.xuanmanager` 写权限。`webcm_user` 是服务器本机 MySQL 的数据库账号，不能用于 SSH；不得在本文件记录对应密码。

- 2026-07-22 已在本机创建 GitHub Ed25519 密钥，文件名为 `~/.ssh/id_ed25519_github_qing`，并配置为本机访问 `github.com` 所有仓库的默认密钥；私钥不得提交、复制到项目或对外发送。
- GitHub SSH 已验证成功，身份为 `teihin`；远程仓库地址为 `git@github.com:teihin/qing.git`，默认分支为 `main`。远端已有持续提交记录，不再是空仓库。
- 当前活跃 Git 仓库位于 `/Volumes/CCCC/qing`，不是历史挂载 `/Volumes/CB/qing`；开始任务时必须先用 `pwd`、`git status -sb` 和 `git remote -v` 重新确认活动检出。
- 公开仓库提交前已将 `Tool.ts` 的短信平台账号字段和 `MobileManager.ts` 的微信、语音及统计平台配置字段置空；恢复这些功能时必须改用服务端或不入库的安全配置，不得再次硬编码真实凭据。
- `.gitignore` 已排除根目录签名文件、`runtime-src.zip`、macOS 元数据以及既有 Creator 缓存目录，避免敏感文件和生成内容进入仓库。
- 2026-07-22 已完成首次提交并成功推送到 `origin/main`；后续改动应继续先检查敏感信息和大文件，再提交并推送该分支。
- 后续所有 Git 提交说明必须使用中文，并详细写明修改内容；提交正文应按实际情况说明修改原因、涉及模块、关键行为变化、验证结果及未验证风险，不使用“更新”“修复问题”等无法追溯的笼统描述。
- 若本机代理导致 GitHub SSH 22 端口在握手阶段断开，可改走 GitHub 官方 `ssh.github.com:443`；2026-07-22 已按 GitHub 官方公布值核对 Ed25519 主机指纹、写入 `known_hosts` 并验证账号认证和推送成功。不得使用跳过主机校验的方式绕过错误。
- 2026-08-13 再次遇到 Clash Fake-IP 下 `github.com:22` 连接被关闭；使用项目专用密钥访问 `ssh.github.com:443` 验证成功。为让 VS Code 的 `origin/main` 跟踪引用同步更新，应继续以命名远端 `origin` 推送，并仅在单次命令中用 Git URL rewrite 把 `git@github.com:` 映射到 `ssh://git@ssh.github.com:443/`；不要永久改坏仓库远端，也不要跳过主机校验。

## 审查范围说明

2026-07-22 已完成项目脚本、场景/Prefab 绑定、构建配置、热更新、KBE 网络及 `runtime-src.zip` 的静态通读；未完成真实后端联调、线上配置确认、Android/iOS 完整打包和真机回归。
