# 排队撮合功能接口文档

> 状态：服务端本地代码已实现，供客户端联调使用。
> 最后更新：2026-08-12。

本文档只描述客户端需要关心的接口、参数、返回和处理流程。服务端内部撮合逻辑对客户端视为黑盒。

## 一、功能范围

第一版只支持大厅普通房排队撮合：

- `roomType` 固定传 `"Custom"`。
- 第一版只对当前线上大厅普通房生效，即 `积分房卡房 / 传销扯旋`；先进房、副本房、俱乐部房、比赛场和代开房不按已支持处理。
- 排队按钮在游戏房间内点击。
- 客户端申请排队时，需要传玩家点击按钮时所在的当前房间 `roomID`。这个房间只作为参考房间，用来让服务端判断要撮合的房型。
- 服务端不保证一定匹配成功；没有合适房间时会保持排队。
- 服务端匹配成功后只通知客户端目标房间和座位，客户端仍按现有流程自行离房、进房、预坐。

## 二、整体流程

1. 客户端在房间内点击排队按钮。
2. 客户端调用 `Account.reqHallCommand()` 申请排队。
3. 服务端返回 `accepted` 只表示申请已受理，不代表已经匹配到房间。
4. 客户端需要显示排队列表时，调用 `查询_排队_列表`，服务端返回当前参考房间同组队列。
5. 服务端匹配到目标后，通过 `QueueMatchInfo` 主动推送给客户端。
6. 客户端收到 `QueueMatchInfo status="matched"` 后，按目标房间是否等于当前房间分支处理。
7. 客户端进入目标房间后，发送带 `queue_id` 的 `预坐_事件`。
8. `预坐_事件` 成功后，客户端按现有预坐流程显示座位、倒计时和带入分数选择。
9. 客户端在倒计时内发送普通 `坐下_事件`，不传 `site`。
10. 如果预坐失败或服务端匹配超时，客户端不要自行循环重试，等待服务端下一次 `QueueMatchInfo`。
11. 如果服务端推送 `QueueMatchInfo status="failed"`，客户端退出排队 UI，提示玩家重新申请。

## 三、申请排队

调用：

```javascript
account.reqHallCommand(JSON.stringify({
  header: "申请_排队_撮合",
  roomType: "Custom",
  roomID: currentRoomID
}), "p@queue_match")
```

完整参数：

```json
{
  "header": "申请_排队_撮合",
  "roomType": "Custom",
  "roomID": 10001,
  "money": 1000
}
```

参数说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `header` | string | 是 | 固定 `"申请_排队_撮合"` |
| `roomType` | string | 是 | 第一版固定 `"Custom"` |
| `roomID` | number | 是 | 玩家点击排队按钮时所在的当前房间 ID |
| `money` | number | 否 | 可选。未选择带入分数时可以不传，服务端按最低带入资格做轻量检查；如果此时已有预计带入金额，建议传入，便于提前拦截金币不足 |

成功返回：

```json
{
  "status": "accepted",
  "message": "已受理排队申请"
}
```

失败返回示例：

```json
{
  "status": "failed",
  "message": "当前状态不能排队"
}
```

注意：

- `accepted` 只表示服务端已受理排队申请，不表示已经有目标房间。
- 即使申请后立刻匹配成功，目标房间也不从申请接口返回，而是统一通过 `QueueMatchInfo` 推送。
- `QueueMatchInfo` 可能早于或晚于 `accepted` 返回到达，客户端不要依赖两者先后顺序。
- 申请排队时的 `money` 只用于提前资格检查，不代表最终带入金额；最终带入仍以预坐成功后普通 `坐下_事件` 里的 `money` 为准。

## 四、匹配通知 QueueMatchInfo

服务端匹配成功后，客户端会收到单人推送：

```text
QueueMatchInfo
```

匹配成功消息：

```json
{
  "status": "matched",
  "queue_id": "Q202608121530001",
  "roomType": "Custom",
  "roomID": 10001,
  "site": 2,
  "timeout": 10,
  "queue_key": "底皮1_0|standard",
  "room_auto_key": "底皮1_0",
  "is_dijiuwang": false
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `status` | string | `"matched"` 表示匹配成功 |
| `queue_id` | string | 本次匹配令牌，后续 `预坐_事件` 必须原样带回 |
| `roomType` | string | 第一版固定 `"Custom"` |
| `roomID` | number | 服务端分配的目标房间 ID |
| `site` | number | 服务端建议预坐的目标座位号 |
| `timeout` | number | 本次匹配等待预坐的超时时间，单位秒 |
| `queue_key` | string | 房型分组标识，客户端可用于日志或调试，不建议作为业务判断 |
| `room_auto_key` | string | 大厅自动房型标识，客户端可用于日志或调试 |
| `is_dijiuwang` | boolean | 是否地九王房型，客户端可用于日志或调试 |

客户端处理规则：

1. 保存当前 `queue_id`，后续只处理和当前排队 UI 一致的 `queue_id`。
2. 如果 `roomID == currentRoomID`，不离房、不重新进房，直接发送带 `queue_id` 的 `预坐_事件`。
3. 如果 `roomID != currentRoomID`，先按现有流程离开当前房间，等待离房成功后，再按观战方式进入目标房间。
4. 进入目标房间成功后，发送带 `queue_id` 的 `预坐_事件`。
5. 如果 `预坐_事件` 失败，客户端不要自行循环重试同一座位，等待服务端下一次 `QueueMatchInfo`。

## 五、排队预坐

目标房间等于当前房间时：

```javascript
account.reqRoomCommand(JSON.stringify({
  header: "预坐_事件",
  site: matchedSite,
  queue_id: queueId
}), "queue_pre_site")
```

目标房间不等于当前房间时：

```javascript
// 先按现有流程离开当前房间，并等待离房成功。
// 再按现有观战进房流程进入服务端通知的目标房间。
// roomSetting 必须保持观战语义，不能按创建房间或直接坐下语义进入。
account.reqEnterRoom("Custom", matchedRoomID, currentWatchRoomSetting)
```

进入目标房间成功后：

```javascript
account.reqRoomCommand(JSON.stringify({
  header: "预坐_事件",
  site: matchedSite,
  queue_id: queueId
}), "queue_pre_site")
```

参数说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `header` | string | 是 | 固定 `"预坐_事件"` |
| `site` | number | 是 | 使用 `QueueMatchInfo.site` |
| `queue_id` | string | 是 | 使用 `QueueMatchInfo.queue_id`，必须原样带回 |

预坐成功后，客户端继续使用现有预坐表现：

- 接收并刷新 `PlayerList`。
- 切换到占位视角。
- 显示 15 秒倒计时。
- 弹出带入分数选择。

预坐失败后：

- 不要自己重复发送同一个 `预坐_事件`。
- 不要自己换座位重试。
- 保持排队 UI，等待服务端下一次 `QueueMatchInfo matched`。
- 如果收到 `QueueMatchInfo failed`，退出排队 UI。

## 六、预坐后坐下

玩家在预坐成功后的倒计时内选择带入分数，然后发送普通坐下：

```javascript
account.reqRoomCommand(JSON.stringify({
  header: "坐下_事件",
  money: 1000,
  ready: 1,
  type: "直接"
}), "site_down")
```

注意：

- 这里不要再传 `site`。
- 服务端会使用预坐阶段保存的座位完成坐下。
- 如果预坐倒计时已过，普通 `坐下_事件` 应失败，客户端按现有失败处理。

## 七、取消排队

调用：

```javascript
account.reqHallCommand(JSON.stringify({
  header: "取消_排队_撮合"
}), "p@queue_match_cancel")
```

返回：

```json
{
  "status": "cancelled"
}
```

说明：

- `queued` 和 `assigning` 状态都可以取消。
- 如果玩家已经预坐成功，排队状态会被服务端清理；取消排队不负责释放已经预坐的座位。
- 已经预坐后，如果玩家不想坐下，客户端应走现有弃座流程或等待 15 秒倒计时释放。

## 八、查询排队状态

调用：

```javascript
account.reqHallCommand(JSON.stringify({
  header: "查询_排队_撮合"
}), "p@queue_match_query")
```

排队中返回：

```json
{
  "status": "queued",
  "rank": 1,
  "queue_count": 3,
  "assign_fail_count": 2
}
```

已分配、等待客户端进房预坐时返回：

```json
{
  "status": "assigning",
  "queue_id": "Q202608121530001",
  "roomID": 10001,
  "site": 2,
  "timeout": 10,
  "left_seconds": 6,
  "assign_fail_count": 2
}
```

无排队返回：

```json
{
  "status": "none"
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `status` | string | `queued`、`assigning` 或 `none` |
| `rank` | number | 当前玩家在同组队列里的参考顺位 |
| `queue_count` | number | 当前同组队列人数 |
| `assign_fail_count` | number | 当前排队过程已经失败或超时的次数 |
| `queue_id` | string | 当前分配令牌，仅 `assigning` 时有 |
| `roomID` | number | 当前分配的目标房间，仅 `assigning` 时有 |
| `site` | number | 当前分配的目标座位，仅 `assigning` 时有 |
| `timeout` | number | 分配超时时间，单位秒 |
| `left_seconds` | number | 当前分配剩余等待秒数 |

## 九、查询排队列表

这个接口用于客户端房间内的排队列表框。它只返回当前参考房间同组队列，不返回全服所有队列。
查询玩家不要求已经在队列中，`roomID` 只作为参考房间，用来确定要展示哪一组排队队列。

调用：

```javascript
account.reqHallCommand(JSON.stringify({
  header: "查询_排队_列表",
  roomType: "Custom",
  roomID: currentRoomID,
  page: 0,
  count: 30
}), "p@queue_match_list")
```

参数说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `header` | string | 是 | 固定 `"查询_排队_列表"` |
| `roomType` | string | 是 | 第一版固定 `"Custom"` |
| `roomID` | number/string | 是 | 当前排队列表所在的参考房间 ID |
| `page` | number/string | 否 | 页码，从 `0` 开始 |
| `count` | number/string | 否 | 每页数量，建议 `30`，服务端最大 `100` |

说明：第二个参数固定建议使用 `"p@queue_match_list"`。不要再在 JSON 参数里额外放 `context`，避免和项目现有 `p@` 回包包装混淆。
`roomID/page/count` 可以传数字或数字字符串，服务端会统一转成整数。
分页参数非法时，服务端会按最小规则修正：`page < 0` 按 `0`，`count <= 0` 按 `30`，`count > 100` 按 `100`。

实际回包：

```json
{
  "context": "queue_match_list",
  "result": {
    "number": 0,
    "count": 2,
    "queue_count": 2,
    "result": [
      {
        "rank": 1,
        "player_id": "731896",
        "player_name": "飘渺人生",
        "photo": "",
        "queue_seconds": 6640,
        "queue_time": "110:40",
        "bottom": "20皮",
        "status": "排队中",
        "is_self": false
      }
    ]
  }
}
```

空列表回包：

```json
{
  "context": "queue_match_list",
  "result": {
    "number": 0,
    "count": 0,
    "queue_count": 0,
    "result": []
  }
}
```

失败回包示例：

```json
{
  "context": "queue_match_list",
  "result": {
    "status": "failed",
    "message": "参考房间不存在"
  }
}
```

参数错误时也按同一结构返回：

```json
{
  "context": "queue_match_list",
  "result": {
    "status": "failed",
    "message": "参数错误"
  }
}
```

其它失败也按同一结构返回，只更换 `message`，第一版常见值包括：

- `房间类型不支持`
- `参考房间不支持排队`
- `房型不支持排队`

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `context` | string | 外层回包字段，值为 `"queue_match_list"`；客户端请求时传 `"p@queue_match_list"`，服务端回包会去掉 `p@` 前缀 |
| `result` | object | 外层业务结果对象 |
| `result.number` | number | 当前页码 |
| `result.count` | number | 当前列表展示总人数，包含同组队列中在线、未销毁、非忙状态的 `排队中` 和 `入座中` 玩家 |
| `result.queue_count` | number | 同 `result.count`，方便客户端直接显示 |
| `result.result` | array | 当前页列表 |
| `result.result[].rank` | number | 当前列表显示用的全局序号，从 `1` 开始。按服务端修正后的每页数量 `per_items` 计算，公式为 `page * per_items + index + 1`，不等同于单人查询里的撮合顺位 |
| `result.result[].player_id` | string | 玩家游戏 ID |
| `result.result[].player_name` | string | 玩家昵称 |
| `result.result[].photo` | string | 玩家头像，没有则为空字符串 |
| `result.result[].queue_seconds` | number | 已排队秒数 |
| `result.result[].queue_time` | string | 服务端格式化的已排队时间，格式 `分钟:秒` |
| `result.result[].bottom` | string | 底皮展示，例如 `20皮`；服务端只基于 `room_auto_key` 做简单转换，例如 `底皮20_0` 转成 `20皮` |
| `result.result[].status` | string | `排队中` 或 `入座中` |
| `result.result[].is_self` | boolean | 是否当前玩家自己 |

说明：请求里的 `count` 表示每页数量，回包 `result.count` 表示当前列表展示总人数，这是沿用项目已有列表接口的字段习惯。

客户端处理建议：

- 客户端每次打开排队面板时调用一次，以本次回包作为最新列表，不做定时刷新，也不依赖上次打开时的旧缓存。后续只在玩家手动刷新、重新打开面板、申请或取消排队后按需调用。
- 如果返回 `result.status == "failed"`，客户端应关闭列表或提示玩家重新进入当前房间后再查看，避免参考房间失效后继续重复请求失败接口。
- 如果当前页 `result.result` 为空但 `result.count > 0`，表示页码超出范围或该页无数据，客户端显示空列表即可，不需要当成错误。
- 这个列表只用于展示，不作为是否切房或预坐的依据。
- 切房和预坐只能根据 `QueueMatchInfo status="matched"` 处理。
- 第一版列表不显示 `离线`。离线或实体已销毁的玩家不会出现在列表中，服务端会在心跳里清理这类队列项。
- `入座中` 表示服务端已经通知该玩家目标房间和座位，正在等待客户端进入并预坐。
- 列表展示建议按截图思路保留四列：玩家信息、已排队时长、底皮、状态；玩家信息可使用头像、昵称和玩家 ID。

## 十、失败结束通知

如果连续失败或超时达到服务端上限，服务端会推送：

```json
{
  "status": "failed",
  "queue_id": "Q202608121530001",
  "reason": "assign_fail_limit",
  "message": "排队失败，请重新申请",
  "assign_fail_count": 10
}
```

客户端处理：

1. 如果消息带 `queue_id`，只处理和当前排队 UI 一致的 `queue_id`。
2. 退出排队 UI。
3. 提示玩家重新申请排队。

## 十一、客户端联调注意事项

- 客户端不要根据申请排队接口的 `accepted` 结果切房；只能根据 `QueueMatchInfo matched` 切房或预坐。
- 客户端不要根据 `查询_排队_列表` 的内容切房；列表只用于展示。
- 客户端必须保存当前 `queue_id`，并在 `预坐_事件` 中原样带回。
- 客户端收到旧的、不匹配当前 UI 的 `queue_id` 消息时，应忽略。
- 目标房间和当前房间相同时，不要离房重进，直接预坐。
- 目标房间和当前房间不同时，必须先按现有流程离开旧房间，离房成功后再按观战方式进入目标房间。
- `预坐_事件` 失败后不要自行循环重试，等待服务端重新匹配。
- 进入目标房间后，客户端仍按现有 `PlayerList` 和预坐倒计时逻辑刷新座位显示。
- 普通 `坐下_事件` 不传 `site`，只传带入分数等现有参数。
