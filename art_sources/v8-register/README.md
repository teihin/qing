# V8 新用户注册正式实施

唯一依据：`design-previews/效果图V8-new/04-登录与弹窗/01-快速注册弹窗-大字版.png`。

`tools/extract_v8_register_assets.swift` 从 941×1672 原图直切，`tools/apply_v8_register.py` 将完整弹窗和动态覆盖层写入正式 `panelLogin.prefab`。固定构图按 750/941 等比，长屏新增高度位于弹窗外。旧 UUID、五项输入、密码隐藏、头像、开关和原注册协议保持。

静态底图保留原字和边框；输入后仅清除对应占位文字区域，动态头像由环形前景遮住方角，状态清底位于动态提示下方且不覆盖确认按钮。13 张正式资源均已核对 Creator 导入。

2026-09-11 检查：`tools/validate_v8_register.py`、Python 语法和差异检查通过；网页 iPhone 6/X 已核对。五项输入、空邀请码拦截、头像前后切换/选择器/换一批、防盗号和关闭重入通过。未发真实注册请求，未构建或真机验证。截图在 `qa/`；测试头像为运行时真实候选头像，不是效果图示例头像。
